---
phase: 02-multi-shot-fast-path
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
  - messages/zh/scriptView.json
  - messages/en/scriptView.json
  - messages/zh/novel-promotion.json
  - messages/en/novel-promotion.json
  - messages/zh/stages.json
  - messages/en/stages.json
  - tests/unit/novel-promotion/script-view-mode-entry.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Multi-shot episodes open a dedicated confirmation surface that clearly says draft creation stopped before video generation and requires manual reference confirmation."
    - "The confirmation surface describes each segment as one 15-second video-generation unit, with model-ready prompt content, embedded dialogue, and compact shot-rhythm guidance visible or explainable in the UI."
    - "The confirmation-stage review body cannot start video generation directly; generation controls only exist once the user explicitly continues to the `videos` stage."
    - "The script page itself downgrades traditional storyboard-script-specific helper text and next-step cues for `multi_shot`, while preserving the traditional storyboard wording for `traditional` episodes."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      provides: "Dedicated multi-shot confirmation surface with reference-confirmation copy"
      exports: ["default"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      provides: "Mode-aware segment body that supports review-only confirmation, mandatory per-segment reference affordances, and no generation actions"
      contains: "review"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx"
      provides: "Script-page mode helper and CTA copy that bypass traditional storyboard-only cues for multi-shot episodes"
      contains: "productionMode"
    - path: "messages/zh/stages.json"
      provides: "Localized stage label for the multi-shot confirmation step"
      contains: "multiShotStoryboard"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      via: "embedded review surface via mode='review'"
      pattern: "ShotGroupVideoSection|mode.?['\"]review['\"]"
    - from: "messages/zh/scriptView.json"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx"
      via: "script-page CTA helper copy"
      pattern: "自动创建多镜头草稿|打开多镜头确认"
    - from: "messages/en/stages.json"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx"
      via: "stage label lookup"
      pattern: "multiShotStoryboard"
---

<objective>
Complete the user-facing multi-shot confirmation step and downgrade legacy storyboard cues on the fast path without touching the preserved traditional flow.

Purpose: make the new route legible to users and enforce the manual confirmation boundary before video generation.
Output: the finished confirmation surface, a review-only segment body, updated multi-shot copy, and tests covering placeholder and reference-confirmation messaging.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-02-PLAN.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
@messages/zh/scriptView.json
@messages/en/scriptView.json
@messages/zh/novel-promotion.json
@messages/en/novel-promotion.json
@messages/zh/stages.json
@messages/en/stages.json

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx`:
```tsx
export default function MultiShotStoryboardStage(): JSX.Element
```

From `src/lib/shot-group/draft-metadata.ts`:
```ts
export interface ShotGroupDraftMetadata {
  segmentOrder: number
  clipId: string
  sceneLabel: string
  narrativePrompt: string | null
  embeddedDialogue: string | null
  shotRhythmGuidance: string | null
  expectedShotCount: number
  sourceStatus: 'ready' | 'placeholder'
  placeholderReason: 'missing_clip_content' | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finish the multi-shot confirmation surface with manual confirmation guardrails</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    .planning/phases/02-multi-shot-fast-path/02-02-PLAN.md
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    src/lib/shot-group/draft-metadata.ts
    tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx (create if missing)
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx</files>
  <behavior>
    - Test 1: the stage renders copy stating that auto-draft creation has finished and video generation has not started yet.
    - Test 2: the stage tells the user to confirm storyboard/reference inputs manually before entering videos, and each segment exposes confirmation-stage reference affordances for upload plus generate-or-replace reference boards/images per D-12.
    - Test 3: `ShotGroupVideoSection` supports an explicit `mode="review" | "video"` prop or an equivalent split component, and `MultiShotStoryboardStage` uses the review-only branch.
    - Test 4: in review mode, the surface explains that one segment equals one 15-second video-generation unit with up to 9 shots, but does not render `创建并开始生成`, `生成视频`, `重新生成视频`, or any direct submit action that can call the video-generation mutation.
    - Test 5: a segment without a confirmed reference board/image shows `上传参考图`, `生成参考板`, or equivalent actionable controls in review mode, and a segment with an existing reference shows a replace action instead of hiding reference management.
    - Test 6: placeholder segments show visible repair guidance instead of appearing complete.
  </behavior>
  <action>Implement `MultiShotStoryboardStage.tsx` as the dedicated confirmation screen introduced in `02-02`, using the exact stage semantics from D-09 and D-10 and the explicit `onContinueToVideos` path added there. The stage must state three things explicitly: draft creation already happened, automatic video generation has not happened yet, and the user must manually confirm storyboard/reference inputs before moving to `videos` per D-04 and D-12. Reuse `ShotGroupVideoSection` only if it is first parameterized with `mode="review" | "video"` or an equivalent split component. In the confirmation stage, `MultiShotStoryboardStage` must pass `mode="review"` and the review branch must hide every generation CTA and any direct call site for `useGenerateProjectShotGroupVideo`, including the inline `创建并开始生成`, per-segment `生成视频`, and `重新生成视频` actions. D-12 is mandatory here: for every segment, review mode must surface confirmation-stage reference management affordances that let the user upload a reference image, generate a reference board/image when none exists, and replace/regenerate that reference when one already exists. Do not leave these as optional or conditional copy-only hints; the affordances must be rendered from the confirmation stage before the user advances to video submission in the later `videos` stage. Review mode may still show prompt/reference details, placeholder repair guidance, and save-only actions needed for confirmation work, but it must not allow final video submission. The review body must describe the fast path as clip-structure-driven drafts per D-02, one 15-second segment equals one video-generation unit per D-03, each segment can carry model-ready prompt content per D-06, embedded dialogue woven into the action beats per D-07, and compact shot-rhythm guidance rather than a traditional storyboard script per D-08. Keep the rhythm guidance copy compact and production-oriented, not screenplay-like. Add visible placeholder-state messaging when `draftMetadata.sourceStatus === 'placeholder'`; the message must say the segment slot was reserved but prompt/reference inputs remain incomplete and must be repaired before video generation. Extend `tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` to assert both sides of the boundary: the confirmation stage renders the per-segment upload/generate/replace reference affordances, and the review branch still omits every video-generation submit action until the later `videos` stage. Do not add asset-library injection, advanced omni-reference inputs, or any auto-start video generation behavior.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mode: 'review'|'review' \\| 'video'|mode=\\\"review\\\"|mode=\\{['\\\"]review['\\\"]\\}" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` returns matches, proving the confirmation stage uses a review-only branch.
    - `rg -n "上传参考图|生成参考板|替换参考图|替换参考板|reference" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` returns assertions proving the confirmation stage exposes per-segment reference upload and generate-or-replace affordances.
    - `rg -n "创建并开始生成|生成视频|重新生成视频" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` returns assertions proving those generation CTAs are absent in review mode.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The confirmation stage clearly enforces the manual confirmation boundary, exposes mandatory per-segment reference management affordances, and the review body cannot directly start video generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Change the script-page fast-path surface so multi-shot bypasses traditional storyboard-only cues</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
    messages/zh/scriptView.json
    messages/en/scriptView.json
    messages/zh/novel-promotion.json
    messages/en/novel-promotion.json
    messages/zh/stages.json
    messages/en/stages.json
    tests/unit/novel-promotion/script-view-mode-entry.test.ts
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx, messages/zh/scriptView.json, messages/en/scriptView.json, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, messages/zh/stages.json, messages/en/stages.json, tests/unit/novel-promotion/script-view-mode-entry.test.ts</files>
  <behavior>
    - Test 1: in `multi_shot`, the script-page helper card and CTA summary say the action auto-creates segment drafts and opens the multi-shot confirmation step, not that it jumps directly to videos or generates a traditional storyboard script.
    - Test 2: in `multi_shot`, the script-page surface hides, downgrades, or bypasses traditional storyboard-script-specific next-step wording on `ScriptViewAssetsPanel.tsx`, while `traditional` still keeps the classic storyboard wording.
    - Test 3: the script-page copy explains the clip-structure-driven, roughly-eight, 15-second segment model and states that reference confirmation happens before later video submission.
  </behavior>
  <action>Update the actual script-page surface in `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx` so `multi_shot` satisfies UI-02 directly on the page where users press `确认并开始绘制`. This task must change the mode helper card, CTA helper text, and any adjacent next-step wording that still frames the action as traditional storyboard generation or a direct jump to video generation. In `multi_shot`, the script page must explicitly downgrade or bypass traditional storyboard-script-specific cues by telling the user that the action auto-creates multi-shot segment drafts, opens the multi-shot confirmation step, and delays video submission until after reference confirmation; it must not describe the next step as generating or editing the classic storyboard script. Preserve the current traditional-mode wording and entry semantics for `traditional` episodes per MODE-03, TRAD-01, and TRAD-02. Back the change with `tests/unit/novel-promotion/script-view-mode-entry.test.ts`, asserting against `ScriptViewAssetsPanel` that the `multi_shot` branch no longer mentions direct video entry or traditional storyboard generation, while the `traditional` branch still does. Add localized labels for the `multiShotStoryboard` stage in `messages/*/stages.json` and supporting helper strings in `messages/*/scriptView.json` or `messages/*/novel-promotion.json` as needed. The multi-shot copy must reinforce the D-02 and D-03 operating model with executable language: the draft set is derived from the episode's clip structure, a typical episode may land around eight 15-second segments, and each segment is one video-generation unit. The confirmation copy must also state that reference confirmation happens here and video generation only begins after the user explicitly continues to the `videos` stage. Preserve traditional storyboard labels; do not rename, delete, or repurpose the existing traditional storyboard path.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/script-view-mode-entry.test.ts tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "multiShotStoryboard|auto-creates segment drafts|打开多镜头确认|opens the multi-shot confirmation" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx messages/zh/scriptView.json messages/en/scriptView.json messages/zh/novel-promotion.json messages/en/novel-promotion.json messages/zh/stages.json messages/en/stages.json` returns matches, proving the script-page surface now names the confirmation step instead of the old direct-jump wording.
    - `rg -n "直接前往多镜头片段视频生产界面|directly to multi-shot.*video|传统分镜" tests/unit/novel-promotion/script-view-mode-entry.test.ts` returns assertions proving multi-shot copy no longer uses the old fast-path phrasing while traditional wording remains covered in test.
    - `rg -n "multiShotStoryboard" messages/zh/stages.json messages/en/stages.json` returns matches in both files, proving the dedicated stage label exists in both locales.
    - `rg -n "自动创建多镜头草稿|打开多镜头确认|auto-creates segment drafts|opens the multi-shot confirmation" messages/zh/scriptView.json messages/en/scriptView.json messages/zh/novel-promotion.json messages/en/novel-promotion.json` returns matches, proving the fast-path helper copy describes draft creation plus confirmation instead of a direct jump to videos.
    - `rg -n "片段结构|clip structure|15 秒|15-second|8 个|around eight" messages/zh/scriptView.json messages/en/scriptView.json messages/zh/novel-promotion.json messages/en/novel-promotion.json` returns matches, proving the copy preserves the D-02 and D-03 operating model.
    - `rg -n "未开始视频生成|video generation has not started|继续后进入视频|continue to videos" messages/zh/novel-promotion.json messages/en/novel-promotion.json messages/zh/scriptView.json messages/en/scriptView.json` returns matches, proving the confirmation boundary is explicit in localized copy.
    - `npx vitest run tests/unit/novel-promotion/script-view-mode-entry.test.ts tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` exits 0, proving both the script-page UI-02 contract and the review-only confirmation surface pass together.
  </acceptance_criteria>
  <done>The script page itself now bypasses traditional storyboard-only next-step cues for `multi_shot`, while localized copy and tests preserve the classic storyboard wording for `traditional` mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirmation UI copy -> user action | Misleading copy could cause users to think video generation already started or that confirmation is optional |
| draft metadata -> placeholder UI state | Missing placeholder messaging could hide incomplete segments and cause bad downstream inputs |
| localized stage labels -> stage navigation UI | Wrong labels can blur the distinction between the multi-shot confirmation stage and the traditional storyboard stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | `MultiShotStoryboardStage.tsx` copy and stage framing | mitigate | State explicitly that draft creation stops before video generation and requires manual reference confirmation per D-04. |
| T-02-10 | E | `ShotGroupVideoSection.tsx` confirmation-mode actions | mitigate | Add a `review` mode or equivalent split that removes every video-generation CTA from the confirmation stage while keeping upload/save affordances for reference confirmation. |
| T-02-11 | S | `ScriptViewAssetsPanel.tsx` mode helper copy | mitigate | In `multi_shot`, replace storyboard-generation next-step wording with confirmation-step wording so the script page cannot misroute users mentally into the traditional chain. |
| T-02-12 | S | `messages/*/stages.json` stage labels | mitigate | Use a distinct `multiShotStoryboard` label so the fast-path confirmation step cannot masquerade as the traditional storyboard stage. |
| T-02-13 | I | localized helper copy | accept | The copy only describes already-authenticated project workflow semantics and does not expose new data. |
</threat_model>

<verification>
Run the unit test for the confirmation stage, then manually check one multi-shot episode in the workspace:
1. Click `确认并开始绘制`.
2. Confirm the next surface says draft creation is complete but video generation has not started.
3. Confirm the review surface shows per-segment upload/generate/replace reference affordances but does not show any `创建并开始生成` or `生成视频` action.
4. Confirm placeholder segments show repair messaging.
5. Return to the script page in `multi_shot` and confirm the helper card says the next step is draft creation plus the confirmation stage, not traditional storyboard generation or direct video entry.
6. Confirm traditional-mode copy still refers to the traditional storyboard path instead of the new confirmation stage.
</verification>

<success_criteria>
Phase 2's user-facing fast path is complete when:
1. The confirmation surface clearly stops before video generation and requires manual reference confirmation.
2. Multi-shot copy explains the real payload model: clip-derived segment set, one 15-second segment per video unit, model-ready prompt content, embedded dialogue, and compact shot rhythm.
3. The confirmation stage exposes mandatory per-segment upload/generate/replace reference-board affordances before later video submission.
4. The confirmation-stage review body cannot directly submit video generation; users must continue to `videos` first.
5. The script page no longer presents traditional storyboard-script steps as the next action for `multi_shot`, while `traditional` mode still does.
6. Placeholder segments are visually identifiable as incomplete repair targets.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-shot-fast-path/02-03-SUMMARY.md`
</output>
