---
phase: 02-multi-shot-fast-path
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - messages/zh/scriptView.json
  - messages/en/scriptView.json
  - messages/zh/novel-promotion.json
  - messages/en/novel-promotion.json
  - messages/zh/stages.json
  - messages/en/stages.json
  - tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx
autonomous: true
requirements:
  - MODE-03
  - TRAD-01
  - TRAD-02
  - UI-02
must_haves:
  truths:
    - "In `multi_shot` mode, clicking `确认并开始绘制` creates episode drafts and lands on a dedicated multi-shot confirmation stage instead of the traditional storyboard stage."
    - "From the video stage, multi-shot episodes navigate back to the multi-shot confirmation stage, while traditional episodes still navigate back to `storyboard`."
    - "Traditional mode continues to use the existing script -> storyboard -> videos chain and keeps the traditional storyboard surface available."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      provides: "Dedicated multi-shot storyboard/reference confirmation surface"
      exports: ["default"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts"
      provides: "Mode-aware launch branching that prepares drafts before entering the new stage"
      contains: "multi-shot-storyboard"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx"
      provides: "Mode-aware back navigation from videos"
      contains: "runtime.episodeProductionMode"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts"
      to: "src/lib/query/mutations/multi-shot-draft-mutations.ts"
      via: "ensureEpisodeMultiShotDrafts()"
      pattern: "useEnsureEpisodeMultiShotDrafts"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx"
      via: "currentStage normalization to multi-shot-storyboard"
      pattern: "multi-shot-storyboard"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      via: "onBack"
      pattern: "multi-shot-storyboard"
---

<objective>
Wire the Phase 2 fast-path contract into a dedicated multi-shot confirmation stage and preserve the classic storyboard path for traditional episodes.

Purpose: make the new draft API user-visible without breaking the existing traditional storyboard workflow.
Output: a new `multi-shot-storyboard` stage, mode-aware launch routing, and stage/back-navigation behavior that matches the locked decisions.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
@.planning/phases/01-episode-mode-entry/01-CONTEXT.md
@.planning/phases/01-episode-mode-entry/01-02-SUMMARY.md
@.planning/phases/01-episode-mode-entry/01-03-SUMMARY.md
@.planning/phases/02-multi-shot-fast-path/02-01-PLAN.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@messages/zh/scriptView.json
@messages/en/scriptView.json

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx`:
```ts
export interface WorkspaceStageRuntimeValue {
  episodeProductionMode: NovelPromotionEpisodeProductionMode
  onRunScriptToStoryboard: () => Promise<void>
  onStageChange: (stage: string) => void
}
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts`:
```ts
onRunScriptToStoryboard: () =>
  episodeProductionMode === 'traditional'
    ? runWithRebuildConfirm('scriptToStoryboard', runScriptToStoryboardFlow)
    : Promise.resolve(handleStageChange('videos'))
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx`:
```tsx
<VideoStage
  ...
  onBack={() => runtime.onStageChange('storyboard')}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make stage routing mode-aware and insert `multi-shot-storyboard` into the workspace flow</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    .planning/phases/02-multi-shot-fast-path/02-01-PLAN.md
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
    src/lib/query/hooks/index.ts
    tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.tsx (create if missing)
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx, tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.tsx</files>
  <behavior>
    - Test 1: in `multi_shot` mode, `onRunScriptToStoryboard` calls `ensureEpisodeMultiShotDrafts` and then moves to `multi-shot-storyboard`.
    - Test 2: in `traditional` mode, `onRunScriptToStoryboard` still routes through `runWithRebuildConfirm('scriptToStoryboard', runScriptToStoryboardFlow)`.
    - Test 3: `urlStage=storyboard` normalizes to `multi-shot-storyboard` for multi-shot episodes, but stays `storyboard` for traditional episodes.
    - Test 4: video-stage `onBack` resolves to `multi-shot-storyboard` for multi-shot episodes and `storyboard` for traditional episodes.
  </behavior>
  <action>Use the exact stage id `multi-shot-storyboard` for the new intermediate stage. Update `useWorkspaceProjectSnapshot.ts` so `currentStage` normalizes `editor -> videos` as before, but also maps `urlStage === 'storyboard'` to `multi-shot-storyboard` when `episode?.episodeProductionMode === 'multi_shot'`, and maps `urlStage === 'multi-shot-storyboard'` back to `storyboard` when the episode mode is `traditional`. In `useNovelPromotionWorkspaceController.ts`, instantiate `useEnsureEpisodeMultiShotDrafts(projectId, episodeId)` from plan `02-01`, expose its pending state on the stage runtime, and pass an `ensureEpisodeMultiShotDrafts` callback into `useWorkspaceStageRuntime`. In `useWorkspaceStageRuntime.ts`, change `onRunScriptToStoryboard` so `traditional` keeps the existing `runWithRebuildConfirm('scriptToStoryboard', runScriptToStoryboardFlow)` branch (D-15 / MODE-03 / TRAD-01), while `multi_shot` awaits `ensureEpisodeMultiShotDrafts()` and then calls `handleStageChange('multi-shot-storyboard')` (D-01, D-09, D-10). Extend `WorkspaceStageRuntimeContext.tsx` with an `isPreparingMultiShotDrafts: boolean` field. Update `useWorkspaceStageNavigation.ts` so the stage list is `config -> script -> multi-shot-storyboard -> videos -> editor` for `multi_shot` episodes and remains `config -> script -> storyboard -> videos -> editor` for traditional episodes; do not remove the traditional `storyboard` stage. Update `WorkspaceStageContent.tsx` to render a dedicated `MultiShotStoryboardStage` when `currentStage === 'multi-shot-storyboard'`. Update `VideoStageRoute.tsx` so `onBack` returns to `multi-shot-storyboard` for `multi_shot` mode and `storyboard` for traditional mode (D-11). Surface routing failures by letting the mutation reject; do not silently fall back to `videos` or `storyboard`.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "multi-shot-storyboard" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useWorkspaceProjectSnapshot.ts src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/VideoStageRoute.tsx` returns matches in all four files.
    - `rg -n "useEnsureEpisodeMultiShotDrafts" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts` returns a match.
    - `rg -n "episodeProductionMode === 'traditional'" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts` returns a match.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The workspace has an explicit multi-shot confirmation stage in the stage graph, multi-shot launch now prepares drafts before entry, and the traditional storyboard branch remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated multi-shot confirmation surface and downgrade traditional storyboard cues in the fast path</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/StoryboardStage.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts
    src/lib/shot-group/draft-metadata.ts
    messages/zh/scriptView.json
    messages/en/scriptView.json
    messages/zh/novel-promotion.json
    messages/en/novel-promotion.json
    messages/zh/stages.json
    messages/en/stages.json
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx (create if missing)
    tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx (create if missing)
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, messages/zh/scriptView.json, messages/en/scriptView.json, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, messages/zh/stages.json, messages/en/stages.json, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx</files>
  <action>Create `MultiShotStoryboardStage.tsx` as a dedicated confirmation screen that reads `clips`, `shotGroups`, and `episodeProductionMode` from the existing hooks and renders only the multi-shot workflow, not the traditional storyboard editor. The stage must show three explicit pieces of product copy: `系统已按本集片段自动创建多镜头草稿`, `请先为每个片段确认或补充分镜参考表`, and `确认参考表后再进入视频生成`. Reuse `ShotGroupVideoSection` as the main body so users review the auto-created segment drafts and upload or regenerate each segment’s composite storyboard image here (D-12), but update that section’s intro copy to describe auto-created `15s` segments, up to `9` shots per segment, and placeholder repair states. Add a visible placeholder-state message when parsed `draftMetadata.sourceStatus === 'placeholder'`; the message must say the segment was reserved but the prompt is incomplete, and direct the user to fill in the segment before video generation. Update `messages/*/scriptView.json` helper copy so multi-shot mode says the CTA now auto-creates segment drafts and opens the multi-shot confirmation step rather than the video stage. Update `messages/*/stages.json` and `messages/*/novel-promotion.json` with a label for `multiShotStoryboard` / `多镜头确认` (or the exact user-facing copy you choose) and make sure no fast-path copy claims that traditional storyboard generation is the next step. Do not remove or rename traditional storyboard copy for traditional episodes (TRAD-02), and do not add asset-library injection or advanced omni-reference inputs (D-16).</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "系统已按本集片段自动创建多镜头草稿|请先为每个片段确认或补充分镜参考表|确认参考表后再进入视频生成" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` returns three matches.
    - `rg -n "15s|9" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns updated fast-path copy.
    - `rg -n "placeholder|占位|repair" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` returns matches for visible incomplete-state messaging.
    - `rg -n "auto-creates segment drafts|自动创建多镜头草稿" messages/en/scriptView.json messages/zh/scriptView.json` returns matches in both files.
    - `rg -n "multiShotStoryboard|多镜头确认" messages/en/novel-promotion.json messages/zh/novel-promotion.json messages/en/stages.json messages/zh/stages.json` returns matches in all four files.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The multi-shot fast path has a dedicated confirmation surface with auto-draft review and placeholder repair messaging, while the traditional storyboard UI remains reserved for traditional episodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL/search params -> workspace stage state | Old or malformed stage values can land users on the wrong workflow surface |
| script CTA -> multi-shot draft mutation | A failed preparation request could otherwise fall through to the wrong stage |
| confirmation stage -> video stage | Users must not bypass reference confirmation accidentally in the multi-shot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | `useWorkspaceProjectSnapshot.ts` stage normalization | mitigate | Normalize `storyboard` and `multi-shot-storyboard` explicitly by episode mode so stale URLs cannot route multi-shot users into the traditional editor. |
| T-02-06 | R | `useWorkspaceStageRuntime.ts` CTA branching | mitigate | Await `ensureEpisodeMultiShotDrafts()` before stage change and let failures reject instead of silently moving to `videos`. |
| T-02-07 | E | `VideoStageRoute.tsx` back navigation | mitigate | Branch `onBack` on `episodeProductionMode` so multi-shot users return to confirmation rather than a traditional-only stage. |
| T-02-08 | I | `ShotGroupVideoSection.tsx` copy and placeholder UI | accept | The UI only reveals authenticated episode data already shown elsewhere in the workspace; no new external exposure is added. |
</threat_model>

<verification>
Run the two unit-test commands for this plan, then do one manual path check in a local workspace:
1. Put an episode in `multi_shot`.
2. Click `确认并开始绘制`.
3. Confirm the URL/current stage becomes `multi-shot-storyboard`, the stage header shows the confirmation messaging, and the video page back action returns there.
4. Switch to `traditional` and confirm the same CTA still triggers the classic storyboard branch.
</verification>

<success_criteria>
The Phase 2 user-visible workflow is complete when:
1. Multi-shot episodes enter `script -> multi-shot-storyboard -> videos`.
2. Traditional episodes still enter `script -> storyboard -> videos`.
3. The multi-shot confirmation surface clearly replaces traditional storyboard editing as the default fast-path step.
4. The video-stage back path respects the episode mode instead of always routing to `storyboard`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-shot-fast-path/02-02-SUMMARY.md`
</output>
