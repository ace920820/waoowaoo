---
phase: 03-editable-production-handoff
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shot-group/draft-metadata.ts
  - src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
  - src/lib/shot-group/prompt.ts
  - tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts
autonomous: true
requirements:
  - MSHT-07
must_haves:
  truths:
    - 用户在视频阶段改写后的多镜头台词会被持久化，而不是只存在本地草稿里
    - 多镜头片段未改写时继续使用剧本派生的默认台词
    - 多镜头片段生成视频时，最终提示词会使用“改写台词优先，否则回退默认台词”的规则
  artifacts:
    - path: src/lib/shot-group/draft-metadata.ts
      provides: 多镜头片段台词改写字段与解析规范
    - path: src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
      provides: shot group PATCH/POST 持久化与合并逻辑
    - path: src/lib/shot-group/prompt.ts
      provides: 多镜头视频生成提示词中的有效台词拼装
    - path: tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts
      provides: 改写台词与默认台词回退的回归保护
  key_links:
    - from: src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
      to: src/lib/shot-group/draft-metadata.ts
      via: draftMetadata merge on PATCH
      pattern: dialogueOverrideText
    - from: src/lib/shot-group/prompt.ts
      to: src/lib/shot-group/draft-metadata.ts
      via: parseShotGroupDraftMetadata
      pattern: embeddedDialogue|dialogueOverrideText
---

<objective>
Lock the persisted contract for editable multi-shot dialogue so downstream video generation can honor script-derived lines and user overrides without inventing a second source of truth.

Purpose: Phase 3 depends on a stable “override over default” rule for multi-shot dialogue per D-03, D-04, and D-05.
Output: A persisted `dialogueOverrideText` field inside shot-group draft metadata and prompt-generation logic that resolves the effective spoken lines deterministically.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-editable-production-handoff/03-CONTEXT.md
@.planning/phases/03-editable-production-handoff/03-UI-SPEC.md
@.planning/phases/02-multi-shot-fast-path/02-05-SUMMARY.md
@src/lib/shot-group/draft-metadata.ts
@src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
@src/lib/shot-group/prompt.ts

<interfaces>
From `src/lib/shot-group/draft-metadata.ts`:
```ts
export interface ShotGroupDraftMetadata {
  embeddedDialogue: string | null
  shotRhythmGuidance: string | null
}
```

From `src/app/api/novel-promotion/[projectId]/shot-groups/route.ts`:
```ts
const nextDraftMetadata = body.draftMetadata && typeof body.draftMetadata === 'object'
  ? {
    ...(previousDraftMetadata || {}),
    ...(body.draftMetadata as Partial<ShotGroupDraftMetadata>),
  } as ShotGroupDraftMetadata
  : previousDraftMetadata
```

From `src/lib/shot-group/prompt.ts`:
```ts
export function buildShotGroupVideoPrompt(params: {
  group: NovelPromotionShotGroup
  template: ShotGroupTemplateSpec
  locale: string
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shot-group draft metadata for editable dialogue overrides</name>
  <read_first>
    - src/lib/shot-group/draft-metadata.ts
    - src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/shot-group/draft-metadata.ts
    src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
    tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts
  </files>
  <action>Per D-05, add a persisted `dialogueOverrideText` field to `ShotGroupDraftMetadata`, parse and normalize it alongside existing `embeddedDialogue`, and keep the PATCH merge path additive so partial saves never erase upstream draft fields unintentionally. Keep `embeddedDialogue` as the script-derived default, keep `dialogueOverrideText` as the video-stage override layer, and add unit coverage proving PATCH merges preserve `embeddedDialogue` when only `dialogueOverrideText` is sent and clear the override when `dialogueOverrideText: null` is saved.</action>
  <acceptance_criteria>
    - `rg -n "dialogueOverrideText" src/lib/shot-group/draft-metadata.ts` returns the new metadata field and parser handling.
    - `rg -n "dialogueOverrideText" src/app/api/novel-promotion/[projectId]/shot-groups/route.ts` returns POST/PATCH merge or snapshot wiring.
    - `rg -n "preserves embedded dialogue|clears the override" tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts` returns explicit regression cases.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts</automated>
  </verify>
  <done>`dialogueOverrideText` is a first-class persisted field in shot-group draft metadata, PATCH merges are deterministic, and regression tests cover save and clear behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Feed the effective dialogue text into multi-shot video prompt generation</name>
  <read_first>
    - src/lib/shot-group/prompt.ts
    - src/lib/shot-group/draft-metadata.ts
    - src/lib/workers/video.worker.ts
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
  </read_first>
  <files>
    src/lib/shot-group/prompt.ts
    tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts
  </files>
  <action>Update `buildShotGroupVideoPrompt` so MSHT-07 is satisfied with a single rule: use `draftMetadata.dialogueOverrideText` when it is a non-empty string, otherwise fall back to `draftMetadata.embeddedDialogue`, and only emit the explicit dialogue-content directive when that effective text exists. Keep the main `videoPrompt` field separate from dialogue content per D-03 by appending the effective dialogue text as its own dialogue block instead of concatenating it into the primary video prompt paragraph.</action>
  <acceptance_criteria>
    - `rg -n "dialogueOverrideText|effective dialogue" src/lib/shot-group/prompt.ts` returns the helper or branch that resolves override-over-default dialogue.
    - `rg -n "override dialogue takes precedence|falls back to embedded dialogue" tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts` returns both prompt-generation assertions.
    - The automated verify command passes without touching unrelated video-panel tests.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts</automated>
  </verify>
  <done>Multi-shot video prompt generation consumes the persisted override field, falls back to script-derived dialogue when no override exists, and keeps prompt and dialogue as separate production concerns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→shot-group PATCH route | user-edited dialogue text crosses into persisted production metadata |
| persisted shot-group metadata→video prompt builder | stored text becomes generation instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `src/app/api/novel-promotion/[projectId]/shot-groups/route.ts` | mitigate | Reuse existing normalize/merge path so only sanitized optional strings become `dialogueOverrideText`, and preserve auth via `requireProjectAuthLight`. |
| T-03-02 | I | `src/lib/shot-group/prompt.ts` | mitigate | Keep dialogue text in a dedicated directive block instead of blending it into unrelated prompt text, reducing accidental leakage or prompt corruption. |
| T-03-03 | R | `tests/unit/novel-promotion/multi-shot/shot-group-editable-dialogue.test.ts` | mitigate | Add regression tests for override precedence and null-clearing so future refactors cannot silently change execution behavior. |
</threat_model>

<verification>
Run the targeted vitest file for the new metadata/prompt contract, then inspect the new prompt assertions to confirm override text and fallback text are both covered.
</verification>

<success_criteria>
- Multi-shot shot-group metadata persists a dedicated dialogue override field without replacing the original script-derived dialogue source.
- The downstream prompt builder uses a deterministic effective-dialogue rule that supports editing and clearing overrides.
- Requirement `MSHT-07` is fully covered before any UI work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editable-production-handoff/03-01-SUMMARY.md`
</output>
