---
phase: 03-editable-production-handoff
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
autonomous: true
gap_closure: true
requirements:
  - MSHT-07
  - UI-04
must_haves:
  truths:
    - Unsaved review-mode edits survive benign shot-group rerenders when the incoming server payload has not materially changed.
    - Unsaved video-mode prompt and dialogue edits survive benign shot-group rerenders until the user saves or the server version actually changes.
    - Multi-shot video handoff remains immediately editable instead of unexpectedly reverting form state on background refetch.
  artifacts:
    - path: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
      provides: dirty-safe review/video draft sync logic
    - path: tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
      provides: regression coverage for review/video unsaved-edit preservation
  key_links:
    - from: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
      to: tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
      via: exported sync helpers and rerender regressions
      pattern: syncReviewDraftsFromShotGroups|preserves unsaved
---

<objective>
Close verification gaps WR-02 and WR-03 by preventing harmless shot-group refreshes from overwriting unsaved local edits in review and video mode.

Purpose: The editable production handoff is not trustworthy if local form state resets before the user hits save.
Output: Dirty-safe draft synchronization in `ShotGroupVideoSection` plus focused regression tests for review-mode and video-mode rerender behavior.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-editable-production-handoff/03-VERIFICATION.md
@.planning/phases/03-editable-production-handoff/03-REVIEW.md
@.planning/phases/03-editable-production-handoff/03-02-SUMMARY.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx`:
```ts
export function syncReviewDraftsFromShotGroups(
  previous: Record<string, ReviewDraftState>,
  groups: NovelPromotionShotGroup[],
)
```

```ts
export function buildShotGroupVideoDraftMetadataPatch(params: {
  group: NovelPromotionShotGroup
  draft: Pick<VideoDraftState, 'dialogueText' | 'embeddedDialogue'>
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make review and video draft sync ignore benign refreshes while drafts are dirty</name>
  <read_first>
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    - .planning/phases/03-editable-production-handoff/03-REVIEW.md
    - .planning/phases/03-editable-production-handoff/03-VERIFICATION.md
  </read_first>
  <files>
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  </files>
  <behavior>
    - Test 1: Review-mode local edits remain intact when the same shot-group payload rerenders.
    - Test 2: Video-mode local prompt/dialogue edits remain intact when the same shot-group payload rerenders.
    - Test 3: Fresh server data can still reseed local drafts after an explicit save or when the upstream payload materially changes.
  </behavior>
  <action>Fix WR-02 and WR-03 inside `ShotGroupVideoSection.tsx` by introducing sync logic that distinguishes benign refreshes from true upstream changes. Preserve local review/video drafts when the incoming shot-group payload is equivalent to the last synced server state, and only reseed when the group is new or the server-backed fields actually changed. Keep this within the existing local draft model; do not add a global store, route persistence, or new save semantics. If needed, export a small pure helper for video-draft synchronization so the preservation rule is testable without rendering the full workspace.</action>
  <acceptance_criteria>
    - `rg -n "last synced|server snapshot|sync.*video" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx'` finds the new dirty-safe sync logic or helper.
    - The review sync path no longer replaces a draft solely because local unsaved edits differ from the seeded server draft.
    - The video sync path no longer overwrites `title`, `groupPrompt`, `videoPrompt`, or `dialogueText` on an unchanged rerender.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts</automated>
  </verify>
  <done>Review and video drafts survive harmless refetches, while genuine upstream changes can still refresh local state deliberately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rerender regressions for unsaved review and video edits</name>
  <read_first>
    - tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    - .planning/phases/03-editable-production-handoff/03-VERIFICATION.md
  </read_first>
  <files>
    tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
  </files>
  <behavior>
    - Test 1: `syncReviewDraftsFromShotGroups` preserves unsaved review edits when rerendered with the same server payload.
    - Test 2: Video draft sync preserves unsaved prompt/dialogue edits when rerendered with the same server payload.
    - Test 3: A materially changed incoming group payload reseeds the draft so saved server updates still appear.
  </behavior>
  <action>Add focused regression tests that cover the exact gap behavior called out in verification: edit local review/video draft state, rerender with unchanged `shotGroups`, and assert the local draft remains intact. Cover the positive reseed case separately so the sync fix does not freeze stale data forever. Reuse the existing `multi-shot-video-stage.test.ts` file unless a new pure helper makes a narrower unit test clearly cleaner.</action>
  <acceptance_criteria>
    - `rg -n "preserves unsaved review edits across benign rerenders|preserves unsaved video edits across benign rerenders|reseeds when server data actually changes" tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts` finds all three regressions.
    - The tests exercise both review-mode and video-mode sync behavior, not only rendered labels.
    - The automated verify command passes without depending on unrelated route suites.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts</automated>
  </verify>
  <done>The UI test suite fails if unchanged shot-group refreshes ever start clobbering unsaved review/video edits again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server shot-group payload→local editor drafts | background refetches can overwrite user edits before save |
| local editor drafts→save mutation | preserved unsaved drafts must still serialize the intended final values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | T | `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` | mitigate | Reseed local state only when the incoming server snapshot materially changes, preventing unintended overwrite from unchanged refetches. |
| T-03-14 | D | `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` | mitigate | Preserve local unsaved drafts so the handoff remains usable during background query invalidation and rerenders. |
| T-03-15 | R | `tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts` | mitigate | Add explicit rerender regressions for both review and video modes, including the positive reseed case when server data changes. |
</threat_model>

<verification>
Run the targeted multi-shot video-stage unit suite and confirm it now covers unchanged rerenders for both review and video draft sync plus the changed-server reseed path.
</verification>

<success_criteria>
- Unsaved review-mode edits survive benign shot-group rerenders/refetches.
- Unsaved video-mode prompt/dialogue edits survive benign shot-group rerenders/refetches.
- Verification gaps WR-02 and WR-03 are closed with automated regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editable-production-handoff/03-05-SUMMARY.md`
</output>
