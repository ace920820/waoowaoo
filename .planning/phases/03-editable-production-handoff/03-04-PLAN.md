---
phase: 03-editable-production-handoff
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
  - tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
autonomous: true
gap_closure: true
requirements:
  - MSHT-07
  - MSHT-08
must_haves:
  truths:
    - Rebuilding multi-shot drafts does not wipe previously saved editable metadata on matched shot groups.
    - Saved dialogue overrides, asset bindings, storyboard mode, and mood metadata survive benign multi-shot draft regeneration.
    - Multi-shot draft rebuilds still refresh derived segment structure without creating a second source of truth for editable fields.
  artifacts:
    - path: src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
      provides: rebuild logic that merges prior editable draft metadata into regenerated snapshots
    - path: tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
      provides: regression coverage for metadata preservation during draft rebuild
  key_links:
    - from: src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
      to: src/lib/shot-group/draft-metadata.ts
      via: parse previous draft metadata before snapshot rebuild
      pattern: previousDraftMetadata|parseShotGroupDraftMetadata
    - from: src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
      to: tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
      via: rebuild-preservation regression
      pattern: preserves saved editable metadata
---

<objective>
Close verification gap WR-01 by preserving saved editable shot-group metadata when multi-shot drafts are rebuilt.

Purpose: Phase 3 is not production-safe until draft regeneration stops deleting previously saved overrides and review metadata.
Output: A rebuild path that refreshes derived segment fields while retaining persisted editable metadata plus an integration regression that proves it.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-editable-production-handoff/03-VERIFICATION.md
@.planning/phases/03-editable-production-handoff/03-REVIEW.md
@.planning/phases/03-editable-production-handoff/03-01-SUMMARY.md
@src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
@src/lib/novel-promotion/multi-shot/persist-drafts.ts
@tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts

<interfaces>
From `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts`:
```ts
const videoReferencesJson = buildShotGroupVideoConfigSnapshot({
  generateAudio: false,
  includeDialogue: draft.includeDialogue,
  dialogueLanguage: 'zh',
  omniReferenceEnabled: false,
  smartMultiFrameEnabled: true,
  generationOptions: {},
  draftMetadata: { ... }
})
```

From `src/lib/novel-promotion/multi-shot/persist-drafts.ts`:
```ts
buildShotGroupVideoConfigSnapshot({
  previousDraftMetadata,
  draftMetadata: { ... }
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge prior editable metadata into multi-shot draft rebuild snapshots</name>
  <read_first>
    - src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
    - src/lib/novel-promotion/multi-shot/persist-drafts.ts
    - .planning/phases/03-editable-production-handoff/03-REVIEW.md
  </read_first>
  <files>
    src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
  </files>
  <behavior>
    - Test 1: When a matched shot group already has saved editable metadata, draft rebuild keeps those editable fields while refreshing derived segment fields.
    - Test 2: When no previous metadata exists, draft rebuild still seeds the standard derived metadata contract.
  </behavior>
  <action>Fix WR-01 by loading `previousDraftMetadata` from `existing.videoReferencesJson` before building the replacement snapshot, then pass it through the rebuild path the same way `persist-drafts.ts` already does. Preserve previously saved editable fields such as `dialogueOverrideText`, selected assets, storyboard mode, reference/composite prompts, and mood settings while still updating segment identity, source status, and other regenerated draft-derived fields. Do not add new storage or skip rebuild updates; this is a merge fix, not a behavior fork.</action>
  <acceptance_criteria>
    - `rg -n "previousDraftMetadata" 'src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts'` finds the merged rebuild path.
    - `rg -n "buildShotGroupVideoConfigSnapshot" 'src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts'` shows `previousDraftMetadata` flowing into the snapshot builder.
    - The route still rebuilds segment identity fields from the latest derived draft data instead of blindly reusing the entire old snapshot.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts</automated>
  </verify>
  <done>Matched shot groups retain saved editable metadata after draft regeneration, and the rebuild path remains based on the latest derived segment draft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an integration regression for metadata preservation across rebuilds</name>
  <read_first>
    - tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
    - src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
    - .planning/phases/03-editable-production-handoff/03-VERIFICATION.md
  </read_first>
  <files>
    tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
  </files>
  <behavior>
    - Test 1: Rebuilding an existing shot group preserves `dialogueOverrideText`, selected assets, storyboard mode, and mood metadata.
    - Test 2: The same rebuild updates the latest regenerated segment key and timing fields so reuse still tracks current draft structure.
  </behavior>
  <action>Add a focused integration test modeled on the existing route suite that seeds an existing shot group with editable metadata inside `videoReferencesJson`, posts to `/multi-shot-drafts`, and asserts the returned and persisted shot group still contains those editable fields after rebuild. Keep the assertion set limited to the metadata classes called out in WR-01 and the verification report; do not broaden into unrelated asset-injection or prompt-generation cases.</action>
  <acceptance_criteria>
    - `rg -n "preserves saved editable metadata when multi-shot drafts are rebuilt" tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` finds the new regression.
    - The test asserts both preservation of editable metadata and refresh of regenerated segment fields.
    - The automated verify command passes without needing unrelated workspace suites.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts</automated>
  </verify>
  <done>The API test suite fails if draft rebuilds ever start dropping saved editable metadata again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted shot-group metadata→draft rebuild route | saved user-edited metadata is read back into server-side rebuild logic |
| route response→workspace state | rebuilt snapshot becomes the source for later review/video editing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T | `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts` | mitigate | Merge only through parsed/normalized prior draft metadata and keep auth/project scoping unchanged. |
| T-03-11 | D | `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts` | mitigate | Preserve editable metadata while still refreshing regenerated segment fields so rebuild stays usable instead of freezing stale drafts. |
| T-03-12 | R | `tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` | mitigate | Lock the preservation contract with a targeted rebuild regression test covering the exact metadata classes from WR-01. |
</threat_model>

<verification>
Run the targeted multi-shot drafts integration suite and confirm the new rebuild-preservation case passes alongside the existing segment-count and reuse cases.
</verification>

<success_criteria>
- Rebuilding multi-shot drafts no longer wipes saved editable metadata on existing shot groups.
- The rebuild path still refreshes current derived segment structure and identity.
- Verification gap WR-01 is closed with automated regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editable-production-handoff/03-04-SUMMARY.md`
</output>
