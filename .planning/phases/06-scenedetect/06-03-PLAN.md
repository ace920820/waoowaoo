---
phase: 06-scenedetect
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - "src/lib/remake-projects/scenedetect/executor-client.ts"
  - "tests/fixtures/scenedetect/executor-fixture.ts"
  - "src/lib/workers/handlers/scenedetect.ts"
  - "src/lib/workers/text.worker.ts"
  - "src/lib/task/types.ts"
  - "src/lib/remake-projects/scenedetect/task-contract.ts"
  - "tests/contracts/task-type-catalog.ts"
  - "tests/contracts/tasktype-behavior-matrix.ts"
  - "tests/unit/remake-projects/scenedetect-executor.test.ts"
autonomous: true
requirements: [SHOT-02]
user_setup:
  - service: scenedetect-executor
    why: "复用现有 FastAPI + pySceneDetect/OpenCV 能力"
    env_vars:
      - { name: SCENEDETECT_EXECUTOR_BASE_URL, source: "可访问 `/api/health`、`/api/analyze`、`/api/keyframes` 的 executor 地址" }
      - { name: SCENEDETECT_EXECUTOR_TOKEN, source: "仅 Waoo worker 持有的 executor service token；不得暴露给浏览器" }
estimate: { tokens: 33000, raw_tokens: 33000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "SHOT-02：SceneDetect analyze/extract Task 经 Waoo text queue 调用现有 pySceneDetect executor，不复制检测算法。"
    - "worker 从 current Waoo source revision 读取媒体，成功响应经 06-02 importer 落库；runtime 目录只作瞬态空间。"
    - "D-06：既有 Task lifecycle、dedupe、attempt/progress/retry 继续生效；只报告 queued/source-read/executor-call/import 等真实阶段，不新增 queue 或任务表。"
  artifacts:
    - { path: "src/lib/remake-projects/scenedetect/executor-client.ts", provides: "受限 FastAPI capability client" }
    - { path: "src/lib/workers/handlers/scenedetect.ts", provides: "analyze/extract worker handler" }
    - { path: "tests/unit/remake-projects/scenedetect-executor.test.ts", provides: "错误、进度、重放和 importer 接线测试" }
  key_links:
    - { from: "text.worker.ts", to: "handlers/scenedetect.ts", via: "两个 SceneDetect Task type 分派", pattern: "SCENEDETECT_(ANALYZE|EXTRACT_KEYFRAMES)" }
    - { from: "handlers/scenedetect.ts", to: "adapter.ts", via: "result envelope 完成前导入", pattern: "commitSceneDetectImport" }
---

<objective>把真实 pySceneDetect 分析/关键帧能力接入 Waoo 既有 Task/Worker 生命周期。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/06-scenedetect/06-02-SUMMARY.md
@src/lib/task/queues.ts
@src/lib/workers/text.worker.ts
@/Volumes/KINGSTON/projects/SceneDetect/backend/main.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现受限 SceneDetect executor client</name>
  <precondition>`SCENEDETECT_EXECUTOR_BASE_URL` 的 `/api/health` 返回 `{ status: 'ok' }`</precondition>
  <files>src/lib/remake-projects/scenedetect/executor-client.ts, src/lib/remake-projects/scenedetect/task-contract.ts, tests/fixtures/scenedetect/executor-fixture.ts, tests/unit/remake-projects/scenedetect-executor.test.ts</files>
  <read_first>/Volumes/KINGSTON/projects/SceneDetect/backend/main.py, /Volumes/KINGSTON/projects/SceneDetect/backend/analyzer.py, src/lib/remake-projects/scenedetect/task-contract.ts, src/lib/storage/index.ts, src/lib/logging/core.ts</read_first>
  <behavior>
    - client 只构造固定 health/analyze/keyframes 路径，携带 server-only service token，上传 Waoo source bytes并限制连接/总超时与响应大小。
    - 网络、非 JSON、4xx/5xx、旧 source/shot revision 映射为稳定错误；用户不能指定任意 URL。
  </behavior>
  <action>先创建 tests/fixtures/scenedetect/executor-fixture.ts，按真实同步 `/api/health`、`/api/analyze`、`/api/keyframes` response shape 提供成功、timeout、4xx/5xx、非 JSON、超限和 schema-error 场景；fixture 不提供虚假 streaming/SSE/算法百分比。再实现 server-only client，只接受服务端配置 base URL/service token 与固定路径；加入 readiness health、连接/总 abort timeout、source/response 大小、JSON/schema 与错误归一化。扩展 task payload 只允许 detector threshold/frame tuple 等白名单字段，dedupe 继续由 project/source/shot revision + operationKey 组成。浏览器 payload、日志和 Task projection 均不得包含 executor URL/token。</action>
  <acceptance_criteria>
    - client 只能请求配置 base URL 下的 `/api/health`、`/api/analyze`、`/api/keyframes`，用户输入不能覆盖 host/path/header token。
    - timeout、超限 source/response、非 JSON、4xx/5xx 与 schema error 均映射为稳定错误且日志不含 token/media bytes。
    - task payload schema 只接受 threshold 或 frame tuple/current revision 白名单字段。
    - executor fixture 的字段与 SceneDetect `backend/main.py` 同步 HTTP 合同一致，且测试不依赖本机 executor 服务。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/remake-projects/scenedetect-executor.test.ts tests/unit/remake-projects/scenedetect-task-contract.test.ts</automated></verify>
  <done>Waoo Worker 能安全调用现有 executor，客户端无法把它变成通用请求代理。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 text worker lifecycle 与版本 importer</name>
  <files>src/lib/workers/handlers/scenedetect.ts, src/lib/workers/text.worker.ts, src/lib/task/types.ts, src/lib/remake-projects/scenedetect/task-contract.ts, tests/contracts/task-type-catalog.ts, tests/contracts/tasktype-behavior-matrix.ts, tests/unit/remake-projects/scenedetect-executor.test.ts</files>
  <read_first>src/lib/workers/text.worker.ts, src/lib/workers/shared.ts, src/lib/task/types.ts, src/lib/task/queues.ts, tests/contracts/task-type-catalog.ts, tests/contracts/tasktype-behavior-matrix.ts</read_first>
  <behavior>
    - analyze handler 读取 current storage object，只发布 queued/source-read/executor-call/import/completed 等真实阶段；同步 executor 内部不伪造百分比或 ETA。
    - 重试沿用 operationKey；Task result 只含 Waoo analysis/revision/media IDs，不含 runtime URL。
  </behavior>
  <action>实现 handler 对 scenedetect_analyze 和 scenedetect_extract_keyframes 的分派：验证 Task user/project/current revision，读取 storage buffer，调用 executor，把可观测阶段映射到 reportTaskProgress；executor-call 使用 indeterminate/stage-only 语义，响应后才进入 import/completed。再经 06-02 envelope/media/importer 提交。更新 TASK_TYPE、text worker switch、task-type catalog 和 behavior matrix，使 coverage guard 识别两个类型；不增加 queue、GraphRun lifecycle 或复制 Python detector。</action>
  <acceptance_criteria>
    - `scenedetect_analyze` 与 `scenedetect_extract_keyframes` 均进入既有 text queue/withTaskLifecycle，未知类型行为不变。
    - 事件只报告 queued/source-read/executor-call/import/completed/failed/waiting_retry 等真实阶段；没有算法百分比或 ETA。
    - stale source/shot revision 的结果不更新 current state，Task result 不含 executor URL、token 或 runtime media URL。
    - `npm run check:test-tasktype-coverage` 退出码为 0。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/remake-projects/scenedetect-executor.test.ts &amp;&amp; npm run check:test-tasktype-coverage</automated></verify>
  <done>真实算法执行、进度、重试与结果导入全部位于 Waoo 既有 Worker 生命周期。</done>
</task>
</tasks>

## Artifacts this phase produces

- `SCENEDETECT_EXECUTOR_BASE_URL` / `SCENEDETECT_EXECUTOR_TOKEN` server-only capability client.
- `scenedetect_analyze` and `scenedetect_extract_keyframes` Task type catalog entries and text worker dispatch.
- SceneDetect worker handler with truthful stage-only progress and importer handoff.
- Executor/task contract fixtures and behavior-matrix coverage.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-07 | SSRF | executor client | high | mitigate | 服务端 base URL、固定路径、超时/大小限制 |
| T-06-08 | Tampering | worker result | high | mitigate | envelope/sourceRevision 校验与 importer 事务 |
| T-06-09 | Denial of Service | executor/queue | medium | mitigate | existing concurrency/dedupe/attempts、媒体与响应上限 |
</threat_model>
<verification>executor/worker focused suite 与 task-type coverage 通过；本计划不修改 API/task projection 或 UI。</verification>
<success_criteria>SceneDetect 作为可替换 capability 执行，Waoo 仍是唯一 Task、存储和结果事实来源。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-03-SUMMARY.md`。</output>
