---
phase: 06-scenedetect
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - "src/lib/remake-projects/scenedetect/result-envelope.ts"
  - "src/lib/remake-projects/scenedetect/contracts.ts"
  - "tests/unit/remake-projects/scenedetect-result-envelope.test.ts"
  - "src/lib/remake-projects/scenedetect/adapter.ts"
  - "src/lib/remake-projects/scenedetect/media.ts"
  - "src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts"
  - "tests/integration/api/remake-projects-scenedetect-import.test.ts"
  - "src/lib/remake-projects/service.ts"
  - "tests/contracts/remake-scenedetect-native-contract.test.ts"
autonomous: true
requirements: [SHOT-03]
estimate: { tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "SHOT-03：SceneDetect 输出经显式版本 envelope 校验后，以 Waoo UUID、不可变 revision 和 provenance 导入。"
    - "D-03/D-06：shotNumber、analysisId、数组位置和 executor 文件名只作为外部 provenance，不成为 Shot 主键。"
    - "executor runtime media 由受限 worker client 读取为 bytes 后复制到 Waoo storage；旧 schemaVersion 2 JSON 只有具名 migration 路径。"
  artifacts:
    - { path: "src/lib/remake-projects/scenedetect/result-envelope.ts", provides: "result/adapter/executor/source 版本合同" }
    - { path: "src/lib/remake-projects/scenedetect/adapter.ts", provides: "幂等稳定身份 importer" }
    - { path: "src/lib/remake-projects/service.ts", provides: "可重建 native project 的 snapshot" }
  key_links:
    - { from: "result-envelope.ts", to: "contracts.ts", via: "先校验 envelope 再解析 native payload", pattern: "parseSceneDetectInput" }
    - { from: "adapter.ts", to: "RemakeShotRevision/Provenance", via: "sourceRevision + operationKey 事务写入", pattern: "\$transaction" }
---

<objective>建立版本化 SceneDetect result/import 边界，并保证 native project 可完全从 Waoo 事实重建。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/06-scenedetect/06-01-SUMMARY.md
@src/lib/remake-projects/scenedetect/contracts.ts
@src/lib/remake-projects/scenedetect/adapter.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 定义严格版本 envelope 与 legacy migration</name>
  <files>src/lib/remake-projects/scenedetect/result-envelope.ts, src/lib/remake-projects/scenedetect/contracts.ts, tests/unit/remake-projects/scenedetect-result-envelope.test.ts</files>
  <read_first>src/lib/remake-projects/scenedetect/contracts.ts, src/vendor/scenedetect/types.ts, src/vendor/scenedetect/utils/projectStore.ts, /Volumes/KINGSTON/projects/SceneDetect/backend/analyzer.py</read_first>
  <behavior>
    - 合法 envelope 含 resultVersion、adapterVersion、executorVersion、analysisId、sourceRevision、operationKey 与 schemaVersion 2 payload。
    - 旧 sourceRevision、未知 major version、重复/越界 frame、非法 metadata/Shot 排序被拒绝。
    - standalone JSON 仅通过 `legacy_json_import` wrapper 进入 provenance。
  </behavior>
  <action>新增 Zod envelope parser，并复用 canonical native字段；校验 source/analysis/frame/time/duration/Shot ID/排序/keyframe bounds。版本不兼容返回具名错误。保留旧 schemaVersion 2 parser，但必须明确 legacy mode，不能伪装 executor result。</action>
  <acceptance_criteria>
    - parser 接受带 resultVersion/adapterVersion/executorVersion/analysisId/sourceRevision/operationKey 的合法 schemaVersion 2 payload。
    - 未知 major、旧 source、重复/越界 frame、非法 metadata/Shot 排序均返回稳定错误码且不会调用 importer。
    - standalone JSON 只有 `legacy_json_import` provenance 路径，不能伪装 executor result。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/remake-projects/scenedetect-result-envelope.test.ts tests/unit/remake-projects/scenedetect-adapter.test.ts</automated></verify>
  <done>过期、损坏或来源不明的 payload 无法到达 DB 写路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 归一化媒体并事务导入稳定 Shot revisions</name>
  <files>src/lib/remake-projects/scenedetect/adapter.ts, src/lib/remake-projects/scenedetect/media.ts, src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts, tests/integration/api/remake-projects-scenedetect-import.test.ts</files>
  <read_first>src/lib/remake-projects/scenedetect/adapter.ts, src/lib/remake-projects/scenedetect/media.ts, src/lib/storage/index.ts, prisma/schema.prisma, src/lib/api-auth.ts</read_first>
  <behavior>
    - 首次导入复制 source/三帧到 Waoo storage，创建 UUID/revision/provenance；重放不重复。
    - 冲突 sourceRevision 返回 409；任意客户端 URL、runtime/blob/data URL 不成为持久化事实。
  </behavior>
  <action>让 importer 接收已验证 envelope，在事务中锁定 current sourceRevision、检查 replay、创建/映射 Waoo UUID、追加 revision/provenance并更新 import status。持久化媒体只接受 06-03 固定 executor client 已验证并受响应上限约束的 worker bytes，禁止 importer 接收客户端 URL 或通用远程抓取地址；校验 MIME/字节数后写 Waoo storage。preview 不写库；路由使用项目所有权并保留旧 revision。</action>
  <acceptance_criteria>
    - 相同 sourceRevision/operationKey 重放不重复创建 Shot、revision、media 或 provenance。
    - importer API 拒绝客户端 URL、blob/data URL、executor runtime URL、跨项目 ID 和旧 sourceRevision。
    - 成功导入后 DB 只含 Waoo UUID/media refs，旧 revision 仍可审计。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-import.test.ts</automated></verify>
  <done>结果可幂等导入为 Waoo media refs、stable Shot revisions 和 provenance。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 从 Waoo snapshot 重建 canonical native project</name>
  <files>src/lib/remake-projects/service.ts, src/lib/remake-projects/scenedetect/contracts.ts, tests/contracts/remake-scenedetect-native-contract.test.ts</files>
  <read_first>src/lib/remake-projects/service.ts, src/lib/remake-projects/scenedetect/contracts.ts, src/vendor/scenedetect/utils/projectStore.ts, prisma/schema.prisma</read_first>
  <behavior>
    - snapshot 包含 current source metadata/revision、active shots latest revision 与 history/provenance 分层。
    - native Shot.id 使用 Waoo UUID，media 字段只暴露 opaque mediaId；externalIdentity 和 raw storage key 不成为 editor ID/URL。
  </behavior>
  <action>扩展 snapshot 与 toSceneDetectProject，只把 active latest revisions 投影进 editor，保留 history/provenance 供审计。native project 通过 opaque mediaId 表达 source/三帧引用，最终同源播放 URL 由 06-04 的项目授权 resolver 生成；数组下标、externalIdentity 和 storage key 不直接暴露为主键或公共 URL。</action>
  <acceptance_criteria>
    - 刷新后只凭 DB/storage snapshot 可重建同一 source metadata、active Shot 顺序、三帧选择和审核状态。
    - native Shot.id 等于稳定 Waoo UUID；响应不含 raw storage key、externalIdentity 主键或 runtime URL。
    - retired revisions 不进入 editor current shots，但 history/provenance 仍在服务端 snapshot 中可审计。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/integration/api/remake-projects-workbench.test.ts</automated></verify>
  <done>刷新或服务重启后可仅凭 Waoo snapshot 恢复同一 native project。</done>
</task>
</tasks>

## Artifacts this phase produces

- Versioned SceneDetect executor/import envelope and named legacy import wrapper.
- Transactional `commitSceneDetectImport` behavior with Waoo UUID/media/provenance mapping.
- Snapshot-to-native `SceneDetectProject` projection using active revisions and opaque media IDs.
- Envelope, importer and native round-trip regression suites.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-04 | Tampering | envelope/import | high | mitigate | schema/frame/sourceRevision 校验与事务写入 |
| T-06-05 | Spoofing | external identity/media | high | mitigate | 服务端 UUID、URL allowlist、DNS 私网阻断 |
| T-06-06 | Information Disclosure | runtime media | high | mitigate | storage-normalize 后只保存 media ref |
</threat_model>
<verification>envelope、import 与 native round-trip focused suites 全部通过。</verification>
<success_criteria>SceneDetect 分析结果版本、身份、媒体和来源全部可追溯且由 Waoo 恢复。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-02-SUMMARY.md`。</output>
