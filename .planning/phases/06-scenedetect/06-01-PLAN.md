---
phase: 06-scenedetect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "prisma/schema.prisma"
  - "prisma/migrations/20260807190000_add_remake_source_ingest/migration.sql"
  - "src/lib/remake-projects/scenedetect/source.ts"
  - "src/lib/remake-projects/scenedetect/video-probe.ts"
  - "src/app/api/remake-projects/[projectId]/source/route.ts"
  - "tests/integration/api/remake-projects-scenedetect-source.test.ts"
autonomous: true
requirements: [SHOT-01]
estimate: { tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "SHOT-01/D-03：已认证用户通过原 App 上传入口提交合法视频，并获得持久化 source revision、服务端 probe metadata 与 uploaded_pending 状态。"
    - "D-04：浏览器 File/Blob、浏览器 probe 和 React state 不是恢复依据；RemakeSource、对象存储和既有 Task 合同是唯一事实来源。"
    - "重复 operation key 幂等；新视频递增 sourceRevision，旧 source 不再被后续分析误用且不生成伪 Shot。"
    - "D-07/D-10：首个 migration 同时建立后续 mutation 所需的显式 source history、Shot current revision、revision retired 状态、关键帧 tuple/media refs、Task 关联和并发版本基础；06-06 不再追加重复 schema migration。"
  artifacts:
    - { path: "src/lib/remake-projects/scenedetect/source.ts", provides: "项目作用域 source revision 与媒体写入服务" }
    - { path: "src/lib/remake-projects/scenedetect/video-probe.ts", provides: "受限 ffprobe 服务端元数据探测" }
    - { path: "src/app/api/remake-projects/[projectId]/source/route.ts", provides: "鉴权、multipart 校验和上传 API" }
    - { path: "tests/integration/api/remake-projects-scenedetect-source.test.ts", provides: "上传/重放/越权/限额回归" }
  key_links:
    - { from: "source/route.ts", to: "source.ts", via: "requireProjectAuthLight 后提交受验证 multipart", pattern: "requireProjectAuthLight" }
    - { from: "source.ts", to: "RemakeSource + object storage", via: "事务切换 current sourceRevision", pattern: "uploadObject|sourceRevision" }
---

<objective>
用生产 tracer 贯通原视频 ingest：Waoo API、对象存储、数据库 source revision 与可恢复上传/探测状态。
<purpose>先证明真实媒体能安全进入 Waoo 唯一事实来源，再由后续计划接入 canonical App 和 pySceneDetect Task。</purpose>
<output>source migration、ingest service、上传 API 和 focused integration tests。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@prisma/schema.prisma
@src/lib/remake-projects/scenedetect/media.ts
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: 贯通 source upload、对象存储与持久化 revision</name>
  <precondition>部署环境可执行 ffprobe；上传上限通过 SceneDetect source 专用配置提供，不复用 20 MiB 图片媒体限制。</precondition>
  <files>prisma/schema.prisma, prisma/migrations/20260807190000_add_remake_source_ingest/migration.sql, src/lib/remake-projects/scenedetect/source.ts, src/lib/remake-projects/scenedetect/video-probe.ts, src/app/api/remake-projects/[projectId]/source/route.ts, tests/integration/api/remake-projects-scenedetect-source.test.ts</files>
  <read_first>prisma/schema.prisma, src/lib/storage/index.ts, src/lib/remake-projects/scenedetect/media.ts, src/lib/api-auth.ts, .planning/phases/06-scenedetect/06-CONTEXT.md, .planning/phases/06-scenedetect/06-RESEARCH.md</read_first>
  <behavior>
    - 合法 MP4/WebM/MOV/M4V 在 source 专用大小上限内写入项目作用域 storage key，sourceRevision 从 0 递增并保存服务端探测元数据/状态。
    - 同 operation key 返回既有 revision；新文件生成下一 revision。
    - 越权、空/超限文件、MIME/扩展/文件头不一致或非法 probe 数值无写入副作用。
  </behavior>
  <action>先写失败集成测试，再在同一个 migration 中完成 Phase 6 的 revision 基础：RemakeProject 使用显式 current source 关联并保留 source history；RemakeSource 增加 sourceRevision、operationKey、fileName、contentType、byteSize、probeMetadata、storage media reference 与唯一索引；RemakeShot 增加 currentRevision/version；RemakeShotRevision 增加 lifecycleState(active/retired)、sourceRevision、keyframeFrames、keyframeMediaRefs、analysisTaskId、keyframeTaskId 与必要索引。旧 Phase 5 row backfill 为 sourceRevision=1/current revision=既有最大 revision，无法证明 current 的数据保持 pending/needs_review，禁止运行时通过 JSON 搜索猜测。实现项目作用域 source POST：使用 requireProjectAuthLight，解析 multipart file、operation key 与浏览器 provisional metadata；以独立于图片 media helper 的可配置 source 上限校验文件头/MIME/扩展/大小，写入对象存储后用受限 ffprobe 读取 duration/fps/width/height/totalFrames，浏览器值只用于上传期间预览且不能覆盖服务端结果。服务端生成不可预测 storage key并在事务中切换 current source。不得生成 Shot、接受客户端 storage key、持久化 Blob URL 或直接启动 SceneDetect 进程。</action>
  <acceptance_criteria>
    - migration 可从 Phase 5 schema 前滚，并显式保留 source/revision history；Prisma schema 不再以 RemakeSource.remakeProjectId 唯一约束表达“只有一个 source”。
    - POST source 对合法 MP4/WebM/MOV/M4V 返回 current sourceRevision 和服务端 metadata；非法/越权/超限请求不改变 current source。
    - `BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-source.test.ts` 退出码为 0。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-source.test.ts</automated></verify>
  <done>一个已认证请求可把真实视频写入 Waoo storage 并形成幂等、可恢复的 current source revision。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定替换 source 与失败恢复不变式</name>
  <files>src/lib/remake-projects/scenedetect/source.ts, src/app/api/remake-projects/[projectId]/source/route.ts, tests/integration/api/remake-projects-scenedetect-source.test.ts</files>
  <read_first>src/lib/remake-projects/scenedetect/source.ts, src/app/api/remake-projects/[projectId]/source/route.ts, prisma/schema.prisma, tests/integration/api/remake-projects-scenedetect-source.test.ts</read_first>
  <behavior>
    - storage 成功/DB 失败不切换 current source，错误可重试且不会出现不可识别重复对象。
    - 新 source 使旧 analysis task/result 按 sourceRevision 失效，但保留审计记录。
  </behavior>
  <action>补齐上传补偿和 source replacement 测试：DB 事务失败时不暴露半成品 current revision；新 sourceRevision 只改变 source/import 状态，不物理删除旧 task/provenance/Shot history。返回稳定错误码与可操作错误摘要，不暴露 storage credentials、绝对路径或对象内部错误。</action>
  <acceptance_criteria>
    - 相同 operationKey 不产生第二个对象或 source revision；不同合法 replacement 只切换一个 current source。
    - storage 成功后 DB 失败不会暴露新 current source，旧 source、Shot、Task 和 provenance 仍可审计。
    - source replacement 后，旧 sourceRevision 的 worker result 无法写入 current Shot state。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-source.test.ts</automated></verify>
  <done>上传失败、重放和替换 source 均保持单一 current revision 与完整审计链。</done>
</task>
</tasks>

## Artifacts this phase produces

- Prisma fields/relations/indexes for source history, current Shot revision, retired revisions, frame/media tuples, Task association and optimistic versioning.
- `src/lib/remake-projects/scenedetect/source.ts`: source ingest/replacement service and operation replay contract.
- `src/lib/remake-projects/scenedetect/video-probe.ts`: bounded server-side ffprobe adapter.
- `POST src/app/api/remake-projects/[projectId]/source/route.ts`.
- `tests/integration/api/remake-projects-scenedetect-source.test.ts`.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-01 | Tampering | source upload | high | mitigate | 项目授权、文件头/MIME/扩展/大小校验、服务端 storage key、事务 revision |
| T-06-02 | Denial of Service | multipart/storage | high | mitigate | 请求/文件上限、拒绝空文件、失败补偿和 operation dedupe |
| T-06-03 | Information Disclosure | media refs | medium | mitigate | DB 只存 media ref/storage key，API 不返回凭据或绝对路径 |
</threat_model>

<verification>focused source API suite 通过；migration 与 Prisma schema 字段一致；本计划不修改 vendor/App/runtime/StageHost。</verification>
<success_criteria>原视频 ingest 成为独立、可执行且可恢复的 Waoo 纵向切片。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-01-SUMMARY.md`。</output>
