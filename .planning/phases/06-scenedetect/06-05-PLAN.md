---
phase: 06-scenedetect
plan: 05
type: execute
wave: 5
depends_on: [06-01, 06-03, 06-04]
files_modified:
  - "src/vendor/scenedetect/App.tsx"
  - "src/vendor/scenedetect/components/Header.tsx"
  - "src/vendor/scenedetect/VENDOR.json"
  - "scripts/vendor-scenedetect.mjs"
  - "scripts/vendor-scenedetect-patches/embedded-runtime.patch"
  - "eslint.config.mjs"
  - "src/lib/remake-projects/scenedetect/integration-runtime.ts"
  - "src/lib/remake-projects/scenedetect/runtime-client.ts"
  - "tests/contracts/remake-scenedetect-native-contract.test.ts"
  - "tests/guards/remake-scenedetect-vendor-provenance.test.ts"
autonomous: true
requirements: [SHOT-05]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "SHOT-05/D-01：App.tsx 原有 undo/redo、边界调整、拆分/合并/删除和 ShotInspector/Timeline/ShotList 编排原样保留。"
    - "D-03/D-05：无 source 也挂载完整 App；runtime 具有 upload、canonical load/save + token/remap、media、analyze/keyframe tuple Task 和 terminal reload 等 native typed ports。"
    - "D-12：registered vendor patch 可由 sync 确定性重放并校验 upstream/vendored digest；允许 AnalysisModal 仅把进度来源映射为真实 Task stage/indeterminate，不改视觉编排、不伪造百分比或 ETA；Waoo lint policy 不迫使复制或重写 vendor UI。"
  artifacts:
    - { path: "src/vendor/scenedetect/App.tsx", provides: "保留原状态机的 embedded runtime 分支" }
    - { path: "scripts/vendor-scenedetect.mjs", provides: "registered patch replay 与双 hash 校验" }
    - { path: "src/lib/remake-projects/scenedetect/integration-runtime.ts", provides: "native persistence/media/task/project/export 合同" }
    - { path: "src/lib/remake-projects/scenedetect/runtime-client.ts", provides: "Waoo API/Task 到 App callback 的实现" }
  key_links:
    - { from: "App.tsx", to: "SceneDetectIntegrationRuntime", via: "原 handler 内的 embedded side-effect branch", pattern: "runtime.*submitAnalyze|saveProject" }
    - { from: "runtime-client.ts", to: "06-04 APIs", via: "apiFetch + task polling + native reload", pattern: "scenedetect/(project|analyze)" }
---

<objective>给 canonical SceneDetect App 注入真实 Waoo runtime，同时完整保留其编辑状态机与交互编排。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/05-remake-project-core-workbench/05-04-PLAN.md
@.planning/phases/06-scenedetect/06-04-SUMMARY.md
@src/vendor/scenedetect/App.tsx
@src/lib/remake-projects/scenedetect/integration-runtime.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 先建立可重放 vendor patch 与受控 lint 基线</name>
  <files>scripts/vendor-scenedetect.mjs, scripts/vendor-scenedetect-patches/embedded-runtime.patch, eslint.config.mjs, src/vendor/scenedetect/VENDOR.json, tests/guards/remake-scenedetect-vendor-provenance.test.ts</files>
  <read_first>scripts/vendor-scenedetect.mjs, eslint.config.mjs, src/vendor/scenedetect/VENDOR.json, package.json, src/vendor/scenedetect/components/ShotInspector.tsx, .planning/phases/06-scenedetect/06-RESEARCH.md</read_first>
  <behavior>
    - VENDOR 对每个文件同时记录 upstream digest 与 vendored digest；未 patch 文件必须完全相同，patched 文件必须匹配登记结果。
    - sync 从干净 upstream closure 复制后确定性重放具名 patch；check 可发现未登记修改、patch 漂移和 upstream 漂移。
    - canonical vendor 的 generic ESLint traversal 被目录级隔离，覆盖已验证的 restricted lucide import、conditional Hook 和 JSX escape upstream failures；所有非 vendor host/runtime 与 patch tooling 继续完整 lint。
    - TypeScript、registered patch digest/replay、native contract、duplicate-source/import graph guards 共同覆盖被 ESLint 隔离的 vendor integration seam。
  </behavior>
  <action>先升级 vendor script/manifest schema：保留 source commit 与 upstream hash，增加 vendored hash、patch ID、说明和 patch 文件；sync 清理后复制 closure并重放 patch，check 分别验证未 patch/已 patch 文件。增加 focused provenance tests 覆盖 clean sync、registered patch、tamper 和 upstream drift。`eslint.config.mjs` 对 `src/vendor/scenedetect/**` 使用精确目录 ignore，完整隔离已验证的上游 restricted lucide import、conditional Hook 与 JSX escape errors；不得添加全局 rule-off，也不得排除 `src/lib/remake-projects/scenedetect/**`、StageHost、patch script 或 tests。由 `npm run typecheck`、vendor digest/replay test、native runtime contract 和 no-deep-import/no-duplicate guard 补足 vendor integration seam 的静态门禁，确保 `npm run verify:commit` 不会被既有 vendor baseline 永久阻断。</action>
  <acceptance_criteria>
    - clean sync 后 registered patch 可确定性重放，`--check` 对 tamper、未登记 patch 和 upstream drift 均失败。
    - ESLint ignore 只匹配 `src/vendor/scenedetect/**`；Waoo runtime、host、scripts 和 tests 仍在 lint 范围。
    - `npm run lint`、`npm run typecheck`、vendor provenance test 与 vendor check 全部退出码为 0。
  </acceptance_criteria>
  <verify><automated>node scripts/vendor-scenedetect.mjs --sync &amp;&amp; node scripts/vendor-scenedetect.mjs --check &amp;&amp; npx vitest run tests/guards/remake-scenedetect-vendor-provenance.test.ts &amp;&amp; npm run lint &amp;&amp; npm run typecheck</automated></verify>
  <done>任何后续 App/Header patch 都能从 upstream 确定性重放、审计和校验，且 Waoo commit gate 不要求重写 vendor UI。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 注入完整 native runtime 并保留 App 状态机</name>
  <files>src/vendor/scenedetect/App.tsx, src/vendor/scenedetect/components/Header.tsx, src/vendor/scenedetect/VENDOR.json, scripts/vendor-scenedetect-patches/embedded-runtime.patch, src/lib/remake-projects/scenedetect/integration-runtime.ts, src/lib/remake-projects/scenedetect/runtime-client.ts, tests/contracts/remake-scenedetect-native-contract.test.ts</files>
  <read_first>src/vendor/scenedetect/App.tsx, src/vendor/scenedetect/components/Header.tsx, src/vendor/scenedetect/utils/projectStore.ts, src/vendor/scenedetect/utils/sceneDetector.ts, src/lib/remake-projects/scenedetect/integration-runtime.ts, src/lib/remake-projects/scenedetect/runtime-client.ts</read_first>
  <behavior>
    - 无 source 时 embedded mount 使用显式 empty state显示原上传入口，并跳过 sample、recent project 和 IndexedDB draft restore。
    - uploadSource 返回 Waoo source revision/native state；saveProject 返回 canonical project、opaque token 与 ID remap；keyframe submit携带保存后的 shotRevision/frame tuple。
    - queued/source-read/executor-call/import 等真实 Task 阶段驱动原 AnalysisModal；同步 executor-call 不显示伪百分比/ETA，终态 reload native project。
    - embedded Header 隐藏 sample/project manager，export 不可触发；undo/redo、adjust/split/merge/delete/batch status仍调用原 handler。
  </behavior>
  <action>扩展 runtime 的 uploadSource、loadProject、canonical save result/token/remap、resolveMedia、submitAnalyze、submitExtractKeyframes(frame tuple/current revision)、onTaskUpdate、project/export ports，参数/结果只使用 SceneDetectProject/Shot/VideoMetadata 及 opaque integration metadata。在 App 内以 embedded 分支替换副作用，保留 state、undo/redo stacks、所有 Shot handler 与 JSX编排；无 source 不回落到 sample/IndexedDB。Task callback 只映射 queued/source-read/executor-call/import/completed/failed/canceled 等真实阶段，failed 保留 source 和可重试状态；若原 AnalysisModal 不能表达 stage text + indeterminate，则使用 D-12 允许的最小 registered patch 替换进度来源，不改变 modal 视觉/焦点/关闭编排，不显示算法百分比或 ETA。Header 只增加最小 embedded policy props，App 不挂载 ProjectManager/ExportModal 副作用；所有差异写入具名 patch并更新 vendored digest。</action>
  <acceptance_criteria>
    - empty embedded state 显示 canonical 上传入口，且不调用 sample、listRecentProjects、loadDraft 或 standalone JSON API。
    - runtime typed ports 包含 uploadSource、canonical save result/token/remap、frame tuple/current revision Task input 和 terminal reload；类型来自 vendor native exports。
    - undo/redo、adjust/split/merge/delete、KeyframeSelector 和 batch status handler 仍由同一个 App.tsx 编排；没有 Waoo 复制实现。
    - queued/source-read/executor-call/import/terminal 状态可映射到原 modal；executor-call 为 indeterminate 且无伪 ETA。
    - AnalysisModal 的任何差异都登记为具名 patch，且只涉及真实 Task stage/indeterminate 数据源；DOM 视觉层级、焦点和命令保持原样。
  </acceptance_criteria>
  <verify><automated>node scripts/vendor-scenedetect.mjs --check &amp;&amp; npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/unit/remake-projects/remake-workbench-contract.test.tsx tests/guards/remake-scenedetect-vendor-provenance.test.ts</automated></verify>
  <done>完整 App 在空/有 source 两种 embedded 状态使用 Waoo runtime，原编辑状态机未被提取或重写。</done>
</task>
</tasks>

## Artifacts this phase produces

- Registered patch manifest fields for upstream/vendored digests, patch IDs and deterministic replay.
- `SceneDetectIntegrationRuntime` ports for source upload, native load/save/remap, media resolution and Task lifecycle.
- `runtime-client.ts` implementation of Waoo API/Task callbacks and terminal native reload.
- Minimal embedded props/branches in canonical `App.tsx`/`Header.tsx`; no replacement editor components.
- Vendor provenance and native contract regression guards.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-13 | Tampering | embedded save/analyze | high | mitigate | server ownership/version/schema 校验；客户端 project 不可信 |
| T-06-14 | Spoofing | task subscription | medium | mitigate | taskId 绑定同 user/project |
| T-06-15 | Denial of Service | autosave/polling | medium | mitigate | debounce、单飞行、终态停止与 unmount abort |
</threat_model>
<verification>native/vendor focused suites 通过；本计划不修改 StageHost、CSS、Waoo shell 或 E2E。</verification>
<success_criteria>完整 App 保留编辑能力，只把 Waoo 集成副作用替换为 native runtime。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-05-SUMMARY.md`。</output>
