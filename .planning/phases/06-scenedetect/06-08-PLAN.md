---
phase: 06-scenedetect
plan: 08
type: execute
wave: 8
depends_on: [06-04, 06-05, 06-06, 06-07]
files_modified:
  - "src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx"
  - "src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css"
  - "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx"
  - "tests/unit/remake-projects/remake-scenedetect-upload-host.test.tsx"
  - "tests/unit/remake-projects/remake-scenedetect-runtime.test.tsx"
  - "tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
  - "tests/guards/remake-scenedetect-phase6-compatibility.test.ts"
  - "tests/e2e/remake-scenedetect-review.spec.ts"
autonomous: true
requirements: [SHOT-04, SHOT-07]
estimate: { tokens: 39000, raw_tokens: 39000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "SHOT-07：真实 canonical App 中的单项/批量 keep 通过 06-07 gate 持久化确认，未确认/需复核 Shot 明确不能进入 Prompt。"
    - "StageHost 只从 canonical index 挂载完整 App；Waoo 不出现第二套播放器、Timeline、ShotInspector、KeyframeSelector 或 editor state。"
    - "D-11：SceneDetect 深色主题、尺寸、布局和交互层级在 scoped stage 内保留；Waoo 浅色 glass/Task drawer 只在项目栏、阶段导航和 overlay，不以常驻侧栏压缩或重排编辑器，三档视口无横向溢出或 modal 越界。"
    - "E2E 使用真实 route/App，不使用 `page.setContent` 仿造 editor；compatibility guard 锁定 VENDOR patches 和唯一事实来源。"
    - "D-02/D-13：视频分析/镜头审核导航复用同一 App 实例；所有 fixed modal/preview 使用明确 portal container，而不是假设 isolation: isolate 自动完成 containment。"
    - "UI empty/loading/error：空 source、上传/分析/提取/保存、失败/canceled 都在 canonical App/host 中有明确可恢复状态，不出现 sample、空白 stage 或无限 spinner。"
    - "UI partial/populated/zero-one-many：问题 Shot 可保持 pending 且其他 Shot 可继续；0/1/多 Shot 与 Task 均有可达命令和滚动容器。"
    - "UI stale/conflict/review-invalidated：过期回调不覆盖 current，409 要求 reload，approved Shot 修改后立即 needs_review。"
    - "UI focus/accessibility/destructive：drawer 恢复焦点，状态有文字/图标/颜色，source replacement 明示历史与复核影响并确认。"
    - statement: "UI overflow：1440x900、1024x768、390x844 无页面横向溢出，stage/modal/drawer 命令可达。"
      verification: backstop
    - statement: "UI long-text：长文件名、项目名、Task ID 和错误文本不会遮挡命令或图标。"
      verification: backstop
    - statement: "UI media：视频可 seek，canvas 像素非空且无 taint，关键帧实际渲染。"
      verification: backstop
    - statement: "UI visual-isolation：浅色 Waoo shell 与深色 SceneDetect stage 的样式、层级和 portal 不互相泄漏。"
      verification: backstop
  artifacts:
    - { path: "SceneDetectStageHost.tsx", provides: "canonical App production mount" }
    - { path: "scenedetect-stage.css", provides: "height/overflow/modal/style containment" }
    - { path: "tests/e2e/remake-scenedetect-review.spec.ts", provides: "完整审核闭环三视口证据" }
    - { path: "tests/guards/remake-scenedetect-phase6-compatibility.test.ts", provides: "防重建/防第二事实来源门禁" }
  key_links:
    - { from: "SceneDetectStageHost.tsx", to: "src/vendor/scenedetect/index.ts", via: "唯一 complete-App import", pattern: "from '@/vendor/scenedetect'" }
    - { from: "RemakeWorkbench.tsx", to: "SceneDetectStageHost.tsx", via: "全宽 stage + overlay Task drawer", pattern: "SceneDetectStageHost" }
---

<objective>最终挂载完整 SceneDetect production stage，并以真实 E2E/compatibility guards 验证上传、分析、编辑、关键帧和确认闭环。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/05-remake-project-core-workbench/05-UI-SPEC.md
@.planning/phases/06-scenedetect/06-05-SUMMARY.md
@.planning/phases/06-scenedetect/06-06-SUMMARY.md
@.planning/phases/06-scenedetect/06-07-SUMMARY.md
@src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 挂载 production App 并完成 scoped host/Task overlay</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, tests/unit/remake-projects/remake-scenedetect-upload-host.test.tsx, tests/unit/remake-projects/remake-scenedetect-runtime.test.tsx</files>
  <read_first>src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, src/vendor/scenedetect/index.ts, src/vendor/scenedetect/App.tsx, .planning/phases/06-scenedetect/06-UI-SPEC.md</read_first>
  <behavior>
    - 无 source 时完整 App 显示原上传入口并禁用 sample/IndexedDB restore；有 source 时传 projectId/native project/runtime并恢复状态。
    - host 占满可用高度；modal/preview 显式 portal 到 host container；Task drawer overlay且焦点恢复。
    - 视频分析与镜头审核导航不卸载/重建 App，选中 Shot、undo/redo 和播放位置保持连续。
    - DOM/import graph 只有 vendor App 的 editor 编排。
  </behavior>
  <action>StageHost 构造 runtime、加载 native/empty project并始终挂载 canonical SceneDetectEmbeddedApp；loading/empty/task error 不创建第二套 editor。视频分析/镜头审核只切换同一实例的 host context/focus，不用 key 触发重建。CSS 只处理 embedded root height/overflow、dark scope与z-index；通过显式 host portal container 将 AnalysisModal、Project/Export disabled boundary、preview overlay 限定在 stage，不把 isolation 当成 containment。RemakeWorkbench 只保留项目栏、阶段导航、非侵入状态提示和 overlay Task drawer，不新增常驻列或 glass 覆盖 vendor 主体。</action>
  <acceptance_criteria>
    - empty 与 populated 项目都从 canonical index 挂载同一个 SceneDetectEmbeddedApp；分析/审核导航不改变实例 identity。
    - host 只负责 runtime/container/height/overflow/overlay/focus，DOM 中没有第二套 player/timeline/inspector/list。
    - AnalysisModal、preview 和 disabled project/export boundary portal 到 host container；Task drawer overlay 不改变 editor 宽度且关闭后恢复焦点。
    - unit suites 覆盖 empty upload、runtime mount、instance continuity 和 Waoo shell boundary。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/remake-projects/remake-scenedetect-upload-host.test.tsx tests/unit/remake-projects/remake-scenedetect-runtime.test.tsx tests/unit/remake-projects/remake-workbench-contract.test.tsx</automated></verify>
  <done>production stage 完整加载原 App，Waoo 壳与深色 editor/Task/modal 互不污染。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Phase 6 canonical/事实来源 compatibility guards</name>
  <files>tests/guards/remake-scenedetect-no-duplicate-source.test.ts, tests/guards/remake-scenedetect-phase6-compatibility.test.ts</files>
  <read_first>tests/guards/remake-scenedetect-no-duplicate-source.test.ts, tests/guards/remake-scenedetect-vendor-provenance.test.ts, scripts/vendor-scenedetect.mjs, src/vendor/scenedetect/VENDOR.json, src/vendor/scenedetect/index.ts</read_first>
  <action>扩展 AST/import graph guard：StageHost 只导入 canonical index；Waoo 非 vendor 目录不得复制/deep-import Timeline、ShotInspector、KeyframeSelector、undo stack或App orchestration。新 guard 校验 VENDOR hashes/allowedPatches、embedded 路径不以 IndexedDB/standalone project JSON/direct analyze/runtime URL为事实、项目入口隐藏、export disabled，并允许精确的 runtime/host identifiers。</action>
  <acceptance_criteria>
    - guard 对非 vendor 目录复制/deep-import editor 子组件、App orchestration 或 undo stack 的 fixture 必须失败。
    - guard 对未登记 patch、vendored hash drift、embedded IndexedDB/standalone JSON/direct analyze/runtime URL 事实来源必须失败。
    - StageHost props 与 canonical SceneDetectEmbeddedApp/native runtime 类型编译匹配。
  </acceptance_criteria>
  <verify><automated>node scripts/vendor-scenedetect.mjs --check &amp;&amp; npx vitest run tests/guards/remake-scenedetect-phase6-compatibility.test.ts tests/guards/remake-scenedetect-no-duplicate-source.test.ts tests/guards/remake-scenedetect-vendor-provenance.test.ts</automated></verify>
  <done>任何 App 拆解重建、未登记 patch 或第二事实来源都会阻断提交。</done>
</task>

<task type="auto">
  <name>Task 3: 验证真实审核闭环与三档视口</name>
  <files>tests/e2e/remake-scenedetect-review.spec.ts</files>
  <read_first>tests/e2e/remake-scenedetect-review.spec.ts, playwright.config.ts, .planning/phases/06-scenedetect/06-UI-SPEC.md, .planning/phases/06-scenedetect/06-VALIDATION.md, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx</read_first>
  <action>使用已认证 remake fixture 和可控 executor/storage 响应访问真实 workspace route，不使用 page.setContent。覆盖无 source 原上传入口且无 sample/IndexedDB restore、上传/probe恢复、queued/source-read/executor-call/import 等真实 modal 阶段、失败/重试、真实 VideoPlayer/Timeline/ShotInspector/ShotList、adjust/split/merge/delete/undo/redo、KeyframeSelector 同源 canvas 与 Task 回载、单项/批量 keep、gate reason、修改后 needs_review/promptEligibility 失效，以及视频分析/镜头审核导航间 App 实例与编辑状态连续。分别在 1440x900、1024x768、390x844 检查非空、深色作用域、可用高度、内部滚动、显式 portal containment、overlay Task drawer、焦点恢复、无页面横向溢出/遮挡。</action>
  <acceptance_criteria>
    - Playwright 访问真实 authenticated workspace route，且测试源码不含 `page.setContent`。
    - SHOT-01..08 的上传、分析、编辑、三帧、审核、失效与恢复闭环全部有断言。
    - 1440x900、1024x768、390x844 均验证非空 stage、内部滚动、portal containment、drawer focus、无横向溢出/遮挡。
    - canvas pixel check 非空且无 taint；视频分析/镜头审核切换前后选中 Shot、播放位置和 undo state 连续。
  </acceptance_criteria>
  <verify><automated>npx playwright test tests/e2e/remake-scenedetect-review.spec.ts</automated></verify>
  <done>自动化证据覆盖完整 Phase 6 用户闭环、响应式和 architecture guards。</done>
</task>
</tasks>

## Artifacts this phase produces

- Production `SceneDetectStageHost` mount, scoped stage CSS and Waoo overlay/focus integration.
- Explicit portal container contract for vendor modals and previews.
- AST/import-graph compatibility guards for canonical App reuse and Waoo-only facts.
- Real-route Playwright review flow at 1440x900, 1024x768 and 390x844.

<source_audit>
| 来源 | ID | 功能/约束 | Owner Plan | 状态 |
|---|---|---|---|---|
| GOAL | — | 完整 App 整体接入，只替换 Waoo 数据/执行边界 | 01-08 | COVERED |
| REQ | SHOT-01 | 上传/probe/状态 | 06-01 | COVERED |
| REQ | SHOT-02 | Waoo 发起真实 SceneDetect，不重写算法 | 06-03 | COVERED |
| REQ | SHOT-03 | 版本导入、稳定 Shot/provenance | 06-02 | COVERED |
| REQ | SHOT-04 | 复用播放器/时间轴/Shot 列表 | 06-08（06-04 提供 API 支撑） | COVERED |
| REQ | SHOT-05 | 复用 edit/undo/redo 编排并写回 Waoo revision | 06-06（06-05 提供 client runtime 支撑） | COVERED |
| REQ | SHOT-06 | Start/Middle/End 选择与重新提取 | 06-07 | COVERED |
| REQ | SHOT-07 | 单项/批量确认与 Prompt gate | 06-07（06-08 负责 production mount/E2E） | COVERED |
| REQ | SHOT-08 | 旧版本与下游 invalidation | 06-06 | COVERED |
| RESEARCH | blockers/high findings | vendor patch/lint、runtime ports、真实 Task 阶段、source/media/revision/canvas/portal 边界 | 01-08 | COVERED |
| CONTEXT | D-01..D-13 | 整 App、同一实例、原上传入口、Waoo truth、审核映射、允许 patch 与 host 边界 | 01-08 | COVERED |
| UI-SPEC | all sections + UI considerations | 原交互、空/加载/错误/冲突/复核、三档视口、视觉/焦点/modal 隔离 | 05, 07, 08 | COVERED |
| VALIDATION | 19 tasks + Wave 0 | focused sampling、vendor/task gates、真实 route Playwright 与 executor readiness | 01-08 | COVERED |
| HANDOFF | Phase 5 D-03..D-09,D-13,D-15..D-20 | stable identity、Waoo truth、完整 App、native types、必要 patch、防重建 | 01-08 | COVERED |
| EXCLUSION | Phase 7-11 | Prompt/生成/final export/editor | — | EXCLUDED |
</source_audit>

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-22 | Tampering | host/runtime inputs | high | mitigate | server auth/version/schema；host不信任客户端 IDs |
| T-06-23 | Information Disclosure | media/E2E | medium | mitigate | 短期签名 URL，fixtures不记录 credentials/paths |
| T-06-24 | Elevation of Privilege | batch confirmation | high | mitigate | server review gate、project ownership与批量上限 |
</threat_model>
<verification>host unit tests、vendor/compatibility guards和真实 Playwright 三视口闭环全部通过；复跑 typecheck 与 Phase 5 compatibility。</verification>
<success_criteria>用户在同一完整 SceneDetect stage 完成上传、分析、编辑、关键帧和人工确认，未确认 Shot 不能进入 Prompt。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-08-SUMMARY.md`。</output>
