---
phase: 06-scenedetect
plan: 06
type: execute
wave: 6
depends_on: [06-02, 06-04, 06-05]
files_modified:
  - "src/lib/remake-projects/scenedetect/mutations.ts"
  - "src/lib/remake-projects/scenedetect/contracts.ts"
  - "src/lib/remake-projects/service.ts"
  - "src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts"
  - "tests/integration/api/remake-projects-scenedetect-mutations.test.ts"
  - "src/lib/remake-projects/scenedetect/integration-runtime.ts"
  - "src/lib/remake-projects/scenedetect/runtime-client.ts"
  - "tests/contracts/remake-scenedetect-native-contract.test.ts"
autonomous: true
requirements: [SHOT-05, SHOT-08]
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "SHOT-08：已确认 Shot 的边界、关键帧或存在性变化保留旧 revision，并标记当前 needs_review 与下游 invalidations。"
    - "adjust/split/merge/delete/undo/redo 使用稳定 Waoo UUID；新 split ID 服务端分配，删除/合并只退役不物理删除。"
    - "过期 save 返回 409/current canonical project，禁止静默 last-write-wins。"
    - "D-07/D-10：schema 具有显式 current/retired revision、sourceRevision、keyframe media tuple、task/revision 关联和 opaque concurrency token；不得依赖 JSON payload 搜索 current state。"
    - "Phase 6 revision retention 固定为 append-only：不压缩、折叠、合并或 GC；retention/compaction 仅在 Phase 7 规划时评估，不在本阶段悄然引入。"
  artifacts:
    - { path: "src/lib/remake-projects/scenedetect/mutations.ts", provides: "native diff、stable identity、revision/invalidation" }
    - { path: "scenedetect/project/route.ts", provides: "If-Match native save API" }
    - { path: "runtime-client.ts", provides: "debounced save、canonical remap 和 conflict recovery" }
  key_links:
    - { from: "project/route.ts", to: "mutations.ts", via: "schema/auth/version 后单事务 diff", pattern: "commit.*Mutation" }
    - { from: "mutations.ts", to: "RemakeInvalidation", via: "changed revision 标记所有下游", pattern: "remakeInvalidation" }
---

<objective>把原生 Shot 编辑持久化为稳定、并发安全、可失效传播的 Waoo revisions。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/06-scenedetect/06-05-SUMMARY.md
@src/vendor/scenedetect/App.tsx
@src/lib/remake-projects/service.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现原子 mutation diff、稳定 ID 与 invalidation</name>
  <files>src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/scenedetect/contracts.ts, src/lib/remake-projects/service.ts, src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts, tests/integration/api/remake-projects-scenedetect-mutations.test.ts</files>
  <read_first>prisma/schema.prisma, prisma/migrations/20260807190000_add_remake_source_ingest/migration.sql, src/lib/remake-projects/scenedetect/contracts.ts, src/lib/remake-projects/service.ts, src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts, src/vendor/scenedetect/App.tsx</read_first>
  <behavior>
    - adjust 只追加改变 Shot/相邻 Shot revision；split 分配 UUID；merge保留 survivor；delete退役；undo/redo形成新 revision。
    - confirmed/有下游的变化设置 needs_review并为所有 outputs 写 invalidation；no-op 不写 revision。
    - 越权/跨项目/重叠越界/过期 token 原子失败。
  </behavior>
  <action>使用 06-01 已建立的 currentRevision/version、active/retired、sourceRevision、keyframe tuple/media refs 和 Task association 字段，不再创建第二个 Phase 6 schema migration。实现 commitNativeProjectMutation，从显式 current revision 构造基线，以 Waoo UUID 识别既有 Shot，服务端映射 new split IDs，缺失项退役。校验范围有序、不重叠、keyframes 在边界内。语义变化追加 immutable revision/provenance；undo/redo 同样追加新 revision，不重写旧 revision。Phase 6 禁止 revision 压缩、折叠、合并、物理删除或 GC；retention/compaction 仅留到 Phase 7 规划时评估。已确认或有 outputs 的 Shot 写 needs_review/invalidation。GET 返回基于 project version/current revision set 的 opaque token，PUT 要求 If-Match；冲突返回 current project/token。禁止通过 payload JSON 搜索推导 current/retired 状态。</action>
  <acceptance_criteria>
    - adjust/split/merge/delete/undo/redo 的每个语义变化只追加 revision；旧 revision 仍存在且不再是 current。
    - new split ID 由服务端生成，跨项目 UUID、过期 If-Match、重叠或越界 frame 以原子错误返回且无部分写入。
    - 修改 approved/有 output 的 Shot 会写 needs_review 与 RemakeInvalidation；no-op 不增加 revision。
    - 连续编辑和 undo/redo 只增加 revision；测试断言没有 update/delete/compaction 路径改写或移除既有 revision。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-mutations.test.ts</automated></verify>
  <done>所有原生编辑命令安全写回 stable revisions，历史与下游失效完整。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 连接 canonical remap、save ordering 与冲突恢复</name>
  <files>src/lib/remake-projects/scenedetect/integration-runtime.ts, src/lib/remake-projects/scenedetect/runtime-client.ts, tests/contracts/remake-scenedetect-native-contract.test.ts</files>
  <read_first>src/lib/remake-projects/scenedetect/integration-runtime.ts, src/lib/remake-projects/scenedetect/runtime-client.ts, src/vendor/scenedetect/App.tsx, tests/contracts/remake-scenedetect-native-contract.test.ts</read_first>
  <behavior>
    - debounce 合并连续编辑；进行中保存后的新 snapshot 不丢失；成功采用对应 canonical remap。
    - 409 停止自动重试并返回 current project，不出现无限 save loop。
  </behavior>
  <action>runtime client 持有 opaque token，发送 If-Match，并实现单飞行、latest-pending、response ordering、abort cleanup。只有回包对应仍为 current base 时才采用 UUID remap；冲突保留 undo/redo 栈并显式返回 current canonical project。</action>
  <acceptance_criteria>
    - 保存进行中产生的新 snapshot 会在前一请求终止后提交，旧响应不能覆盖新 base。
    - 409 停止自动重试并返回 current project/token；不会出现无限 save loop。
    - canonical UUID remap 后选中 Shot 与播放位置保持不变，contract/integration suites 退出码为 0。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/integration/api/remake-projects-scenedetect-mutations.test.ts</automated></verify>
  <done>编辑顺序和并发冲突可见，不静默覆盖或丢失后续操作。</done>
</task>
</tasks>

## Artifacts this phase produces

- `commitNativeProjectMutation` and native project optimistic-concurrency contract.
- Stable split-ID remap, append-only revision retirement and downstream invalidation behavior.
- Runtime save ordering, abort cleanup and explicit 409 recovery.
- `tests/integration/api/remake-projects-scenedetect-mutations.test.ts` coverage for all native edit commands.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-16 | Tampering | native PUT | high | mitigate | ownership、UUID归属、frame invariants、事务 diff |
| T-06-17 | Elevation of Privilege | cross-project IDs | high | mitigate | path project 绑定，新 ID 服务端生成 |
| T-06-18 | Tampering | concurrent save | high | mitigate | opaque If-Match 与 409 current snapshot |
</threat_model>
<verification>mutation与native contract suites 覆盖所有编辑、invalidation、ownership、no-op和concurrency。</verification>
<success_criteria>修改已确认 Shot 时历史保留、下游明确失效、current revision 并发安全。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-06-SUMMARY.md`。</output>
