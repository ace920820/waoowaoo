---
phase: 06-scenedetect
plan: 07
type: execute
wave: 7
depends_on: [06-03, 06-06]
files_modified:
  - "src/lib/remake-projects/scenedetect/keyframes.ts"
  - "src/lib/remake-projects/scenedetect/mutations.ts"
  - "src/lib/remake-projects/scenedetect/task-contract.ts"
  - "src/lib/workers/handlers/scenedetect.ts"
  - "tests/integration/api/remake-projects-scenedetect-keyframes.test.ts"
  - "src/lib/remake-projects/scenedetect/review-gate.ts"
  - "src/lib/remake-projects/service.ts"
  - "src/lib/query/hooks/useRemakeProject.ts"
  - "tests/unit/remake-projects/scenedetect-review-gate.test.ts"
autonomous: true
requirements: [SHOT-06, SHOT-07]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "SHOT-06：用户沿用 SceneDetect Start/Middle/End 选择器；采用帧经 `scenedetect_extract_keyframes` Task 从原视频提取并存入 Waoo storage。"
    - "候选 canvas/data URL只作瞬态预览；current revision 只保存 frame tuple 与 Waoo media refs。"
    - "D-08：KeyframeSelector 只使用 06-04 项目授权同源 media route；视频 seek/canvas 抽帧不依赖任意 storage sign 或未验证跨域 CORS。"
    - "D-09：复用 SceneDetect 原 keep/pending/discard 单项与批量交互并映射 approved/pending/rejected；Task 完成不等于批准，只有 current boundary、三帧、Task 和 needsReview 全部满足服务端 gate 时才成为 confirmed/promptEligible，且不新增第二套审核工具栏。"
    - "修改 confirmed Shot 后 eligibility 立即失效，直到 extraction 完成并再次人工确认。"
  artifacts:
    - { path: "src/lib/remake-projects/scenedetect/keyframes.ts", provides: "frame extraction command、dedupe和result commit" }
    - { path: "src/lib/remake-projects/scenedetect/review-gate.ts", provides: "确认与 Phase 7 eligibility 纯合同" }
    - { path: "tests/integration/api/remake-projects-scenedetect-keyframes.test.ts", provides: "提取、过期、失败、媒体版本测试" }
  key_links:
    - { from: "mutations.ts", to: "keyframes.ts", via: "boundary/keyframeFrames 变化提交 extract Task", pattern: "extract_keyframes" }
    - { from: "review-gate.ts", to: "service.ts", via: "snapshot 服务端计算 confirmed/promptEligible/reasons", pattern: "promptEligib" }
---

<objective>完成关键帧重新提取和服务端人工确认门禁，为最终完整 App E2E 提供稳定业务合同。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/06-scenedetect/06-06-SUMMARY.md
@src/vendor/scenedetect/components/KeyframeSelectorPopover.tsx
@src/vendor/scenedetect/components/ShotList.tsx
@src/lib/remake-projects/scenedetect/task-contract.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 将 frame selection/boundary changes 接入版本化 extraction Task</name>
  <files>src/lib/remake-projects/scenedetect/keyframes.ts, src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/scenedetect/task-contract.ts, src/lib/workers/handlers/scenedetect.ts, tests/integration/api/remake-projects-scenedetect-keyframes.test.ts</files>
  <read_first>src/vendor/scenedetect/components/KeyframeSelectorPopover.tsx, src/vendor/scenedetect/utils/sceneDetector.ts, src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/scenedetect/task-contract.ts, src/lib/workers/handlers/scenedetect.ts</read_first>
  <behavior>
    - keyframeFrames 或边界变化先保存 revision，再按 sourceRevision + shotRevision + frame tuple hash 创建唯一 Task。
    - worker 只提交 current source/current revision，三张结果写 Waoo storage并追加 provenance revision；浏览器通过同源 media route 预览且 canvas 不被污染。
    - 过期回调不覆盖新 revision；失败保留旧 refs并标记 stale/failed；越界/跨项目/retired 被拒绝。
  </behavior>
  <action>实现 keyframe command service。incoming browser URL 全部剥离，只持久化 selected frame tuple；边界变化涉及相邻 Shot 时分别提交 extract Task。扩展 task contract/worker 调用既有 /api/keyframes，归一化结果到 Waoo storage，提交前再次校验 current revision。native reload 为 selector 提供 06-04 同源 opaque media URL，集成测试覆盖 Range/seek/canvas 可读且 DB 无 blob/data/runtime URL。过期结果只记 provenance，失败保留上一可查看版本。不得修改或重建 KeyframeSelector/ShotList。</action>
  <acceptance_criteria>
    - keyframeFrames/boundary mutation 成功写 revision 后才以 sourceRevision+shotRevision+tupleHash 提交唯一 extraction Task。
    - current result 写入三张 Waoo media refs；stale/retired/cross-project result 只留 provenance，不覆盖 current refs。
    - DB 不含 blob/data/executor runtime URL；同源 media route 可 seek 且 canvas 像素非空。
    - vendor KeyframeSelector/ShotList 不被复制、deep-import 或重新编排。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-keyframes.test.ts tests/unit/remake-projects/scenedetect-task-contract.test.ts</automated></verify>
  <done>Start/Middle/End 选择最终形成 current Waoo media refs，重试/过期不覆盖更新结果。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现单项/批量确认与 Prompt eligibility gate</name>
  <files>src/lib/remake-projects/scenedetect/review-gate.ts, src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/service.ts, src/lib/query/hooks/useRemakeProject.ts, tests/unit/remake-projects/scenedetect-review-gate.test.ts</files>
  <read_first>src/vendor/scenedetect/components/ShotList.tsx, src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/service.ts, src/lib/query/hooks/useRemakeProject.ts, prisma/schema.prisma</read_first>
  <behavior>
    - active latest revision 边界合法、三帧完整、无 stale/running/failed extraction、needsReview=false且 status=keep 才 confirmed/eligible。
    - pending/discard/retired、缺帧、旧 source、needsReview 返回 false + reason codes。
    - batch keep 逐 Shot校验；不合格项保持 pending；Task completed 本身不改变 reviewStatus。
  </behavior>
  <action>新增 evaluateSceneDetectReviewGate，从 DB latest revision/source/media/task/review事实计算 confirmed、promptEligible 与稳定 reason codes。keep/pending/discard 映射为 approved/pending/rejected，但只有 gate 满足才允许 approved；批量返回 accepted/rejected 明细。gate 拒绝通过非侵入 host 状态提示并可在 Task drawer 查看详情，不增加第二套审核工具栏或改写 ShotList 布局。snapshot 服务端返回逐 Shot/项目确认计数，query hook 只消费，不在客户端重算。明确 Phase 7 默认批量只接受 promptEligible=true，但不实现 Prompt 分析或生成。</action>
  <acceptance_criteria>
    - 只有 current source/revision、合法 boundary、三帧完整、无 stale/running/failed extraction、needsReview=false 且用户 keep 时返回 approved/promptEligible=true。
    - Task completed 本身不批准 Shot；pending/discard/retired/缺帧/旧 source 均返回稳定 reason code。
    - batch keep 返回逐项 accepted/rejected，失败项保持 pending；客户端只显示服务端结果，不重算 eligibility。
    - Phase 6 不创建 Prompt、图片生成或视频生成 Task。
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/remake-projects/scenedetect-review-gate.test.ts tests/integration/api/remake-projects-scenedetect-mutations.test.ts</automated></verify>
  <done>关键帧完整且人工确认的 current revisions 才能进入后续 Prompt 阶段。</done>
</task>
</tasks>

## Artifacts this phase produces

- Versioned keyframe extraction command and tuple-hash dedupe contract.
- `evaluateSceneDetectReviewGate` with stable reason codes and batch results.
- Server snapshot fields for confirmed/promptEligible counts and reasons.
- Keyframe stale-result/media/canvas and review-gate regression suites.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-19 | Tampering | frame/result | high | mitigate | frame/source/current revision 校验，过期结果不更新 current refs |
| T-06-20 | Spoofing | confirmation | high | mitigate | server gate 计算，拒绝客户端 eligibility/completed 推导 |
| T-06-21 | Denial of Service | extraction | medium | mitigate | tuple dedupe、current Task、queue concurrency与媒体上限 |
</threat_model>
<verification>keyframe integration 与 review-gate unit suites 通过；本计划不修改 StageHost/CSS/VENDOR/Playwright/compatibility guards。</verification>
<success_criteria>关键帧重提取和确认资格由 Waoo revision/media/task事实决定，未确认 Shot 无法默认进入 Phase 7。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-07-SUMMARY.md`。</output>
