---
phase: 06-scenedetect
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - "src/lib/remake-projects/scenedetect/executor.ts"
  - "src/app/api/remake-projects/[projectId]/scenedetect/analyze/route.ts"
  - "src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts"
  - "src/app/api/remake-projects/[projectId]/scenedetect/media/[mediaId]/route.ts"
  - "tests/fixtures/scenedetect/media-fixture.ts"
  - "src/app/api/remake-projects/[projectId]/tasks/route.ts"
  - "tests/integration/api/remake-projects-scenedetect-runtime.test.ts"
autonomous: true
requirements: [SHOT-04]
estimate: { tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "SHOT-04：embedded App 所需 native project、analyze command 和 Task callback 数据均由项目作用域 Waoo API 提供。"
    - "分析 double-click 幂等；旧 source revision、越权项目和非 remake 项目不能创建 Task。"
    - "刷新后可恢复 queued/processing/waiting_retry/completed/failed/canceled、attempt/progress 与脱敏错误。"
    - "D-04/D-08：source/关键帧通过项目授权、同源、支持 Range 的 opaque media resolver 播放；客户端拿不到 raw storage key，通用 storage sign route 不作为授权边界。"
  artifacts:
    - { path: "scenedetect/analyze/route.ts", provides: "幂等分析提交 API" }
    - { path: "scenedetect/project/route.ts", provides: "canonical native project load API" }
    - { path: "tasks/route.ts", provides: "安全可恢复 Task projection" }
    - { path: "scenedetect/media/[mediaId]/route.ts", provides: "项目授权同源视频/关键帧 resolver" }
  key_links:
    - { from: "analyze/route.ts", to: "createSceneDetectExecutor", via: "current sourceRevision + operationKey", pattern: "submitAnalyze" }
    - { from: "project/route.ts", to: "toSceneDetectProject", via: "Waoo snapshot 生成 native model", pattern: "toSceneDetectProject" }
---

<objective>提供完整 App runtime 所需的幂等分析、native project load 和 Task 恢复 API。</objective>
<execution_context>@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-scenedetect/06-CONTEXT.md
@.planning/phases/06-scenedetect/06-RESEARCH.md
@.planning/phases/06-scenedetect/06-UI-SPEC.md
@.planning/phases/06-scenedetect/06-VALIDATION.md
@.planning/phases/06-scenedetect/06-03-SUMMARY.md
@src/lib/remake-projects/scenedetect/executor.ts
@src/app/api/remake-projects/[projectId]/tasks/route.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 暴露幂等 analyze 与 native project API</name>
  <files>src/lib/remake-projects/scenedetect/executor.ts, src/app/api/remake-projects/[projectId]/scenedetect/analyze/route.ts, src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts, tests/integration/api/remake-projects-scenedetect-runtime.test.ts</files>
  <read_first>src/lib/remake-projects/scenedetect/executor.ts, src/lib/remake-projects/service.ts, src/lib/api-auth.ts, src/app/api/remake-projects/[projectId]/tasks/route.ts, src/vendor/scenedetect/utils/projectStore.ts</read_first>
  <behavior>
    - current source 的 owner 提交分析得到 taskId；重复 operationKey 返回同一 Task。
    - GET project 在无 source 时返回显式 empty native state 供完整 App 显示原上传入口；有 source 时返回 opaque media refs；旧 revision、越权请求明确失败。
  </behavior>
  <action>新增 analyze POST route，使用 requireProjectAuthLight，服务端绑定 current sourceRevision、adapter/executor/capability version，只接受 operationKey 与允许 threshold。新增 project GET route，复用 06-02 snapshot/native conversion；无 source 时返回显式 embedded empty state而非 sample/IndexedDB fallback，有 source 时只返回 opaque mediaId，不读取 IndexedDB/standalone JSON。</action>
  <acceptance_criteria>
    - analyze POST 只接受 authenticated remake project 的 operationKey/threshold，并把 current sourceRevision 与版本信息写入 Task payload。
    - 无 source 的 project GET 返回可挂载 canonical App 的 explicit empty native state；有 source 响应只含 opaque mediaId。
    - 越权、非 remake project、旧 source 或重复 operation 不产生重复 Task。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-runtime.test.ts</automated></verify>
  <done>runtime 可通过鉴权 API提交分析并加载 current native project。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提供项目授权、同源且支持视频 Range 的 media resolver</name>
  <files>src/app/api/remake-projects/[projectId]/scenedetect/media/[mediaId]/route.ts, tests/fixtures/scenedetect/media-fixture.ts, tests/integration/api/remake-projects-scenedetect-runtime.test.ts</files>
  <read_first>src/app/api/storage/sign/route.ts, src/lib/storage/index.ts, src/lib/api-auth.ts, src/app/api/remake-projects/[projectId]/scenedetect/project/route.ts</read_first>
  <behavior>
    - owner 可通过 opaque mediaId 播放 current source/三帧；Range、HEAD、content type 与缓存头适合 video seek 和 canvas 读取。
    - 跨项目/未知/retired unauthorized media 返回 404/403；响应和 native payload 不泄漏 storage key。
  </behavior>
  <action>先创建 tests/fixtures/scenedetect/media-fixture.ts，提供尺寸受控、可 seek 的短视频、确定性的首/中/尾帧像素锚点和跨项目 mediaId，用于 Range/HEAD/canvas/authorization 断言；fixture 不持久化 blob/data/runtime URL。再实现 project-scoped same-origin media route：鉴权并确认 remake project、mediaId 与允许的 source/shot revision 关联，通过 storage abstraction 读取或流式代理，支持浏览器视频 seek 所需 Range/HEAD 和 canvas 同源读取。禁止把 raw key 传给通用 storage sign endpoint，也禁止形成任意对象签名代理。</action>
  <acceptance_criteria>
    - GET/HEAD 与合法 Range 返回正确 status/content-range/content-type；播放器可 seek，canvas 读取不产生 taint。
    - 跨项目、未知/retired-unreferenced mediaId 返回拒绝，route 不能接受 raw storage key 或任意 URL。
    - project/native API 响应中不出现通用 `/api/storage/sign?key=` 链接。
    - media fixture 可同时驱动 integration 与 Playwright 的 Range、seek、非空 canvas pixel 和跨项目拒绝断言。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-runtime.test.ts</automated></verify>
  <done>播放器和 KeyframeSelector 获得可授权、可 seek、不会污染 canvas 的同源媒体 URL。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 完成项目 Task 恢复与安全投影</name>
  <files>src/app/api/remake-projects/[projectId]/tasks/route.ts, tests/integration/api/remake-projects-scenedetect-runtime.test.ts</files>
  <read_first>src/app/api/remake-projects/[projectId]/tasks/route.ts, src/lib/task/types.ts, src/lib/workers/shared.ts, tests/integration/api/remake-projects-task-projection.test.ts</read_first>
  <behavior>
    - Task projection 包含 progress/attempt/result IDs/capability/retry/terminal states，刷新后不丢失。
    - 其他项目 taskId、executor URL、环境变量、绝对路径和原始日志不被返回。
  </behavior>
  <action>扩展现有 GET tasks 投影，保持 user/project ownership过滤，加入 Waoo callback 所需字段并统一 waiting_retry。错误只返回稳定 code 与脱敏 message；不实现 Phase 10 的批量取消/重试控制。</action>
  <acceptance_criteria>
    - 刷新后可恢复本项目 SceneDetect Task 的 queued/running/waiting_retry/completed/failed/canceled 状态和真实 stage。
    - 响应不含其他项目 Task、executor URL/token、raw storage key、stack 或未脱敏 payload。
    - route 只扩展读投影，不新增 Phase 10 的通用取消或批量重试 API。
  </acceptance_criteria>
  <verify><automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-scenedetect-runtime.test.ts tests/integration/api/remake-projects-task-projection.test.ts</automated></verify>
  <done>App 和 Task drawer 可在刷新/Worker 重启后恢复同一任务状态。</done>
</task>
</tasks>

## Artifacts this phase produces

- Project-scoped SceneDetect analyze and native-project routes.
- Project-authorized, same-origin, Range/HEAD-capable opaque media route.
- SceneDetect Task projection fields for truthful stage recovery.
- Runtime/media/task authorization integration tests.

<threat_model>
| Threat ID | 类别 | 组件 | 严重度 | 处置 | 缓解计划 |
|---|---|---|---|---|---|
| T-06-10 | Elevation of Privilege | runtime routes | high | mitigate | project ownership、remake type、current revision/target 绑定 |
| T-06-11 | Information Disclosure | task projection | medium | mitigate | payload 白名单与错误脱敏 |
| T-06-12 | Denial of Service | analyze submissions | medium | mitigate | operation dedupe、existing queue limits |
</threat_model>
<verification>runtime API 与 task projection suites 通过；本计划不修改 Worker、vendor App 或 host CSS。</verification>
<success_criteria>canonical App 的数据与任务入口均为安全、可恢复的 Waoo API。</success_criteria>
<output>创建 `.planning/phases/06-scenedetect/06-04-SUMMARY.md`。</output>
