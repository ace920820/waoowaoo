---
phase: 03.3-import-api-and-persistence
plan: 02
type: implementation
wave: 2
depends_on:
  - 03.3-01-PLAN.md
files_modified:
  - src/lib/novel-promotion/storyboard-package/import-service.ts
  - src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import/route.ts
  - tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts
autonomous: true
requirements:
  - SBPI-13
  - SBPI-15
  - SBPI-16
  - SBPI-17
  - SBPI-18
  - SBPI-19
---

<objective>
Implement commit mode for storyboard package imports. Commit must reuse the preview planner, write imported segments into multi-shot shot groups and shot-group items transactionally, and persist all review/video/draft metadata needed by the multi-shot confirmation and video stages.
</objective>

<context>
Read first:
- `.planning/phases/03.3-import-api-and-persistence/03.3-RESEARCH.md`
- `src/lib/novel-promotion/storyboard-package/import-service.ts`
- `src/lib/shot-group/video-config-snapshot.ts`
- `src/lib/shot-group/draft-metadata.ts`
- `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts`
- `prisma/schema.prisma` model snippets for `NovelPromotionShotGroup` and `NovelPromotionShotGroupItem`

Use progressive code disclosure. Expand only if transaction code, type errors, or failing tests point to another file.
</context>

<tasks>
  <task id="03.3-02-T1" type="commit-route">
    <files>src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import/route.ts, src/lib/novel-promotion/storyboard-package/import-service.ts</files>
    <action>
      Add `mode: "commit"` handling. The route must call the same planner used by preview, then commit the resulting segment plan in a Prisma transaction. Return the same preview summary plus commit results containing created/updated shot group ids and counts.
    </action>
    <verify>
      Add integration tests proving commit creates records and preview remains non-mutating.
    </verify>
    <acceptance_criteria>
      - Commit reruns parse/validation/planning server-side and does not trust a client-supplied preview.
      - Commit is transactional for all segment writes in one request.
      - Commit response identifies created and updated shot groups.
    </acceptance_criteria>
  </task>

  <task id="03.3-02-T2" type="shot-group-persistence">
    <files>src/lib/novel-promotion/storyboard-package/import-service.ts</files>
    <action>
      Persist each imported segment into `NovelPromotionShotGroup` fields: `episodeId`, `title`, `templateKey`, `groupPrompt`, `videoPrompt`, `generateAudio`, `includeDialogue`, `dialogueLanguage`, reference mode flags, `videoModel`, and sanitized generation options inside `videoReferencesJson`. Use `buildShotGroupVideoConfigSnapshot` so draft metadata and video config stay compatible with existing generation flows.
    </action>
    <verify>
      Assert persisted shot group fields match imported package values for a committed package.
    </verify>
    <acceptance_criteria>
      - `omniReferenceEnabled` / `smartMultiFrameEnabled` reflect package `referenceMode`.
      - `videoReferencesJson` contains normalized draft metadata and generation options.
      - Commit does not trigger image/video generation tasks.
    </acceptance_criteria>
  </task>

  <task id="03.3-02-T3" type="draft-metadata-and-items">
    <files>src/lib/novel-promotion/storyboard-package/import-service.ts</files>
    <action>
      Persist imported draft metadata fields: `referencePromptText`, `compositePromptText`, storyboard mode id/label/prompt, selected or script-derived asset bindings, mood metadata, `dialogueOverrideText`, `cinematicPlan`, package identity, and segment identity. Replace shot-group items for imported segments with ordered item rows from the mapper, preserving generated item images only when `preserveGeneratedMedia` is true and the item index still exists.
    </action>
    <verify>
      Assert committed item titles/prompts and parsed draft metadata contain the imported fields required by SBPI-16 and SBPI-17.
    </verify>
    <acceptance_criteria>
      - Imported items produce one row per mapped shot item with correct `itemIndex`, `title`, and `prompt`.
      - Draft metadata includes enough identity for repeated import overwrite detection.
      - Asset bindings use matched assets when available and script-derived fallback when missing.
      - Existing generated media is preserved by default and can be reset only through an explicit request option.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Commit mode writes shot groups, draft metadata, video config, asset bindings, and item prompts.
- Repeated import updates the matching imported groups instead of duplicating them.
- Manual/non-imported groups are not modified by package commit.
</success_criteria>
