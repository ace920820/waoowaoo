# Plan 03.3-01 Summary — Preview API And Asset Matching

## Status

Completed on 2026-04-26.

## Delivered

- Added secured route `POST /api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import`.
- Added `src/lib/novel-promotion/storyboard-package/import-service.ts` as the shared backend import service.
- Implemented preview mode for raw JSON and Markdown fenced package content through Phase 03.2 parser/validator/mapper.
- Preview returns package summary, segment rows, shot counts, create/update classification, overwrite strategy, warnings, and asset matches.
- Asset matching supports project characters, locations, and props, with props resolved from location rows where `assetKind === "prop"`.
- Missing assets return script-derived fallback warnings and do not create assets.

## Verification

- `npx vitest run tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts`
- Included in full focused Phase 03.3 regression: 4 files / 25 tests passed.
- `npx eslint src/lib/novel-promotion/storyboard-package/import-service.ts 'src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import/route.ts' tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts src/lib/shot-group/draft-metadata.ts`
- `npm run typecheck`

## Notes

- Preview mode is non-mutating and does not call shot-group create/update/delete operations.
- The route stays thin and delegates package-specific logic to the service.
