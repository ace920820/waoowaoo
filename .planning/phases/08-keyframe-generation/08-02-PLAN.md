---
phase: 08-keyframe-generation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/remake-projects/keyframes/service.ts
  - src/lib/remake-projects/scenedetect/mutations.ts
  - src/lib/task/types.ts
  - src/lib/task/queues.ts
  - src/lib/workers/image.worker.ts
  - src/lib/workers/handlers/remake-keyframe-image.ts
  - src/lib/workers/handlers/remake-keyframe-action-sheet.ts
  - src/app/api/remake-projects/[projectId]/keyframes/route.ts
  - src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts
  - tests/unit/worker/remake-keyframe-image.test.ts
  - tests/integration/api/remake-projects-keyframes.test.ts
  - tests/integration/remake-projects/keyframe-service.test.ts
  - tests/integration/api/remake-projects-scenedetect-mutations.test.ts
autonomous: true
requirements: [KFRM-01, KFRM-02, KFRM-03, KFRM-04, KFRM-05, KFRM-06, KFRM-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "KFRM-01/KFRM-02 and D-09/D-13/D-14/D-15/D-16: an authenticated selected slot with a current adopted Prompt submits through existing config, Task, image queue, Worker, gateway, storage, and billing paths with frozen legal inputs."
    - "KFRM-03/KFRM-05 and D-17: one Task appends one immutable batch with 1..4 ordered candidates; regeneration and Prompt/config changes preserve prior batches, outputs, Tasks, and adoption history."
    - "KFRM-04 and D-18/D-19/D-20: history reads do not adopt; only an authorized current-revision transaction changes the sole slot pointer and records replacement."
    - "KFRM-06/KFRM-07 and D-07/D-08: unselected, unapproved, invalidated, missing, or capability-unsupported slots are rejected by the server and projected with truthful reasons."
    - "D-10/D-11/D-12: the existing image queue executes Plan 08-01's deterministic action-sheet contract without treating the sheet as a generated candidate."
    - "No batch scheduler, video Task type, video generation API, or direct provider invocation is introduced."
  artifacts:
    - path: "src/lib/remake-projects/keyframes/service.ts"
      provides: "Server-authoritative selection, eligibility, immutable histories, adoption, and stale-input checks"
    - path: "src/lib/workers/handlers/remake-keyframe-image.ts"
      provides: "Remake persistence adapter around the current image generation infrastructure"
    - path: "src/app/api/remake-projects/[projectId]/keyframes/route.ts"
      provides: "Authenticated selection/submission boundary using server-owned facts"
    - path: "src/lib/workers/handlers/remake-keyframe-action-sheet.ts"
      provides: "Existing-image-queue executor for Plan 08-01's D-11 renderer and persistence contract"
  key_links:
    - from: "keyframes/route.ts"
      to: "config-service.ts and task-contract.ts"
      via: "central capability resolution followed by a strict frozen descriptor"
      pattern: "resolveProjectModelCapabilityGenerationOptions"
    - from: "task/queues.ts and image.worker.ts"
      to: "remake-keyframe-image.ts"
      via: "existing image queue dispatch"
      pattern: "REMAKE_KEYFRAME_IMAGE_GENERATE"
    - from: "tracks/[trackId]/route.ts"
      to: "keyframes/service.ts"
      via: "authorized explicit selection/history/adoption mutations"
      pattern: "adoptKeyframeCandidate"
    - from: "remake-keyframe-action-sheet.ts"
      to: "Plan 08-01 keyframes/action-sheet.ts"
      via: "bounded image-queue handler calls the durable renderer/persistence interface"
      pattern: "REMAKE_KEYFRAME_ACTION_SHEET"
---

<objective>
Integrate Remake keyframe generation with the current Task/image infrastructure and complete the single-Shot generation, history, adoption, and action-sheet execution lifecycle on top of Plan 08-01 persistence.

Purpose: Deliver KFRM-01..07 and D-07..D-20 backend authority while reusing the current model gateway, config resolver, image queue/Worker, storage, Task lifecycle, billing, and Remake provenance contracts. Phase 8 adds Remake adapters and APIs only; it adds neither orchestration nor video execution.
Output: Strict contracts, authenticated routes, existing-queue handlers, immutable generation/adoption services, action-sheet Task execution, and focused backend tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-keyframe-generation/08-CONTEXT.md
@.planning/phases/08-keyframe-generation/08-RESEARCH.md
@.planning/phases/08-keyframe-generation/08-PATTERNS.md
@.planning/phases/08-keyframe-generation/08-01-SUMMARY.md

<interfaces>
- `getAdoptedPromptForGeneration({ projectId, shotId, targetKey })` is the approved Prompt read and rejects non-approved, invalidated, or stale versions.
- `resolveProjectModelCapabilityGenerationOptions(...)` is the generation-options authority.
- `submitTask(...)`, `TASK_TYPE`, `getQueueTypeByTaskType`, and `image.worker.ts` are the asynchronous execution seam.
- The panel image handler is a source for gateway/reference/upload/billing helpers only; its Panel overwrite persistence is not reusable.
- `requireProjectAuthLight(projectId)` and `ApiError` are the route auth/error conventions.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Generate one approved Start candidate through the existing production image path</name>
  <precondition>Plan 08-01 migration is applied and Phase 7 adopted-Prompt focused tests pass.</precondition>
  <reversibility rating="costly">D-14/D-17 freeze an append-only Task contract that later Phase 8 and Phase 9 reads depend on.</reversibility>
  <files>src/lib/remake-projects/keyframes/service.ts, src/lib/task/types.ts, src/lib/task/queues.ts, src/lib/workers/image.worker.ts, src/lib/workers/handlers/remake-keyframe-image.ts, src/app/api/remake-projects/[projectId]/keyframes/route.ts, tests/unit/worker/remake-keyframe-image.test.ts, tests/integration/api/remake-projects-keyframes.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-08, D-09, D-13..D-17
    - `src/lib/remake-projects/prompt/service.ts` `getAdoptedPromptForGeneration` and fingerprint checks
    - `src/lib/remake-projects/prompt/task-contract.ts` strict descriptor/dedupe pattern
    - `src/lib/config-service.ts` `resolveProjectModelCapabilityGenerationOptions`
    - `src/lib/workers/handlers/panel-image-task-handler.ts` gateway, reference normalization, upload, progress, billing, and compensation calls only
    - `src/app/api/remake-projects/[projectId]/prompts/analyze/route.ts` route auth/submit/error pattern
  </read_first>
  <behavior>
    - Test 1: one explicitly selected Start slot with a current adopted approved Prompt produces a strict descriptor from server facts and an authenticated POST returns 202 with one Task id.
    - Test 2: the Task routes to the existing image queue; the Worker uses the current resolver/gateway/storage/billing lifecycle and appends one batch/candidate/output/provenance graph.
    - Test 3: operation replay is recognizable and does not create a second unidentified Task/batch; an explicit regeneration operation can append history.
    - Test 4: cross-project, stale-revision, stale-Prompt, invalid model/options/reference, upload failure, and persistence failure cannot report a complete candidate or change adoption.
  </behavior>
  <action>Define strict `start|middle|end` slot, selection, frozen submission, and Task payload schemas. Build the descriptor only from authenticated Project ownership, the current active revision, persisted selection, `getAdoptedPromptForGeneration`, project-owned reference assets, the central image capability resolver, and D-14/D-16 candidate inputs. Add one Remake image Task type to the existing image queue and worker switch. Implement the handler as a Remake persistence adapter around existing gateway, normalized references, object upload, progress, billing, logging, and compensation helpers; do not copy Panel writes or call a provider from the route. Recheck revision, Prompt, selection, and fingerprint in the short append transaction. Make the integration tracer complete one Start candidate end to end before expanding inputs.</action>
  <verify>
    <automated>docker compose -f docker-compose.test.yml up -d mysql redis &amp;&amp; BILLING_TEST_BOOTSTRAP=0 DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/unit/worker/remake-keyframe-image.test.ts tests/integration/api/remake-projects-keyframes.test.ts</automated>
  </verify>
  <done>One D-08-selected Start Prompt produces one attributable stored candidate through the existing config/Task/image/gateway/storage stack, with fail-closed ownership and stale-input checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete selection, legal inputs, multi-candidate history, and explicit adoption</name>
  <files>src/lib/remake-projects/keyframes/service.ts, src/lib/remake-projects/service.ts, src/lib/workers/handlers/remake-keyframe-image.ts, src/app/api/remake-projects/[projectId]/keyframes/route.ts, src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts, tests/integration/api/remake-projects-keyframes.test.ts, tests/integration/remake-projects/keyframe-service.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-07..D-10 and D-13..D-20
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` original-frame selection, candidate history, and adoption contracts
    - `src/lib/model-config-contract.ts` image capability allowlists
    - `src/lib/remake-projects/prompt/service.ts` adopted-pointer transaction and history projection
    - `src/app/api/remake-projects/[projectId]/prompts/tracks/[trackId]/route.ts` conflict mapping
  </read_first>
  <behavior>
    - Test 1: a fresh revision has no selected slot; selection persists only for current approved, uninvalidated Prompts and returns readable missing/ineligible reasons.
    - Test 2: model, size, quality, reference assets, extra allowed options, and count 1..4 are centrally validated; the per-submit override is frozen but does not mutate project defaults.
    - Test 3: one Task appends one chronological batch with the requested ordered candidates; regeneration keeps all old batches/candidates/Tasks/outputs/adoption events.
    - Test 4: history/preview reads perform no write; authorized adoption verifies Project/track/candidate/current revision/current Prompt/non-invalidation and changes one pointer in a transaction.
    - Test 5: Start/End-only or no-Middle capability projects an exact legal subset and never substitutes a source; unavailable Prompt slots remain visible as missing items for default batch eligibility.
  </behavior>
  <action>Implement persisted selection, current eligibility, complete frozen generation options, batch history, and explicit adoption services per D-07..D-10 and D-13..D-20. Expand the handler to 1..4 ordered candidates under one batch and preserve truthful Task state for failures. Expose authenticated selection/submission/history/adoption operations through the two declared routes, map malformed inputs to invalid-parameter errors, inaccessible relations to not-found, and stale facts to conflict. Extend the allowlisted Remake snapshot with original slots, Prompt eligibility/reasons, persisted selection, Task/batch/candidate summaries, adopted pointers, frozen parameters, and KFRM-07 project missing counts; exclude raw provider settings and raw Prompt output. Keep preview/comparison identifiers out of persistence and API mutations.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/integration/api/remake-projects-keyframes.test.ts tests/integration/remake-projects/keyframe-service.test.ts</automated>
  </verify>
  <done>All legal D-07..D-20 single-Shot inputs generate immutable history, all illegal inputs remain truthfully blocked, and only explicit adoption changes one server-owned pointer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Execute Plan 08-01's D-11 action-sheet contract on the existing image queue</name>
  <files>src/lib/remake-projects/scenedetect/mutations.ts, src/lib/task/types.ts, src/lib/task/queues.ts, src/lib/workers/image.worker.ts, src/lib/workers/handlers/remake-keyframe-action-sheet.ts, tests/unit/worker/remake-keyframe-image.test.ts, tests/integration/api/remake-projects-scenedetect-mutations.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-10..D-12 and D-17
    - `.planning/phases/08-keyframe-generation/08-01-SUMMARY.md` action-sheet renderer, persistence, invalidation, and revision-integration interfaces
    - `src/lib/remake-projects/keyframes/action-sheet.ts` callable D-11 renderer/persistence boundary
    - `src/lib/task/types.ts`, `src/lib/task/queues.ts`, and `src/lib/workers/image.worker.ts` existing image Task registration and dispatch
    - `src/lib/workers/handlers/remake-keyframe-image.ts` Task lifecycle, failure, logging, and compensation conventions from Task 1
  </read_first>
  <behavior>
    - Test 1: confirming a current Shot revision creates and enqueues exactly one D-11 action-sheet Task using a revision/fingerprint idempotency key; replay does not enqueue a duplicate.
    - Test 2: incomplete, unconfirmed, stale, or cross-project inputs fail without a current sheet; replay of the same revision/fingerprint reuses the durable idempotency result.
    - Test 3: Task progress, actionable error, redacted logging, and terminal status use the current image Worker lifecycle while output/provenance/invalidation writes remain owned by Plan 08-01 services.
    - Test 4: the handler never adopts a candidate, mutates original frames, invokes an external provider, or registers a video Task.
  </behavior>
  <action>Add one strict Remake action-sheet Task descriptor to the existing image Task catalog and route it through the existing image queue/Worker switch. Extend the confirmed-revision transaction boundary to create and enqueue that Task after confirmation, using the current Shot revision plus frozen source-frame fingerprint as the idempotency key; replay must reuse the existing Task/result. Keep the handler thin: validate server-owned identifiers, enter the current Task lifecycle, call Plan 08-01's renderer/persistence interface, and report its current, waiting, stale, or failed result without duplicating composition, output, provenance, or invalidation logic. Do not invoke a model provider, create a candidate/adoption, or add any video execution declaration.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/unit/worker/remake-keyframe-image.test.ts tests/integration/remake-projects/keyframe-action-sheet.test.ts tests/integration/api/remake-projects-scenedetect-mutations.test.ts &amp;&amp; npm run check:test-tasktype-coverage</automated>
  </verify>
  <done>D-11 action-sheet work executes through the existing image queue and Plan 08-01's durable contract, with no duplicate persistence/invalidation implementation and no video/provider path.</done>
</task>

</tasks>

<threat_model>
Concrete risks are cross-project ids, asynchronous stale writes, provider/storage failures after partial work, and duplicate action-sheet state ownership. Route/service/Worker ownership traversal, strict frozen payloads, in-transaction current-state checks, existing compensation/redaction, and the focused failure tests above must fail closed. The action-sheet handler delegates all durable writes to Plan 08-01, and a guard must prove that this plan registers no video Task type or submit route.
</threat_model>

<verification>
- All focused contract, API, service, image Worker, and action-sheet dispatch suites pass; Plan 08-01 owns the SceneDetect invalidation regression.
- `npm run typecheck`, `npm run check:api-handler`, `npm run check:test-tasktype-coverage`, `npm run check:no-media-provider-bypass`, and `npm run check:config-center-guards` pass.
- Search of changed Task/API declarations shows one Remake image-generation type, one action-sheet type, and zero video-generation declarations.
</verification>

<success_criteria>
- Approved selected image Prompts generate through existing infrastructure with exact frozen provenance.
- Selection, history, adoption, missing-state, action-sheet, and invalidation facts persist and survive refresh.
- Phase 8 backend exposes no batch scheduler and no video execution path.
</success_criteria>

<output>
Create `.planning/phases/08-keyframe-generation/08-02-SUMMARY.md` when done.
</output>
