---
phase: 08-keyframe-generation
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/remake-projects/keyframes/contracts.ts
  - src/lib/remake-projects/keyframes/task-contract.ts
  - src/lib/remake-projects/keyframes/service.ts
  - src/lib/remake-projects/keyframes/action-sheet.ts
  - src/lib/remake-projects/service.ts
  - src/lib/remake-projects/scenedetect/mutations.ts
  - src/lib/remake-projects/scenedetect/keyframes.ts
  - src/lib/workers/handlers/remake-keyframe-image.ts
  - src/app/api/remake-projects/[projectId]/keyframes/route.ts
  - src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts
  - tests/unit/remake-projects/remake-keyframe-task-contract.test.ts
  - tests/integration/remake-projects/keyframe-eligibility.test.ts
  - tests/integration/remake-projects/keyframe-service.test.ts
  - tests/integration/remake-projects/keyframe-action-sheet.test.ts
  - tests/integration/api/remake-projects-keyframes-adopt.test.ts
autonomous: true
requirements: [KFRM-01, KFRM-02, KFRM-03, KFRM-04, KFRM-05, KFRM-06, KFRM-07]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "KFRM-01/KFRM-07: only current selected slots with current adopted, approved, non-invalidated image Prompts are eligible; unavailable slots are excluded from default generation and appear in project missing items."
    - "KFRM-02: model, size, quality, references, candidate count, and extra parameters are validated against project allow-lists and the selected model's capabilities before a Task is created."
    - "KFRM-03/KFRM-05: every generation or regeneration appends immutable batch/candidate/output/provenance records and never rewrites historical Prompts, images, Tasks, batches, adopted pointers, or invalidation history."
    - "KFRM-04: preview reads do not move an adopted pointer; an explicit authorized adoption transaction returns the server pointer, and any adoption failure preserves the previous pointer."
    - "KFRM-06: Start/End-only Shots and models without Middle support expose the actual legal slot set; neither service, Worker, nor snapshot fabricates a frame."
    - "D-08/D-16: a new revision starts with no selected slots; valid selection persists on the server, and a per-submit candidate-count override of 1..4 is frozen without changing the project default."
    - "D-11: an approved current Shot revision has one deterministic Start -> Middle -> End action-sheet output with ordered timestamps and source provenance; older sheets remain stored and explicitly invalidated."
    - "D-10/D-12/D-19: adopted generated images are primary future video references, while the action sheet remains a distinct auxiliary action reference and at most one current candidate is adopted per legal slot."
  artifacts:
    - path: "src/lib/remake-projects/keyframes/service.ts"
      provides: "Eligibility, selection, append-only history, transactional adoption, and missing-item projection"
    - path: "src/lib/remake-projects/keyframes/action-sheet.ts"
      provides: "Deterministic revision-bound three-frame action-sheet renderer/persistence"
    - path: "src/lib/remake-projects/service.ts"
      provides: "Snapshot projection for tracks, batches, candidates, adopted pointers, legal slots, action sheet, and missing items"
    - path: "src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts"
      provides: "Authorized server selection/adoption mutation boundary"
  key_links:
    - from: "src/app/api/remake-projects/[projectId]/keyframes/route.ts"
      to: "src/lib/config-service.ts"
      via: "all runtime selections are resolved and validated before Task submission"
      pattern: "resolveProjectModelCapabilityGenerationOptions"
    - from: "src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts"
      to: "src/lib/remake-projects/keyframes/service.ts"
      via: "explicit selection/adoption mutations revalidate active revision and ownership"
      pattern: "adoptKeyframeCandidate"
    - from: "src/lib/remake-projects/scenedetect/keyframes.ts"
      to: "src/lib/remake-projects/keyframes/action-sheet.ts"
      via: "revision/keyframe changes invalidate prior generated candidates and action sheet without deleting assets"
      pattern: "invalidateKeyframeOutputsForRevision"
  prohibitions:
    - statement: "MUST NOT silently use a Middle frame, reference asset, or model parameter that the chosen Shot/model/project contract does not permit."
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "Descriptor-less actual-input prohibition retained for judgment review."
    - statement: "MUST NOT treat the original three-frame action sheet as an adopted generated image or allow it to replace the user's primary new-image choice."
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "Descriptor-less semantic-separation prohibition retained for judgment review."
---

<objective>
Expand the tracer into the complete legal slot/parameter contract, immutable version selection, missing-item projection, and revision-bound action-sheet lifecycle.

<purpose>Users need exact inputs, preserved history, explicit adoption, and a reliable auxiliary action reference before the Storyboard and Video workspaces can render truthful server state.</purpose>
<output>Full eligibility/configuration service, selection/adoption API, enriched snapshot, action-sheet derivation, invalidation wiring, and focused service/API tests.</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-keyframe-generation/08-CONTEXT.md
@.planning/phases/08-keyframe-generation/08-RESEARCH.md
@.planning/phases/08-keyframe-generation/08-PATTERNS.md
@.planning/phases/08-keyframe-generation/08-UI-SPEC.md
@.planning/phases/08-keyframe-generation/08-01-SUMMARY.md
</execution_context>

<context>
<interfaces>
Plan 08-01 provides `KeyframeSlot`, `RemakeKeyframeTaskPayload`, `buildRemakeKeyframeTaskDescriptor`, `assertKeyframeGenerationInputCurrent`, `appendKeyframeGenerationBatch`, the keyframe Prisma client delegates, and `REMAKE_KEYFRAME_IMAGE_GENERATE` routing.
Existing `resolveProjectModelCapabilityGenerationOptions` must remain the only effective-options resolver. Existing Remake snapshot `shots[].keyframes` maps source slots as start/middle/end, and existing Prompt summaries distinguish latest, adopted, status, and invalidation. Existing `createMany` invalidation paths in SceneDetect mutations/keyframes preserve prior rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist explicit slot selection and validate every legal generation input</name>
  <files>src/lib/remake-projects/keyframes/contracts.ts, src/lib/remake-projects/keyframes/task-contract.ts, src/lib/remake-projects/keyframes/service.ts, src/lib/remake-projects/service.ts, src/app/api/remake-projects/[projectId]/keyframes/route.ts, tests/unit/remake-projects/remake-keyframe-task-contract.test.ts, tests/integration/remake-projects/keyframe-eligibility.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-07..D-09 and D-13..D-16
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` Original frame selection and Data/state/task presentation
    - `src/lib/config-service.ts` `resolveProjectModelCapabilityGenerationOptions`
    - `src/lib/model-config-contract.ts` capability selection types and allow-lists
    - `src/lib/remake-projects/prompt/service.ts` `getAdoptedPromptForGeneration`
    - `src/lib/remake-projects/service.ts` current snapshot mapping and missing-state conventions
  </read_first>
  <behavior>
    - Test 1: a fresh current revision exposes Start/Middle/End source evidence but selects none; selecting eligible slots persists and survives a fresh snapshot.
    - Test 2: missing, unapproved, invalidated, non-adopted, or stale Prompts cannot be selected through API/keyboard-equivalent mutation and are projected with a readable reason.
    - Test 3: model key, size, quality, reference assets, 1..4 candidate count, and extra options are accepted only when project/model capability contracts allow them; no provider/model fallback occurs.
    - Test 4: Start/End-only and no-Middle model contracts return the exact legal selected subset and missing reasons without inserting a synthetic Middle ref.
    - Test 5: unapproved/missing Prompt slots appear in project `missingItems` and the default-generation eligibility count excludes them.
  </behavior>
  <action>Extend the strict contracts with server-owned selection mutation input, legal-slot/capability result, resolved generation options, and missing-item reason codes. Implement `setKeyframeTrackSelection` and `getKeyframeGenerationEligibility`: selection is stored on the track for the active revision, defaults false, and requires a current adopted Prompt that `getAdoptedPromptForGeneration` accepts. Reject an unavailable slot regardless of client UI. At submission, resolve model, size, quality, reference assets, and all capability selections through the existing configuration resolver and project ownership checks; store the exact resolved result in the frozen batch input. Candidate count accepts the original 1..4 contract, using the project default unless the user supplies a one-request override; never write the override back to project config per D-16. Enrich `getRemakeProjectSnapshot` with selected tracks, legal slots, eligibility counts, explicit disabled reasons, and project missing items. Represent absent Middle as absence plus reason, not a copied Start/End source. Preserve the separate current original-frame evidence in every response per D-07/D-10/D-19.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-keyframe-task-contract.test.ts tests/integration/remake-projects/keyframe-eligibility.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New revisions have zero default selections and persist only explicit valid choices.
    - Every accepted parameter/reference belongs to the Project and selected model capability allow-list.
    - The snapshot exposes exact legal slots, eligibility, disabled reasons, candidate default/override, and missing items.
    - No test response contains a fabricated Middle source.
  </acceptance_criteria>
  <done>All single-Shot generation inputs are server-authoritative, capability-valid, refresh-stable, and truthfully projected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append complete batch histories and adopt candidates transactionally</name>
  <files>src/lib/remake-projects/keyframes/service.ts, src/lib/remake-projects/service.ts, src/lib/workers/handlers/remake-keyframe-image.ts, src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts, tests/integration/remake-projects/keyframe-service.test.ts, tests/integration/api/remake-projects-keyframes-adopt.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-17..D-20
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` Candidate, comparison and adoption
    - `src/lib/remake-projects/prompt/service.ts` adopted-pointer transaction and history projection
    - `src/app/api/remake-projects/[projectId]/prompts/tracks/[trackId]/route.ts` authorization/conflict mapping
    - `src/lib/workers/handlers/panel-image-task-handler.ts` candidate loop, excluding Panel overwrite persistence
  </read_first>
  <behavior>
    - Test 1: candidateCount 1..4 appends one batch and exactly that many immutable, ordered candidates with Task/output/provenance links.
    - Test 2: regeneration appends a later batch and retains every prior batch/candidate and the existing adopted pointer.
    - Test 3: preview/history reads perform no write; only `adoptKeyframeCandidate` changes a pointer after ownership/current-revision/non-invalidated checks.
    - Test 4: adopting a candidate replaces at most one pointer for that slot while preserving the old candidate and an auditable adoption-history event.
    - Test 5: failed/stale/unauthorized adoption returns a conflict/not-found response and the prior pointer remains byte-for-byte unchanged.
  </behavior>
  <action>Expand the Worker candidate loop to persist every successful uploaded output under one batch, preserving candidate order and partial Task state without hiding unfinished candidates. Implement `getKeyframeTrackHistory` and `adoptKeyframeCandidate` as server services; adoption verifies project ownership, candidate-to-batch-to-current-track identity, active revision, current Prompt provenance, candidate success, and non-invalidation inside one transaction. Record an append-only adoption history/provenance event before moving the sole nullable pointer for that slot. Implement GET history and PATCH adoption on the track route using existing error mapping. Enrich the project snapshot with frozen batch summaries, per-candidate identity/output/task status, server-authoritative current adopted id, and invalidation state; do not place full Prompt content in the snapshot. Keep preview/comparison absent from these mutations because it is local UI state per D-18/D-20.</action>
  <verify>
    <automated>npx vitest run tests/integration/remake-projects/keyframe-service.test.ts tests/integration/api/remake-projects-keyframes-adopt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each batch is immutable and includes exactly the candidates produced by its Task.
    - Regeneration never changes older batch/candidate/input summaries.
    - Successful adoption returns the new server pointer; all failed adoptions retain the previous pointer.
    - One active revision has no more than one adopted candidate for each legal Start/Middle/End slot.
  </acceptance_criteria>
  <done>Users can build permanent candidate histories and move a single server-owned adopted pointer without conflating preview and adoption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive and invalidate the current revision action sheet</name>
  <reversibility rating="costly">D-11 makes the ordered action sheet and provenance a stable Phase 9 auxiliary input; changing its identity/layout contract later requires asset and provenance migration.</reversibility>
  <files>src/lib/remake-projects/keyframes/action-sheet.ts, src/lib/remake-projects/keyframes/service.ts, src/lib/remake-projects/service.ts, src/lib/remake-projects/scenedetect/mutations.ts, src/lib/remake-projects/scenedetect/keyframes.ts, src/lib/workers/handlers/remake-keyframe-image.ts, tests/integration/remake-projects/keyframe-action-sheet.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-10..D-12
    - `.planning/phases/08-keyframe-generation/08-RESEARCH.md` Pattern 4 and action-sheet assumptions
    - `src/lib/remake-projects/scenedetect/mutations.ts` revision creation/invalidation transaction
    - `src/lib/remake-projects/scenedetect/keyframes.ts` keyframe result/invalidation path
    - `src/lib/workers/handlers/image-task-handler-shared.ts` media normalization helpers
    - `src/lib/storage/factory.ts` existing object-storage abstraction
  </read_first>
  <behavior>
    - Test 1: a review-confirmed current revision with real Start/Middle/End media produces one horizontal ordered action sheet with position/timestamp labels.
    - Test 2: action-sheet provenance freezes revision id, ordered source media ids/timestamps, renderer schema/version, Task, and output asset.
    - Test 3: repeating the same confirmed revision is idempotent and does not create duplicate current sheets.
    - Test 4: Shot boundary/keyframe revision change retains the old sheet and candidates, appends invalidations, clears them from current eligibility, and allows a new sheet for the new confirmed revision.
    - Test 5: incomplete/unconfirmed source frames produce a waiting/missing state and no invented pixels or output record.
  </behavior>
  <action>Implement a narrowly scoped deterministic Sharp-based renderer in `action-sheet.ts` using the existing installed media stack: load the active revision's real Start, Middle, and End media in exact order, preserve the project video aspect ratio in three stable cells, and burn the required position and source timestamps into the derived asset. Invoke it through the existing image Worker/Task lifecycle when a Shot transitions to review-confirmed; use a canonical revision fingerprint/operation key so retries are idempotent. Persist the sheet as a distinct `RemakeOutputVersion` kind with ordered provenance and current-revision projection. Extend both SceneDetect native mutation and keyframe-result revision paths to call one `invalidateKeyframeOutputsForRevision` service that appends invalidations for prior sheets, tracks, batches/candidates, and adopted pointers while retaining all media/history. Do not turn the action sheet into an adopted candidate. Snapshot it under an explicit auxiliary-action reference field with current/missing/invalidated reason per D-11/D-12.</action>
  <verify>
    <automated>npx vitest run tests/integration/remake-projects/keyframe-action-sheet.test.ts tests/integration/remake-projects/keyframe-service.test.ts tests/integration/api/remake-projects-scenedetect-mutations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Only confirmed complete current revisions create a sheet, in Start -> Middle -> End order.
    - The stored sheet can be reproduced from its exact source/timestamp/revision/renderer provenance.
    - Any upstream revision change preserves but invalidates old sheets and generated-image history.
    - Current snapshot semantics never mix the action sheet with primary adopted new images.
  </acceptance_criteria>
  <done>The current Shot revision exposes one traceable auxiliary action sheet, and every superseded sheet/history remains retained with explicit invalidation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/lib/remake-projects/keyframes/contracts.ts`: selection, legal-slot, effective-options, missing-item, history, and adoption DTOs.
- `src/lib/remake-projects/keyframes/service.ts`: `setKeyframeTrackSelection`, `getKeyframeGenerationEligibility`, `getKeyframeTrackHistory`, `adoptKeyframeCandidate`, and `invalidateKeyframeOutputsForRevision`.
- `src/lib/remake-projects/keyframes/action-sheet.ts`: deterministic `renderRemakeStoryboardActionSheet` and revision fingerprint/provenance helpers.
- `src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts`: track-history `GET`, selection/adoption `PATCH`.
- `src/lib/remake-projects/service.ts`: keyframe generation, missing-item, action-sheet, and future video-input readiness snapshot fields.
- The five focused test paths listed in frontmatter.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> selection/generation/adoption APIs | Untrusted ids and capability selections attempt to change durable project state. |
| SceneDetect revision -> Phase 8 histories | Upstream edits must invalidate current eligibility without deleting evidence. |
| Stored source media -> action-sheet Worker | User/project media is decoded and composed into a derived object-store asset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07 | Elevation | selection/adoption track route | high | mitigate | Authorize Project, follow candidate relation to current Shot/revision, and repeat checks inside transaction. |
| T-08-08 | Tampering | adopted pointer | high | mitigate | Use one transactional service; reject stale/invalidated candidate and record history before pointer update. |
| T-08-09 | Tampering | capability/reference selections | medium | mitigate | Strict DTO plus central model resolver and project-owned reference lookup; no silent fallback. |
| T-08-10 | Repudiation | regeneration/adoption/action sheet | medium | mitigate | Freeze batch inputs and append Task/adoption/derivation provenance with timestamps and actor/task identity. |
| T-08-11 | Denial of Service | image composition/provider candidate loops | medium | mitigate | Bound candidates to 1..4, bound media via existing storage validation, and use current image queue controls/idempotency. |
| T-08-12 | Information Disclosure | action-sheet/source media | high | mitigate | Resolve only project-owned opaque media through existing storage boundary and expose authorized signed/media routes only. |
</threat_model>

<verification>
- Focused contract/eligibility/service/adoption/action-sheet tests pass.
- `npm run typecheck` proves snapshot and Task contracts agree.
- Existing SceneDetect mutation tests prove Phase 8 invalidation does not regress revision handling.
- `npm run test:guards` accepts capability, reference normalization, Task coverage, and API boundaries.
</verification>

<success_criteria>
- All legal slots/parameters and only those inputs can generate.
- Histories are append-only, preview is non-mutating, and adoption is explicit/transactional.
- Start/End-only and no-Middle cases remain truthful throughout service/snapshot state.
- Missing approved Prompts are visible without implementing Phase 10 batch scheduling.
- Action sheets are reproducible auxiliary references tied to exact confirmed revisions.
</success_criteria>

<output>
Create `.planning/phases/08-keyframe-generation/08-02-SUMMARY.md` when done.
</output>
