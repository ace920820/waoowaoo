---
phase: 08-keyframe-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql
  - src/lib/remake-projects/keyframes/action-sheet.ts
  - src/lib/remake-projects/keyframes/contracts.ts
  - src/lib/remake-projects/keyframes/task-contract.ts
  - src/lib/remake-projects/keyframes/invalidation.ts
  - src/lib/remake-projects/service.ts
  - src/lib/remake-projects/scenedetect/keyframes.ts
  - tests/unit/remake-projects/remake-keyframe-task-contract.test.ts
  - tests/integration/remake-projects/keyframe-persistence.test.ts
  - tests/integration/remake-projects/keyframe-action-sheet.test.ts
autonomous: true
requirements: [KFRM-03, KFRM-04, KFRM-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "KFRM-03/KFRM-05 and D-14/D-17: every submission can retain an immutable batch, candidates, frozen inputs, Task/output/provenance links, and prior history."
    - "KFRM-04 and D-08/D-19/D-20: explicit slot selection and one nullable adopted pointer persist per active Shot revision/slot, while adoption replacements append history."
    - "D-10/D-11/D-12: original frames remain revision evidence and the action sheet is a revision-bound derived Remake output, distinct from generated candidates."
    - "No Phase 8 write creates or depends on NovelPromotionEpisode, NovelPromotionStoryboard, or NovelPromotionPanel rows."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Small durable Remake keyframe track/batch/candidate/adoption relations using the existing output, provenance, and invalidation spine"
    - path: "prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql"
      provides: "Forward migration preserving existing Remake and Novel Promotion data"
    - path: "tests/integration/remake-projects/keyframe-persistence.test.ts"
      provides: "Persistence, append-only, action-sheet, and no-Novel-duplication proof"
    - path: "src/lib/remake-projects/keyframes/action-sheet.ts"
      provides: "D-11 deterministic renderer, revision fingerprint, and provenance preparation"
    - path: "src/lib/remake-projects/keyframes/invalidation.ts"
      provides: "Shared append-only invalidation service for revision and original-keyframe changes"
  key_links:
    - from: "RemakeKeyframeCandidate"
      to: "RemakeOutputVersion"
      via: "one immutable output relation instead of Panel image fields"
      pattern: "outputVersionId"
    - from: "RemakeKeyframeTrack"
      to: "RemakeKeyframeCandidate"
      via: "explicit selectedForGeneration state and nullable adoptedCandidateId"
      pattern: "adoptedCandidateId"
    - from: "RemakeOutputVersion"
      to: "RemakeProvenanceRecord / RemakeInvalidation"
      via: "revision-bound action-sheet and generated-output evidence"
      pattern: "outputVersionId"
    - from: "remake keyframe contracts and scenedetect/keyframes.ts"
      to: "keyframes/invalidation.ts"
      via: "same-transaction invalidation for both upstream mutation paths"
      pattern: "invalidateKeyframeOutputsForRevision"
---

<objective>
Adapt the existing Remake persistence graph for keyframe generation without creating a parallel Novel Promotion domain.

Purpose: Establish the smallest durable representation required by KFRM-03..05 and locked decisions D-08, D-10, D-11, D-12, D-14, D-17, D-19, and D-20 before any API or UI writes depend on it.
Output: Audited Prisma relations, durable action-sheet/invalidation services, one forward migration, and integration proof for persisted selection, immutable history, explicit adoption, and revision-bound action sheets.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-keyframe-generation/08-CONTEXT.md
@.planning/phases/08-keyframe-generation/08-RESEARCH.md
@.planning/phases/08-keyframe-generation/08-PATTERNS.md

<interfaces>
The current schema already provides `RemakeShot`, `RemakeShotRevision`, `RemakeOutputVersion`, `RemakeProvenanceRecord`, `RemakeInvalidation`, `RemakePromptTrack`, and immutable `RemakePromptVersion`. Extend those identities and relations; do not introduce an asset/version/provenance substitute.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one selected Start slot through batch, candidate, adoption, and action-sheet history</name>
  <reversibility rating="costly">The D-11/D-14/D-17 persistence contract becomes the durable input history consumed by Phase 9.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql, src/lib/remake-projects/keyframes/contracts.ts, src/lib/remake-projects/keyframes/task-contract.ts, tests/unit/remake-projects/remake-keyframe-task-contract.test.ts, tests/integration/remake-projects/keyframe-persistence.test.ts</files>
  <read_first>
    - `prisma/schema.prisma` models RemakeShotRevision through RemakePromptRun
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-08, D-10..D-12, D-14, D-17, D-19, D-20
    - `.planning/phases/08-keyframe-generation/08-RESEARCH.md` Summary, Pattern 2, Pattern 4, and Assumptions Log
    - `prisma/migrations/20260809100000_add_remake_prompt_analysis/migration.sql`
  </read_first>
  <behavior>
    - Test 1: a `(shotRevisionId, slot)` track is unique, starts unselected, and persists an explicit selected state across a fresh query.
    - Test 2: a batch freezes Prompt version, Task, model/options/references, requested candidate count, operation key, fingerprint, and input snapshot; candidates are ordered and immutable.
    - Test 3: adoption changes one nullable track pointer and appends an event containing the previous and next candidate without deleting either candidate.
    - Test 4: a Start/Middle/End action sheet is an output for one exact revision/fingerprint with ordered source-frame provenance and append-only invalidation.
  </behavior>
  <action>Audit `RemakeOutputVersion`, `RemakeProvenanceRecord`, and `RemakeInvalidation` first, then extend them rather than replacing them. Add only the relational objects needed for durable querying: `RemakeKeyframeTrack` unique by revision and `start|middle|end` slot with `selectedForGeneration` default false and a nullable adopted-candidate pointer; `RemakeKeyframeBatch` with the frozen D-14/D-16 submission snapshot and operation identity; ordered immutable `RemakeKeyframeCandidate` rows linked one-to-one to existing output versions; and append-only `RemakeKeyframeAdoptionEvent` rows. Represent the D-11 action sheet through the existing output/provenance/invalidation graph, adding output kind/fingerprint/task/provenance relations and a revision-scoped uniqueness constraint instead of another asset hierarchy. Add reverse relations and indexes required for current-revision snapshot reads and invalidation. Do not add foreign keys to any Novel Promotion entity.</action>
  <verify>
    <automated>DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx prisma validate --schema prisma/schema.prisma &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/unit/remake-projects/remake-keyframe-task-contract.test.ts tests/integration/remake-projects/keyframe-persistence.test.ts</automated>
  </verify>
  <done>One Start-slot record round-trips with explicit selection, immutable batch/candidate evidence, adoption history, and a distinct revision action-sheet output, satisfying D-08/D-10/D-11/D-12/D-14/D-17/D-19/D-20.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare deterministic action-sheet provenance and shared upstream invalidation</name>
  <reversibility rating="costly">D-11 makes the ordered action-sheet identity and invalidation semantics a stable Phase 9 auxiliary-input contract.</reversibility>
  <files>src/lib/remake-projects/keyframes/action-sheet.ts, src/lib/remake-projects/keyframes/invalidation.ts, src/lib/remake-projects/service.ts, src/lib/remake-projects/scenedetect/keyframes.ts, tests/integration/remake-projects/keyframe-action-sheet.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-10..D-12 and D-17
    - `.planning/phases/08-keyframe-generation/08-RESEARCH.md` resolved action-sheet primitive and Pattern 4
    - `src/lib/image-label.ts` label SVG pattern
    - `src/lib/workers/utils.ts` existing Sharp label/composition and upload interfaces
    - `src/lib/remake-projects/prompt/service.ts` append-only invalidation analog
    - `src/lib/remake-projects/scenedetect/keyframes.ts` keyframe revision write boundary
  </read_first>
  <behavior>
    - Test 1: a confirmed complete revision prepares deterministic Start -> Middle -> End pixels with slot/timestamp labels, a canonical renderer version/fingerprint, and exact ordered source provenance.
    - Test 2: incomplete or unconfirmed source frames return an explicit waiting/missing result and prepare no output; the same revision/fingerprint is idempotent.
    - Test 3: both Shot revision edits and original-keyframe changes invoke one Project/Shot-scoped invalidation service in the upstream transaction.
    - Test 4: old action sheets, selections, batches, candidates, outputs, and adoption events remain stored but become ineligible; a new revision begins unselected.
  </behavior>
  <action>Implement the durable, queue-independent part of D-11 before Plan 08-02 wires execution: use installed Sharp and existing label/composition interfaces to define deterministic normalization, Start/Middle/End ordering, position/timestamp labels, renderer schema, canonical fingerprint, and the exact output/provenance write contract from Task 1. Add current/missing/invalidated action-sheet and keyframe-history projection to the Remake snapshot. Implement one idempotent `invalidateKeyframeOutputsForRevision` service and invoke it inside both SceneDetect revision and original-keyframe mutation transactions per D-10/D-12/D-17. Preserve every historical row and prevent cross-Project/Shot invalidation. Expose a callable action-sheet preparation/persistence interface for Plan 08-02's existing image-queue handler; do not add Task types, queue routing, Workers, or provider calls in this plan.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/integration/remake-projects/keyframe-action-sheet.test.ts</automated>
  </verify>
  <done>The D-11 renderer/provenance and D-17 invalidation contracts are durable, deterministic, idempotent, revision-scoped, and ready for Plan 08-02 queue wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply the forward migration and prove compatibility with existing Remake and Novel Promotion rows</name>
  <precondition>The tracked `docker-compose.test.yml` can start isolated MySQL on port 3307.</precondition>
  <files>prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql, tests/integration/remake-projects/keyframe-persistence.test.ts</files>
  <read_first>
    - `docker-compose.test.yml`
    - `tests/integration/remake-projects/prompt-service.test.ts` database fixture pattern
    - `tests/integration/api/remake-project-phase5-compatibility.test.ts` legacy-mode compatibility assertions
  </read_first>
  <behavior>
    - Test 1: the migration applies to a database containing existing Remake Shot/Prompt/output/provenance/invalidation records and preserves them.
    - Test 2: keyframe persistence creates no Novel Promotion episode/storyboard/panel row and changes no pre-existing Novel Promotion row.
    - Test 3: repeated batch/action-sheet operation identities are rejected or reused by their declared uniqueness contract without deleting history.
  </behavior>
  <action>Write forward-only SQL in dependency order, apply it to the isolated database, and regenerate Prisma. Extend the persistence test with before/after counts and representative existing records so the migration proves compatibility and the Phase 8 write path proves domain separation. Keep all new columns additive or safely defaulted for existing rows; use neither schema push nor database reset.</action>
  <verify>
    <automated>docker compose -f docker-compose.test.yml up -d mysql &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx prisma migrate deploy --schema prisma/schema.prisma &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx prisma generate --schema prisma/schema.prisma &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/integration/remake-projects/keyframe-persistence.test.ts</automated>
  </verify>
  <done>The forward migration preserves existing domains, creates no Novel Promotion compatibility data, and leaves a generated Prisma client for Plan 08-02.</done>
</task>

</tasks>

<threat_model>
Concrete risks: migration constraint ordering could damage existing history, a loose candidate pointer could cross Shot ownership, and invalidation could target the wrong revision. Mitigate with forward-only SQL, candidate-to-track-to-revision foreign keys, scoped uniqueness, same-transaction Project/Shot traversal, and the focused compatibility/invalidation tests.
</threat_model>

<verification>
- Prisma validation, isolated migration deploy, client generation, persistence, action-sheet, and SceneDetect invalidation suites pass.
- `git diff --check -- prisma/schema.prisma prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql tests/integration/remake-projects/keyframe-persistence.test.ts` passes.
</verification>

<success_criteria>
- The existing Remake output/provenance/invalidation graph remains the media evidence spine.
- Persisted selection, immutable batches/candidates, adoption events, and action-sheet identity survive refresh and replacement.
- No Novel Promotion entity is duplicated or mutated by Phase 8 persistence.
</success_criteria>

<output>
Create `.planning/phases/08-keyframe-generation/08-01-SUMMARY.md` when done.
</output>
