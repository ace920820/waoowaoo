---
phase: 08-keyframe-generation
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - src/lib/remake-projects/keyframes/video-inputs.ts
  - src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoReferenceInputGroups.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoPanelCardBody.tsx
  - tests/unit/remake-projects/remake-video-input-contract.test.ts
  - tests/unit/remake-projects/remake-keyframe-stage.test.ts
  - tests/e2e/fixtures/remake-keyframe-project.ts
  - tests/e2e/remake-keyframes.spec.ts
  - scripts/verify-remake-keyframes-ui.mjs
autonomous: false
requirements: [KFRM-01, KFRM-02, KFRM-03, KFRM-04, KFRM-05, KFRM-06, KFRM-07]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02/D-12: the real Video page reads current Remake Shots, adopted new images as primary visual references, current original action sheets as auxiliary action references, approved Video Prompt state, project assets, and model configuration; every video submission control remains disabled until Phase 9."
    - "KFRM-06/D-19: the Video page names the actual legal adopted Start/Middle/End subset for the selected video model and never silently hides, substitutes, or fabricates a frame."
    - "D-05/D-13: Storyboard and Video preserve the original Asset Library and Project Configuration capabilities and share the same project source of truth."
    - "[UI covered: empty] No source/Shot, incomplete original keyframes, no selected Prompt, no generated slot, no adopted image, and missing Video inputs each render the specified Chinese empty state and upstream navigation without fictional cards."
    - "[UI covered: loading] Snapshot, original frames, action sheet, candidates, adoption, selection, and project configuration use stable-ratio skeleton/Task overlays; old main previews and other Shots remain usable, with no invented percentage or ETA."
    - "[UI covered: error] Image generation, snapshot, action sheet, adoption, and Video-input load failures show a Chinese summary plus retry/Task drawer route; one slot failure does not block others or hide adopted/history state."
    - "[UI covered: populated] Original frames, selected slots, immutable batches/candidates, adopted pointers, and Video reference groups render in fixed Start -> Middle -> End order with visually distinct authority/status labels."
    - "[UI covered: partial] One/two selected slots, partially completed candidates, and model-limited frame subsets render only real available inputs, preserve unfinished placeholders, and name the legal subset."
    - "[UI covered: overflow] Stage navigation and candidate tracks scroll at narrow widths; responsive cards/comparison never crop action bars or create page-level horizontal overflow."
    - "[UI covered: zero-one-many] Zero items show a truthful empty state, one item keeps the same stable grid/singular copy, and many chronological batches remain scannable without moving the adopted pointer."
    - statement: "[UI backstop: long text] Chinese long Prompt/error/disabled reason and long model/asset/stable-key values wrap or ellipsize with a focusable complete value at desktop and mobile widths."
      verification: backstop
    - statement: "[UI backstop: mutation loading] Selection/adoption/replace submission disables only the active control with inline loading; adopted state updates only from the server requery."
      verification: backstop
    - statement: "[UI backstop: adoption error] A failed adoption keeps the confirmation/error context and previous adopted pointer, exposes retry, and never moves the adopted label optimistically."
      verification: backstop
    - statement: "[UI backstop: comparison and targets] At 320px and 1440px, both comparison images remain visible without overlap, button copy wraps, and every operation has a 44px target."
      verification: backstop
  artifacts:
    - path: "src/lib/remake-projects/keyframes/video-inputs.ts"
      provides: "Pure actual-input/capability/readiness contract for the Phase 8 Video page and Phase 9 handoff"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      provides: "Real Remake Video workspace with grouped inputs/configuration and hard-disabled submission"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoReferenceInputGroups.tsx"
      provides: "Shared original Video reference-group presentation used by Novel Promotion and Remake"
    - path: "tests/e2e/remake-keyframes.spec.ts"
      provides: "Desktop/mobile real-route Storyboard/Video workflow and UI backstop verification"
    - path: "scripts/verify-remake-keyframes-ui.mjs"
      provides: "Repeatable local test DB/server/Playwright verification harness"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      to: "src/lib/remake-projects/keyframes/video-inputs.ts"
      via: "selected video model capability derives visible actual primary subset and readiness reasons"
      pattern: "buildRemakeVideoInputReadiness"
    - from: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoReferenceInputGroups.tsx"
      via: "shared original grouped-reference layout renders primary and auxiliary sources distinctly"
      pattern: "VideoReferenceInputGroups"
    - from: "scripts/verify-remake-keyframes-ui.mjs"
      to: "tests/e2e/remake-keyframes.spec.ts"
      via: "controlled services, migration, seed, server, and Playwright projects"
      pattern: "playwright"
  prohibitions:
    - statement: "MUST NOT imply that video generation is enabled, queued, progressing, or completed during Phase 8, and MUST NOT create a video Task through any visible or hidden action."
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "Descriptor-less phase-boundary prohibition retained for judgment review."
    - statement: "MUST NOT label the original action sheet as a primary adopted new-image reference or omit which adopted frames the selected video model would actually use."
      category: transparency
      status: unresolved
      verification: null
      resolution: null
      reason: "Descriptor-less actual-input prohibition retained for judgment review."
---

<objective>
Complete the real Phase 8 Video workspace and verify the entire keyframe workflow against the approved UI contract on desktop and mobile.

<purpose>The Video page must be useful before generation is enabled: users can inspect true future inputs and configure the project without being misled about main versus auxiliary references or Phase 9 availability.</purpose>
<output>Actual-input contract, shared original reference layout, full disabled Video page, deterministic browser harness/tests, and blocking human UI acceptance.</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-keyframe-generation/08-CONTEXT.md
@.planning/phases/08-keyframe-generation/08-RESEARCH.md
@.planning/phases/08-keyframe-generation/08-PATTERNS.md
@.planning/phases/08-keyframe-generation/08-UI-SPEC.md
@.planning/phases/08-keyframe-generation/08-03-SUMMARY.md
</execution_context>

<context>
<interfaces>
Plan 08-03 provides the real `RemakeVideoStage` boundary, shared `RemakeProductionTools`, full `RemakeSnapshot`, and Storyboard components. Plan 08-02 provides adopted primary candidates, current/invalidated action-sheet facts, approved Video Prompt summary, legal slots, frozen histories, and model/config facts.
The original `VideoPanelCardBody` reads a runtime-shaped card. Extract only the reference-group presentation into `VideoReferenceInputGroups`; keep the original component behavior and call it from the original body. `ModelCapabilityDropdown`, SettingsModal/project config mutation, audio/duration/resolution fields, and Task presentation remain the existing source of controls and values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render truthful future video inputs and retain original Video controls without submission</name>
  <files>src/lib/remake-projects/keyframes/video-inputs.ts, src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoReferenceInputGroups.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoPanelCardBody.tsx, tests/unit/remake-projects/remake-video-input-contract.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-01..D-03, D-05, D-10..D-15, D-19
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` Video adaptation, reference groups, readiness summary, copy
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx`
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoStageShell.tsx`
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoPanelCardBody.tsx`
    - `src/components/ui/config-modals/ModelCapabilityDropdown.tsx`
    - `src/lib/model-config-contract.ts`
  </read_first>
  <behavior>
    - Test 1: each Video card reads one current Remake Shot, approved/missing/needs-review Video Prompt status, adopted generated-image refs, current/invalidated action sheet, and shared project config.
    - Test 2: `buildRemakeVideoInputReadiness` derives the exact supported adopted subset for Start/End, first/last, or multi-frame capability without substituting missing Middle/source action frames.
    - Test 3: primary `主画面参考（采用的新画面）` and auxiliary `辅助动作参考（原始三帧动作表）` groups use distinct labels/status semantics and truthful missing/revision reasons.
    - Test 4: model, duration, resolution, audio, and capability fields remain viewable/configurable through existing project defaults/overrides.
    - Test 5: single, regenerate, and batch video submission controls are disabled with the fixed Phase 9 explanation and no callback/API/Task-type path can create a video Task.
    - Test 6: the original Novel Promotion Video card still renders its prior reference behavior after the shared presentational extraction.
  </behavior>
  <action>Create the pure `buildRemakeVideoInputReadiness` contract from current snapshot plus selected video model capability. Return adopted count, exact legal main-frame subset with slot/candidate ids, approved/missing/needs-review Video Prompt state, current/missing/invalidated action-sheet state, and readable reasons. Extract the original card's grouped reference display into `VideoReferenceInputGroups` with explicit props and update `VideoPanelCardBody` to consume it unchanged; then use that shared component from `RemakeVideoStage`. Expand the real page into the original horizontal card structure with Prompt, model, duration, resolution, audio, capabilities, Task information area, and shared Asset/Configuration tools. Group adopted generated images under the exact primary title and action sheet under the exact auxiliary title. Add the header readiness summary `已采用画面 {n}/3`, `Video Prompt`, and `动作表`. Disable every video generate/regenerate/batch control and render the fixed UI-SPEC Phase 9 message beneath it; omit all video submission mutation imports and do not add a VGEN Task type/route. Preserve Novel Promotion behavior with the focused regression.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-video-input-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Video cards expose real current Remake inputs, shared project controls, and exact readiness states.
    - Main adopted images and auxiliary action sheet are distinct in copy, status, data, and semantics.
    - The selected model's actual legal subset is named and contains no fabricated frame.
    - All video submissions remain inert with Phase 9 explanation, while original Novel Promotion cards retain their behavior.
  </acceptance_criteria>
  <done>The real Video page is inspectable/configurable and truthful about exact future inputs while generation remains impossible in Phase 8.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Automate UI states, accessibility, mutation failure, and responsive backstops</name>
  <files>tests/unit/remake-projects/remake-keyframe-stage.test.ts, tests/e2e/fixtures/remake-keyframe-project.ts, tests/e2e/remake-keyframes.spec.ts, scripts/verify-remake-keyframes-ui.mjs</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` UI Considerations, Responsive/accessibility, Copywriting
    - `tests/e2e/remake-prompt.spec.ts` real-route Remake Playwright style
    - `playwright.config.ts` desktop/tablet/mobile projects and base URL
    - `tests/setup/env.ts` isolated database defaults
    - `docker-compose.test.yml` controlled MySQL/Redis services
    - `tests/integration/api/remake-projects-prompt-review.test.ts` authenticated Remake fixture conventions
  </read_first>
  <behavior>
    - Test 1: empty, loading, error, populated, partial, zero/one/many, failed Task, and invalidated revision fixtures render their exact truthful state without fake data.
    - Test 2: long Chinese Prompt/model/asset/stable-key/disabled-reason values fit and remain inspectable at 320px and 1440px.
    - Test 3: selection/adoption pending state disables only its own action; an injected adoption 409 retains the previous adopted label, dialog/error, and retry.
    - Test 4: preview/comparison and adoption remain distinct after reload; success uses the refetched pointer and history still contains the replaced candidate.
    - Test 5: 320px/390px/1024px/1440px have no page overflow or incoherent overlap; comparison images, navigation, candidate tracks, wrapped CTA copy, and 44px targets remain operable.
    - Test 6: checkbox/radio semantics, focus entry/return, Escape, visible focus, aria-live, text+icon statuses, and exact alt formats pass browser assertions.
    - Test 7: Video page renders real inputs/tools with all submission actions disabled and no intercepted video POST/Task request.
  </behavior>
  <action>Extend the unit contract for every UI-SPEC covered/backstop state. Create a deterministic Playwright fixture that seeds an authenticated Remake Project with multiple Shots/revisions, approved and unavailable Prompts, one/two/three selected slots, several immutable batches/candidates, an adopted pointer, action-sheet current/invalidated states, tasks, long text, project assets, and model config. Do not depend on an operator-created project id and do not skip the required scenarios. Implement `scripts/verify-remake-keyframes-ui.mjs` to start the tracked isolated services, apply migrations non-interactively, seed the fixture, launch Next on port 3100, wait for health, run the single Playwright spec across the required viewports, and terminate its child server on success/failure without deleting user data outside the controlled test database. In the spec, assert DOM dimensions and take stable screenshots for Storyboard populated/comparison/error and Video disabled-input states at 320px and 1440px. Intercept only the adoption error/success and video-request observation needed by each scenario; use real route/snapshot rendering for the rest. Verify Asset Library and Project Configuration open and retain full existing controls on both pages.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-keyframe-stage.test.ts tests/unit/remake-projects/remake-video-input-contract.test.ts &amp;&amp; node scripts/verify-remake-keyframes-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Every covered UI consideration and all four declared backstops have an automated assertion.
    - Mutation loading/error behavior preserves server authority and previous adoption.
    - Desktop/mobile screenshots and dimension assertions show no overlap/overflow and retain readable targets/text.
    - Storyboard and Video each expose the complete Asset Library/Configuration operations.
    - The browser observes zero video submission requests during Phase 8 scenarios.
  </acceptance_criteria>
  <done>Automated unit and real-browser evidence covers the complete approved UI state matrix and Phase 8 boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Accept the real Storyboard and Video workflow</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/storyboard/RemakeStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, tests/e2e/remake-keyframes.spec.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` complete approved visual/interaction contract
    - `08-04-SUMMARY.md` automated verification evidence produced before this checkpoint
  </read_first>
  <action>After Tasks 1-2 and all automated gates pass, keep the agent-started local server and seeded authenticated fixture available at the URL reported by `scripts/verify-remake-keyframes-ui.mjs`. Ask the user to verify only the high-judgment visual/interaction semantics listed below; no manual data setup or implementation work is delegated.</action>
  <what-built>Real Remake Storyboard generation/comparison/adoption and real Remake Video input/configuration page with Phase 9 submission disabled.</what-built>
  <how-to-verify>
    1. Open the seeded Storyboard URL at desktop width. Confirm each Shot reads as one workspace with original evidence above and new-image slots below; the current adopted main preview and `采用此版本` are the visual focus.
    2. Select an eligible original Prompt, generate/retry, switch viewed candidates, open/close comparison, and adopt/replace. Confirm preview never looks adopted until explicit confirmation and old batches remain visible.
    3. Open Asset Library and Project Configuration from Storyboard, change a reversible project setting, then enter Video and confirm the same setting and full tools remain available.
    4. On Video, confirm adopted new images and the original action sheet are unmistakably separate, the actual legal input subset is clear, and every video submission action is disabled with the Phase 9 explanation.
    5. Repeat key screens at a narrow viewport. Confirm navigation/candidate tracks scroll locally, text wraps, controls remain reachable, and no content overlaps or creates page-level horizontal overflow.
  </how-to-verify>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-keyframe-task-contract.test.ts tests/unit/remake-projects/remake-keyframe-stage.test.ts tests/unit/remake-projects/remake-video-input-contract.test.ts &amp;&amp; node scripts/verify-remake-keyframes-ui.mjs</automated>
    <human-check>Approve only if the five steps match the UI-SPEC and no visual state implies automatic selection, adoption, fabricated input, or enabled video generation.</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms two-layer visual hierarchy and explicit adoption semantics.
    - User confirms the original shared Asset Library/Configuration capabilities on both pages.
    - User confirms primary/auxiliary Video inputs and Phase 9 boundary are unambiguous.
    - User confirms responsive layout, focus/interaction, and long content remain usable.
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe the exact Shot, state, viewport, and issue.</resume-signal>
  <done>Human acceptance confirms the automated implementation matches the approved Phase 8 UI contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/lib/remake-projects/keyframes/video-inputs.ts`: `buildRemakeVideoInputReadiness`, `RemakeVideoInputReadiness`, and actual-primary/auxiliary reference types.
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoReferenceInputGroups.tsx`: shared original reference-group presentation.
- Expanded `RemakeVideoStage.tsx`: real card/input/configuration/readiness page with no video submission path.
- `tests/e2e/fixtures/remake-keyframe-project.ts`: deterministic authenticated Phase 8 UI fixture.
- `tests/e2e/remake-keyframes.spec.ts`: Storyboard/Video desktop/mobile state, adoption, accessibility, and boundary suite.
- `scripts/verify-remake-keyframes-ui.mjs`: controlled end-to-end verification harness.

## Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Approved image Prompt -> existing gateway -> traceable generation, comparison, adoption | 08-01..04 | COVERED | Tracer, expansion, Storyboard, Video handoff |
| REQ | KFRM-01 | Current adopted image Prompt generates Start/Middle/End | 08-01, 08-02, 08-03 | COVERED | Server gate plus UI |
| REQ | KFRM-02 | Model/size/quality/references/allowed params | 08-02, 08-03 | COVERED | Central resolver plus controls |
| REQ | KFRM-03 | Immutable version plus Prompt/params/Task/output relations | 08-01, 08-02 | COVERED | Schema, Worker, provenance |
| REQ | KFRM-04 | Original/candidate comparison and one adopted per slot | 08-02, 08-03 | COVERED | Server pointer plus inline comparison |
| REQ | KFRM-05 | Regenerate after Prompt edit without overwrite | 08-01, 08-02, 08-03 | COVERED | Append-only/invalidation/history UI |
| REQ | KFRM-06 | Actual Start/End/no-Middle contract | 08-02, 08-03, 08-04 | COVERED | Service and both pages |
| REQ | KFRM-07 | Missing approved Prompts excluded and visible | 08-02, 08-03 | COVERED | Eligibility and project missing items |
| RESEARCH | R-01 | Dedicated Remake track/batch/candidate domain, no synthetic Novel entities | 08-01, 08-03 | COVERED | Schema and adapter |
| RESEARCH | R-02 | Existing Task/image queue/gateway/storage/config only | 08-01, 08-02 | COVERED | No new dependency/external provider API |
| RESEARCH | R-03 | Strict frozen input, ownership, stale recheck, short transaction | 08-01, 08-02 | COVERED | Security and service tests |
| RESEARCH | R-04 | Server selection/adoption authority; client preview only | 08-02, 08-03 | COVERED | API and UI tests |
| RESEARCH | R-05 | Revision-bound deterministic action sheet | 08-02 | COVERED | Renderer/provenance/invalidation |
| RESEARCH | R-06 | Real Video page with no Phase 8 video submission | 08-03, 08-04 | COVERED | Real mount plus hard boundary |
| RESEARCH | R-07 | Focused unit/API/Worker/Playwright validation | 08-01..04 | COVERED | Every risk boundary mapped |
| CONTEXT | D-01 | Mount real Storyboard and Video pages | 08-03, 08-04 | COVERED | No static page |
| CONTEXT | D-02 | Enable images, disable Video generation | 08-03, 08-04 | COVERED | No VGEN route/type |
| CONTEXT | D-03 | Free navigation, action-level gates | 08-03 | COVERED | Stage registry and reasons |
| CONTEXT | D-04 | Prompt -> Storyboard CTA/count, no auto jump | 08-03 | COVERED | Navigation test |
| CONTEXT | D-05 | Preserve original Asset Library/Config | 08-03, 08-04 | COVERED | Shared existing components |
| CONTEXT | D-06 | One two-layer workspace per Shot | 08-03 | COVERED | Adapter/UI |
| CONTEXT | D-07 | Immutable upper original frames/status/selection | 08-02, 08-03 | COVERED | Snapshot/UI |
| CONTEXT | D-08 | Explicit persisted selection; invalid disabled | 08-02, 08-03 | COVERED | Server/API/UI |
| CONTEXT | D-09 | Full original image-card capabilities per slot | 08-03 | COVERED | Slot UI |
| CONTEXT | D-10 | Adopted image primary; originals evidence | 08-02..04 | COVERED | Distinct contracts/copy |
| CONTEXT | D-11 | Automatic revision action sheet | 08-02 | COVERED | Worker/invalidation |
| CONTEXT | D-12 | Primary adopted refs vs auxiliary sheet | 08-02, 08-04 | COVERED | Video groups |
| CONTEXT | D-13 | Shared project facts | 08-03, 08-04 | COVERED | Production tools/config |
| CONTEXT | D-14 | Freeze submit snapshot | 08-01, 08-02 | COVERED | Batch contract |
| CONTEXT | D-15 | Reuse existing config/gates/states | 08-02..04 | COVERED | No Remake rule fork |
| CONTEXT | D-16 | 1..4 default/one-submit override | 08-02, 08-03 | COVERED | Persisted batch summary |
| CONTEXT | D-17 | Immutable candidates/batches/regeneration | 08-01..03 | COVERED | Append-only all layers |
| CONTEXT | D-18 | Main preview + batch thumbnails + comparison | 08-03 | COVERED | Storyboard components |
| CONTEXT | D-19 | One adopted per selected slot; legal subset | 08-02..04 | COVERED | DB/service/Video |
| CONTEXT | D-20 | Preview non-mutating; explicit replace adoption | 08-02, 08-03 | COVERED | Transaction/dialog |

Deferred Phase 9 video generation/comparison/adoption, Phase 10 cross-Shot batch orchestration, automatic adoption/quality loops, and pre-use redesign are excluded by explicit scope and are not audit gaps.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot/model capability -> Video presentation | The browser must describe exact future inputs without granting itself generation authority. |
| Shared original Video component -> Remake adapter | Presentational reuse must not leak Novel entity identity or regress the original workflow. |
| Playwright fixture/harness -> controlled local services | Automated verification creates test facts and launches processes without touching user databases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-19 | Spoofing | Video readiness/status UI | medium | mitigate | Derive from server Prompt/adopted/action-sheet/Task facts and show text+icon states; no client-generated completed state. |
| T-08-20 | Tampering | disabled Video controls | high | mitigate | Omit generation mutation/callback and VGEN route/Task type; browser test observes zero submit requests. |
| T-08-21 | Information Disclosure | Video primary/auxiliary media | high | mitigate | Use authorized snapshot/media routes and shared Project hooks; display no provider/storage secret. |
| T-08-22 | Repudiation | project configuration edits | low | accept | Existing project config mutation/audit behavior is unchanged; Phase 8 adds no independent configuration store. |
| T-08-23 | Denial of Service | visual history rendering | low | accept | Server candidate bounds and stable responsive media tracks limit per-batch work; existing lazy media behavior is retained. |
| T-08-24 | Elevation | test harness database/process targets | high | mitigate | Hard-code only tracked isolated test service host/port/database, validate target before migrate/seed, and kill only the child server PID. |
</threat_model>

<verification>
- `npx prisma validate`, `npm run typecheck`, and `npm run test:guards` pass.
- All Phase 8 focused unit, integration API/service/Worker, existing SceneDetect regression, and Video compatibility tests pass.
- `node scripts/verify-remake-keyframes-ui.mjs` applies migrations to the controlled database and passes desktop/tablet/mobile Playwright.
- Human acceptance confirms visual hierarchy, semantics, original tool preservation, and Phase 9 boundary.
</verification>

<success_criteria>
- A user can select approved image Prompts, configure and generate immutable batches, compare candidates, and explicitly adopt one per legal slot.
- Every failure and upstream change preserves historical evidence and the previous valid adopted state.
- Start/End-only and capability-limited subsets remain truthful on Storyboard and Video.
- Video is a real data/configuration workspace with zero Phase 8 submission path.
- Every UI-SPEC covered item and backstop has evidence, and the user accepts the real workflow.
</success_criteria>

<output>
Create `.planning/phases/08-keyframe-generation/08-04-SUMMARY.md` when done.
</output>
