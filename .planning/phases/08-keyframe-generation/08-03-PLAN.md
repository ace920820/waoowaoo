---
phase: 08-keyframe-generation
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeProductionTools.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/prompt/PromptStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/storyboard/RemakeStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx
  - src/lib/remake-projects/keyframes/adapter.ts
  - src/lib/remake-projects/keyframes/video-inputs.ts
  - src/lib/query/hooks/useRemakeProject.ts
  - src/lib/query/mutations/remake-keyframe-mutations.ts
  - messages/zh/workspaceDetail.json
  - messages/en/workspaceDetail.json
  - tests/unit/remake-projects/remake-workbench-keyframe-navigation.test.ts
  - tests/unit/remake-projects/remake-keyframe-stage.test.ts
  - tests/unit/remake-projects/remake-video-input-contract.test.ts
autonomous: true
requirements: [KFRM-01, KFRM-02, KFRM-03, KFRM-04, KFRM-05, KFRM-06, KFRM-07]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-03/D-04: Prompt, Storyboard, and Video remain freely navigable; `进入分镜` reports eligible/total and only navigates."
    - "D-05/D-13/D-15: Storyboard and Video reuse the complete existing Asset Library, Project Configuration, model-capability, generation-state, and Task presentation capabilities from one Project fact source."
    - "D-06/D-07/D-08/D-10: each Remake Shot is one two-layer workspace; original Start/Middle/End evidence is never overwritten, and no slot is selected until the user persists a legal choice."
    - "D-09/D-14/D-16/D-17: selected slots retain original image-generation controls and show immutable batches with their frozen parameters, references, counts, and Task state."
    - "KFRM-04 and D-18/D-19/D-20: main preview, batch thumbnails, comparison, and adopted state are distinct; explicit confirmed adoption is the only pointer mutation."
    - "KFRM-06/KFRM-07: missing, invalidated, unselected, and capability-unsupported inputs render as true absence/reasons rather than synthetic frames or cards."
    - "D-02/D-11/D-12: Video is the real existing page adapted to Remake facts, clearly separates adopted main images from the action sheet, keeps config usable, and has no enabled submit callback/request/Task."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx"
      provides: "Five-stage free navigation and real Storyboard/Video mounts"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeProductionTools.tsx"
      provides: "Thin reuse controller for the complete existing asset/config tools"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/storyboard/RemakeStoryboardStage.tsx"
      provides: "Thin Remake adapter into the shared Storyboard page capabilities"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      provides: "Thin Remake adapter into the real Video page with submission disabled"
    - path: "src/lib/remake-projects/keyframes/adapter.ts"
      provides: "Stable Shot/revision/track/batch/candidate view model without Novel persistence identities"
  key_links:
    - from: "RemakeWorkbench.tsx"
      to: "RemakeStoryboardStage.tsx and RemakeVideoStage.tsx"
      via: "URL stage registry without readiness redirects"
      pattern: "storyboard|video"
    - from: "RemakeStoryboardStage.tsx"
      to: "StoryboardStageShell.tsx and ImageSection.tsx"
      via: "Remake-owned wrapper composition through their existing props; original components remain unchanged"
      pattern: "StoryboardStageShell"
    - from: "remake-keyframe-mutations.ts"
      to: "useRemakeProject.ts"
      via: "successful selection/generation/adoption waits for snapshot refetch"
      pattern: "refetch"
    - from: "RemakeVideoStage.tsx"
      to: "VideoStageShell.tsx and VideoPanelCardBody.tsx"
      via: "Remake-owned read-only wrapper composition through existing props and an absent/disabled submission seam"
      pattern: "videoSubmissionDisabled"
---

<objective>
Mount the existing Storyboard and Video capabilities inside Remake with only the moderate adaptations required by D-01..D-20.

Purpose: Deliver the full KFRM-01..07 user workflow by adapting stable Remake facts into current page shells, controls, tools, Task states, and configuration. Preserve the learned original page interaction model; do not create a second Storyboard/Video system or redesign the pages.
Output: Free navigation, shared tools, typed adapters/hooks, the two-layer Storyboard workflow, immutable comparison/adoption UX, and a real inspectable/configurable Video page with submission hard-disabled.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-keyframe-generation/08-CONTEXT.md
@.planning/phases/08-keyframe-generation/08-RESEARCH.md
@.planning/phases/08-keyframe-generation/08-PATTERNS.md
@.planning/phases/08-keyframe-generation/08-UI-SPEC.md
@.planning/phases/08-keyframe-generation/08-02-SUMMARY.md

<interfaces>
- The original route wrappers depend on Novel Promotion workspace context; `RemakeStoryboardStage` and `RemakeVideoStage` must be thin adapters into lower shared page shells/components, not copied page implementations.
- `StoryboardStageShell`, `ImageSection`, `ImageSectionCandidateMode`, `CandidateSelector`, `VideoStageShell`, and `VideoPanelCardBody` are read-only dependencies in this plan: consume their current exported/default component props without changing those files or their Novel Promotion behavior.
- `WorkspaceTopActions`, `WorkspaceAssetLibraryModal`, `SettingsModal`, and `useUpdateProjectConfig(projectId)` remain the full shared tools and fact source.
- Plan 08-02 snapshot fields own durable selection, batches, candidates, adoption, action-sheet status, Task state, and missing reasons. Component state may own only viewed/comparison ids.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Mount free stages, shared production tools, and stable Remake page adapters</name>
  <reversibility rating="costly">D-05/D-13 make the existing Project tools and configuration source a shared dependency for Storyboard, Video, and later task inputs.</reversibility>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeProductionTools.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/prompt/PromptStage.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/storyboard/RemakeStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, src/lib/remake-projects/keyframes/adapter.ts, src/lib/query/hooks/useRemakeProject.ts, src/lib/query/mutations/remake-keyframe-mutations.ts, messages/zh/workspaceDetail.json, messages/en/workspaceDetail.json, tests/unit/remake-projects/remake-workbench-keyframe-navigation.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-01..D-05, D-13, D-15
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` global navigation, stage boundary, design system, and copy contract
    - `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx`
    - `src/app/[locale]/workspace/[projectId]/modes/remake/prompt/PromptStage.tsx`
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceTopActions.tsx`
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceAssetLibraryModal.tsx`
    - `src/components/ui/config-modals/ConfigEditModal.tsx` `SettingsModal`
    - `src/lib/query/mutations/useProjectConfigMutations.ts` `useUpdateProjectConfig`
    - `src/lib/query/mutations/remake-prompt-mutations.ts` refetch/error pattern
  </read_first>
  <behavior>
    - Test 1: overview, SceneDetect, Prompt, Storyboard, and Video are always visible and directly navigable without readiness redirects.
    - Test 2: Prompt `进入分镜` changes only the URL stage, displays server `eligible/total`, and creates neither selection nor image Task.
    - Test 3: both downstream stages mount real snapshot-backed adapters and the same full Asset Library/Project Configuration controller.
    - Test 4: the adapter keeps one stable Shot/revision identity and separates original frames, selection, batches/candidates/adoption, action sheet, Video Prompt, and missing facts.
    - Test 5: selection/generation/adoption mutations wait for Plan 08-02 responses and snapshot refetch; failure does not optimistically alter server facts.
  </behavior>
  <action>Extend the Remake stage registry and render switch per D-01/D-03 with real `storyboard` and `video` adapters and no prerequisite redirect. Add the D-04 Prompt CTA/count as navigation only. Build `RemakeProductionTools` as a thin controller around the existing complete top actions, asset-library modal/AssetsStage, SettingsModal, authorized Project queries, and `useUpdateProjectConfig`; mount the same controller on both pages per D-05/D-13. Extend the Remake query DTO and create stable-id adapter/mutation contracts for the Plan 08-02 APIs. Keep server facts authoritative and do not construct Episode/Storyboard/Panel objects. Add bilingual copy through existing locale files and preserve Glass/AppIcon/focus conventions.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-workbench-keyframe-navigation.test.ts &amp;&amp; npm run check:locale-navigation</automated>
  </verify>
  <done>D-01..D-05/D-13/D-15 navigation, handoff, shared tools, and stable adapter authority work on one real Remake snapshot without submitting work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt the original Storyboard capability to the two-layer Remake Shot workflow</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/storyboard/RemakeStoryboardStage.tsx, src/lib/remake-projects/keyframes/adapter.ts, src/lib/query/mutations/remake-keyframe-mutations.ts, tests/unit/remake-projects/remake-keyframe-stage.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-06..D-10 and D-14..D-20
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` Shot structure, candidate/comparison/adoption, states, responsive, and accessibility sections
    - `StoryboardStage.tsx` and `StoryboardStageShell.tsx` original composition
    - `ImageSection.tsx`, `ImageSectionCandidateMode.tsx`, and `CandidateSelector.tsx` current model/options/references/Task/candidate behavior
    - `src/components/ui/ConfirmDialog.tsx` replacement confirmation/focus behavior
  </read_first>
  <behavior>
    - Test 1: one Shot remains one continuous block with header, immutable ordered original Start/Middle/End/action-sheet evidence above, and only selected legal generation slots below.
    - Test 2: a fresh revision selects nothing; checkboxes persist through the API/refetch, and unapproved/invalidated/unavailable controls expose readable reasons and cannot invoke mutations.
    - Test 3: each selected slot retains original model, allowed options, reference assets, candidate count 1..4, Task state, generate/regenerate, preview, and failure behavior.
    - Test 4: chronological batches retain their own frozen summaries; thumbnail selection changes `正在查看` only, while `当前采用` follows the server pointer.
    - Test 5: inline original/candidate and candidate/candidate comparison performs no mutation; `采用此版本` is the sole mutation and replacement requires confirmation.
    - Test 6: true empty/loading/error/partial/invalidated states, stable aspect ratios, mobile local scrolling, 44px targets, checkbox/radio semantics, alt text, aria-live, focus entry/return, and Escape behavior match the approved UI contract.
  </behavior>
  <action>Implement the two-layer Shot entirely in the Remake adapter and `RemakeStoryboardStage` wrapper. Compose the unchanged original Storyboard/ImageSection/CandidateSelector capabilities through their current props for model, capability, reference-asset, Task, loading/error, and candidate presentation behavior; wrap them with Remake-owned original-evidence, immutable-batch, comparison, and explicit-adoption presentation where their Panel-specific confirmation semantics do not match D-17/D-20. Never construct Novel entities or pass a generated media URL as an original-frame replacement. Keep preview/comparison local, persist only through the Plan 08-02 mutations, preserve D-14/D-16 frozen summaries, and implement the UI-SPEC missing/responsive/accessibility states with existing tokens/components. The original Novel Promotion component files and behavior remain unchanged.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-keyframe-stage.test.ts</automated>
  </verify>
  <done>D-06..D-10/D-14..D-20 and KFRM-01..07 are usable in the adapted original Storyboard surface with original evidence, history, comparison, and adoption semantics intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adapt the real Video page to truthful Remake inputs with submission hard-disabled</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, src/lib/remake-projects/keyframes/video-inputs.ts, tests/unit/remake-projects/remake-video-input-contract.test.ts</files>
  <read_first>
    - `.planning/phases/08-keyframe-generation/08-CONTEXT.md` D-02, D-05, D-10..D-15, D-19
    - `.planning/phases/08-keyframe-generation/08-UI-SPEC.md` Video adaptation, readiness, states, copy, responsive, and accessibility sections
    - `VideoStageRoute.tsx`, `VideoStageShell.tsx`, and `VideoPanelCardBody.tsx` original page/control composition
    - `src/lib/model-config-contract.ts` Video capability/reference modes
    - `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeProductionTools.tsx` from Task 1
  </read_first>
  <behavior>
    - Test 1: every card reads a current Remake Shot, approved/missing/needs-review Video Prompt, adopted generated images, current/missing/invalidated action sheet, assets, and shared model config.
    - Test 2: adopted images appear under `主画面参考（采用的新画面）`; the action sheet appears under `辅助动作参考（原始三帧动作表）` with distinct status and semantics.
    - Test 3: selected Video capability reports the exact legal Start/Middle/End adopted subset and never substitutes original frames or action sheet for missing main images.
    - Test 4: existing model, duration, resolution, audio, capability, Task-information layout, Asset Library, and Project Configuration remain inspectable/configurable.
    - Test 5: single, regenerate, and batch video submissions are disabled/absent with the approved Phase 9 message; no mutation import, network request, Task type, fake progress, or candidate state exists.
  </behavior>
  <action>Create a pure read-only readiness mapper for current Remake facts and selected Video capability. In `RemakeVideoStage`, compose the unchanged `VideoStageShell`/`VideoPanelCardBody` capabilities through their existing props and Remake-owned wrapper data rather than copying their layout or editing their files. Render D-10/D-12 primary and auxiliary input groups, the compact adopted/Prompt/action-sheet readiness summary, and true missing/invalidated reasons. Preserve the existing model/duration/resolution/audio/capability UI and the shared tools per D-05/D-13/D-15, but pass an absent/disabled submission seam and fixed Phase 9 explanation per D-02. Do not add or import any video mutation, VGEN route, video Task, scheduler, or fabricated task/output state; the original Novel Promotion card behavior remains unchanged.</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-video-input-contract.test.ts tests/unit/novel-promotion/video-panel-card-body.test.ts tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts</automated>
  </verify>
  <done>The real Video page exposes D-02/D-05/D-10..D-15/D-19 inputs and configuration truthfully while remaining structurally incapable of video submission.</done>
</task>

</tasks>

<threat_model>
The browser is not an authority boundary: disabled controls, candidate ids, and local preview state remain untrusted. Render durable labels only from the authorized snapshot, wait for server refetch after mutations, and prove the Video adapter has no submission dependency. The original Novel Promotion files are unchanged; their compatibility suites remain required read-only regression gates.
</threat_model>

<verification>
- Focused navigation, Storyboard, Video, and Novel compatibility tests pass.
- `npm run typecheck`, `npm run check:locale-navigation`, and `npm run check:no-multiple-sources-of-truth` pass.
- Changed frontend code contains one thin Remake adapter/wrapper per page and consumes existing shared shells/components/tools through current props; no original Novel Promotion component file changes, copied Novel page tree, or Remake-specific asset/config store exist.
</verification>

<success_criteria>
- Users freely enter Prompt, Storyboard, and Video and retain full shared tools.
- Storyboard supports explicit selection, generation, immutable history, comparison, and adoption without altering original frames.
- Video exposes real inputs/config and no Phase 8 video execution path.
</success_criteria>

<output>
Create `.planning/phases/08-keyframe-generation/08-03-SUMMARY.md` when done.
</output>
