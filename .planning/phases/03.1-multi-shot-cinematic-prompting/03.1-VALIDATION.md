---
phase: 03.1
status: draft
---

# Phase 03.1 Validation Matrix

| Requirement | Validation Type | Evidence |
|-------------|-----------------|----------|
| New segmentation schema accepted | Unit test | Parser accepts rows with `referencePrompt`, `storyboardPrompt`, `videoPrompt`, `emotionalIntent`, `visualStrategy`, and `shots[]`. |
| Old schema remains compatible | Unit test | Existing rows with only `narrativePrompt` still produce ready drafts. |
| Specialized review fields seed correctly | Unit/component test | `referencePromptText` and `compositePromptText` seed from distinct generated fields. |
| Shot-level prompts persist | Integration/unit test | Created shot-group items include prompt text from `shots[].imagePrompt`. |
| Composite prompt uses ordered slots | Unit test | `buildShotGroupCompositePrompt(...)` output includes generated slot prompts. |
| Video prompt uses ordered beats | Unit test | `buildShotGroupVideoPrompt(...)` output includes generated shot beats and dialogue policy. |
| Rebuild preserves user edits | Regression test | Existing reference/composite prompt overrides survive multi-shot draft rebuild. |

## Suggested Commands

- `npx vitest run tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts`
- `npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts`
- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts`
- Add a focused test file for the new cinematic parser/persistence behavior if adjacent coverage is not sufficient.
