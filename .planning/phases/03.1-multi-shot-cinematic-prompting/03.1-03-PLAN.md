---
phase: 03.1-multi-shot-cinematic-prompting
plan: 03
type: implementation
wave: 3
depends_on:
  - 03.1-01-PLAN.md
  - 03.1-02-PLAN.md
files_modified:
  - src/lib/shot-group/prompt.ts
  - src/lib/workers/handlers/shot-group-image-task-handler.ts
  - src/lib/workers/video.worker.ts
  - tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
autonomous: true
requirements:
  - MSHT-04
  - MSHT-05
  - MSHT-07
---

<objective>
Route specialized cinematic data into reference image, storyboard composite, and multi-shot video generation so the generated outputs use professional shot-level film language instead of only a shared narrative paragraph.
</objective>

<context>
Read first:
- `.planning/phases/03.1-multi-shot-cinematic-prompting/03.1-CONTEXT.md`
- `.planning/phases/03.1-multi-shot-cinematic-prompting/03.1-02-PLAN.md`
- `src/lib/shot-group/prompt.ts`
- `src/lib/workers/handlers/shot-group-image-task-handler.ts`
- `src/lib/workers/video.worker.ts`
</context>

<tasks>
  <task id="03.1-03-T1" type="prompt-builders">
    <files>src/lib/shot-group/prompt.ts</files>
    <action>
      Strengthen the three shot-group prompt builders. Reference image generation should emphasize a single concept/key visual using reference prompt, emotional intent, visual strategy, assets, mood, and style. Composite storyboard generation should explicitly follow ordered slot prompts as shot-level commands. Video generation should treat ordered slots as a continuous 15-second beat sequence with camera movement, blocking, dialogue timing, and continuity constraints.
    </action>
    <verify>
      Add unit assertions around generated prompt strings or extend existing tests to prove reference/composite/video prompt outputs include the intended specialized data.
    </verify>
    <acceptance_criteria>
      - Reference prompt stays one-image oriented and does not ask for a grid.
      - Composite prompt includes ordered shot-level prompts.
      - Video prompt includes ordered beat progression and dialogue content/override rules.
      - Existing asset/mood constraints remain included.
    </acceptance_criteria>
  </task>

  <task id="03.1-03-T2" type="worker-regression">
    <files>src/lib/workers/handlers/shot-group-image-task-handler.ts, src/lib/workers/video.worker.ts</files>
    <action>
      Keep worker invocation behavior stable while ensuring the richer prompts produced by `src/lib/shot-group/prompt.ts` are the prompts passed into image and video model calls. Do not change provider selection or model fallback behavior.
    </action>
    <verify>
      Rerun focused image/video tests and add payload/prompt assertions where adjacent tests already mock generation calls.
    </verify>
    <acceptance_criteria>
      - Image generation still submits `IMAGE_SHOT_GROUP` tasks with reference images and aspect ratio behavior intact.
      - Video generation still requires `compositeImageUrl` and submits `VIDEO_SHOT_GROUP` with existing reference mode behavior intact.
      - No provider guessing or model key downgrade is introduced.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run focused cross-phase regression commands:
- `npx vitest run tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts`
- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts`
- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts`
</verification>

<success_criteria>
- Specialized prompt fields materially affect reference image, storyboard board, and video generation prompts.
- Shot-level cinematic language flows into ordered slots and video beats.
- Existing multi-shot asset, mood, dialogue, and video reference behavior remains stable.
</success_criteria>
