---
phase: 03.1-multi-shot-cinematic-prompting
plan: 02
type: implementation
wave: 2
depends_on:
  - 03.1-01-PLAN.md
files_modified:
  - src/lib/novel-promotion/multi-shot/persist-drafts.ts
  - src/lib/shot-group/draft-metadata.ts
  - src/lib/shot-group/video-config-snapshot.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts
autonomous: true
requirements:
  - MSHT-03
  - MSHT-04
  - UI-04
---

<objective>
Persist specialized cinematic prompt fields and generated shot-level prompts into the existing shot-group draft model so review cards no longer seed `辅助参考图提示词` and `剧情内容` from the same text when richer LLM output exists.
</objective>

<context>
Read first:
- `.planning/phases/03.1-multi-shot-cinematic-prompting/03.1-CONTEXT.md`
- `.planning/phases/03.1-multi-shot-cinematic-prompting/03.1-01-PLAN.md`
- `src/lib/novel-promotion/multi-shot/persist-drafts.ts`
- `src/lib/shot-group/draft-metadata.ts`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx`
</context>

<tasks>
  <task id="03.1-02-T1" type="persistence">
    <files>src/lib/novel-promotion/multi-shot/persist-drafts.ts, src/lib/shot-group/draft-metadata.ts, src/lib/shot-group/video-config-snapshot.ts</files>
    <action>
      Persist the new draft fields using existing storage surfaces. Store `referencePromptText` and `compositePromptText` in `draftMetadata`; store final `videoPrompt` on the shot group; preserve `cinematicPlan` metadata if added in plan 01. Populate shot-group items from generated `shots[]` so each slot has a title and prompt. Keep previous user edits when rebuilding drafts.
    </action>
    <verify>
      Add or update persistence tests that create multi-shot drafts from rich rows and assert stored metadata plus item prompts.
    </verify>
    <acceptance_criteria>
      - `referencePromptText` and `compositePromptText` persist when generated.
      - `videoPrompt` can diverge from `groupPrompt` when generated.
      - Shot-group item prompts are populated from generated shot-level prompts.
      - Rebuilds preserve existing user-edited reference/composite/video/dialogue fields where prior phase guarantees require preservation.
    </acceptance_criteria>
  </task>

  <task id="03.1-02-T2" type="ui-seeding">
    <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx</files>
    <action>
      Adjust review-card seed logic only as needed so `辅助参考图提示词` uses generated `referencePromptText`, while `剧情内容` uses generated `compositePromptText`. Keep the existing textareas and save behavior; do not introduce a complex new UI for cinematic plans in this phase unless tests require a visible indicator.
    </action>
    <verify>
      Add or update UI unit tests proving distinct generated fields appear in the two review textareas and user edits are not clobbered by benign refreshes.
    </verify>
    <acceptance_criteria>
      - Existing UI layout remains stable.
      - Generated reference and storyboard texts can be distinct.
      - Existing user override preservation behavior remains intact.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run targeted persistence/UI tests, starting with:
- `npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts`
- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts`
</verification>

<success_criteria>
- Rich LLM output survives persistence.
- Review cards seed the right specialized fields.
- Old generated drafts and existing user edits remain compatible.
</success_criteria>
