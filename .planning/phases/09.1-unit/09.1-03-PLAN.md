---
phase: 09.1-unit
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260813120000_add_remake_video_unit/migration.sql
  - src/lib/remake-projects/unit/service.ts
  - src/lib/remake-projects/unit/invalidation.ts
  - src/lib/remake-projects/keyframes/invalidation.ts
  - tests/integration/remake-projects/remake-video-unit-models.test.ts
  - tests/integration/remake-projects/remake-video-unit-service.test.ts
  - tests/integration/remake-projects/remake-video-unit-invalidation.test.ts
autonomous: true
requirements: [D-01, D-04, D-07, D-17, D-19, D-22]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-04: a unit is a first-class delivery granularity; each shot revision belongs to at most one unit (DB unique constraint), and unit member ordinals are unique within the unit."
    - "D-17: unit generations append immutable unit batches/versions with a unit-level adoption pointer and append-only adoption events; playback targets the whole unit video."
    - "D-19: member mutation (add/remove/reorder) is rejected once the unit has a pending/running generation task OR any committed batch (members freeze at generate-submit, not first batch)."
    - "D-22: upstream keyframe/prompt invalidation propagates to unit versions as needs_review, idempotently, via unit-track/batch/version invalidation columns."
    - "D-07: the merged unit action sheet persists as a unit-scoped, fingerprint-deduped output version with member-source provenance."
  artifacts:
    - path: "prisma/migrations/20260813120000_add_remake_video_unit/migration.sql"
      provides: "Forward-only unit/member/track/batch/version/adoption/action-sheet + invalidation-column migration"
    - path: "src/lib/remake-projects/unit/service.ts"
      provides: "Unit CRUD, member freeze gate, currentness assertion, idempotent batch append, adoption/note/reconfirm, track detail"
    - path: "src/lib/remake-projects/unit/invalidation.ts"
      provides: "Unit-version invalidation driven by upstream keyframe/prompt changes"
  key_links:
    - from: "prisma/schema.prisma"
      to: "src/lib/remake-projects/unit/service.ts"
      via: "appendVideoUnitBatch reuses the trackId_operationKey idempotency + outputVersion media spine pattern from appendVideoGenerationBatch"
      pattern: "appendVideoUnitBatch"
    - from: "src/lib/remake-projects/unit/invalidation.ts"
      to: "src/lib/remake-projects/keyframes/invalidation.ts"
      via: "keyframe invalidation additionally marks every unit version referencing the member's media as needs_review"
      pattern: "invalidateRemakeVideoUnitVersions"
    - from: "prisma/schema.prisma"
      to: "src/lib/remake-projects/unit/service.ts"
      via: "RemakeVideoUnitMember @@unique([shotRevisionId]) enforces D-04 at the DB; the app gate rejects single-shot submission (Plan 09.1-04)"
      pattern: "RemakeVideoUnitMember"
---

<objective>
Establish the durable unit data model and the unit-level persistence closure: unit/member tables, unit track/batch/version/adoption, merged-sheet output version, invalidation propagation, and the CRUD/freeze/currentness service.

Purpose: D-01/D-04/D-17/D-19/D-22 need durable, append-only, unit-granular persistence with unique membership and full provenance; this plan is the Phase 11-compatible foundation (unit delivery granularity must be queryable by export/完整性 checks later).
Output: Forward-only migration, unit service (CRUD + freeze + append + adoption + note + reconfirm + detail), unit invalidation wired into existing keyframe invalidation, and integration tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-unit/09.1-CONTEXT.md
@.planning/phases/09.1-unit/09.1-RESEARCH.md
@.planning/remake-video-merge-unit-design.md

<interfaces>
Mirror the existing `RemakeVideoTrack/Batch/Version/AdoptionEvent` structure (schema.prisma lines 773-850) exactly for the unit variants, and reuse `RemakeOutputVersion` as the media-evidence spine for unit versions (shotId = first member's shotId, kind 'video_candidate_unit', documented discretion). Reuse the transaction pattern from `appendVideoGenerationBatch` (video/service.ts:538-617) for idempotent append, and `invalidateRemakeVideoVersions` (video/invalidation.ts) as the invalidation template. The single-shot gate (rejecting submission of unit members) is application-layer and lands in Plan 09.1-04; this plan only provides the DB uniqueness and the query helpers the gate needs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the unit/member/track/batch/version/adoption/action-sheet models and invalidation columns with a forward migration</name>
  <reversibility rating="costly">D-01 makes unit the delivery granularity; D-04's @@unique([shotRevisionId]) is a one-way membership constraint the migration locks in.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/20260813120000_add_remake_video_unit/migration.sql, tests/integration/remake-projects/remake-video-unit-models.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-01, D-04, D-17
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 3, A1, Open Question 2
    - `prisma/schema.prisma` RemakeVideoTrack/Batch/Version/AdoptionEvent (773-850), RemakeOutputVersion (549-574), RemakeInvalidation (599-635), RemakeProvenanceRecord (575-598)
    - `prisma/migrations/20260812090000_add_remake_video_generation/migration.sql` forward-migration convention
  </read_first>
  <behavior>
    - Test 1: the migration creates RemakeVideoUnit, RemakeVideoUnitMember (with @@unique([unitId, ordinal]) and @@unique([shotRevisionId])), RemakeVideoUnitTrack, RemakeVideoUnitBatch, RemakeVideoUnitVersion, RemakeVideoUnitAdoptionEvent, and RemakeVideoUnitActionSheet (@@unique([unitId, fingerprint])).
    - Test 2: RemakeInvalidation gains nullable unitTrackId/unitBatchId/unitVersionId columns with relations and indexes; RemakeProvenanceRecord gains nullable unitBatchId.
    - Test 3: the existing RemakeVideoTrack @@unique([shotRevisionId]) and all prior Remake rows survive the migration (no drops, no data loss, forward-only).
    - Test 4: schema contract test (fs.readFileSync on prisma/schema.prisma) pins the unique constraints and unit batch trackId_operationKey idempotency key.
  </behavior>
  <action>Add to `prisma/schema.prisma`: `RemakeVideoUnit` (id, remakeProjectId relation, userLabel String?, members[], tracks[], actionSheets[], createdAt/updatedAt, @@index([remakeProjectId])); `RemakeVideoUnitMember` (id, unitId, shotRevisionId, ordinal Int, unit relation, @@unique([unitId, ordinal]), @@unique([shotRevisionId]), @@index([unitId])); `RemakeVideoUnitTrack` (id, unitId, adoptedVersionId String? @unique, batches[], adoptedVersion relation "RemakeVideoUnitTrackAdoptedVersion", adoptionEvents[], createdAt/updatedAt, @@unique([unitId])); `RemakeVideoUnitBatch` (id, trackId, promptVersionId String?, taskId String?, operationKey, inputFingerprint, inputSnapshot Json, modelId, modelOptions Json, orderedReferences Json, versions[], provenanceRecords, invalidations, @@unique([trackId, operationKey]), @@index([taskId]), @@index([inputFingerprint])); `RemakeVideoUnitVersion` (id, batchId, outputVersionId String? @unique, ordinal Int, note String? @db.Text, reviewerId String?, adoptedByTrack relation, previousAdoptions/nextAdoptions, invalidations, @@unique([batchId, ordinal])); `RemakeVideoUnitAdoptionEvent` (id, trackId, previousVersionId String?, nextVersionId String?, reviewerId String?, @@index([trackId, createdAt])); `RemakeVideoUnitActionSheet` (id, unitId, fingerprint, mediaId String?, sources Json, taskId String?, status String @default("pending"), createdAt, @@unique([unitId, fingerprint]), @@index([unitId])). On `RemakeOutputVersion` add an optional `unitVersion RemakeVideoUnitVersion?` back-relation; on `RemakeInvalidation` add optional unitTrackId/unitBatchId/unitVersionId with relations and indexes; on `RemakeProvenanceRecord` add optional unitBatchId with relation. Map every table with @@map to the remake_ prefix convention. Generate the forward migration `20260813120000_add_remake_video_unit` with prisma migrate dev --create-only or write the SQL by hand following the 20260812090000 convention; the migration must be forward-only, preserve all existing data and relations, and use no destructive flags (no db push, reset, drop, truncate). DB bootstrap is NOT a per-task step: the vitest global setup (tests/setup/global-setup.ts) already brings up docker compose mysql/redis and runs `prisma db push --skip-generate` whenever BILLING_TEST_BOOTSTRAP=1 — rely on that bootstrap instead of a per-task `docker compose up` + `prisma migrate deploy` sequence, and run `npx prisma generate --schema prisma/schema.prisma` after schema edits so the client has the new models. Keep `npx prisma migrate dev` / `npx prisma db push` as local-dev instructions only (the migration file is for production deploy; the test DB is synced by the bootstrap's db push); the automated proof is the test command, not a compose/migrate sequence. Integration test `remake-video-unit-models.test.ts` mirrors the schema-contract style of remake-video-task-contract.test.ts (fs.readFileSync assertions on the unique keys) and runs against the bootstrapped test database.</action>
  <verify>
    <automated>npx prisma generate --schema prisma/schema.prisma &amp;&amp; BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/remake-projects/remake-video-unit-models.test.ts</automated>
  </verify>
  <done>The migration deploys forward-only, Prisma Client generates with all unit relations, the D-04 unique membership constraint exists, and existing Remake rows/constraints are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unit CRUD, member freeze gate, and submission currentness (D-04/D-19/D-22)</name>
  <files>src/lib/remake-projects/unit/service.ts, tests/integration/remake-projects/remake-video-unit-service.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-02, D-04, D-19
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 3, Pitfall 4
    - `src/lib/remake-projects/video/service.ts` getCurrentShot / assertVideoSubmissionCurrent ownership-query pattern
    - `src/lib/remake-projects/keyframes/service.ts` transaction conventions
  </read_first>
  <behavior>
    - Test 1: createVideoUnit requires at least 2 members, assigns ordinals from the submitted order, rejects any member whose shot revision is not active/current, and rejects any member already in another unit (D-04).
    - Test 2: updateVideoUnitMembers (add/remove/reorder) succeeds only while the unit has no pending/running generation task and zero committed batches; it throws REMAKE_VIDEO_UNIT_GENERATION_IN_FLIGHT when a queued/processing unit task exists and REMAKE_VIDEO_UNIT_MEMBERS_FROZEN after the first committed batch (D-19 — members freeze at generate-submit, not first batch).
    - Test 3: getVideoUnitDetail returns members (ordinal + shot info + durationSeconds), unit track with batches/versions/adoptedVersion, and adoption events.
    - Test 4: assertVideoUnitSubmissionCurrent verifies the unit exists, the frozen member set matches the DB (same ordinals/shotRevisionIds), each member's shot revision is still active, and each member's adopted keyframe is unchanged; it throws REMAKE_VIDEO_UNIT_INPUT_STALE otherwise.
  </behavior>
  <action>Start RED with the four tests above using the vi.hoisted prisma-mock pattern. Create `src/lib/remake-projects/unit/service.ts` exporting: `createVideoUnit(input: { projectId, userId, memberShotRevisionIds: string[] })` — validates the project belongs to the user, loads each shot revision with active lifecycle/current revision, checks no member is already in a RemakeVideoUnitMember row (throw REMAKE_VIDEO_UNIT_MEMBER_ALREADY_ASSIGNED:{shotRevisionId}), and creates the unit with members in the submitted order (ordinals 1..n) inside one transaction; `getVideoUnitDetail(input)` scoped by project ownership returning members, unit track (adoptedVersionId, batches with versions + outputVersion), and adoption events; `updateVideoUnitMembers(input: { unitId, members: Array<{ shotRevisionId, ordinal }> })` — inside a transaction, re-fetch the unit with batches AND scan for a pending generation task (query the Task table for targetType 'remake_unit', targetId = unitId, status in [TASK_STATUS.QUEUED, TASK_STATUS.PROCESSING], reusing the project's active-status helpers in src/lib/task/service.ts / reconcile.ts); if a pending task exists throw REMAKE_VIDEO_UNIT_GENERATION_IN_FLIGHT (D-19 — freeze at generate-submit), and if any batch exists throw REMAKE_VIDEO_UNIT_MEMBERS_FROZEN (committed = frozen); otherwise diff the member set (add/remove/reorder, each new member checked against D-04 uniqueness) and persist; the append-time assertVideoUnitSubmissionCurrent / fingerprint check (Task 3, D-22) remains the backstop for races; `assertVideoUnitSubmissionCurrent(snapshotLike, client?)` — verifies unit existence, member set equality with the frozen snapshot members (same ordinals/shotRevisionIds), active shot revisions, and per-member adopted-keyframe media equality (reusing the slotFromRole-style check against RemakeKeyframeTrack.adoptedCandidate.outputVersion.mediaId for each member's selectedKeyframe.slot), throwing REMAKE_VIDEO_UNIT_INPUT_STALE on any mismatch. All ownership queries must filter by remakeProject.projectId + project.userId exactly like getVideoTrackDetail.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/remake-projects/remake-video-unit-service.test.ts</automated>
  </verify>
  <done>Unit creation enforces >=2 members and unique membership; members freeze at generate-submit (pending task) or first committed batch; currentness assertion guards every submission path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append unit batches/versions, close the adoption/reconfirm/note loop, and propagate upstream invalidation (D-17/D-22)</name>
  <files>src/lib/remake-projects/unit/service.ts, src/lib/remake-projects/unit/invalidation.ts, src/lib/remake-projects/keyframes/invalidation.ts, tests/integration/remake-projects/remake-video-unit-service.test.ts, tests/integration/remake-projects/remake-video-unit-invalidation.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-17, D-22
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 3, Pattern 4, Open Question 1/2
    - `src/lib/remake-projects/video/service.ts` appendVideoGenerationBatch / adoptVideoVersion / setVideoReviewNote / reconfirmVideoVersion (538-852)
    - `src/lib/remake-projects/video/invalidation.ts` invalidateRemakeVideoVersions (11-113)
    - `src/lib/remake-projects/keyframes/invalidation.ts` invalidateKeyframeOutputsForRevision (calls video invalidation at line 61)
  </read_first>
  <behavior>
    - Test 1: appendVideoUnitBatch is idempotent on trackId_operationKey — retry returns the existing batch/version ids without duplicating output/provenance rows; fingerprint mismatch throws REMAKE_VIDEO_UNIT_FINGERPRINT_INVALID.
    - Test 2: adoptVideoUnitVersion sets the unit-track adoption pointer, writes an adoption event, requires explicit confirmReplace when replacing, and rejects non-completed/invalidated versions; reconfirmVideoUnitVersion clears needs_review idempotently; setVideoUnitReviewNote writes the note.
    - Test 3: invalidateRemakeVideoUnitVersions marks unit versions whose orderedReferences include an invalidated member mediaId or whose member promptVersionId is invalidated as needs_review, creates unit invalidation rows idempotently, and leaves adopted pointer/history intact.
    - Test 4: keyframe invalidation for a member shot also triggers unit invalidation (the keyframes/invalidation.ts hook calls the unit path).
  </behavior>
  <action>Start RED with the four tests above. Extend `src/lib/remake-projects/unit/service.ts` with `appendVideoUnitBatch(input: { taskId, operationKey, inputSnapshot, inputFingerprint, mediaId })` mirroring appendVideoGenerationBatch: parse the unit snapshot, verify the fingerprint, inside one transaction run assertVideoUnitSubmissionCurrent, ensure the unit track (create on first batch), check trackId_operationKey dedup (return existing on retry), create batch (JSON-cloned snapshot/modelOptions/orderedReferences; promptVersionId = first member's promptVersionId per research Open Question 1), create version ordinal 1 with an outputVersion (shotId = first member's shotId, revisionId = first member's shotRevisionId, kind 'video_candidate_unit', fingerprint `${operationKey}:${inputFingerprint}:1`, taskId, inputSnapshot, status completed), and write a remakeProvenanceRecord with unitBatchId + payload (inputFingerprint/model/durationSeconds/referenceCount). Add `adoptVideoUnitVersion` (unit-track adoption pointer + RemakeVideoUnitAdoptionEvent + REPLACE_CONFIRM_REQUIRED semantics + stale-input checks), `setVideoUnitReviewNote`, `reconfirmVideoUnitVersion` (clears unit invalidation rows + outputVersion.invalidatedAt), and `getVideoUnitTrackDetail`. Create `src/lib/remake-projects/unit/invalidation.ts` exporting `invalidateRemakeVideoUnitVersions(input: { tx?, shotId, revisionId, reason })` that finds invalidated keyframe/action-sheet media ids for the shot, scans unit batches whose orderedReferences.mediaId intersect OR whose snapshot members[].promptVersionId references an invalidated prompt version, marks their unit versions' outputVersions needs_review, and creates idempotent RemakeInvalidation rows with unitVersionId/unitBatchId. In `src/lib/remake-projects/keyframes/invalidation.ts`, after the existing video invalidation call, invoke the unit invalidation path so a member keyframe change reviews the owning unit's versions (D-22); guard with a try/catch that does not break the keyframe invalidation when the shot has no unit.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/remake-projects/remake-video-unit-service.test.ts tests/integration/remake-projects/remake-video-unit-invalidation.test.ts tests/integration/remake-projects/remake-video-invalidation.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Unit generations append immutably with unit-level adoption/note/reconfirm; upstream keyframe/prompt invalidation propagates to unit versions idempotently.</done>
</task>

</tasks>

## Artifacts This Plan Produces

| New symbol | Kind | Downstream consumers |
|------------|------|----------------------|
| `RemakeVideoUnit` / `RemakeVideoUnitMember` | Prisma model | CRUD (this plan), submission gate + preview (09.1-04), export/完整性 (Phase 11) |
| `RemakeVideoUnitTrack` / `Batch` / `Version` / `AdoptionEvent` | Prisma model | version loop (this plan), worker append (09.1-05), API (09.1-04), UI (09.1-06) |
| `RemakeVideoUnitActionSheet` | Prisma model | merged-sheet persist/dedup (09.1-04/05) |
| `RemakeInvalidation.unit*` / `RemakeProvenanceRecord.unitBatchId` | columns | invalidation propagation (this plan), review UI (09.1-06) |
| `createVideoUnit` / `getVideoUnitDetail` / `updateVideoUnitMembers` | service fn | unit API routes (09.1-04) |
| `assertVideoUnitSubmissionCurrent` | service fn | submission service (09.1-04), worker (09.1-05) |
| `appendVideoUnitBatch` / `adoptVideoUnitVersion` / `setVideoUnitReviewNote` / `reconfirmVideoUnitVersion` / `getVideoUnitTrackDetail` | service fn | worker append (09.1-05), unit track API (09.1-04), UI (09.1-06) |
| `invalidateRemakeVideoUnitVersions` | service fn | keyframe invalidation hook (this plan, T3), prompt-invalidation propagation (this plan, T3) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/service -> unit tables | Member sets, ordinals, and freeze state cross into durable storage. |
| Worker append -> outputVersion/provenance | Generated media evidence and provenance cross the persistence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-091-09 | Tampering | member uniqueness | high | mitigate | D-04 enforced by RemakeVideoUnitMember @@unique([shotRevisionId]) plus in-transaction re-checks in createVideoUnit/updateVideoUnitMembers. |
| T-091-10 | Tampering | member freeze bypass | high | mitigate | updateVideoUnitMembers re-fetches batches and scans queued/processing unit tasks inside the transaction, rejecting any mutation while a task is pending (REMAKE_VIDEO_UNIT_GENERATION_IN_FLIGHT) or after the first batch (REMAKE_VIDEO_UNIT_MEMBERS_FROZEN, D-19); submission currentness re-asserted in append. |
| T-091-11 | Tampering | append idempotency | high | mitigate | trackId_operationKey unique + fingerprint re-verification before any write; retry returns existing rows (mirrors appendVideoGenerationBatch). |
| T-091-12 | Tampering | invalidation records | medium | mitigate | Idempotent findFirst-before-create on (unitVersionId, reason); only completed unit versions are marked needs_review; history/adoption pointers preserved. |
| T-091-13 | Information Disclosure | ownership scoping | high | mitigate | Every unit query filters remakeProject.projectId + project.userId, mirroring getVideoTrackDetail ownership queries. |
| T-091-SC | Tampering | package installs | high | accept | No package install occurs in this plan; schema/service use existing Prisma/vitest tooling. |
</threat_model>

<verification>
- Migration deploys to the isolated test DB and Prisma Client generates.
- remake-video-unit-service, remake-video-unit-invalidation, and existing remake-video-invalidation tests pass.
- `npm run typecheck` passes.
</verification>

<success_criteria>
A unit is durably modeled with unique membership and members frozen once a generation task is pending or a batch is committed; unit generations append immutable batches/versions with a unit-level adoption pointer and events; upstream invalidation reviews the owning unit's versions idempotently.
</success_criteria>

<output>
Create `.planning/phases/09.1-unit/09.1-03-SUMMARY.md` when done.
</output>
