---
phase: 09.1-unit
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/lib/remake-projects/unit/submission.ts
  - src/lib/remake-projects/unit/action-sheet.ts
  - src/lib/remake-projects/video/service.ts
  - src/app/api/remake-projects/[projectId]/units/route.ts
  - src/app/api/remake-projects/[projectId]/units/[unitId]/route.ts
  - src/app/api/remake-projects/[projectId]/units/[unitId]/members/route.ts
  - src/app/api/remake-projects/[projectId]/units/[unitId]/tracks/[trackId]/route.ts
  - src/app/api/remake-projects/[projectId]/units/preview/route.ts
  - tests/unit/remake-projects/remake-video-unit-submission.test.ts
  - tests/integration/api/remake-projects-video-unit.test.ts
autonomous: true
requirements: [D-02, D-04, D-05, D-08, D-09, D-21, D-22]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-05: a unit is only generated when the user manually submits it; the total duration is normalized through deriveDefaultVideoDuration, and a total over the model max fails with an explicit split hint instead of silently truncating."
    - "D-21: submitting a unit where any member lacks an adopted keyframe, an approved Video Prompt, or legal parameters fails before provider work with one aggregated error listing every member's reason."
    - "D-04/D-18: single-shot submission of a shot that is a unit member is rejected by the service (app-layer gate), so a shot cannot be delivered twice."
    - "D-09/D-22/W5: the submitted unit freezes the WYSIWYG preview inputs (member keyframes + dedup assets + timed prompt, with the merged action sheet as a deterministic deferred entry) into a fingerprinted task enqueued via submitTask — nothing is rendered or persisted in the request path; the worker (Plan 09.1-05) renders+persists the merged sheet and appends a playable unit version."
  artifacts:
    - path: "src/lib/remake-projects/unit/submission.ts"
      provides: "buildVideoUnitSubmission — per-member gate, model/capability resolution, reference assembly (deferred action-sheet entry), fingerprint, task descriptor"
    - path: "src/lib/remake-projects/unit/action-sheet.ts"
      provides: "renderAndPersistUnitActionSheet — merged-sheet render/upload/persist with fingerprint dedup and provenance (invoked by the worker, Plan 09.1-05; never by submission/preview)"
    - path: "src/app/api/remake-projects/[projectId]/units/route.ts"
      provides: "Authenticated unit create endpoint"
    - path: "src/app/api/remake-projects/[projectId]/units/preview/route.ts"
      provides: "On-demand merged-sheet render via the pure renderer (no persist, W5)"
  key_links:
    - from: "src/lib/remake-projects/unit/submission.ts"
      to: "src/lib/remake-projects/unit/preview.ts"
      via: "the server freeze calls buildUnitSubmissionPreview so the frozen inputs equal the previewed inputs (D-16)"
      pattern: "buildUnitSubmissionPreview"
    - from: "src/lib/remake-projects/unit/submission.ts"
      to: "src/lib/remake-projects/video/service.ts"
      via: "buildVideoGenerationSubmission rejects member shots to enforce D-04"
      pattern: "buildVideoGenerationSubmission"
    - from: "src/app/api/remake-projects/[projectId]/units/preview/route.ts"
      to: "src/lib/remake-projects/keyframes/action-sheet.ts"
      via: "the preview endpoint renders on demand with the pure renderUnitActionSheet without persisting (W5/D-16 WYSIWYG)"
      pattern: "renderUnitActionSheet"
---

<objective>
Deliver the unit submission service and the authenticated unit API: the per-member-gated submission service with the merged-sheet persist helper, the unit create/members/generate/preview/track routes, and the single-shot membership gate (D-04).

Purpose: D-02/D-04/D-05/D-21/D-22 make submission the enforcement point (manual merge, single-shot rejection, duration policy, per-member gate, fingerprint freeze); this plan wires the submission path and routes. Task execution (queue registration + worker handler, including the merged-sheet render/persist inside the task flow) is Plan 09.1-05 — the submission path never renders or persists anything synchronously (W5).
Output: unit submission service, merged-sheet persist helper (worker-invoked), unit API routes with the D-04/D-21 gates, all tested.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-unit/09.1-CONTEXT.md
@.planning/phases/09.1-unit/09.1-RESEARCH.md
@.planning/remake-video-merge-unit-design.md

<interfaces>
Reuse, do not duplicate: `resolveProjectModelCapabilityGenerationOptions` / `getProjectModelConfig` / `getUserModelConfig` (capability normalization), `buildRemakeReferencePlan` + `buildRemakeReferencePromptSuffix` (refs + suffix), `deriveDefaultVideoDuration` (duration), `resolveShotAssetMedia` (asset media), `getAdoptedPromptForGeneration` (member prompts), `submitTask` (queue), `renderUnitActionSheet` + `unitActionSheetFingerprint` (Plan 09.1-02 pure renderer), `assertVideoUnitSubmissionCurrent` + `createVideoUnit` + `updateVideoUnitMembers` + `getVideoUnitDetail` + `getVideoUnitTrackDetail` + `adoptVideoUnitVersion` + `setVideoUnitReviewNote` + `reconfirmVideoUnitVersion` (Plan 09.1-03 service). Task type REMAKE_VIDEO_UNIT_GENERATE was added to TASK_TYPE in Plan 09.1-01; its queue/intent/worker registration lands in Plan 09.1-05 — the API tests mock `submitTask`, so the generate route is testable independently. The single-shot route/payload/handler stay untouched except the D-04 membership gate in `buildVideoGenerationSubmission`. The merged action sheet is a deferred entry at freeze time: the snapshot carries its deterministic source list (member ordinals + adopted keyframe mediaIds, covered by unitActionSheetFingerprint); rendering + persisting happens in the worker task flow (Plan 09.1-05), and the preview endpoint renders on demand without persisting (W5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the per-member-gated unit submission service with the merged-sheet persist helper and the single-shot membership gate (D-02/D-04/D-05/D-21/D-22/W5)</name>
  <files>src/lib/remake-projects/unit/submission.ts, src/lib/remake-projects/unit/action-sheet.ts, src/lib/remake-projects/video/service.ts, tests/unit/remake-projects/remake-video-unit-submission.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-02, D-04, D-05, D-21, D-22
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 1, Pattern 2, Pitfall 4/5, Open Question 3
    - `src/lib/remake-projects/video/service.ts` buildVideoGenerationSubmission (305-465) as the exact analog
    - `src/lib/remake-projects/unit/preview.ts`, `src/lib/remake-projects/unit/references.ts` (Plan 09.1-02 outputs)
    - `src/lib/remake-projects/unit/service.ts` assertVideoUnitSubmissionCurrent (Plan 09.1-03 output)
  </read_first>
  <behavior>
    - Test 1: buildVideoUnitSubmission aggregates per-member gate failures (missing adopted keyframe, missing approved Video Prompt, illegal params) into one error listing every member's ordinal + reason (D-21); no provider work starts on any missing member.
    - Test 2: the frozen snapshot equals buildUnitSubmissionPreview output for the same inputs (WYSIWYG, D-16/D-22), the descriptor fingerprint changes when any member input changes, and the merged action sheet enters the plan as a deferred entry — the submission path performs no rendering and no persistence (W5; render/persist is the worker's job in Plan 09.1-05).
    - Test 3: total member duration normalized through deriveDefaultVideoDuration; a sum over the model max throws REMAKE_VIDEO_UNIT_TOTAL_TOO_LONG with a split hint (D-05).
    - Test 4: buildVideoGenerationSubmission rejects a shot that is a unit member (D-04), and renderAndPersistUnitActionSheet (invoked by the worker) persists the merged sheet with fingerprint dedup + provenance (D-07).
  </behavior>
  <action>Start RED with the four tests above using the vi.hoisted prisma-mock pattern. Create `src/lib/remake-projects/unit/action-sheet.ts` exporting `renderAndPersistUnitActionSheet(input: { projectId, unitId, sources: Array<{ ordinal, mediaId, timestamp, buffer }> })` — render via renderUnitActionSheet (Plan 09.1-02), upload with generateUniqueKey(`remake/${projectId}/action-sheets`, 'jpg') + uploadObject, ensure a MediaObject, and persist a RemakeVideoUnitActionSheet row deduped by unitActionSheetFingerprint, recording member sources in the sources column and a provenance payload — reusing the persistActionSheet conventions. This helper is invoked by the worker task flow (Plan 09.1-05); the submission service and the preview route must NOT call it (W5 — no rendering or persistence in the synchronous request path). Create `src/lib/remake-projects/unit/submission.ts` exporting `buildVideoUnitSubmission(input: { projectId, userId, unitId, operationKey, model?, options? })` modeled on buildVideoGenerationSubmission: verify project ownership; load the unit with members + track; run assertVideoUnitSubmissionCurrent; per member, load the adopted keyframe (collectUnitMemberKeyframeCandidates) and adopted Video Prompt (getAdoptedPromptForGeneration, targetKey video) collecting every missing item into a single REMAKE_VIDEO_UNIT_MEMBER_MISSING:{ordinal}:{KEYFRAME|PROMPT|PARAMS} aggregate error (D-21); resolve the model (explicit > project videoModel > user videoModel, throw REMAKE_VIDEO_MODEL_NOT_CONFIGURED); normalize capabilities via resolveProjectModelCapabilityGenerationOptions; compute per-member timeRangeSeconds (numeric, from the Plan 09.1-01 duration fix) and the total duration through deriveDefaultVideoDuration, throwing REMAKE_VIDEO_UNIT_TOTAL_TOO_LONG when the raw sum exceeds the model max (D-05); assemble the reference plan through buildUnitSubmissionPreview (WYSIWYG) with the merged action sheet as a deferred entry — freeze its deterministic source list (member ordinals + adopted keyframe mediaIds, covered by unitActionSheetFingerprint) in the snapshot instead of rendering or persisting anything in the request path (W5: the worker renders+persists it in the task flow, Plan 09.1-05; the preview endpoint renders it on demand without persisting); build the prompt with the D-09 timed prompt + buildRemakeReferencePromptSuffix for ark_content_multireference (referenceMode via supportsShotGroupMultiReferenceModes, generationMode normal — never firstlastframe for multi-segment, per the design doc); freeze the videoUnitInputSnapshotSchema and return buildVideoUnitTaskDescriptor output. The submission path is purely manual per D-02; per D-03 (已取消), do not implement any sceneTag/characterTags homogeneity判定 or auto-merge suggestion logic anywhere in the submission path — the server only validates, it never suggests merge targets. In `src/lib/remake-projects/video/service.ts`, add the D-04 gate at the top of buildVideoGenerationSubmission: look up RemakeVideoUnitMember by the current shotRevisionId and throw REMAKE_VIDEO_SHOT_IN_UNIT when found (the UI hides this path in Plan 09.1-06, but the service is authoritative). Do not change the single-shot snapshot/descriptor contracts.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-submission.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Submission enforces the per-member gate with aggregated reasons, freezes exactly the previewed inputs into a fingerprinted descriptor (merged sheet deferred to the worker), applies D-05 duration policy, and single-shot submission of a member shot is rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose authenticated unit create/members/generate/preview/track routes (D-02/D-14/D-19/D-21)</name>
  <files>src/app/api/remake-projects/[projectId]/units/route.ts, src/app/api/remake-projects/[projectId]/units/[unitId]/route.ts, src/app/api/remake-projects/[projectId]/units/[unitId]/members/route.ts, src/app/api/remake-projects/[projectId]/units/preview/route.ts, src/app/api/remake-projects/[projectId]/units/[unitId]/tracks/[trackId]/route.ts, tests/integration/api/remake-projects-video-unit.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-02, D-14, D-19, D-21
    - `src/app/api/remake-projects/[projectId]/video/route.ts` auth/Zod/submitTask/error-mapping pattern
    - `src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts` track note/adopt/reconfirm pattern
    - `tests/integration/api/remake-projects-video-tracks.test.ts` requireProjectAuthLight mock pattern
  </read_first>
  <behavior>
    - Test 1: POST /units creates a unit with >=2 members (202/201); unauthenticated or cross-project requests are rejected; a member already in another unit returns a D-04 conflict.
    - Test 2: PATCH /units/[unitId]/members add/remove/reorder succeeds before freeze and returns REMAKE_VIDEO_UNIT_MEMBERS_FROZEN (or REMAKE_VIDEO_UNIT_GENERATION_IN_FLIGHT while a task is pending) after submit (D-19).
    - Test 3: POST /units/[unitId] with action generate returns 202 with taskId + inputFingerprint; a unit with a missing member input returns one aggregated per-member error (D-21) mapped to a conflict/INVALID_PARAMS response.
    - Test 4: GET /units/preview renders the merged action sheet (auth'd, no persist) and returns the buffer or media URL; GET /units/[unitId] returns members + track detail; unit track note/adopt/reconfirm routes mirror the single-shot track routes.
  </behavior>
  <action>Start RED with the API tests mocking requireProjectAuthLight/isErrorResponse/submitTask. Create `src/app/api/remake-projects/[projectId]/units/route.ts` with POST create (strict Zod body: memberShotRevisionIds array min 2, optional userLabel) calling createVideoUnit and returning the unit id + members; `[unitId]/route.ts` with GET detail (getVideoUnitDetail), PATCH for userLabel, and the generate action pinned to this file — POST { operationKey, model?, options? } handled as an action on `[unitId]/route.ts` itself (matching the existing video route's action-style; there is NO separate `[unitId]/generate/route.ts` file), calling buildVideoUnitSubmission then submitTask with the descriptor (type REMAKE_VIDEO_UNIT_GENERATE, targetType 'remake_unit', targetId unitId, dedupeKey, maxAttempts 1) and returning 202 { taskId, inputFingerprint }, mapping STALE/MISSING/NOT_APPROVED/INVALID errors to CONFLICT/INVALID_PARAMS like the video route (submitTask is mocked here; queue/worker registration lands in Plan 09.1-05); `[unitId]/members/route.ts` with PATCH calling updateVideoUnitMembers (frozen / in-flight → 409 conflict); `units/preview/route.ts` with GET (query: unitId or memberShotRevisionIds) that renders the merged action sheet on demand via the pure renderUnitActionSheet WITHOUT persisting — optionally cached by unitActionSheetFingerprint (W5) — and returns the image buffer with image/jpeg content type; `[unitId]/tracks/[trackId]/route.ts` with PATCH actions note/adopt/reconfirm calling setVideoUnitReviewNote / adoptVideoUnitVersion / reconfirmVideoUnitVersion. Every route uses requireProjectAuthLight(projectId) and project-ownership-scoped service calls. Update tests/integration/api/remake-projects-video-unit.test.ts to cover all four behaviors plus the route-coverage guard expectation.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-video-unit.test.ts &amp;&amp; npm run check:test-route-coverage</automated>
  </verify>
  <done>Authenticated create/members/generate(preview/track routes exist with D-04/D-19/D-21 enforcement, the generate endpoint is pinned to the [unitId] route action, and the route-coverage guard passes.</done>
</task>

</tasks>

## Artifacts This Plan Produces

| New symbol | Kind | Downstream consumers |
|------------|------|----------------------|
| `buildVideoUnitSubmission(input)` | service fn | generate action (this plan), frontend submit (09.1-06) |
| `renderAndPersistUnitActionSheet(input)` | service fn | merged-sheet persist inside the worker task flow (09.1-05); preview uses the pure renderer instead (this plan) |
| `REMAKE_VIDEO_SHOT_IN_UNIT` gate | service check | single-shot route (this plan), single-shot UI disable (09.1-06) |
| `POST /units` + `PATCH /units/[unitId]/members` + generate action on `[unitId]/route.ts` + `GET /units/preview` + track PATCH | API | frontend unit mode (09.1-06) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> unit API routes | Member ids, operation keys, and options enter project-scoped submission logic. |
| Submission service -> task payload | The frozen unit snapshot (with the deferred merged-sheet entry) crosses into the enqueued task payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-091-14 | Elevation | unit routes | high | mitigate | requireProjectAuthLight on every route + project-ownership-scoped service queries (unit/member/track filtered by project.userId). |
| T-091-15 | Tampering | D-04 bypass | high | mitigate | buildVideoGenerationSubmission rejects member shots at the service (REMAKE_VIDEO_SHOT_IN_UNIT); the worker (09.1-05) re-checks membership currentness before provider work. |
| T-091-16 | Tampering | frozen input drift | high | mitigate | Submission freezes the previewed inputs and re-verifies currentness (assertVideoUnitSubmissionCurrent); the worker (09.1-05) re-runs the same checks + fingerprint re-verification; append re-checks inside the transaction (appendVideoUnitBatch). |
| T-091-17 | Denial of Service | preview render | low | mitigate | Preview endpoint renders on demand with a bounded 2..9 source set and no persistence; auth required; optional unitActionSheetFingerprint cache key bounds repeat work. |
| T-091-18 | Information Disclosure | task payload handoff | medium | mitigate | Only snapshot-frozen managed media refs enter the task payload; provider credentials never cross the payload. |
| T-091-SC | Tampering | package installs | high | accept | No package install occurs in this plan; all execution reuses existing in-repo queue/gateway/storage modules. |
</threat_model>

<verification>
- Unit submission and API tests pass.
- Route-coverage guard passes for the new routes.
- `npm run typecheck` passes.
- Worker execution of the enqueued task is covered by Plan 09.1-05, not here.
</verification>

<success_criteria>
An authenticated user can create a unit, adjust members pre-freeze, preview the merged sheet (rendered on demand, never persisted in the request path), and submit generation; the service gates every member (D-21), freezes exactly the previewed inputs into a fingerprinted task (D-16/D-22), applies D-05 duration policy, and enqueues the task for the worker (Plan 09.1-05).
</success_criteria>

<output>
Create `.planning/phases/09.1-unit/09.1-04-SUMMARY.md` when done.
</output>
