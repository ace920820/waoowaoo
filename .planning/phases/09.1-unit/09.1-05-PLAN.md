---
phase: 09.1-unit
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/lib/task/queues.ts
  - src/lib/task/intent.ts
  - src/lib/workers/video.worker.ts
  - src/lib/workers/handlers/remake-video-unit.ts
  - src/lib/workers/handlers/remake-video.ts
  - src/lib/remake-projects/video/service.ts
  - tests/unit/worker/remake-video-unit.test.ts
autonomous: true
requirements: [D-07, D-09, D-22]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09/D-22: REMAKE_VIDEO_UNIT_GENERATE is queued, intent-mapped, and switched; the unit handler re-verifies the fingerprint and currentness before provider work and appends an immutable, playable unit version idempotently."
    - "D-07/W5: the merged action sheet is rendered and persisted (fingerprint-deduped) inside the worker task flow via renderAndPersistUnitActionSheet — never in the submission request path (Plan 09.1-04)."
    - "Single-shot parity: extracting the shared buildArkContentItems helper keeps the existing remake-video handler output byte-identical (existing worker tests still pass)."
  artifacts:
    - path: "src/lib/workers/handlers/remake-video-unit.ts"
      provides: "handleRemakeVideoUnitTask — merged-sheet render/persist, reference resolution, provider call, upload, idempotent append"
    - path: "src/lib/workers/handlers/remake-video.ts"
      provides: "shared buildArkContentItems export reused by the single-shot and unit handlers"
  key_links:
    - from: "src/lib/workers/handlers/remake-video-unit.ts"
      to: "src/lib/remake-projects/unit/action-sheet.ts"
      via: "the merged sheet renders and persists inside the task flow (W5/D-07), deduped by unitActionSheetFingerprint"
      pattern: "renderAndPersistUnitActionSheet"
    - from: "src/lib/workers/handlers/remake-video-unit.ts"
      to: "src/lib/remake-projects/unit/service.ts"
      via: "successful generation appends through appendVideoUnitBatch (idempotent)"
      pattern: "appendVideoUnitBatch"
    - from: "src/lib/task/queues.ts"
      to: "src/lib/workers/video.worker.ts"
      via: "REMAKE_VIDEO_UNIT_GENERATE routes to the VIDEO queue and the processVideoTask switch"
      pattern: "processVideoTask"
---

<objective>
Deliver the unit task execution path: register the unit task type in the queue/intent/worker switch, extract the shared Ark content-items helper, and run the frozen unit input through the existing video gateway, COS storage, and billing path — rendering and persisting the merged action sheet inside the task flow (W5).

Purpose: D-07/D-09/D-22 need the unit generation to actually run: the merged action sheet is materialized (render + persist, deduped by fingerprint) as part of the worker task flow rather than in the submission request path (W5), and the frozen snapshot is executed through the existing single-shot gateway/storage/billing machinery with currentness + fingerprint re-checks before provider work.
Output: unit task registration (queue/intent/switch), shared buildArkContentItems extraction, and the unit worker handler, all tested.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-unit/09.1-CONTEXT.md
@.planning/phases/09.1-unit/09.1-RESEARCH.md
@.planning/remake-video-merge-unit-design.md

<interfaces>
Reuse, do not duplicate: `parseVideoUnitTaskPayload` (Plan 09.1-01 output), `renderUnitActionSheet` + `unitActionSheetFingerprint` (Plan 09.1-02 pure renderer), `assertVideoUnitSubmissionCurrent` + `appendVideoUnitBatch` (Plan 09.1-03 service), `renderAndPersistUnitActionSheet` (Plan 09.1-04 persist helper — invoked here, inside the task flow), and the existing worker utils `resolveVideoSourceFromGeneration` + `uploadVideoSourceToCos` + `ensureMediaObjectFromStorageKey` + `assertTaskActive` from the single-shot path. TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE already exists in src/lib/task/types.ts (Plan 09.1-01); register it in the queue/intent/switch here. The single-shot handler `handleRemakeVideoTask` keeps its behavior identical after the shared buildArkContentItems extraction (existing remake-video worker tests are the parity gate).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register the unit task type in queue/intent and extract the shared Ark content-items helper (D-09/D-22)</name>
  <files>src/lib/task/queues.ts, src/lib/task/intent.ts, src/lib/workers/handlers/remake-video.ts, tests/unit/worker/remake-video-unit.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 1, A4
    - `src/lib/task/queues.ts` VIDEO_TYPES set (line 64)
    - `src/lib/task/intent.ts` task-type -> intent map (line 74)
    - `src/lib/workers/handlers/remake-video.ts` buildArkContentItems definition and its call site inside handleRemakeVideoTask
  </read_first>
  <behavior>
    - Test 1: REMAKE_VIDEO_UNIT_GENERATE is in VIDEO_TYPES (routes to the VIDEO queue) and the intent map maps it to 'generate'.
    - Test 2: buildArkContentItems is exported from a shared location, the single-shot handler imports it from there, and the existing remake-video worker test passes unchanged (single-shot parity).
  </behavior>
  <action>Start RED with the two tests above (add the unit task-type registration assertions to tests/unit/worker/remake-video-unit.test.ts). Register TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE in the VIDEO_TYPES set in `src/lib/task/queues.ts` (one line, next to REMAKE_VIDEO_GENERATE) and add the intent mapping in `src/lib/task/intent.ts` (task-type -> 'generate'). Extract `buildArkContentItems(referenceRefs)` from `src/lib/workers/handlers/remake-video.ts` into a shared export (export it from the handler module or a small shared module under src/lib/workers/ — pick one location so both handlers import it) and update handleRemakeVideoTask to import it; do not change its behavior or the single-shot contentItems shape.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/worker/remake-video-unit.test.ts tests/unit/worker/remake-video.test.ts &amp;&amp; npm run check:test-tasktype-coverage</automated>
  </verify>
  <done>The unit task type is queued and intent-mapped, the shared content-items helper is extracted with single-shot parity, and the tasktype-coverage guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run unit generation through the existing video worker path, rendering + persisting the merged action sheet in the task flow (D-07/D-09/D-22/W5)</name>
  <files>src/lib/workers/handlers/remake-video-unit.ts, src/lib/workers/video.worker.ts, tests/unit/worker/remake-video-unit.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pattern 1, A4
    - `src/lib/workers/handlers/remake-video.ts` full handler (preflight/currentness/contentItems/gateway/upload/append)
    - `src/lib/remake-projects/unit/task-contract.ts` parseVideoUnitTaskPayload (Plan 09.1-01 output)
    - `src/lib/remake-projects/unit/action-sheet.ts` renderAndPersistUnitActionSheet (Plan 09.1-04 output)
    - `src/lib/workers/video.worker.ts` processVideoTask switch (849-850)
  </read_first>
  <behavior>
    - Test 1: the unit handler parses the payload, re-verifies the fingerprint, runs assertVideoUnitSubmissionCurrent, renders + persists the merged action sheet via renderAndPersistUnitActionSheet as part of the task flow (W5/D-07 — the only persist call for the sheet, deduped by unitActionSheetFingerprint), resolves reference storage keys for the unit snapshot, builds Ark contentItems in ordinal order, and rejects when no image reference exists.
    - Test 2: generation uses the frozen promptText + durationSeconds with generationMode normal (never firstlastframe), uploads to remake/{projectId}/videos, and appends through appendVideoUnitBatch (idempotent).
    - Test 3: retry returns the existing batch/version without re-running the provider (dedupe path).
    - Test 4: a stale/member-changed unit fails before provider work (REMAKE_VIDEO_UNIT_INPUT_STALE / fingerprint mismatch).
  </behavior>
  <action>Start RED with the worker tests mocking @/lib/workers/utils, @/lib/remake-projects/unit/service, @/lib/remake-projects/unit/action-sheet, and @/lib/media/* per the remake-video.test.ts pattern. Create `src/lib/workers/handlers/remake-video-unit.ts` exporting `handleRemakeVideoUnitTask(job)` modeled on handleRemakeVideoTask: parse with parseVideoUnitTaskPayload; assertVideoUnitSubmissionCurrent; assertTaskActive(job, 'remake_video_unit_preflight'); report progress stages (preparing_references / rendering_action_sheet / submitting_to_provider / uploading_result / persisting_result); render + persist the merged action sheet from the frozen member sources via renderAndPersistUnitActionSheet (W5/D-07 — this is the only place the merged sheet is persisted; the submission path and preview endpoint never call it, and dedup by unitActionSheetFingerprint makes re-renders idempotent); resolve reference storage keys (extend resolveVideoReferenceStorageKeys in video/service.ts with a small unit variant accepting the unit snapshot, or add one — prefer a small unit variant to avoid widening the single-shot signature); pick the first image reference, normalize to base64, and call resolveVideoSourceFromGeneration with promptText, durationSeconds, filtered options, contentItems built from the shared buildArkContentItems (ark_content_multireference) and generationMode forced to normal; uploadVideoSourceToCos to remake/{remakeProjectId}/videos; ensureMediaObjectFromStorageKey; assertTaskActive again; appendVideoUnitBatch. Add the case TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE to the processVideoTask switch in `src/lib/workers/video.worker.ts` (one case, calling handleRemakeVideoUnitTask).</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/worker/remake-video-unit.test.ts tests/unit/worker/remake-video.test.ts &amp;&amp; npm run check:test-tasktype-coverage &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The unit task type is switched and handled through the existing gateway/storage/billing path; the merged sheet is persisted inside the task flow (W5), unit generation appends a playable immutable unit version, and retries dedupe safely.</done>
</task>

</tasks>

## Artifacts This Plan Produces

| New symbol | Kind | Downstream consumers |
|------------|------|----------------------|
| `handleRemakeVideoUnitTask` | worker handler | video.worker switch (this plan), task state UI (09.1-06) |
| shared `buildArkContentItems` | helper | single-shot + unit handlers |
| VIDEO_TYPES + intent + switch registration for REMAKE_VIDEO_UNIT_GENERATE | config | queue routing (this plan), submission generate action (09.1-04, enabled at runtime) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task payload -> video worker/provider/storage | The frozen unit snapshot crosses async execution and the external provider/storage boundary. |
| Worker -> merged-sheet storage | Member keyframe buffers are composited, uploaded, and persisted inside the task flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-091-19 | Tampering | task payload drift | high | mitigate | parseVideoUnitTaskPayload re-verifies the fingerprint and strips runtime keys; the handler re-runs assertVideoUnitSubmissionCurrent before provider work; append re-checks inside the transaction. |
| T-091-20 | Tampering | merged-sheet provenance | medium | mitigate | renderAndPersistUnitActionSheet only accepts snapshot-frozen member keyframe refs (never client-supplied URLs) and dedups by unitActionSheetFingerprint with source provenance recorded. |
| T-091-21 | Denial of Service | provider re-run on retry | high | mitigate | trackId_operationKey idempotency (appendVideoUnitBatch) returns the existing batch/version without re-running the provider; dedupe path covered by Test 3. |
| T-091-22 | Information Disclosure | provider/storage handoff | medium | mitigate | Only snapshot-frozen managed media refs resolve to signed URLs; provider credentials never cross the payload. |
| T-091-SC | Tampering | package installs | high | accept | No package install occurs in this plan; all execution reuses existing in-repo queue/worker/gateway/storage modules. |
</threat_model>

<verification>
- Worker unit tests pass, including the existing remake-video worker tests (single-shot parity after the buildArkContentItems extraction).
- Tasktype-coverage guard passes for the new task type.
- `npm run typecheck` passes.
</verification>

<success_criteria>
REMAKE_VIDEO_UNIT_GENERATE is queued, intent-mapped, and switched; the worker re-checks fingerprint/currentness, renders + persists the merged action sheet inside the task flow (W5/D-07), and runs the frozen unit through the existing gateway/storage/billing path to an immutable, playable unit version — with safe retry dedup and single-shot behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/09.1-unit/09.1-05-SUMMARY.md` when done.
</output>
