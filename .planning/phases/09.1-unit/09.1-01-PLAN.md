---
phase: 09.1-unit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/remake-projects/unit/timecode.ts
  - src/lib/remake-projects/unit/time-anchors.ts
  - src/lib/remake-projects/unit/contracts.ts
  - src/lib/remake-projects/unit/task-contract.ts
  - src/lib/remake-projects/keyframes/adapter.ts
  - src/lib/remake-projects/video/reference-roles.ts
  - src/lib/remake-projects/video/contracts.ts
  - src/lib/remake-projects/video/reference-plan.ts
  - src/lib/task/types.ts
  - tests/unit/remake-projects/remake-video-unit-time-anchors.test.ts
  - tests/unit/remake-projects/remake-video-reference-roles-unit.test.ts
  - tests/unit/remake-projects/remake-video-unit-snapshot.test.ts
autonomous: true
requirements: [D-06, D-09, D-10, D-22]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-09/Pitfall 1: every member duration is a real numeric duration derived from timecodes (no silent 3s fallback), and unit time anchors always close (the scaled segment sum equals the normalized total duration)."
    - "D-09: the merged unit prompt is a time-anchored shot timeline with a cut-declaration header and a total-duration line, in member ordinal order."
    - "D-22: the unit input snapshot is strict/frozen, its fingerprint changes when any member, time anchor, or keyframe changes, and the task descriptor is deterministic with a dedupe key."
    - "D-06/D-10: N member keyframes pass the reference order contract via the new shot_keyframe role; single-shot role ordering behavior is unchanged."
  artifacts:
    - path: "src/lib/remake-projects/unit/timecode.ts"
      provides: "Client-safe timecode-to-seconds pure parser shared by adapter and unit time anchors"
    - path: "src/lib/remake-projects/unit/time-anchors.ts"
      provides: "Client-safe proportional time-anchor scaling and the D-09 timed-prompt builder"
    - path: "src/lib/remake-projects/unit/contracts.ts"
      provides: "Strict unit input snapshot schema and fingerprint (D-22)"
    - path: "src/lib/remake-projects/unit/task-contract.ts"
      provides: "Deterministic unit task descriptor and payload parser"
    - path: "src/lib/remake-projects/video/reference-roles.ts"
      provides: "shot_keyframe role, order, media-type, and ordered-reference schema extension (D-10)"
  key_links:
    - from: "src/lib/remake-projects/keyframes/adapter.ts"
      to: "src/lib/remake-projects/unit/timecode.ts"
      via: "durationSeconds parses projected string timecodes instead of falling back to 3"
      pattern: "timecodeToSeconds"
    - from: "src/lib/remake-projects/unit/time-anchors.ts"
      to: "src/lib/remake-projects/unit/contracts.ts"
      via: "timed prompt text and member time anchors are frozen into the unit snapshot promptText and members[].timeRangeSeconds"
      pattern: "buildUnitTimedPrompt"
    - from: "src/lib/remake-projects/unit/contracts.ts"
      to: "src/lib/remake-projects/unit/task-contract.ts"
      via: "fingerprint change on member/time-anchor change propagates to the dedupe key"
      pattern: "unitInputFingerprint"
---

<objective>
Deliver the Phase 09.1 tracer slice: the unit input-formation pipeline, end to end, from raw member timecodes to a frozen, fingerprinted unit task descriptor.

Purpose: Prove the riskiest architectural unknowns of the phase — the timecode-to-seconds gap (Pitfall 1), the D-09 proportional time anchors and timed prompt, the D-10 shot_keyframe role extension, and the D-22 unit snapshot/fingerprint contract — before any DB/worker/UI expansion.
Output: Client-safe timecode/anchors/prompt pure functions, the strict unit snapshot + fingerprint, the deterministic unit task descriptor, the shot_keyframe role contract, and focused unit tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-unit/09.1-CONTEXT.md
@.planning/phases/09.1-unit/09.1-RESEARCH.md
@.planning/remake-video-merge-unit-design.md

<interfaces>
Mirror the Phase 09 single-shot contract chain (`video/contracts.ts` → `video/task-contract.ts`) exactly, but in a new `src/lib/remake-projects/unit/` directory with unit-shaped schemas. Keep `video/contracts.ts` single-shot fields and `video/task-contract.ts` payload untouched except for the D-10 order-contract relaxation. The unit snapshot's `orderedReferences` reuses the existing `orderedVideoReferenceSchema` from `video/reference-roles.ts`. Pure functions must be client-safe (no `node:` imports), mirroring the reference-roles.ts bundling constraint, so the Plan 09.1-06 preview panel can import them (D-16 WYSIWYG).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one raw member input through timecode parsing, duration projection, proportional anchors, and the D-09 timed prompt</name>
  <reversibility rating="costly">D-09 fixes the duration/time-anchor semantics that every later unit plan and the preview panel depend on; the prompt template and anchor math are the phase contract.</reversibility>
  <files>src/lib/remake-projects/unit/timecode.ts, src/lib/remake-projects/unit/time-anchors.ts, src/lib/remake-projects/keyframes/adapter.ts, tests/unit/remake-projects/remake-video-unit-time-anchors.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-09, D-16, D-22
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pitfall 1, Pitfall 5, Code Examples (timecode parsers, timed prompt template)
    - `src/lib/remake-projects/scenedetect/contracts.ts` startTimecode/endTimecode string fields and numeric duration field
    - `src/lib/remake-projects/keyframes/adapter.ts` current durationSeconds fallback (lines 193-195)
    - `src/lib/remake-projects/video/duration.ts` deriveDefaultVideoDuration (normalized total T input)
  </read_first>
  <behavior>
    - Test 1: timecodeToSeconds parses MM:SS.mmm, HH:MM:SS.mmm, SS.mmm, and plain seconds; malformed or empty input returns 0 without throwing.
    - Test 2: adaptRemakeShot durationSeconds uses a numeric timeRange when present, otherwise parses projected string start/end timecodes; it never falls back to 3 when parseable timecodes exist.
    - Test 3: buildRemakeVideoUnitTimeAnchors scales each member duration by memberDur / sum(memberDurations) x normalized total T, first segment starts at 0, and the sum of all scaled segments equals T (closed intervals).
    - Test 4: buildUnitTimedPrompt emits the cut-declaration header, the 总时长 line, and one line per member in ordinal order formatted as start-end（镜头 N）：prompt with 0.1s-precision rounding.
  </behavior>
  <action>Start RED with the four tests above. Create `src/lib/remake-projects/unit/timecode.ts` exporting a pure client-safe `timecodeToSeconds(timecode: string): number` supporting the three colon formats plus plain seconds, returning 0 for empty/malformed input (no throw, so duration derivation degrades gracefully). In `src/lib/remake-projects/keyframes/adapter.ts`, replace the numeric-only durationSeconds computation so it prefers a numeric timeRange start/end pair, else parses the projected string timecodes with timecodeToSeconds, else falls back to 3 only when no parseable value exists. Create `src/lib/remake-projects/unit/time-anchors.ts` exporting two pure client-safe functions: `buildRemakeVideoUnitTimeAnchors(members, totalDurationSeconds)` returning per-member `{ ordinal, startSeconds, endSeconds }` computed by proportional scaling with a floating-point epsilon closure assertion, and `buildUnitTimedPrompt(members, totalDurationSeconds)` returning the D-09 timeline: a cut-declaration header stating the video switches between shots by cut rather than continuous camera movement, a 总时长约 N 秒 line, one line per member in ordinal order formatted as `0-1s（镜头 1）：prompt` using 0.1s-precision formatting, and the overall consistency requirements (visual anchors follow the matching keyframe for each segment). Both functions take numeric durationSeconds only; do not accept string timecodes inside them. Keep both files free of node: imports and prisma imports so the client preview can import them unchanged.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-time-anchors.test.ts tests/unit/remake-projects/scenedetect-adapter.test.ts</automated>
  </verify>
  <done>The single input path raw member timecodes → numeric durationSeconds → closed scaled anchors → D-09 timed prompt works end-to-end and is committed; the adapter never reports 3s when timecodes exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the reference-role contract with the per-member shot_keyframe role (D-06/D-10)</name>
  <files>src/lib/remake-projects/video/reference-roles.ts, src/lib/remake-projects/video/contracts.ts, src/lib/remake-projects/video/reference-plan.ts, tests/unit/remake-projects/remake-video-reference-roles-unit.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-06, D-10
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pitfall 2, Pattern 2 (reference role extension)
    - `src/lib/remake-projects/video/reference-roles.ts` full file (roles, order map, ordered schema)
    - `src/lib/remake-projects/video/contracts.ts` assertVideoReferencesHaveKeyframe / assertVideoReferenceOrder
    - `src/lib/remake-projects/video/reference-plan.ts` remakeReferenceRoleLabel/Usage and isKeyframeRole helpers
  </read_first>
  <behavior>
    - Test 1: VIDEO_REFERENCE_ROLES contains 'shot_keyframe' and VIDEO_REFERENCE_ROLE_ORDER places it between end_keyframe and action_sheet; all other role relative order is unchanged.
    - Test 2: assertVideoReferenceOrder accepts N consecutive shot_keyframe references with contiguous ordinals (equal role allowed), still rejects strictly-decreasing role order and non-contiguous ordinals.
    - Test 3: assertVideoReferencesHaveKeyframe accepts a plan whose only keyframe role is shot_keyframe.
    - Test 4: remakeReferenceRoleLabel and remakeReferenceRoleUsage return shot_keyframe text; buildRemakeReferencePromptSuffix groups shot_keyframe refs into the 视觉锚点 section with @Image tokens in ordinal order, and the multi-keyframe rule parenthetical appears only for classic start/middle/end keyframes.
  </behavior>
  <action>Start RED with the four tests above. In `src/lib/remake-projects/video/reference-roles.ts`, add 'shot_keyframe' to VIDEO_REFERENCE_ROLES and renumber VIDEO_REFERENCE_ROLE_ORDER so shot_keyframe sits between end_keyframe and action_sheet (action_sheet and every later role shift up by one; persisted rows store role strings, not order values, so renumbering is safe). In `src/lib/remake-projects/video/contracts.ts`, relax assertVideoReferenceOrder from a strictly-increasing role-order check to a strictly-non-decreasing check (throw only when the previous role order is greater than the next), keeping the contiguous-ordinal check — this admits N consecutive shot_keyframe refs while preserving every existing single-shot ordering guarantee. assertVideoReferencesHaveKeyframe already accepts shot_keyframe via the endsWith('_keyframe') check; add a comment documenting that. In `src/lib/remake-projects/video/reference-plan.ts`, add shot_keyframe cases to remakeReferenceRoleLabel (a per-shot visual anchor for its time segment, e.g. 主画面参考关键帧 · 镜头锚点) and remakeReferenceRoleUsage (anchor this segment's visuals: composition, style, lighting, character identity; the segment must match this frame), extend isKeyframeRole to include shot_keyframe, and restrict the multi-keyframe rule parenthetical (按 Start→Middle→End 推进) to render only when every keyframe ref is start/middle/end_keyframe so unit prompts are not mislabeled. Run the existing remake-video-reference-plan.test.ts and remake-video-task-contract.test.ts after the change; if any assertion pins the old rule text or role count, update the assertion to the new generic contract only if the behavior intent is unchanged.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-reference-roles-unit.test.ts tests/unit/remake-projects/remake-video-reference-plan.test.ts tests/unit/remake-projects/remake-video-task-contract.test.ts</automated>
  </verify>
  <done>The role contract accepts N per-member shot_keyframe references with contiguous ordinals, single-shot ordering/assertion behavior is unchanged, and labels/usage render for unit plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze the unit snapshot, fingerprint, and task descriptor (D-22)</name>
  <files>src/lib/remake-projects/unit/contracts.ts, src/lib/remake-projects/unit/task-contract.ts, src/lib/task/types.ts, tests/unit/remake-projects/remake-video-unit-snapshot.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-22
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Open Question 1, Code Examples (unit snapshot schema skeleton), Pattern 3
    - `src/lib/remake-projects/video/contracts.ts` videoInputSnapshotSchema / stableJson / videoInputFingerprint
    - `src/lib/remake-projects/video/task-contract.ts` full descriptor/parser shape and RUNTIME_PAYLOAD_KEYS
    - `src/lib/task/types.ts` TASK_TYPE enum
  </read_first>
  <behavior>
    - Test 1: videoUnitInputSnapshotSchema parses the full research-skeleton shape (projectId, remakeProjectId, unitId, members array min 2 with shotRevisionId/ordinal/selectedKeyframe{slot,mediaId}/promptVersionId/timeRangeSeconds{start,end}, orderedReferences, model, options, referenceMode?, durationSeconds) and is strict (unknown keys rejected).
    - Test 2: unitInputFingerprint changes when member order changes, when a member time anchor changes, and when a member keyframe mediaId changes; identical snapshots produce identical fingerprints.
    - Test 3: buildVideoUnitTaskDescriptor returns TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE, targetType 'remake_unit', targetId equal to the unitId, and a dedupeKey of the form remake-video-unit:{projectId}:{operationKey}:{fingerprint}.
    - Test 4: parseVideoUnitTaskPayload recomputes the fingerprint, strips runtime keys (flowId/runId/meta/flowStage*), and rejects tampered snapshots.
  </behavior>
  <action>Start RED with the four tests above. Create `src/lib/remake-projects/unit/contracts.ts` exporting a strict `videoUnitInputSnapshotSchema` matching the research skeleton: projectId/remakeProjectId/unitId uuids, members array (min 2) of { shotRevisionId uuid, ordinal positive int, selectedKeyframe { slot enum start/middle/end, mediaId uuid }, promptVersionId uuid, timeRangeSeconds { start/end nonnegative numbers } }, orderedReferences as an array of the existing orderedVideoReferenceSchema (min 1), model { id, provider? } strict, options record default {}, referenceMode optional, durationSeconds positive int, and a `promptText` min-1 string holding the full D-09 timed prompt plus reference-usage suffix. Export `unitInputFingerprint(snapshot)` using the same stableJson hashing approach as videoInputFingerprint (sha256 over sorted stable JSON) without importing node:crypto at module top in a way that breaks client bundling — server-only is fine here since the snapshot is never imported by the browser. Create `src/lib/remake-projects/unit/task-contract.ts` mirroring `video/task-contract.ts`: a strict payload schema (kind literal 'video_unit', operationKey, inputSnapshot, inputFingerprint length 64), `buildVideoUnitTaskDescriptor` that parses the snapshot, asserts the project matches, recomputes the fingerprint, and returns { taskType, targetType: 'remake_unit', targetId: unitId, inputFingerprint, payload, dedupeKey: remake-video-unit:{projectId}:{operationKey}:{fingerprint} }, plus `parseVideoUnitTaskPayload` that strips RUNTIME_PAYLOAD_KEYS, re-parses, and re-verifies the fingerprint. Add TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE = 'remake_video_unit_generate' to `src/lib/task/types.ts` (one line, next to REMAKE_VIDEO_GENERATE). Do not modify the single-shot payload schema or descriptor. The batch-level promptVersionId is resolved in Plan 09.1-03 T3 (per research Open Question 1): the unit batch stores promptVersionId = the first member's promptVersionId, while member-level versions live in members[].promptVersionId inside the snapshot.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-snapshot.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The unit snapshot is strict and frozen, its fingerprint is sensitive to every member/time-anchor/keyframe change (D-22), and the deterministic descriptor carries the new task type and dedupe key.</done>
</task>

</tasks>

## Artifacts This Plan Produces

| New symbol | Kind | Downstream consumers |
|------------|------|----------------------|
| `timecodeToSeconds(timecode: string): number` | pure fn | adapter.durationSeconds (this plan), unit submission duration projection (09.1-04) |
| `buildRemakeVideoUnitTimeAnchors(members, totalDurationSeconds)` | pure fn | timed prompt (this plan), submission + preview panel (09.1-04/06) |
| `buildUnitTimedPrompt(members, totalDurationSeconds): string` | pure fn | unit snapshot promptText (this plan), submission (09.1-04), preview (09.1-06) |
| `videoUnitInputSnapshotSchema` | zod schema | unit/task-contract (this plan), submission (09.1-04), worker (09.1-05) |
| `unitInputFingerprint(snapshot): string` | pure fn | task descriptor dedupe key (this plan), submission (09.1-04), worker append (09.1-05) |
| `buildVideoUnitTaskDescriptor` / `parseVideoUnitTaskPayload` | pure fn | API generate route (09.1-04), worker handler (09.1-05) |
| `shot_keyframe` role + renumbered order map | contract | unit reference plan (09.1-02), submission (09.1-04) |
| `TASK_TYPE.REMAKE_VIDEO_UNIT_GENERATE` | constant | queue registration + worker switch + intent (09.1-05), task filter (09.1-06) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> pure contract layer | Untrusted member selections, timecodes, and prompt text enter the frozen snapshot/fingerprint boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-091-01 | Tampering | unit snapshot | high | mitigate | Strict videoUnitInputSnapshotSchema rejects unknown keys and malformed member/time-range shapes; fingerprint recomputed on every parse path. |
| T-091-02 | Tampering | task payload | high | mitigate | parseVideoUnitTaskPayload strips runtime keys and re-verifies unitInputFingerprint before returning (mirrors parseRemakeVideoTaskPayload). |
| T-091-03 | Denial of Service | time-anchor math | low | mitigate | Non-positive or NaN member durations coerce to a safe minimum; scale division guards sum<=0 with factor 1; no unbounded loops. |
| T-091-04 | Spoofing | project identity | medium | mitigate | buildVideoUnitTaskDescriptor asserts snapshot.projectId equals the route projectId (REMAKE_VIDEO_UNIT_PROJECT_MISMATCH), mirroring the single-shot contract. |
| T-091-SC | Tampering | package installs | high | accept | No package install occurs in this plan or phase; all modules are existing in-repo code (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Focused unit tests for time anchors, role extension, and contracts pass.
- `npm run typecheck` passes with the renumbered role order and new task type.
- Existing remake-video-reference-plan / remake-video-task-contract tests still pass (single-shot contract unchanged in behavior).
</verification>

<success_criteria>
Raw member inputs (timecodes, prompts, keyframes) flow through parsing, duration projection, proportional anchors, and the D-09 timed prompt into a strict, fingerprinted, deterministic unit task descriptor, with the shot_keyframe role accepted by every order/assertion consumer — without changing single-shot behavior.
</success_criteria>

<output>
Create `.planning/phases/09.1-unit/09.1-01-SUMMARY.md` when done.
</output>
