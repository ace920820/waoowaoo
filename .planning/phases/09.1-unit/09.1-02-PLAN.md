---
phase: 09.1-unit
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/lib/remake-projects/unit/references.ts
  - src/lib/remake-projects/keyframes/action-sheet.ts
  - src/lib/remake-projects/unit/preview.ts
  - tests/unit/remake-projects/remake-video-unit-references.test.ts
  - tests/unit/remake-projects/remake-video-unit-action-sheet.test.ts
  - tests/unit/remake-projects/remake-video-unit-preview.test.ts
autonomous: true
requirements: [D-06, D-07, D-08, D-16]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-06: every unit member contributes exactly one adopted keyframe, and the reference plan carries N member keyframes in ordinal order plus the merged action sheet plus deduplicated assets."
    - "D-08: cross-member characters/scenes/props/voices are deduplicated by asset id, and the final plan never exceeds 9 images / 3 audio (priority keyframes > action sheet > characters > scene > props > voice)."
    - "D-07: renderUnitActionSheet produces a 6- or 9-cell grid (each cell = one member frame + shot number label), while the existing 3-slot single-shot rendering stays byte-identical."
    - "D-16: the preview assembly (member list with durations, reference order, timed prompt, total duration) is one shared pure function the server freeze and the client preview both consume."
  artifacts:
    - path: "src/lib/remake-projects/unit/references.ts"
      provides: "Per-member adopted-keyframe collection, asset-id dedup, and the unit reference-plan builder respecting content[] caps"
    - path: "src/lib/remake-projects/keyframes/action-sheet.ts"
      provides: "N-grid action-sheet renderer (6/9 grid) generalized from the 3-slot renderer"
    - path: "src/lib/remake-projects/unit/preview.ts"
      provides: "Client-safe WYSIWYG preview assembly shared by server freeze and frontend preview"
  key_links:
    - from: "src/lib/remake-projects/unit/references.ts"
      to: "src/lib/remake-projects/video/reference-plan.ts"
      via: "merged candidates are sorted/truncated by buildRemakeReferencePlan under REMAKE_VIDEO_IMAGE_CAP/REMAKE_VIDEO_AUDIO_CAP"
      pattern: "buildRemakeReferencePlan"
    - from: "src/lib/remake-projects/unit/preview.ts"
      to: "src/lib/remake-projects/unit/time-anchors.ts"
      via: "preview prompt text is produced by the same buildUnitTimedPrompt the server freezes"
      pattern: "buildUnitTimedPrompt"
    - from: "src/lib/remake-projects/keyframes/action-sheet.ts"
      to: "src/lib/remake-projects/unit/references.ts"
      via: "the merged action sheet mediaId enters the reference plan as the action_sheet candidate"
      pattern: "renderUnitActionSheet"
---

<objective>
Build the unit reference-merge layer: per-member adopted keyframes, cross-member asset deduplication under the 9-image / 3-audio caps, the merged 6/9-grid action sheet renderer, and the shared WYSIWYG preview assembly.

Purpose: D-06/D-07/D-08 define how the unit's actual provider inputs are formed; this plan makes them pure, testable, and identical between what the server freezes and what the user previews (D-16).
Output: unit reference-plan builder, N-grid action-sheet renderer (single-shot path unchanged), and the shared preview assembler, all unit-tested.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-unit/09.1-CONTEXT.md
@.planning/phases/09.1-unit/09.1-RESEARCH.md
@.planning/remake-video-merge-unit-design.md

<interfaces>
Reuse the existing `buildRemakeReferencePlan` + `REMAKE_VIDEO_IMAGE_CAP/REMAKE_VIDEO_AUDIO_CAP` from `video/reference-plan.ts` as the single truncation authority (do not re-implement caps). Reuse `resolveShotAssetMedia` / `resolveMediaRef` signatures from the remake semantics layer for candidate media resolution. The `shot_keyframe` role, label, and usage come from Plan 09.1-01. The preview assembler must stay client-safe (no node: imports, no prisma) so Plan 09.1-06 can import it; DB-backed collection helpers live in `unit/references.ts` and are mocked in unit tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collect one adopted keyframe per member, deduplicate assets across members, and build the capped unit reference plan (D-06/D-08)</name>
  <files>src/lib/remake-projects/unit/references.ts, tests/unit/remake-projects/remake-video-unit-references.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-06, D-08
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pitfall 2, Pitfall 3, Pattern 2
    - `src/lib/remake-projects/video/service.ts` resolveKeyframeReferenceCandidates and buildAssetReferenceCandidates (the single-shot collection analogs)
    - `src/lib/remake-projects/video/reference-plan.ts` buildRemakeReferencePlan / caps / candidate shape
    - `src/lib/remake-projects/semantics/asset-media.ts` resolveShotAssetMedia result maps
    - `src/lib/remake-projects/keyframes/adapter.ts` REMAKE_KEYFRAME_SLOTS
  </read_first>
  <behavior>
    - Test 1: collectUnitMemberKeyframeCandidates returns exactly one adopted keyframe per member (default middle slot, falling back to the only adopted slot) with resolved stable media, in ordinal order, and throws a per-member error when a member has no adopted keyframe.
    - Test 2: dedupeUnitAssetCandidates collapses the same asset id across members to one candidate each for characters/scenes/props/voices.
    - Test 3: buildUnitReferencePlan emits shot_keyframe refs (ordinal order) then action_sheet then characters/scene/props then audio, and truncation under image cap 9 / audio cap 3 follows the keyframe > action sheet > characters > scene > props > voice priority.
    - Test 4: a plan whose N member keyframes + action sheet + dedup assets exceed 9 images drops the lowest-priority assets first and never the member keyframes.
  </behavior>
  <action>Start RED with the four tests above, mocking prisma with the vi.hoisted pattern from tests/integration/remake-projects/remake-video-generation.test.ts. Create `src/lib/remake-projects/unit/references.ts` exporting: `collectUnitMemberKeyframeCandidates(params: { members: Array<{ shotRevisionId, ordinal, preferredSlot? }> })` that for each member finds the RemakeKeyframeTrack row for the preferred slot (default middle) with its adoptedCandidate.outputVersion.mediaId, falls back to any adopted slot when the preferred slot is missing, resolves the stable media reference through resolveMediaRef the same way resolveKeyframeReferenceCandidates does, and throws `REMAKE_VIDEO_UNIT_MEMBER_KEYFRAME_MISSING:{ordinal}` when no adopted keyframe exists; `dedupeUnitAssetCandidates(candidates)` keyed by assetId keeping the first occurrence per role family; and `buildUnitReferencePlan(input: { memberKeyframes, actionSheetMediaRef?, assetCandidates })` that assembles candidates in the order shot_keyframes (ordinal) -> action_sheet -> characters -> scene -> props -> audio, then delegates sorting/truncation to the existing buildRemakeReferencePlan so the 9/3 caps and priority degrade are single-sourced. Per-member shot_keyframe label/usage come from remakeReferenceRoleLabel/remakeReferenceRoleUsage (Plan 09.1-01), augmented with the member ordinal in the label (e.g. 镜头 2 关键帧). Do not modify video/reference-plan.ts here.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-references.test.ts</automated>
  </verify>
  <done>Per-member 1-keyframe collection, asset-id dedup, and capped plan building are unit-covered; member keyframes are never dropped by truncation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the action-sheet renderer to a 6/9-cell grid and render the merged unit sheet (D-07)</name>
  <files>src/lib/remake-projects/keyframes/action-sheet.ts, tests/unit/remake-projects/remake-video-unit-action-sheet.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-07
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Pitfall 6, Pattern 3 (renderer fingerprint), A5
    - `src/lib/remake-projects/keyframes/action-sheet.ts` full file (renderActionSheet, labelSvg, prepareActionSheet)
    - `src/lib/remake-projects/keyframes/action-sheet-task-contract.ts` how the renderer is invoked today
  </read_first>
  <behavior>
    - Test 1: renderActionSheet with exactly three sources produces byte-identical output to the current implementation (backward compatibility for the single-shot path).
    - Test 2: renderUnitActionSheet with 6 sources produces a 2-column grid and with 9 sources a 3-column grid; each cell contains one member frame and a shot-number label (镜头 N).
    - Test 3: the unit sheet fingerprint is deterministic over (renderer version, unit member ordinal+mediaId list) and changes when a member mediaId changes.
    - Test 4: renderUnitActionSheet rejects fewer than 2 or more than 9 sources.
  </behavior>
  <action>Start RED with the four tests above. Refactor `src/lib/remake-projects/keyframes/action-sheet.ts` so the slot/frame pipeline is expressed over an ordered list of sources instead of the fixed ACTION_SHEET_SLOTS triple: keep ACTION_SHEET_SLOTS and the existing 3-slot renderActionSheet path internally delegating to the generalized renderer with the same widths/labels/timestamps so single-shot output stays byte-identical (assert with Test 1 before refactor, after refactor, and in the persisted fingerprint). Add `renderUnitActionSheet(sources: Array<{ ordinal, mediaId, timestamp, buffer }>)` that lays cells left-to-right, top-to-bottom in a 2-column grid for 3-6 sources and a 3-column grid for 7-9 sources (discretion: 6 vs 9 grid rule — use cell count 3-6 => 2 columns, 7-9 => 3 columns), each cell a 640x360 cover-fit frame plus a 镜头{N} SVG label bar, composited with sharp exactly like the existing renderer. Add `unitActionSheetFingerprint(input: { unitId, sources })` hashing the renderer version + member ordinals/mediaIds. Do not change the storage key naming or upload flow here — rendering only; upload/persist belongs to Plan 09.1-04's action-sheet persist helper (`renderAndPersistUnitActionSheet`), invoked inside the worker task flow (09.1-05).</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-action-sheet.test.ts tests/unit/remake-projects/remake-action-sheet.test.ts</automated>
  </verify>
  <done>The renderer supports N-cell grids with member numbering, the unit fingerprint is deterministic, and the existing single-shot action-sheet rendering is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assemble the shared WYSIWYG unit submission preview (D-16)</name>
  <files>src/lib/remake-projects/unit/preview.ts, tests/unit/remake-projects/remake-video-unit-preview.test.ts</files>
  <read_first>
    - `.planning/phases/09.1-unit/09.1-CONTEXT.md` D-16, D-22
    - `.planning/phases/09.1-unit/09.1-RESEARCH.md` Anti-Patterns (前端算时间锚点、服务端另算一套), Pattern 1
    - `src/lib/remake-projects/unit/time-anchors.ts` (Plan 09.1-01 outputs)
    - `src/lib/remake-projects/unit/references.ts` (Task 1 outputs)
    - `src/lib/remake-projects/video/duration.ts` deriveDefaultVideoDuration
  </read_first>
  <behavior>
    - Test 1: buildUnitSubmissionPreview returns, from member inputs, the ordered member list with numeric durations, the reference order (per-member keyframe + merged action sheet + dedup assets), the timed prompt text, and the normalized total duration via deriveDefaultVideoDuration.
    - Test 2: the preview prompt text equals buildUnitTimedPrompt output for the same members and total (single source of truth).
    - Test 3: the preview reference order equals buildUnitReferencePlan output (WYSIWYG: what the user sees is what the server freezes).
    - Test 4: preview is pure — the same inputs always produce the same output object (no prisma, no storage, no node: imports).
  </behavior>
  <action>Start RED with the four tests above. Create `src/lib/remake-projects/unit/preview.ts` exporting a pure client-safe `buildUnitSubmissionPreview(input: { members: Array<{ ordinal, durationSeconds, adoptedPrompt, keyframeMediaRef }>, actionSheetMediaRef?, assetCandidates?, totalDurationSeconds? })` that (1) computes per-member anchors with buildRemakeVideoUnitTimeAnchors, (2) builds the timed prompt with buildUnitTimedPrompt, (3) assembles the reference plan with buildUnitReferencePlan, (4) normalizes the total duration with deriveDefaultVideoDuration when not supplied, and returns { members, orderedReferences, promptText, totalDurationSeconds, referenceCounts: { images, audio } }. This function is the single assembly contract for both the Plan 09.1-04 server freeze and the Plan 09.1-06 preview panel; server-only concerns (prisma reads, merged-sheet rendering) stay outside this file and are injected as refs. Keep it free of node: imports and prisma imports.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-unit-preview.test.ts</automated>
  </verify>
  <done>The preview assembler is pure, client-safe, and produces exactly the prompt/refs/total the server freeze will consume.</done>
</task>

</tasks>

## Artifacts This Plan Produces

| New symbol | Kind | Downstream consumers |
|------------|------|----------------------|
| `collectUnitMemberKeyframeCandidates(params)` | async fn (prisma) | submission service (09.1-04) |
| `dedupeUnitAssetCandidates(candidates)` | pure fn | references plan + submission (09.1-04) |
| `buildUnitReferencePlan(input)` | pure fn | preview (this plan), submission (09.1-04) |
| `renderUnitActionSheet(sources)` | async fn (sharp) | merged-sheet persist helper (09.1-04) + worker persist (09.1-05), preview endpoint (09.1-04) |
| `unitActionSheetFingerprint(input)` | pure fn | merged-sheet dedup (09.1-04/05) |
| `buildUnitSubmissionPreview(input)` | pure fn (client-safe) | server freeze (09.1-04), preview panel (09.1-06) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB adopted-keyframe/media rows -> reference plan | Adopted media identities cross into the provider-bound reference plan. |
| Sharp render inputs -> composite buffer | Member frame buffers are composited into the merged action sheet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-091-05 | Tampering | reference plan caps | high | mitigate | Truncation is delegated to the existing buildRemakeReferencePlan caps; member keyframes are emitted before assets so degrade never drops a required frame; tests pin the priority order. |
| T-091-06 | Tampering | merged sheet sources | medium | mitigate | renderUnitActionSheet only accepts media refs collected from adopted DB rows (collectUnitMemberKeyframeCandidates) — never client-supplied URLs. |
| T-091-07 | Denial of Service | sharp composite | low | mitigate | Source count is bounded 2..9 (schema+renderer reject outside), and cell buffers are resized cover-fit before compositing. |
| T-091-08 | Information Disclosure | preview assembly | low | accept | The preview carries media refs/labels only; URLs are signed at render time by storage helpers in later plans, never embedded here. |
| T-091-SC | Tampering | package installs | high | accept | No package install occurs in this plan; sharp is the existing in-repo renderer. |
</threat_model>

<verification>
- Unit tests for references, action sheet, and preview pass.
- Existing remake-action-sheet.test.ts still passes (single-shot rendering unchanged).
- `npm run typecheck` passes.
</verification>

<success_criteria>
The unit's provider-bound inputs — one keyframe per member, merged 6/9-grid action sheet, deduplicated assets under the 9/3 caps — are produced by pure, tested builders that the server freeze and the client preview share verbatim (D-16 WYSIWYG).
</success_criteria>

<output>
Create `.planning/phases/09.1-unit/09.1-02-SUMMARY.md` when done.
</output>
