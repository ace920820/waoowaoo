---
phase: 09-video-generation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/lib/remake-projects/video/service.ts
  - src/lib/remake-projects/video/invalidation.ts
  - src/lib/remake-projects/keyframes/invalidation.ts
  - src/lib/remake-projects/service.ts
  - src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts
  - tests/integration/api/remake-projects-video-tracks.test.ts
  - tests/integration/remake-projects/remake-video-invalidation.test.ts
autonomous: true
requirements: [VGEN-04, VGEN-05, VGEN-06, VGEN-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "VGEN-04/VGEN-06 and D-06/D-12: batches and versions remain immutable and old generation receipts survive every later edit or generation."
    - "VGEN-05 and D-13..D-16: notes are simple per-version text, adoption is explicit, replacements append history, and playback/history reads never mutate adoption."
    - "VGEN-06/VGEN-07 and D-17..D-19: upstream changes retain old playable versions and adopted pointers, mark review required, remove completion/readiness, and allow explicit reconfirmation or new adoption."
  artifacts:
    - path: "src/lib/remake-projects/video/service.ts"
      provides: "Transactional note, adoption, replacement, reconfirmation, history, and readiness operations"
    - path: "src/lib/remake-projects/video/invalidation.ts"
      provides: "Idempotent provenance-linked video review invalidation while preserving history and adoption pointers"
    - path: "src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts"
      provides: "Authorized GET and explicit note/adopt/reconfirm action boundary"
    - path: "src/lib/remake-projects/service.ts"
      provides: "Per-Shot video lifecycle and readiness projection in the existing Remake snapshot"
  key_links:
    - from: "src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts"
      to: "src/lib/remake-projects/video/service.ts"
      via: "strict explicit actions call transactional lifecycle commands"
      pattern: "setVideoReviewNote|adoptVideoVersion|reconfirmVideoVersion"
    - from: "src/lib/remake-projects/keyframes/invalidation.ts"
      to: "src/lib/remake-projects/video/invalidation.ts"
      via: "selected keyframe/action-sheet changes propagate existing provenance review state"
      pattern: "invalidateRemakeVideoVersions"
    - from: "src/lib/remake-projects/video/invalidation.ts"
      to: "src/lib/remake-projects/service.ts"
      via: "needs-review state removes Shot completion/readiness without clearing adopted media"
      pattern: "needsReview"
---

<objective>
Complete the immutable video version lifecycle, explicit review/adoption history, and upstream invalidation/reconfirmation loop.

Purpose: Make generated Shot video history trustworthy and recoverable without expanding beyond the D-13..D-19 MVP.
Output: Lifecycle service actions, authenticated track/version API, snapshot projection, invalidation wiring, and focused integration tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-generation/09-CONTEXT.md
@.planning/phases/09-video-generation/09-PATTERNS.md
@.planning/phases/09-video-generation/09-01-SUMMARY.md

<interfaces>
Plan 09-01 provides revision-bound tracks, immutable batches/versions, frozen receipts, Task/output/provenance links, and the existing Task execution path. This plan adds only the lightweight review lifecycle and existing invalidation projection; it does not mutate provider execution.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add notes and explicit append-only adoption history</name>
  <files>src/lib/remake-projects/video/service.ts, src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts, tests/integration/api/remake-projects-video-tracks.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-13 through D-16
    - `.planning/phases/09-video-generation/09-PATTERNS.md` service and track-route assignments
    - `src/lib/remake-projects/keyframes/service.ts` adoption transaction and event shape
    - `src/app/api/remake-projects/[projectId]/keyframes/tracks/[trackId]/route.ts` auth/action/error convention
  </read_first>
  <behavior>
    - Test 1: a user can write or replace one plain review-note string on a generated version; no score, tag, rank, or automatic judgment is stored.
    - Test 2: explicit adopt changes the track's current pointer and appends an event containing previous/next version, reviewer, action, and timestamp while retaining both versions.
    - Test 3: replacing an adopted version requires an explicit replacement confirmation field; missing confirmation returns conflict and leaves the pointer unchanged.
    - Test 4: GET, playback selection, history selection, and note reads never move the adoption pointer.
  </behavior>
  <action>Implement `setVideoReviewNote` and `adoptVideoVersion` as project-owned transactions. Keep D-14 to one optional text field. Per D-15, adoption exists only as an explicit command; verify track/version ownership, completed playable media, and current review state, require an explicit confirmation boolean when another version is already adopted, update exactly one nullable pointer, and append an immutable event with previous/next IDs and reviewer identity. Expose strict GET/PATCH action schemas through the track route with project auth and conflict/not-found mappings. Do not attach mutations to playback or history selection, and do not add scoring, tags, ranking, automatic adoption, or synchronized-player state.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-video-tracks.test.ts</automated>
  </verify>
  <done>Notes remain lightweight, every adoption/replacement is explicit and auditable, and all read/playback paths are mutation-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate Prompt, keyframe, and action-sheet changes into review-required state</name>
  <files>src/lib/remake-projects/video/invalidation.ts, src/lib/remake-projects/keyframes/invalidation.ts, src/lib/remake-projects/video/service.ts, tests/integration/remake-projects/remake-video-invalidation.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-17 through D-19
    - `.planning/phases/09-video-generation/09-PATTERNS.md` invalidation assignment
    - `src/lib/remake-projects/keyframes/invalidation.ts` existing retain-and-review pattern
    - `src/lib/remake-projects/service.ts` existing Prompt/revision invalidation entry points named by PATTERNS
  </read_first>
  <behavior>
    - Test 1: adopted Video Prompt changes invalidate versions whose receipt references the prior Prompt version, without deleting their media, note, snapshot, provenance, or adoption event.
    - Test 2: a selected adopted keyframe or action-sheet change invalidates only versions whose ordered receipt referenced the changed identity.
    - Test 3: invalidation is idempotent, preserves a current adopted pointer, and marks the version/Shot as needing review.
    - Test 4: an unrelated unselected candidate or project parameter-default change does not retroactively invalidate historical versions.
  </behavior>
  <action>Create `invalidateRemakeVideoVersions` using existing provenance/invalidation records and frozen receipt identities. Call it from the current Video Prompt adoption/edit invalidation path and the keyframe/action-sheet invalidation path named by PATTERNS. Preserve output rows, playable media, review note, snapshots, adoption pointer, and adoption history. Mark affected versions and the Shot review gate idempotently; use exact selected-reference/Prompt identities so unrelated candidates and later project-default edits do not invalidate old receipts. Reuse the existing invalidation graph per D-19 and do not add automatic regeneration or another dependency graph.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/remake-projects/remake-video-invalidation.test.ts</automated>
  </verify>
  <done>All three upstream input classes propagate precise review-required state while every historical version and adopted pointer remains traceable and playable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Project history, readiness reasons, and explicit reconfirmation in the existing snapshot</name>
  <files>src/lib/remake-projects/video/service.ts, src/lib/remake-projects/service.ts, src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts, tests/integration/api/remake-projects-video-tracks.test.ts, tests/integration/remake-projects/remake-video-invalidation.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-13, D-17, D-18, D-19 and VGEN-07
    - `.planning/phases/09-video-generation/09-PATTERNS.md` truthful snapshot projection assignment
    - `src/lib/remake-projects/service.ts` `getRemakeProjectSnapshot` include/projection shape
  </read_first>
  <behavior>
    - Test 1: the existing Remake snapshot returns original Shot media, current adopted version, chronological generated versions, notes, frozen receipts, task state, and adoption/reconfirmation events.
    - Test 2: a Shot is ready/complete only with required current inputs, legal parameter capability, and an adopted version that is not awaiting review.
    - Test 3: when the adopted version is invalidated, the pointer and playback remain, but readiness is false with a specific reason.
    - Test 4: explicit reconfirmation appends an event, clears the matching review requirement, preserves the same adopted version, and restores readiness only if all other gates pass.
  </behavior>
  <action>Extend `getRemakeProjectSnapshot` rather than introducing a second fetch shape. Project the original media URL, current adopted version, generated history newest-first, notes, immutable receipts, Task/status, invalidation reason, adoption history, and a machine-readable readiness reason list. Implement `reconfirmVideoVersion` as a deliberate authorized action valid only for the currently adopted invalidated version; append a reconfirmation event and resolve its review state without changing historical receipt facts. Keep the adopted pointer during invalidation, but ensure `complete`, default batch readiness, and later export readiness remain false until reconfirmation or new adoption. This plan exposes single-Shot readiness facts only; Phase 10 orchestration and Phase 11 export are outside scope.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-video-tracks.test.ts tests/integration/remake-projects/remake-video-invalidation.test.ts</automated>
  </verify>
  <done>The snapshot is the truthful lifecycle source, stale adoption cannot count as complete, and explicit reconfirmation restores readiness without erasing history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> lifecycle API | Untrusted note/adopt/reconfirm actions target project-owned tracks and versions. |
| Upstream mutation -> invalidation state | Prompt/keyframe/revision changes alter readiness while historical media must remain intact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | adoption/reconfirmation route | high | mitigate | Authorize project ownership, validate action schemas, and update pointers plus append-only events transactionally. |
| T-09-06 | Repudiation | adoption history | medium | mitigate | Record reviewer identity, previous/next version, action kind, and timestamp for adoption and reconfirmation. |
| T-09-07 | Tampering | invalidation propagation | high | mitigate | Use idempotent provenance-linked invalidation records and preserve historical rows/media/adoption pointers. |
| T-09-SC | Tampering | package installs | high | accept | No package install is planned; the phase uses existing dependencies only. |
</threat_model>

<verification>
- Focused lifecycle API tests prove note/adopt/replace/read behavior.
- Focused invalidation tests prove precise Prompt/keyframe/action-sheet propagation, retained history, readiness loss, and reconfirmation.
- Snapshot projection returns one authoritative lifecycle/readiness shape for the existing Remake workspace.
</verification>

<success_criteria>
Users can note, adopt, replace, inspect, and reconfirm immutable versions; upstream changes cannot erase history or leave a stale adopted version counted as complete.
</success_criteria>

<output>
Create `.planning/phases/09-video-generation/09-02-SUMMARY.md` when done.
</output>
