---
phase: 09-video-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260812090000_add_remake_video_generation/migration.sql
  - src/lib/remake-projects/video/contracts.ts
  - src/lib/remake-projects/video/task-contract.ts
  - src/lib/remake-projects/video/service.ts
  - src/app/api/remake-projects/[projectId]/video/route.ts
  - src/lib/task/types.ts
  - src/lib/task/queues.ts
  - src/lib/task/intent.ts
  - src/lib/workers/handlers/remake-video.ts
  - src/lib/workers/video.worker.ts
  - tests/unit/remake-projects/remake-video-task-contract.test.ts
  - tests/integration/api/remake-projects-video.test.ts
  - tests/unit/worker/remake-video.test.ts
autonomous: true
requirements: [VGEN-01, VGEN-02, VGEN-03, VGEN-04, VGEN-07]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "VGEN-01/VGEN-02 and D-01..D-05: one authenticated Shot submission uses only explicitly selected adopted keyframes plus the optional action sheet, in fixed Start/Middle/End/action-sheet order, and the frozen payload is the exact displayed input list."
    - "VGEN-03 and D-07..D-12: the existing video capability system supplies and validates model, duration, resolution, audio, reference mode, and supported options, then freezes every normalized value without changing project defaults."
    - "VGEN-04 and D-06/D-12: the existing Task/video worker/model gateway/storage/billing path appends a playable immutable version with exact input, Task, media, and provenance links."
    - "VGEN-07 and D-03/D-09: missing adopted keyframes, adopted Video Prompt, stale inputs, or illegal model parameters fail before provider work with an actionable conflict."
  artifacts:
    - path: "src/lib/remake-projects/video/contracts.ts"
      provides: "Strict ordered-reference, immutable input-snapshot, normalized model-parameter, and generation result contracts"
    - path: "src/lib/remake-projects/video/task-contract.ts"
      provides: "Deterministic fingerprinted Remake video Task descriptor and payload parser"
    - path: "prisma/migrations/20260812090000_add_remake_video_generation/migration.sql"
      provides: "Forward-only durable video track/batch/version/adoption schema migration"
    - path: "src/lib/workers/handlers/remake-video.ts"
      provides: "Remake adapter into the existing video generation, storage, billing, progress, and cancellation lifecycle"
    - path: "src/app/api/remake-projects/[projectId]/video/route.ts"
      provides: "Authenticated single-Shot generation endpoint"
  key_links:
    - from: "src/app/api/remake-projects/[projectId]/video/route.ts"
      to: "src/lib/remake-projects/video/task-contract.ts"
      via: "validated request becomes one immutable fingerprinted Task descriptor"
      pattern: "buildRemakeVideoTaskDescriptor"
    - from: "src/lib/remake-projects/video/task-contract.ts"
      to: "src/lib/workers/video.worker.ts"
      via: "new Task type is routed through the existing video queue"
      pattern: "REMAKE_VIDEO_GENERATE"
    - from: "src/lib/workers/handlers/remake-video.ts"
      to: "src/lib/remake-projects/video/service.ts"
      via: "preflight/currentness, existing gateway execution, managed upload, then transactional immutable append"
      pattern: "appendRemakeVideoGeneration"
---

<objective>
Deliver the Phase 09 end-to-end tracer from authenticated single-Shot submission to an immutable, persisted, playable generated version.

Purpose: Prove the Remake Shot path reuses the existing Task/video worker/model gateway/storage/billing system while enforcing D-01..D-12 and VGEN-01..04/VGEN-07.
Output: Durable video-generation schema, strict task contract, authenticated submit route, Remake video worker handler, and focused contract/API/worker tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-generation/09-CONTEXT.md
@.planning/phases/09-video-generation/09-PATTERNS.md

<interfaces>
Use the exact analogs assigned by 09-PATTERNS.md: keyframe contracts/task descriptor/service/route/handler for the Remake boundary, and the existing `video.worker.ts` flow for gateway, storage, billing, progress, and cancellation. Do not create a second video queue, provider resolver, storage uploader, billing path, or capability catalog.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one exact-input Remake Shot through the existing video execution path</name>
  <files>src/lib/remake-projects/video/contracts.ts, src/lib/remake-projects/video/task-contract.ts, src/lib/remake-projects/video/service.ts, src/app/api/remake-projects/[projectId]/video/route.ts, src/lib/task/types.ts, src/lib/task/queues.ts, src/lib/task/intent.ts, src/lib/workers/handlers/remake-video.ts, src/lib/workers/video.worker.ts, tests/unit/remake-projects/remake-video-task-contract.test.ts, tests/integration/api/remake-projects-video.test.ts, tests/unit/worker/remake-video.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-01 through D-12
    - `.planning/phases/09-video-generation/09-PATTERNS.md` contract, task descriptor, service, API, and worker assignments
    - `src/lib/remake-projects/keyframes/task-contract.ts` exact descriptor/parser shape
    - `src/app/api/remake-projects/[projectId]/keyframes/route.ts` auth/Zod/Task submission/error mapping
    - `src/lib/workers/handlers/remake-keyframe-image.ts` preflight/cancellation/persist checkpoints
    - `src/lib/workers/video.worker.ts` existing model gateway, ordered reference conversion, storage, billing, and lifecycle helpers named by PATTERNS
  </read_first>
  <behavior>
    - Test 1: D-01..D-05 accepts an explicit subset containing at least one adopted Start/Middle/End candidate, optionally includes the current action sheet, emits fixed ordinals, and rejects action-sheet-only, reordered, duplicate, stale, or foreign selections.
    - Test 2: D-07..D-12 resolves the current video model through existing project/user configuration and capabilities, derives the legal duration, freezes mode/resolution/audio/options, and rejects unsupported values.
    - Test 3: authenticated POST returns 202 with one deduplicated existing Task; unauthenticated, cross-project, missing Prompt/keyframe, and stale input cases never create provider work.
    - Test 4: the video worker parses and fingerprints the frozen snapshot, rechecks currentness, passes the ordered references and normalized parameters unchanged to the existing generation path, and returns one managed playable media result.
  </behavior>
  <action>Start RED with the contract, route, and worker tests for one Start-keyframe happy path. Define `RemakeVideoInputSnapshot` as the complete frozen receipt: project/Shot/revision identity, adopted Video Prompt version and full text, ordered selected references `{ role, ordinal, mediaId, stable media identity }`, model key, resolved reference mode, final duration/resolution/audio/capability options, operation key, and provenance identities. Per D-02, admit only current adopted generated Start/Middle/End keyframes and the current revision action sheet; exclude original single Start/Middle/End frames. Build the ordered list exclusively from caller-selected eligible candidates in D-04 order; use one pure builder for both returned preview data and payload serialization per D-05. Register `TASK_TYPE.REMAKE_VIDEO_GENERATE` in the existing video queue and intent maps. Implement the authenticated collection route with `requireProjectAuthLight`, strict Zod parsing, conflict/not-found mapping, descriptor dedupe, and `submitTask`. Implement `handleRemakeVideoTask` as a thin adapter invoked by `video.worker.ts`: parse, assert current, check cancellation, call the existing video generation/provider/reference/storage/billing helpers, check cancellation again, and hand the managed output to persistence. Keep runtime Task metadata outside the fingerprint parser and never resolve a provider, capability, upload path, or billing record independently of the existing system.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-task-contract.test.ts tests/integration/api/remake-projects-video.test.ts tests/unit/worker/remake-video.test.ts</automated>
  </verify>
  <done>One authorized exact-input Start-keyframe request reaches the existing video execution stack and yields a managed playable result; all invalid/stale/cross-project cases stop before provider work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist immutable video batches and playable versions with a checked-in migration</name>
  <reversibility rating="costly">D-06/D-12 make the exact-input receipt and append-only version identity a durable historical contract.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/20260812090000_add_remake_video_generation/migration.sql, src/lib/remake-projects/video/service.ts, tests/integration/api/remake-projects-video.test.ts, tests/unit/worker/remake-video.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-PATTERNS.md` schema and immutable persistence assignments
    - `prisma/schema.prisma` existing `RemakeOutputVersion`, `RemakeKeyframeTrack`, `RemakeKeyframeBatch`, `RemakeKeyframeCandidate`, and adoption event models
    - `prisma/migrations/20260810120000_add_remake_keyframe_generation/migration.sql` forward-migration convention
    - `src/lib/remake-projects/keyframes/service.ts` operation-key idempotency, JSON cloning, output, and provenance transaction
  </read_first>
  <behavior>
    - Test 1: one Shot revision owns a durable video track; each operation key creates at most one immutable batch and one or more immutable output versions.
    - Test 2: a completed version retains the exact snapshot/fingerprint, ordered references, model/mode/options, Task ID, managed media ID, status, timestamps, and provenance after project defaults or upstream records change.
    - Test 3: replaying the same Task/persistence operation returns the existing batch/version and cannot overwrite the stored receipt or duplicate billing/output relations.
    - Test 4: a failed/cancelled Task does not create a playable completed version, while prior versions remain untouched.
  </behavior>
  <action>Add the smallest Prisma video lifecycle models consistent with the keyframe append-only analog: a revision-bound track, immutable generation batch, immutable generated version linked to the existing `RemakeOutputVersion` media evidence spine, nullable current-adopted pointer, and adoption-event relation needed by wave 2. Store the full D-06/D-12 snapshot and normalized model facts on immutable rows, with operation-key and fingerprint uniqueness for idempotency. Check in a forward SQL migration that preserves existing data and relations. Complete `appendRemakeVideoGeneration` as one transaction that rechecks Shot/revision/Prompt/selected reference/action-sheet currency immediately before persistence, JSON-clones the snapshot, creates output and provenance links, and returns the existing result on retry. Do not update an older snapshot/version row or create a parallel generic media/provenance model.</action>
  <verify>
    <automated>docker compose -f docker-compose.test.yml up -d mysql &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx prisma migrate deploy --schema prisma/schema.prisma &amp;&amp; DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx prisma generate --schema prisma/schema.prisma &amp;&amp; BILLING_TEST_BOOTSTRAP=0 DATABASE_URL=mysql://root:root@127.0.0.1:3307/waoowaoo_test npx vitest run tests/integration/api/remake-projects-video.test.ts tests/unit/worker/remake-video.test.ts</automated>
  </verify>
  <done>The checked-in migration applies without destructive flags, Prisma Client generates, and successful/retried/cancelled executions preserve correct immutable output and provenance history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce capability-derived duration and full parameter snapshot rules</name>
  <files>src/lib/remake-projects/video/contracts.ts, src/lib/remake-projects/video/service.ts, tests/unit/remake-projects/remake-video-task-contract.test.ts, tests/integration/api/remake-projects-video.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-07 through D-12
    - `.planning/phases/09-video-generation/09-PATTERNS.md` capability validation and model-switch section
    - `src/lib/model-config-contract.ts` video capability option contract named by PATTERNS
    - `src/lib/model-capabilities/video-effective.ts` effective definitions and normalization named by PATTERNS
    - `src/lib/shot-group/video-config.ts` reference-mode support and option cleaning named by PATTERNS
  </read_first>
  <behavior>
    - Test 1: D-10 rounds Shot duration upward, applies the model minimum, caps at the lesser of 15 seconds and the model maximum, and persists that legal default.
    - Test 2: D-11 then rounds upward to the nearest legal discrete duration and never offers or accepts an unsupported value.
    - Test 3: D-08/D-09 accepts only the selected model's real reference modes, resolutions, audio support, and declared extra parameters; incompatible values normalize to declared defaults and produce a reset summary.
    - Test 4: D-07/D-12 per-submission overrides do not update project defaults and remain frozen on historical tasks/versions.
  </behavior>
  <action>Place the duration derivation and server-authoritative capability normalization in the Remake video contract/service boundary using `resolveProjectModelCapabilityGenerationOptions`, `resolveEffectiveVideoCapabilityDefinitions`, existing reference-mode helpers, and declared pricing/capability choices. Apply D-10 before D-11. Return a structured normalization/reset summary for the UI without inventing options. Persist only the final legal model/mode/parameter snapshot. Keep the project configuration read-only during single-Shot submission and ensure route tests prove later default changes do not alter old tasks or versions.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-task-contract.test.ts tests/integration/api/remake-projects-video.test.ts &amp;&amp; npm run check:config-center-guards</automated>
  </verify>
  <done>Continuous and discrete duration rules, reference modes, audio/resolution/options, model-switch resets, and immutable overrides are all capability-derived and server validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> authenticated Remake API | Untrusted selected IDs, model settings, and operation keys enter project-scoped server logic. |
| Task payload -> video worker/provider/storage | Persisted input crosses asynchronous execution and external provider/storage boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | video route | high | mitigate | Require project auth and verify every Shot, revision, Prompt, candidate, and media identity belongs to the authorized Remake project. |
| T-09-02 | Tampering | task payload/worker | high | mitigate | Strictly parse the immutable snapshot, recompute its fingerprint, and re-check current inputs before provider invocation and persistence. |
| T-09-03 | Information Disclosure | provider/storage handoff | medium | mitigate | Resolve only managed media references selected in the snapshot and avoid exposing provider credentials or raw storage keys. |
| T-09-04 | Denial of Service | submission/task creation | medium | mitigate | Reuse operation-key/fingerprint dedupe and the existing Task retry/cancellation lifecycle. |
| T-09-SC | Tampering | package installs | high | accept | No package install is planned; the phase uses existing dependencies only. |
</threat_model>

<verification>
- Focused contract, API, and worker tests pass.
- The migration deploys to the isolated test database and Prisma Client generates.
- Existing capability/config guards pass, and no new queue/provider/storage/billing system exists.
</verification>

<success_criteria>
One authorized single-Shot request produces one playable immutable version whose stored ordered references, prompt, model, mode, parameters, Task, output media, and provenance match the exact normalized submission.
</success_criteria>

<output>
Create `.planning/phases/09-video-generation/09-01-SUMMARY.md` when done.
</output>
