---
phase: 09-video-generation
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - tests/integration/remake-projects/remake-video-generation.test.ts
  - tests/e2e/fixtures/remake-video-project.ts
  - tests/e2e/remake-video-generation.spec.ts
  - scripts/verify-remake-video-generation.mjs
  - .planning/phases/09-video-generation/09-UAT.md
autonomous: false
requirements: [VGEN-01, VGEN-02, VGEN-03, VGEN-04, VGEN-05, VGEN-06, VGEN-07]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "VGEN-01..VGEN-07 and D-01..D-19 have focused automated coverage across contracts, API, worker, schema, lifecycle, invalidation, UI, and browser flow."
    - "The checked-in Prisma migration applies non-destructively to a clean verification database and Prisma Client generation succeeds without any data-loss acceptance flag."
    - "A real authorized generation reaches the existing provider/worker/storage/billing path, produces playable media, and completes the manual UAT receipt/adoption/invalidation loop."
    - "The verification harness observes only single-Shot Phase 09 work and cannot create Phase 10 bulk orchestration or Phase 11 export artifacts."
  artifacts:
    - path: "tests/integration/remake-projects/remake-video-generation.test.ts"
      provides: "Focused schema/API/worker/lifecycle/invalidation cross-layer regression proof"
    - path: "tests/e2e/remake-video-generation.spec.ts"
      provides: "Authenticated real-route desktop/mobile workflow and exact-input network assertions"
    - path: "scripts/verify-remake-video-generation.mjs"
      provides: "Isolated migration/seed/server/worker/Playwright verification harness"
    - path: ".planning/phases/09-video-generation/09-UAT.md"
      provides: "Real generation and human acceptance record"
  key_links:
    - from: "scripts/verify-remake-video-generation.mjs"
      to: "prisma/migrations/20260812090000_add_remake_video_generation/migration.sql"
      via: "isolated database migrate-deploy and Prisma Client generation before tests"
      pattern: "prisma migrate deploy"
    - from: "tests/e2e/remake-video-generation.spec.ts"
      to: "real Remake video routes and Task worker"
      via: "authenticated browser actions plus request/Task/database observation"
      pattern: "remake.*video"
    - from: ".planning/phases/09-video-generation/09-UAT.md"
      to: "one real generated version"
      via: "recorded Task/provider/media/receipt/adoption/invalidation/reconfirmation evidence"
      pattern: "VGEN-01"
---

<objective>
Verify Phase 09 as a complete single-Shot video generation, review, adoption, and reconfirmation workflow.

Purpose: Catch contract drift between schema, API, worker, provider input, persisted receipt, and UI before Phase 10 depends on readiness state.
Output: Focused regression coverage, migration/client verification, Playwright evidence, real generation evidence, and a completed Phase 09 UAT record.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-generation/09-CONTEXT.md
@.planning/phases/09-video-generation/09-PATTERNS.md
@.planning/phases/09-video-generation/09-01-SUMMARY.md
@.planning/phases/09-video-generation/09-02-SUMMARY.md
@.planning/phases/09-video-generation/09-03-SUMMARY.md

<interfaces>
Plans 09-01..03 own all production implementation. This plan may add focused integration/E2E fixtures, specs, a safe harness, and UAT evidence. Implementation defects found here are fixed in the owning production file with its focused test, while preserving unrelated worktree changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close unit, API, worker, lifecycle, invalidation, and schema verification gaps</name>
  <files>tests/integration/remake-projects/remake-video-generation.test.ts</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-01 through D-19
    - `.planning/phases/09-video-generation/09-PATTERNS.md` all assigned interfaces and shared patterns
    - focused test files created by Plans 09-01..03
    - `tests/integration/remake-projects/keyframe-service.test.ts` isolated Remake persistence test pattern
    - `tests/integration/billing/worker-lifecycle.integration.test.ts` existing Task/billing lifecycle assertions
  </read_first>
  <behavior>
    - Test 1: exact selected images, role/order, full Prompt, model/mode/options, Task, managed media, provenance, and fingerprint remain identical across API -> queue -> worker -> persisted version.
    - Test 2: operation retry is idempotent; cancellation/failure does not append a completed playable version or duplicate billing; old versions remain intact.
    - Test 3: note/adopt/replace/invalidate/reconfirm history is append-only and readiness follows D-17..D-19.
    - Test 4: auth, cross-project, stale identity, missing Prompt/keyframe, action-sheet-only, unsupported parameters, and unsupported mode all fail at the correct boundary.
  </behavior>
  <action>Audit the focused tests from Plans 09-01..03 against every VGEN requirement and D-01..D-19. Add only missing cross-layer cases to `remake-video-generation.test.ts`, using isolated database records and mocked existing provider/storage seams where real external generation is not the subject. Assert the complete receipt round trip, existing Task queue type, worker cancellation/progress, billing single-effect behavior, managed output/provenance, lifecycle history, and readiness. Keep the test focused on one Shot at a time and avoid batch/export fixtures.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-task-contract.test.ts tests/integration/api/remake-projects-video.test.ts tests/unit/worker/remake-video.test.ts tests/integration/api/remake-projects-video-tracks.test.ts tests/integration/remake-projects/remake-video-invalidation.test.ts tests/unit/remake-projects/remake-video-input-contract.test.ts tests/unit/remake-projects/remake-video-stage.test.tsx tests/integration/remake-projects/remake-video-generation.test.ts &amp;&amp; npm run check:test-coverage-guards &amp;&amp; npm run check:config-center-guards</automated>
  </verify>
  <done>Focused automated evidence maps every VGEN requirement and D-01..D-19 across all implementation boundaries with no skipped critical case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify migration, desktop/mobile browser flow, and one real generation</name>
  <precondition>The executor has an authenticated test user/project and existing configured video provider credentials with quota; if unavailable, complete all migration and mocked browser checks, record the unmet real-generation gate in 09-UAT.md, and halt before the human checkpoint.</precondition>
  <files>tests/e2e/fixtures/remake-video-project.ts, tests/e2e/remake-video-generation.spec.ts, scripts/verify-remake-video-generation.mjs, .planning/phases/09-video-generation/09-UAT.md</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` complete decisions and deferred scope
    - `.planning/phases/09-video-generation/09-PATTERNS.md` component, API, worker, storage, and billing assignments
    - `tests/e2e/remake-keyframes.spec.ts` authenticated Remake desktop/mobile pattern
    - `playwright.config.ts`, `tests/setup/env.ts`, and `docker-compose.test.yml`
    - `tests/system/generate-video.system.test.ts` existing real video generation/provider convention
  </read_first>
  <behavior>
    - Test 1: the harness validates its exact disposable database/ports, runs checked-in `prisma migrate deploy`, runs `prisma generate`, and rejects destructive migration SQL or any data-loss acceptance option.
    - Test 2: desktop 1440x900 and mobile 390x844 cover manual selection, fixed numbered preview, legal model controls, submit, task progress, playable history, receipt, note, adopt replacement confirmation, invalidation, and reconfirmation without overlap or page overflow.
    - Test 3: browser request inspection proves the exact preview array equals the POST selected-reference array, and Task/database inspection proves the worker/persisted receipt preserve it.
    - Test 4: one credentialed real provider run produces managed playable media through the existing video worker/storage/billing path; no raw secret or storage key enters logs/UAT evidence.
  </behavior>
  <action>Create a deterministic authenticated Remake fixture with current approved Video Prompt, adopted Start/Middle/End keyframes, optional action sheet, multiple historical versions, one adopted version, notes, and an invalidation scenario. Build a safe harness that starts only tracked isolated MySQL/Redis/app/video-worker processes on recorded ports, deploys the checked-in migration, generates Prisma Client, seeds, runs focused Playwright, performs one opt-in credentialed real generation using the existing configured model path, records sanitized Task/provider/media/receipt evidence in `09-UAT.md`, and terminates only its own child processes. Inspect the migration SQL for destructive operations and ensure the harness never invokes a data-loss acceptance option. Cover exact-input identity, capability resets, refresh persistence, separate original/generated playback, history, adoption, invalidation, and reconfirmation on desktop/mobile. Do not exercise cross-Shot bulk actions or export.</action>
  <verify>
    <automated>node scripts/verify-remake-video-generation.mjs</automated>
  </verify>
  <done>The migration/client gates, focused desktop/mobile browser suite, and one real existing-provider generation pass with sanitized evidence and no Phase 10/11 artifacts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Accept the real single-Shot video generation and review workflow</name>
  <files>.planning/phases/09-video-generation/09-UAT.md, tests/e2e/remake-video-generation.spec.ts, scripts/verify-remake-video-generation.mjs</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-01 through D-19 and Deferred Ideas
    - `.planning/phases/09-video-generation/09-UAT.md` automated and real-generation evidence produced by Task 2
  </read_first>
  <action>After every automated gate and the real generation pass, keep the executor-started seeded app available at the harness-reported URL. Ask the user to verify the generated media and human-review semantics below; require no manual setup or data creation beyond viewing and clicking the prepared authenticated workspace.</action>
  <what-built>Single-Shot Remake video generation using exact manually selected references and existing capability/provider/worker/storage/billing infrastructure, with immutable receipts, playback/history, notes, adoption, invalidation, and reconfirmation.</what-built>
  <how-to-verify>
    1. Select adopted generated keyframes and optionally the action sheet. Confirm the numbered preview is fixed Start/Middle/End/action-sheet order and exactly matches the prepared generation receipt.
    2. Change the model and parameters. Confirm only supported controls appear, incompatible values visibly reset, and the Shot duration default obeys the model's legal range/options.
    3. Run or inspect the completed real generation. Confirm task progress resolves to a playable managed video and the receipt shows the exact images, full Video Prompt, model, mode, parameters, Task, and timestamp.
    4. Play original and generated videos separately, edit the simple note, browse history, and replace adoption. Confirm browsing never adopts and replacement requires confirmation.
    5. Trigger the prepared upstream-change scenario. Confirm the old adopted video remains playable and selected but is marked for review/not complete; reconfirm it or adopt a new version and confirm readiness returns.
    6. Repeat the critical inspection at mobile width and confirm controls, long receipts, players, notices, and history remain readable and non-overlapping.
  </how-to-verify>
  <verify>
    <automated>node scripts/verify-remake-video-generation.mjs &amp;&amp; npm run typecheck &amp;&amp; npm run test:guards</automated>
    <human-check>Approve only if the exact-input promise, capability controls, real playable output, immutable receipt, explicit adoption, and review-required/reconfirmation semantics are truthful and usable.</human-check>
  </verify>
  <resume-signal>Type `approved` or describe the exact Shot, version, state, viewport, and issue.</resume-signal>
  <done>Human acceptance confirms the real Phase 09 single-Shot workflow and its safety/history semantics.</done>
</task>

</tasks>

## Source Coverage Audit

| SOURCE | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Generate, review, and adopt new Shot video versions from adopted keyframes and approved Video Prompt through the existing gateway | 09-01..04 | COVERED | Tracer, lifecycle, UI, and real verification |
| REQ | VGEN-01 | Shot duration + adopted keyframes + adopted Video Prompt through existing gateway | 09-01, 09-03, 09-04 | COVERED | Exact submit, worker, UI, real run |
| REQ | VGEN-02 | Legal reference modes and visible actual input | 09-01, 09-03, 09-04 | COVERED | Capability validation and shared preview/payload array |
| REQ | VGEN-03 | Model, duration, resolution, audio, supported parameters | 09-01, 09-03, 09-04 | COVERED | Existing capability system only |
| REQ | VGEN-04 | Immutable version with keyframe/Prompt/params/Task/output links | 09-01, 09-02, 09-04 | COVERED | Migration, receipt, provenance, tests |
| REQ | VGEN-05 | Original/generated playback, notes, current adoption | 09-02, 09-03, 09-04 | COVERED | API, UI, Playwright/UAT |
| REQ | VGEN-06 | Regenerate after edits while preserving old versions/adoptions | 09-01..04 | COVERED | Append-only batches, invalidation, history |
| REQ | VGEN-07 | Block readiness for missing keyframe/Prompt/legal parameters with reasons | 09-01..04 | COVERED | Server gate, snapshot reasons, UI, E2E |
| RESEARCH | - | Research disabled; 09-PATTERNS.md is the authoritative implementation analog/source-interface map | 09-01..04 | COVERED | Every task cites assigned analogs; no RESEARCH.md dependency |
| CONTEXT | D-01 | All supported models use explicit manual multi-image selection | 09-01, 09-03, 09-04 | COVERED | No auto-selection or hidden references |
| CONTEXT | D-02 | Candidates are adopted new keyframes plus current revision action sheet only | 09-01, 09-03, 09-04 | COVERED | Original single frames excluded |
| CONTEXT | D-03 | At least one adopted keyframe; action-sheet-only blocked | 09-01, 09-03, 09-04 | COVERED | Contract, UI, E2E |
| CONTEXT | D-04 | Fixed Start/Middle/End/action-sheet order | 09-01, 09-03, 09-04 | COVERED | Ordered contract and preview |
| CONTEXT | D-05 | Preview every exact submitted image in order | 09-01, 09-03, 09-04 | COVERED | Same array drives UI and POST |
| CONTEXT | D-06 | Immutable complete generation receipt | 09-01..04 | COVERED | Schema, receipt UI, round-trip tests |
| CONTEXT | D-07 | Reuse existing video parameter workflow; per-run override only | 09-01, 09-03, 09-04 | COVERED | Project defaults remain unchanged |
| CONTEXT | D-08 | Model/duration/resolution/audio/declared options and real reference modes | 09-01, 09-03, 09-04 | COVERED | Capability-derived controls |
| CONTEXT | D-09 | Model switch retains legal values, resets incompatible values with notice | 09-01, 09-03, 09-04 | COVERED | Structured reset summary |
| CONTEXT | D-10 | Duration ceil/min/cap at min(15, model max) | 09-01, 09-03, 09-04 | COVERED | Server and UI tests |
| CONTEXT | D-11 | Discrete duration rounds upward to legal tier | 09-01, 09-03, 09-04 | COVERED | Capability contract tests |
| CONTEXT | D-12 | Freeze full model and parameter snapshot | 09-01..04 | COVERED | Task/version receipt |
| CONTEXT | D-13 | Original always playable; adopted default; chronological history | 09-02..04 | COVERED | Snapshot, UI, E2E |
| CONTEXT | D-14 | One simple review note; no structured scoring/ranking | 09-02..04 | COVERED | Text-only note |
| CONTEXT | D-15 | Explicit adoption and confirmed replacement; reads do not adopt | 09-02..04 | COVERED | Transaction, UI, E2E |
| CONTEXT | D-16 | No synchronized/frame-linked dual-player comparison | 09-03, 09-04 | COVERED | Separate native players only |
| CONTEXT | D-17 | Upstream changes retain old playable versions and mark review required | 09-02..04 | COVERED | Precise invalidation |
| CONTEXT | D-18 | Invalid adopted pointer remains but Shot is not complete/ready | 09-02..04 | COVERED | Snapshot readiness gate |
| CONTEXT | D-19 | Explicit reconfirm old or generate/adopt new; reuse existing invalidation | 09-02..04 | COVERED | Reconfirmation event and no auto-regeneration |

Deferred Phase 10 cross-Shot bulk execution/retry/recovery, Phase 11 export/manifest, synchronized or frame-linked comparison, structured scoring/ranking, automatic adoption, and automatic regeneration are explicit exclusions rather than audit gaps. `--skip-ui` means no UI-SPEC is created or required; Plan 09-03 still implements the product UI from locked decisions and authoritative patterns.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> application boundaries | Tests must exercise authorization, stale input, immutable snapshot, provider payload, and readiness behavior rather than bypassing them. |
| Real provider/storage -> UAT workspace | External generation consumes credentials/quota and returns media that must remain project-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Elevation of Privilege | API/browser verification | high | mitigate | Include unauthorized/cross-project API cases and run UAT through an authenticated project session. |
| T-09-12 | Tampering | migration verification | high | mitigate | Apply the checked-in migration on a disposable database, regenerate Prisma Client, and inspect for destructive statements/data-loss acceptance options. |
| T-09-13 | Information Disclosure | test/UAT artifacts | medium | mitigate | Keep secrets and raw/signed storage details out of snapshots, logs, screenshots, and checked-in UAT evidence. |
| T-09-SC | Tampering | package installs | high | accept | No package install is planned; the phase uses existing dependencies only. |
</threat_model>

<verification>
- All focused commands from Plans 09-01..03 and Task 1 pass.
- `node scripts/verify-remake-video-generation.mjs` applies the checked-in migration, generates Prisma Client, runs desktop/mobile Playwright, and completes one credentialed real generation.
- `npm run typecheck` and `npm run test:guards` pass.
- Human acceptance confirms exact-input truthfulness, playable media, immutable receipt, explicit adoption, and invalidation/reconfirmation behavior.
</verification>

<success_criteria>
- Every VGEN requirement and D-01..D-19 has automated evidence and final human acceptance where judgment is required.
- The checked-in migration is non-destructive and all execution stays on the existing Task/video worker/model gateway/storage/billing system.
- Fixed image order, exact submitted-image preview, immutable history, notes, adoption, and review gates survive refresh and real provider execution.
- No Phase 10 bulk orchestration, Phase 11 export, or deferred automatic/comparison feature is introduced.
</success_criteria>

<output>
Create `.planning/phases/09-video-generation/09-04-SUMMARY.md` when done.
</output>
