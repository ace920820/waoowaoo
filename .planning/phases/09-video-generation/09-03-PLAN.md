---
phase: 09-video-generation
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/lib/remake-projects/keyframes/video-inputs.ts
  - src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx
  - tests/unit/remake-projects/remake-video-input-contract.test.ts
  - tests/unit/remake-projects/remake-video-stage.test.tsx
autonomous: true
requirements: [VGEN-01, VGEN-02, VGEN-03, VGEN-05, VGEN-06, VGEN-07]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "VGEN-01/VGEN-02 and D-01..D-05: the user manually selects eligible references and sees the numbered exact fixed-order provider input before submit."
    - "VGEN-03 and D-07..D-11: the page reuses capability-driven video controls, derives the bounded/discrete Shot duration default, and visibly resets incompatible values after model changes."
    - "VGEN-05/VGEN-06 and D-13..D-19: original/current/history playback, notes, receipts, explicit adopt confirmation, invalidation status, and reconfirmation are available without synchronized comparison or automatic actions."
    - "VGEN-07: missing inputs and illegal settings disable only the affected Shot action and explain every readiness reason."
  artifacts:
    - path: "src/lib/remake-projects/keyframes/video-inputs.ts"
      provides: "Eligible manual reference candidates and one fixed-order exact-input builder shared by preview and request"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      provides: "Single-Shot generation, exact preview, capability controls, playback, history, receipt, note, adoption, invalidation, and reconfirmation UI"
    - path: "tests/unit/remake-projects/remake-video-stage.test.tsx"
      provides: "Interaction proof for selection, controls, history, and explicit lifecycle commands"
  key_links:
    - from: "src/lib/remake-projects/keyframes/video-inputs.ts"
      to: "src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx"
      via: "the same ordered selected-reference array renders the preview and serializes POST input"
      pattern: "orderedReferences"
    - from: "RemakeVideoStage.tsx"
      to: "src/app/api/remake-projects/[projectId]/video/route.ts"
      via: "single-Shot generate command with operation key and normalized explicit inputs"
      pattern: "/video"
    - from: "RemakeVideoStage.tsx"
      to: "src/app/api/remake-projects/[projectId]/video/tracks/[trackId]/route.ts"
      via: "note, adopt, and reconfirm commands followed by authoritative snapshot refresh"
      pattern: "adopt|reconfirm|note"
---

<objective>
Turn the existing Remake video stage into the complete single-Shot generation and review UI.

Purpose: Expose the exact backend contract from waves 1-2 with truthful manual input selection and reuse of the existing video capability controls.
Output: Input adapter extensions, production Remake video stage interactions, and focused component/adapter tests.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-video-generation/09-CONTEXT.md
@.planning/phases/09-video-generation/09-PATTERNS.md
@.planning/phases/09-video-generation/09-02-SUMMARY.md

<interfaces>
Plans 09-01..02 expose the existing Remake snapshot, collection POST, track lifecycle actions, exact frozen receipt, task state, readiness reasons, and playable media. Reuse the current `RemakeVideoStage` Shot card/input grouping and the capability-control behavior identified in `ShotGroupVideoSection`; do not create a new route, page, config source, or video parameter system. The user explicitly skipped UI-SPEC for this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manual eligible-reference selection with one exact fixed-order preview</name>
  <files>src/lib/remake-projects/keyframes/video-inputs.ts, src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, tests/unit/remake-projects/remake-video-input-contract.test.ts, tests/unit/remake-projects/remake-video-stage.test.tsx</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-01 through D-06
    - `.planning/phases/09-video-generation/09-PATTERNS.md` truthful input projection and component assignments
    - `src/lib/remake-projects/keyframes/video-inputs.ts` current `mapRemakeVideoInputs`
    - `src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx` current real-Shot layout
  </read_first>
  <behavior>
    - Test 1: candidates contain only current adopted Start/Middle/End generated keyframes plus the current revision action sheet; original single frames are absent.
    - Test 2: nothing is silently selected, and submit remains blocked until at least one adopted generated keyframe is selected.
    - Test 3: user choices always derive `Start -> Middle -> End -> action sheet`, skip absent choices, and cannot be dragged or reordered.
    - Test 4: numbered preview cards and the POST body consume the same ordered array and show every exact image the model will receive.
  </behavior>
  <action>Extend `mapRemakeVideoInputs` to expose eligible reference candidates with stable candidate/media identity, role, availability, and fixed ordinal metadata. In `RemakeVideoStage`, render explicit checkboxes for each eligible current adopted keyframe and optional action sheet, defaulting to no hidden selection. Build one memoized ordered array from the selected IDs in D-04 order; render its numbered managed-media previews and serialize that exact array into the generation request. Disable submission with specific reasons for no adopted keyframe selection, missing adopted Video Prompt, stale/review gate, or other backend readiness failures. Keep order fixed and do not add drag handles, original Start/Middle/End candidates, auto-selection, or model-side hidden references.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-input-contract.test.ts tests/unit/remake-projects/remake-video-stage.test.tsx</automated>
  </verify>
  <done>Manual selection is truthful and stable: preview order and submitted order are identical, action-sheet-only is blocked, and no source can be silently added, removed, replaced, or reordered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse capability-driven video controls and legal model-switch behavior</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, tests/unit/remake-projects/remake-video-stage.test.tsx</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-07 through D-12
    - `.planning/phases/09-video-generation/09-PATTERNS.md` `ShotGroupVideoSection` and model-change normalization assignments
    - `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` exact capability control symbols named by PATTERNS
    - `src/lib/model-capabilities/video-effective.ts` effective definition/normalization exports named by PATTERNS
    - `src/lib/shot-group/video-config.ts` reference-mode support exports named by PATTERNS
  </read_first>
  <behavior>
    - Test 1: the current project video model/defaults populate model, legal duration, resolution, audio, real reference modes, and declared extra controls without writing back on edit.
    - Test 2: D-10/D-11 default duration displays the server-compatible rounded/clamped/discrete value and only legal duration choices are selectable.
    - Test 3: model changes retain supported legal selections, reset incompatible selections to declared defaults, and show a concise reset notice.
    - Test 4: unsupported Omni/Smart/audio/resolution/capability choices are absent and cannot enter the request.
  </behavior>
  <action>Reuse the existing model option source, `resolveEffectiveVideoCapabilityDefinitions`, `normalizeVideoGenerationSelections`, reference-mode helpers, and established controls/interactions from `ShotGroupVideoSection`. Initialize per-Shot local form state from the snapshot's project defaults plus legal duration derivation. On model change, normalize against the next model definitions, retain only still-legal values, reset the rest to declared defaults, and display the structured reset summary from wave 1. Render Omni/Smart and other controls only when the current model declares support. Submit local normalized values only; never update project defaults or fork capability/provider rules in the Remake component.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-stage.test.tsx &amp;&amp; npm run check:config-center-guards</automated>
  </verify>
  <done>The Remake stage feels and behaves like the existing video parameter workflow while exposing only the current model's legal controls and preserving immutable per-run overrides.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Playback, immutable receipts, notes, adoption, invalidation, and reconfirmation</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx, tests/unit/remake-projects/remake-video-stage.test.tsx</files>
  <read_first>
    - `.planning/phases/09-video-generation/09-CONTEXT.md` D-13 through D-19
    - `.planning/phases/09-video-generation/09-PATTERNS.md` component playback/history/adoption assignment
    - `src/app/[locale]/workspace/[projectId]/modes/remake/video/RemakeVideoStage.tsx` existing original-video and Shot status rendering
  </read_first>
  <behavior>
    - Test 1: original Shot is always independently playable; current adopted generated version is the default candidate; history is newest-first and selecting a version changes playback only.
    - Test 2: each generated version exposes its simple note and full frozen receipt including numbered references, full Prompt, model, mode, parameters, Task, and timestamps.
    - Test 3: explicit adoption prompts for confirmation when replacing another version and refreshes authoritative state; browsing/playback never adopts.
    - Test 4: invalidated versions remain playable and marked review-required; an invalidated adopted version stays selected but incomplete until explicit reconfirmation or new adoption.
  </behavior>
  <action>Replace the Phase 8 disabled submission panel with production single-Shot controls while retaining the current Shot card layout. Provide separate native players for original and selected generated video, with no synchronized or frame-linked comparison. Default generated playback to the current adopted version, otherwise the newest completed version, and list all versions newest-first. Add a compact receipt drawer/modal or inline disclosure containing every frozen D-06/D-12 field and exact numbered images. Wire plain note save, explicit adoption with replacement confirmation, and explicit reconfirmation to wave 2 actions followed by snapshot refresh. Display task progress/errors and the review-required gate without hiding old media or moving the adopted pointer. Do not add automatic ranking/adoption/regeneration, structured review fields, Phase 10 multi-Shot controls, or Phase 11 export.</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/unit/remake-projects/remake-video-stage.test.tsx</automated>
  </verify>
  <done>One Shot supports separate original/generated playback, complete immutable history inspection, lightweight notes, explicit adoption/replacement, and visible invalidation/reconfirmation with no automatic lifecycle mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot/API data -> browser UI | Stored media, prompt, capability, task, and lifecycle state drives rendering and actions. |
| Browser form -> submit/lifecycle APIs | User-selected references and settings may be stale or manipulated before server validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Tampering | input selection UI | medium | mitigate | Derive preview and request body from the same fixed-order selected-reference function; server remains authoritative. |
| T-09-09 | Information Disclosure | generation receipt | medium | mitigate | Render only authorized project snapshot fields and managed media URLs, never provider secrets or storage keys. |
| T-09-10 | Repudiation | adoption UI | medium | mitigate | Require an explicit command and replacement confirmation; playback and history selection call no mutation path. |
| T-09-SC | Tampering | package installs | high | accept | No package install is planned; the phase uses existing dependencies only. |
</threat_model>

<verification>
- Input adapter and stage component tests prove exact selection/preview/request identity.
- Capability tests and existing config guards prove no duplicated parameter/provider rules.
- Lifecycle interaction tests prove playback/history are read-only and adoption/reconfirmation are explicit.
</verification>

<success_criteria>
The product UI lets one Shot generate and review video versions with exact visible inputs, legal parameters, explicit adoption/reconfirmation, and no Phase 10/11 behavior.
</success_criteria>

<output>
Create `.planning/phases/09-video-generation/09-03-SUMMARY.md` when done.
</output>
