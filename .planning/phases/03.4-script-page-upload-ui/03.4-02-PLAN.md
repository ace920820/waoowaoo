---
phase: 03.4-script-page-upload-ui
plan: 02
type: implementation
wave: 2
depends_on:
  - 03.4-01-PLAN.md
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/StoryboardPackageImportDialog.tsx
  - messages/zh/scriptView.json
  - messages/en/scriptView.json
  - tests/unit/novel-promotion/script-view-mode-entry.test.ts
autonomous: true
requirements:
  - SBPI-22
  - SBPI-23
  - SBPI-24
  - SBPI-25
  - SBPI-26
---

<objective>
Add the visible script-page upload entry and preview UI. Users in multi-shot mode should see “上传分镜表” beside “确认并开始绘制”, choose `.md` / `.json` files, preview the Phase 03.3 import plan, and inspect segments, prompt summaries, asset matches, warnings, and overwrite behavior before committing.
</objective>

<context>
Read first:
- `.planning/phases/03.4-script-page-upload-ui/03.4-RESEARCH.md`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx`
- `messages/zh/scriptView.json`
- `messages/en/scriptView.json`
- `tests/unit/novel-promotion/script-view-mode-entry.test.ts`

Use progressive code disclosure. If preview UI would make `ScriptViewAssetsPanel.tsx` too large, extract `StoryboardPackageImportDialog.tsx` and keep the panel as the button/file-input owner.
</context>

<tasks>
  <task id="03.4-02-T1" type="upload-entry">
    <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx, messages/zh/scriptView.json, messages/en/scriptView.json</files>
    <action>
      Add a secondary “上传分镜表” button beside the primary “确认并开始绘制” button when `episodeProductionMode === 'multi_shot'`. Add a hidden file input that accepts `.md` and `.json` files. Read selected file text with `File.text()` and pass content/filename/contentType to the preview callback. Show pending/error state and reset the input value after each selection so the same file can be reselected.
    </action>
    <verify>
      Extend `script-view-mode-entry.test.ts` to assert the upload button appears in multi-shot mode and not in traditional mode.
    </verify>
    <acceptance_criteria>
      - Upload button is visually secondary and adjacent to the primary CTA.
      - Traditional mode does not show upload import entry.
      - File selection supports `.md` and `.json`.
      - Canceling file picker is a no-op.
    </acceptance_criteria>
  </task>

  <task id="03.4-02-T2" type="preview-dialog">
    <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/StoryboardPackageImportDialog.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx</files>
    <action>
      Add a focused preview dialog/component that renders the preview response from Phase 03.3. Show package title, scene/segment counts, per-segment title, scene label, duration, template, shot count, reference/video prompt snippets if available from response or UI-derived summaries, asset matching grouped by location/character/prop, warnings, create/update counts, and preserve-generated-media/overwrite strategy copy.
    </action>
    <verify>
      Add focused component/unit tests for preview rendering, matched asset badges, script-derived fallback warnings, validation errors, and overwrite counts.
    </verify>
    <acceptance_criteria>
      - Preview clearly distinguishes create vs update segments.
      - Asset rows distinguish matched vs script-derived fallback / needs manual binding.
      - Validation or parse errors are visible and block commit.
      - Preview state can be closed without mutation.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/unit/novel-promotion/script-view-mode-entry.test.ts tests/unit/novel-promotion/storyboard-package-upload-ui.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Script page multi-shot mode exposes upload storyboard package entry.
- Preview UI communicates import contents, asset matching, warnings, and overwrite behavior before commit.
- Traditional mode remains unchanged.
</success_criteria>
