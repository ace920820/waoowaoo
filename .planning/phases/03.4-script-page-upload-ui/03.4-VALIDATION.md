# Phase 03.4 Plan Validation

## Verdict

PASS — planned as three executable UI-focused plans with backend/API boundaries preserved.

## Checks

- Scope isolation: PASS. Plans do not modify package schema/parser/mapper or backend persistence unless response typing requires a minimal client contract.
- Dependency correctness: PASS. Plans consume Phase 03.3 preview/commit route and invalidate existing episode data queries.
- UX coverage: PASS. Plans cover upload button, browser file read, preview display, asset warnings, overwrite strategy, confirm/cancel, and navigation.
- Data safety: PASS. Preview is non-mutating; commit requires explicit user confirmation; cancel is no-op.
- Test coverage: PASS. Plans include focused tests for button visibility, preview rendering, asset warning, commit success navigation, and cancel/no-op.

## Plan Set

- `03.4-01-PLAN.md` — Client import mutation contract and runtime wiring.
- `03.4-02-PLAN.md` — Script page upload button, file read, and preview UI.
- `03.4-03-PLAN.md` — Commit/cancel flow, navigation, i18n, and focused UI tests.

## Risks To Watch During Execution

- `ScriptViewAssetsPanel.tsx` is already large; prefer extracting a focused import dialog component if preview UI grows.
- Server-rendered tests cannot exercise browser file picker directly; isolate file-read logic or test callbacks/preview rendering with injected props.
- Phase 03.4 should avoid creating a second source of truth for imported fields; after commit, rely on episode query refresh and existing shot-group/draft metadata UI.
