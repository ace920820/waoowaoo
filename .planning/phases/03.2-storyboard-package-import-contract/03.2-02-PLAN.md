---
phase: 03.2-storyboard-package-import-contract
plan: 02
type: implementation
wave: 1
depends_on:
  - 03.2-01-PLAN.md
files_modified:
  - src/lib/novel-promotion/storyboard-package/parser.ts
  - src/lib/novel-promotion/storyboard-package/index.ts
  - tests/unit/novel-promotion/storyboard-package/storyboard-package-parser.test.ts
autonomous: true
requirements:
  - SBPI-02
  - SBPI-11
---

<objective>
Implement package text parsing for raw JSON and Markdown fenced `waoo-storyboard-package+json` blocks without attempting fragile free-form Markdown table inference.
</objective>

<context>
Read first:
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-CONTEXT.md`
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-RESEARCH.md`
- `src/lib/novel-promotion/storyboard-package/schema.ts`

Use progressive code disclosure: this plan should only need the schema module and parser tests.
</context>

<tasks>
  <task id="03.2-02-T1" type="parser">
    <files>src/lib/novel-promotion/storyboard-package/parser.ts, src/lib/novel-promotion/storyboard-package/index.ts</files>
    <action>
      Add a pure parser that accepts text plus an optional filename/content type hint. If input appears to be JSON, parse it directly. If input is Markdown, extract the first fenced code block whose info string is `waoo-storyboard-package+json`, trim the block content, parse it as JSON, and validate it using the schema from plan 01. Return a discriminated result object for success/failure rather than throwing untyped errors.
    </action>
    <verify>
      Add unit tests for raw JSON success and fenced Markdown success.
    </verify>
    <acceptance_criteria>
      - Raw JSON and fenced Markdown produce the same normalized package shape.
      - Parser exports are available through `src/lib/novel-promotion/storyboard-package/index.ts`.
      - Parser does not read files directly; callers pass text content.
    </acceptance_criteria>
  </task>

  <task id="03.2-02-T2" type="parser-errors">
    <files>src/lib/novel-promotion/storyboard-package/parser.ts, tests/unit/novel-promotion/storyboard-package/storyboard-package-parser.test.ts</files>
    <action>
      Add typed parser failures for missing fenced block, malformed JSON, and schema validation failure. Error messages should be actionable for upload preview UI, especially the Markdown case: instruct users to include a `waoo-storyboard-package+json` fenced block.
    </action>
    <verify>
      Unit tests assert missing block, malformed JSON, and invalid schema/version errors.
    </verify>
    <acceptance_criteria>
      - Markdown without the import block does not attempt table inference.
      - Malformed JSON identifies parse failure without leaking stack traces.
      - Schema validation errors preserve field paths from plan 01.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/unit/novel-promotion/storyboard-package/storyboard-package-parser.test.ts tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts`
</verification>

<success_criteria>
- Parser supports `.json` content and Markdown fenced JSON blocks.
- Parser rejects unsupported Markdown with clear guidance.
- Parser remains pure and reusable by Phase 03.3 API and Phase 03.4 browser upload UI.
</success_criteria>
