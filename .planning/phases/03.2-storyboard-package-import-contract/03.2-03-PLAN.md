---
phase: 03.2-storyboard-package-import-contract
plan: 03
type: implementation
wave: 2
depends_on:
  - 03.2-01-PLAN.md
  - 03.2-02-PLAN.md
files_modified:
  - src/lib/novel-promotion/storyboard-package/mapper.ts
  - src/lib/novel-promotion/storyboard-package/index.ts
  - tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts
autonomous: true
requirements:
  - SBPI-06
  - SBPI-07
  - SBPI-08
  - SBPI-12
---

<objective>
Map a validated director-authored storyboard package into internal multi-shot draft / shot-group payloads that Phase 03.3 can persist without redesigning the contract.
</objective>

<context>
Read first:
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-CONTEXT.md`
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-RESEARCH.md`
- `src/lib/novel-promotion/storyboard-package/schema.ts`
- `src/lib/shot-group/draft-metadata.ts`
- `src/lib/novel-promotion/multi-shot/episode-draft-builder.ts`
- `src/lib/shot-group/video-config.ts`

Use progressive code disclosure: do not inspect Prisma routes or UI files. This mapper should remain persistence-free.
</context>

<tasks>
  <task id="03.2-03-T1" type="normalized-output">
    <files>src/lib/novel-promotion/storyboard-package/mapper.ts, src/lib/novel-promotion/storyboard-package/index.ts</files>
    <action>
      Define and implement a pure mapper from validated package to a normalized import plan. Include package metadata, flattened scenes/segments, stable import identity (`packageId`, `sceneId`, `segmentId`), shot-group fields (`title`, `templateKey`, `groupPrompt`, `videoPrompt`, audio/dialogue/reference mode/generation options), draft metadata patch, asset reference requests, and shot item payloads. Preserve director-only fields in `cinematicPlan` where they are useful downstream.
    </action>
    <verify>
      Unit tests assert the mapper output for a representative one-scene, one-segment package.
    </verify>
    <acceptance_criteria>
      - `reviewConfig.referencePromptText` maps to draft metadata `referencePromptText`.
      - `reviewConfig.compositePromptText` maps to draft metadata `compositePromptText` and shot-group `groupPrompt`.
      - `videoConfig.videoPrompt` maps to shot-group `videoPrompt`.
      - Storyboard mode fields map to draft metadata fields.
      - `videoConfig.dialogueText` maps to dialogue override metadata when non-empty.
    </acceptance_criteria>
  </task>

  <task id="03.2-03-T2" type="shot-and-asset-mapping">
    <files>src/lib/novel-promotion/storyboard-package/mapper.ts, tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts</files>
    <action>
      Map asset references and shot table data. Asset refs should remain match requests or script-derived binding candidates, not database writes. Shot data should create ordered item payloads with `itemIndex`, `title`, `prompt`, duration, shot size, angle, movement, composition, lighting, blocking, and emotional beat where present.
    </action>
    <verify>
      Unit tests assert asset references, script-derived fallback labels, item title/prompt mapping, and cinematic plan preservation.
    </verify>
    <acceptance_criteria>
      - Missing package assets produce warnings or unresolved match requests, not thrown errors after schema validation succeeds.
      - Shot item count matches the validated template slot count compatibility rules.
      - Mapper output includes enough identity data for Phase 03.3 overwrite detection by `packageId + segmentId`.
      - Mapper output is deterministic and side-effect free.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/unit/novel-promotion/storyboard-package/storyboard-package-parser.test.ts tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts`
</verification>

<success_criteria>
- Mapper translates director package semantics into Waoo internal import payloads without persistence.
- Review, video, asset, and cinematic shot data are preserved for Phase 03.3.
- All Phase 03.2 parser, validator, and mapper unit tests pass together.
</success_criteria>
