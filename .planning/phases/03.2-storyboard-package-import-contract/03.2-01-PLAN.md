---
phase: 03.2-storyboard-package-import-contract
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/lib/novel-promotion/storyboard-package/schema.ts
  - src/lib/novel-promotion/storyboard-package/index.ts
  - tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts
autonomous: true
requirements:
  - SBPI-01
  - SBPI-03
  - SBPI-04
  - SBPI-05
  - SBPI-06
  - SBPI-07
  - SBPI-08
  - SBPI-09
  - SBPI-10
---

<objective>
Define the `waoo.storyboard_package` v1.0 schema and exported TypeScript contract so director-authored packages can be validated before parsing, mapping, API persistence, or UI upload work begins.
</objective>

<context>
Read first:
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-CONTEXT.md`
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-RESEARCH.md`
- `.planning/REQUIREMENTS.md`
- `src/lib/shot-group/template-registry.ts`
- `src/lib/shot-group/video-config.ts`

Use progressive code disclosure: do not inspect API or UI files unless a type import or test failure requires it.
</context>

<tasks>
  <task id="03.2-01-T1" type="schema-module">
    <files>src/lib/novel-promotion/storyboard-package/schema.ts, src/lib/novel-promotion/storyboard-package/index.ts</files>
    <action>
      Create the storyboard-package module and define a Zod schema for `waoo.storyboard_package` v1.0. Include exported TypeScript types inferred from the schema. The schema must cover top-level package metadata, global settings, assets, scenes, segments, `reviewConfig`, `videoConfig`, `cinematicPlan`, and shot-level data. Reuse existing domain unions where practical: `grid-4 | grid-6 | grid-9`, `zh | en | ja`, and `omni-reference | smart-multi-frame`.
    </action>
    <verify>
      Add validator unit tests for a minimal valid package and invalid schema/version cases.
    </verify>
    <acceptance_criteria>
      - Schema requires `schema: "waoo.storyboard_package"` and `schemaVersion: "1.0"`.
      - Top-level required fields match SBPI-03.
      - Scene and segment required fields match SBPI-04 and SBPI-05.
      - Review config fields cover SBPI-06.
      - Video config fields cover SBPI-07.
      - Cinematic shot fields cover SBPI-08 while allowing optional director-specific descriptive fields.
    </acceptance_criteria>
  </task>

  <task id="03.2-01-T2" type="schema-validation">
    <files>src/lib/novel-promotion/storyboard-package/schema.ts, tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts</files>
    <action>
      Add semantic validation on top of the structural schema. Validate supported `templateKey`, segment duration limit, reference mode, required prompt text, and shot count compatibility against the selected template slot count. Return actionable validation errors with paths suitable for preview/API display in Phase 03.3.
    </action>
    <verify>
      Unit tests must assert invalid `templateKey`, invalid reference mode, `targetDurationSec > 15`, missing required fields, and shot count greater than template slot count.
    </verify>
    <acceptance_criteria>
      - Invalid package errors include stable codes or messages and field paths.
      - `grid-4`, `grid-6`, and `grid-9` limits are enforced.
      - No Prisma, API, browser, or filesystem dependencies are introduced in the schema module.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts`
</verification>

<success_criteria>
- A typed, exported `waoo.storyboard_package` v1.0 schema exists.
- Structural and semantic validation cover SBPI-01, SBPI-03 through SBPI-10.
- Later plans can import the schema without duplicating field definitions.
</success_criteria>
