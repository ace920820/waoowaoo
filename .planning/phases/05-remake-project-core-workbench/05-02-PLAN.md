---
phase: 05-remake-project-core-workbench
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/remake-projects/scenedetect/contracts.ts
  - src/lib/remake-projects/scenedetect/adapter.ts
  - src/lib/remake-projects/scenedetect/media.ts
  - src/lib/remake-projects/scenedetect/id-map.ts
  - src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts
  - tests/unit/remake-projects/scenedetect-adapter.test.ts
  - tests/integration/api/remake-projects-scenedetect-import.test.ts
autonomous: true
requirements: [RMP-03, RMP-06, TASK-02]
must_haves:
  truths:
    - "适配器同时接受 SceneDetect API response 与 schemaVersion=2 项目 JSON，并拒绝未知版本或不合法帧范围。"
    - "外部 shotNumber/startFrame 只用于建立可追溯映射；Waoo RemakeShot 使用稳定 UUID，重复导入不会重复 Shot。"
    - "首/中/尾关键帧、视频 URL、帧范围和 detector/threshold 被转换为 Waoo media/provenance DTO，不把外部 runtime URL 当永久资产 URL。"
    - "导入是幂等的，并能在 source analysis revision 变化时追加 Waoo revision，而不是覆盖已采用结果。"
    - "适配器不包含 pySceneDetect 算法，不复制 SceneDetect 项目存储或前端状态。"
    - "适配器明确提供双向边界：Waoo snapshot 可转换为 SceneDetect 原生 `SceneDetectProject`，SceneDetect 原生 Shot mutation/result 可提交为 Waoo revision/provenance；StageHost 不直接消费 normalized Waoo Shot DTO。"
    - "媒体导入不接受任意客户端 URL；只接受受信 SceneDetect executor 字节流、已签名对象存储引用，或通过配置 allowlist、逐跳重定向与私网地址校验的 HTTPS 来源，并限制类型和字节数。"
  artifacts:
    - path: "src/lib/remake-projects/scenedetect/contracts.ts"
      provides: "SceneDetect v2/API 输入、规范化 Shot、媒体和 provenance 合同"
    - path: "src/lib/remake-projects/scenedetect/id-map.ts"
      provides: "externalAnalysisId/externalShotKey 到稳定 Waoo Shot ID 的映射"
    - path: "src/lib/remake-projects/scenedetect/adapter.ts"
      provides: "校验、规范化、幂等导入和 revision 生成"
    - path: "src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts"
      provides: "所有权安全的 preview/commit 导入入口"
  key_links:
    - from: "src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts"
      to: "src/lib/remake-projects/scenedetect/adapter.ts"
      via: "preview/commit 分支复用同一 parser/mapping"
    - from: "src/lib/remake-projects/scenedetect/adapter.ts"
      to: "src/lib/remake-projects/service.ts"
      via: "事务写入 RemakeShot/revision/media/provenance"
    - from: "src/lib/remake-projects/scenedetect/media.ts"
      to: "src/lib/storage/"
      via: "外部媒体转为 Waoo 对象存储引用"
    - from: "src/lib/remake-projects/scenedetect/adapter.ts"
      to: "SceneDetectProject/Shot/VideoMetadata native contract"
      via: "toSceneDetectProject/commitSceneDetectMutation 双向适配；05-04 对 canonical vendored types 做编译期校验"
  prohibitions:
    - statement: "不得把 SceneDetect 的 shotNumber、数组位置或文件名直接作为 Waoo Shot 主键。"
      verification: "tests/unit/remake-projects/scenedetect-adapter.test.ts"
    - statement: "不得让 SceneDetect JSON、IndexedDB 或 runtime media 成为 Waoo 状态事实来源。"
      verification: "tests/integration/api/remake-projects-scenedetect-import.test.ts"
    - statement: "不得复制 pySceneDetect 或在 Next route 内执行视频分析算法。"
      verification: "npm run check:no-api-direct-llm-call && adapter source guard"
---

<objective>
定义 SceneDetect 与 waoowaoo 之间唯一的输入、输出、身份、媒体和版本适配合同。

<purpose>Phase 6 可以直接调用已有 SceneDetect 前后端能力并把结果安全导入 Waoo，而不重建算法或项目存储。</purpose>
<output>版本化 adapter、稳定 ID 映射、media ingestion、preview/commit API 和幂等回归。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-01-PLAN.md
@/Volumes/KINGSTON/projects/SceneDetect/src/types.ts
@/Volumes/KINGSTON/projects/SceneDetect/src/utils/projectStore.ts
@/Volumes/KINGSTON/projects/SceneDetect/backend/main.py
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立 SceneDetect 外部合同与稳定身份映射</name>
  <read_first>
    - `/Volumes/KINGSTON/projects/SceneDetect/src/types.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/utils/projectStore.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/backend/analyzer.py`
    - `05-01-PLAN.md`
  </read_first>
  <files>src/lib/remake-projects/scenedetect/contracts.ts, src/lib/remake-projects/scenedetect/id-map.ts, tests/unit/remake-projects/scenedetect-adapter.test.ts</files>
  <action>以 SceneDetect 真实 `SceneDetectProject`、`Shot`、`VideoMetadata` 和 v2/API response 为输入依据，定义 zod/边界合同并规范化 metadata、analysis、shot boundary、keyframeFrames、status、media refs 和 detector provenance。明确双向函数：`toSceneDetectProject(waooSnapshot)` 为完整 App 生成原生项目模型；`commitSceneDetectMutation(nativeProjectOrShots, baseRevision)` 将原生编辑结果转换为 Waoo revision/provenance/invalidation command。以 projectId+externalAnalysisId+externalShotKey 建立可追溯映射；首轮导入分配 Waoo UUID，后续导入按映射复用，拆分/合并产生新的 Waoo revision/Shot 关系。所有帧范围必须满足 fps、totalFrames 和 start&lt;=end。05-04 必须用 vendored 原生类型对这两个方向做编译期兼容检查，不能让 UI 直接接 normalized Waoo Shot DTO。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/scenedetect-adapter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - schema v2、API response、未知字段、未知版本和非法帧范围都有确定解析结果。
    - 重放/并发同一外部 Shot 返回同一 Waoo Shot ID；不同 analysis revision 不覆盖旧版本。
    - 外部序号变化不会改变稳定 Waoo ID；映射保留 externalShotKey 和来源 provenance。
    - adapter fixture 可以往返 `Waoo snapshot -> SceneDetectProject -> native mutation -> Waoo revision`，且往返不丢失 stable ID、边界、关键帧、媒体引用或 provenance。
  </acceptance_criteria>
  <done>SceneDetect 与 Waoo 的数据口径和稳定身份规则可被 Phase 6 直接使用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现媒体规范化和 preview/commit 导入 API</name>
  <read_first>
    - `src/lib/storage/`
    - `src/lib/api-auth.ts`
    - `src/lib/remake-projects/service.ts`
    - `src/app/api/projects/[projectId]/data/route.ts`
  </read_first>
  <files>src/lib/remake-projects/scenedetect/adapter.ts, src/lib/remake-projects/scenedetect/media.ts, src/app/api/remake-projects/[projectId]/scenedetect/import/route.ts, tests/integration/api/remake-projects-scenedetect-import.test.ts</files>
  <action>实现 preview（只校验和返回映射/warning）与 commit（事务导入 source、Shot、revision、keyframe media refs、provenance）的同源流程，并为 embedded runtime 提供按 Waoo Asset/Output 引用解析短期签名媒体 URL 的服务端边界；签名 URL 只是可刷新展示引用，不写回为永久事实。媒体只允许三种受信输入：已认证 SceneDetect executor 直接传输的受限字节流、Waoo 对象存储签名引用、配置中显式 allowlist 的 SceneDetect HTTPS origin；拒绝客户端任意 URL。对 allowlist 拉取逐次校验 scheme/host/port、禁止 URL credentials，DNS 解析后拒绝 loopback/private/link-local/metadata 地址，每次重定向重新校验并限制跳数，同时校验 Content-Type allowlist、Content-Length 和流式实际字节上限。通过 storage adapter 复制为 Waoo 永久对象或登记可刷新签名引用，API 不把本地 runtime 路径直接返回为永久资产。入口先做项目所有权和 remake type 校验，commit 使用 operationKey 幂等；为内网 IP、DNS rebinding/重定向、超限媒体、错误 MIME 和截断流增加 hostile fixture 测试。</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-projects-scenedetect-import.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - preview 不写数据库；commit 重放不重复 source/Shot/revision/media。
    - 导入结果可从 Waoo snapshot 恢复，媒体引用不依赖 SceneDetect runtime 目录。
    - 跨用户、错误 project type、非法 payload、过期 operationKey 和 partial failure 都有可理解错误。
    - 任意客户端 URL、私网/metadata 地址、跨 allowlist 重定向、错误 MIME、缺失或伪造长度及超过流式字节上限的媒体均在写库前失败；测试不发起真实内网请求。
  </acceptance_criteria>
  <done>Waoo 可安全接收现有 SceneDetect 结果，为 Phase 6 的真实执行和编辑回写提供唯一入口。</done>
</task>
</tasks>

<verification>
1. `npx vitest run tests/unit/remake-projects/scenedetect-adapter.test.ts`。
2. `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-projects-scenedetect-import.test.ts`。
3. 检查 route 不导入 SceneDetect 算法、不访问 IndexedDB/SceneDetect 项目文件事实来源。
4. ASVS L1：所有权、输入校验、媒体路径脱敏、SSRF/重定向/DNS 私网防护、MIME 与流式大小上限、幂等和 provenance 证据通过前保持阻塞。
</verification>

<success_criteria>
SceneDetect 结果可以通过版本化 adapter 映射到 Waoo 稳定 Shot、媒体和 provenance；没有第二套 Shot 主键或项目存储。本计划只交付 Phase 6 的 SHOT-02/SHOT-03 前置合同，不把真实分析/导入误报为已完成。
</success_criteria>
