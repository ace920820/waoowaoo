---
phase: 05-remake-project-core-workbench
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/lib/remake-projects/scenedetect/executor.ts
  - src/lib/remake-projects/scenedetect/task-contract.ts
  - src/lib/task/types.ts
  - src/lib/task/intent.ts
  - src/app/api/remake-projects/[projectId]/tasks/route.ts
  - tests/unit/remake-projects/scenedetect-task-contract.test.ts
  - tests/integration/api/remake-projects-task-projection.test.ts
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, RMP-06]
must_haves:
  truths:
    - "SceneDetect 分析和关键帧提取通过既有 Task/GraphRun 状态模型登记，业务页面不直接启动 FastAPI/CLI/进程。"
    - "executor/capability 合同明确区分 scenedetect.analyze、scenedetect.extract_keyframes 和后续 Prompt/生成能力；不新增 queue 或生命周期表。"
    - "同一 project/source revision/operationKey 的任务幂等，重试和 late event 不会复制结果或回退终态。"
    - "Task API 可从 Project/Shot 定位 SceneDetect 任务，并只返回脱敏状态、错误、run/provenance 摘要。"
    - "Phase 5 只建立可调用的 executor bridge；真实上传、SceneDetect 分析执行和 UI 编辑回写由 Phase 6 使用该 bridge 完成，不提前关闭 SHOT requirements。"
    - "executor bridge 同时提供供 `SceneDetectIntegrationRuntime` 使用的 analyze/extract task port；Task/GraphRun 进度和结果可转换为 SceneDetect 现有分析回调语义，不重写 `App.tsx` 或 AnalysisModal 状态机。"
  artifacts:
    - path: "src/lib/remake-projects/scenedetect/task-contract.ts"
      provides: "SceneDetect task descriptor、dedupe 和 input revision 合同"
    - path: "src/lib/remake-projects/scenedetect/executor.ts"
      provides: "既有 Task/Worker 到 SceneDetect adapter 的可替换 executor bridge"
    - path: "src/app/api/remake-projects/[projectId]/tasks/route.ts"
      provides: "安全的项目级 SceneDetect/翻拍 Task 查询投影"
  key_links:
    - from: "src/lib/remake-projects/scenedetect/executor.ts"
      to: "src/lib/task/submitter.ts"
      via: "复用 submitTask/GraphRun，不创建第二个 queue"
    - from: "src/lib/remake-projects/scenedetect/executor.ts"
      to: "src/lib/remake-projects/scenedetect/adapter.ts"
      via: "执行结果交给 adapter normalize/import"
    - from: "src/app/api/remake-projects/[projectId]/tasks/route.ts"
      to: "src/lib/task/service.ts"
      via: "复用 queryTasks 和安全 DTO projection"
    - from: "SceneDetectIntegrationRuntime"
      to: "src/lib/remake-projects/scenedetect/executor.ts"
      via: "submitAnalyze/submitExtract/status subscription task port；05-04 组装，Phase 6 注入真实 runtime"
  prohibitions:
    - statement: "不得把 FastAPI/CLI 命令、进程、Session 或环境变量暴露给业务 API/UI。"
      verification: "tests/integration/api/remake-projects-task-projection.test.ts"
    - statement: "不得新增 SceneDetect 专用 queue、Task 状态表或独立 Task Center。"
      verification: "npm run check:test-tasktype-coverage"
    - statement: "不得把 Task completed 自动转换为 Shot approved/adopted。"
      verification: "tests/unit/remake-projects/scenedetect-task-contract.test.ts"
---

<objective>
把 SceneDetect 接入统一 Task/GraphRun/Worker 体系，建立后续 Phase 6 可以直接调用的 executor bridge。

<purpose>SceneDetect 是外部能力提供者，不是第二个产品状态中心；Waoo 负责排队、恢复、追踪和安全投影。</purpose>
<output>SceneDetect task descriptor、executor bridge、Task query API 和状态回归。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-02-PLAN.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 登记 SceneDetect executor/capability 与幂等合同</name>
  <read_first>
    - `src/lib/task/types.ts`
    - `src/lib/task/intent.ts`
    - `src/lib/task/service.ts`
    - `src/lib/task/submitter.ts`
    - `src/lib/remake-projects/scenedetect/contracts.ts`
  </read_first>
  <files>src/lib/remake-projects/scenedetect/task-contract.ts, src/lib/remake-projects/scenedetect/executor.ts, src/lib/task/types.ts, src/lib/task/intent.ts, tests/unit/remake-projects/scenedetect-task-contract.test.ts</files>
  <action>增加 SceneDetect capability descriptor 和 operation input：analyze 与 extract_keyframes 均必须带 project/source/shot revision、adapterVersion、operationKey。复用现有 TASK_TYPE、intent、submitTask、GraphRun 和 dedupeKey；executor bridge 只接收规范化请求并返回 adapter 可消费的结果，不在 route 中运行分析算法。定义 UI runtime 可复用的 task port：提交 analyze/extract、订阅/查询脱敏进度、获取 terminal result；提供从 Waoo Task 状态到 SceneDetect 现有 progress/result/error callback 语义的纯转换，不复制 AnalysisModal 或 `App.tsx` 的状态机。为尚未开放的 Prompt/图片/视频能力保留 capability 位置但不误路由。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/scenedetect-task-contract.test.ts &amp;&amp; npm run check:test-tasktype-coverage</automated>
  </verify>
  <acceptance_criteria>
    - 相同输入 revision/operationKey 只有一个 canonical Task；不同 revision 必须产生不同任务。
    - Task terminal 状态优先于 late GraphRun active 状态；人工 review 不被执行状态修改。
    - 没有新增 queue、worker lifecycle 表或独立状态中心。
    - task port 的 callback adapter 覆盖 queued/running/completed/failed/canceled/retry，且只传递原应用需要的进度/结果/错误语义，不暴露 Task 内部 payload 或运行配置。
  </acceptance_criteria>
  <done>Phase 6 可以通过统一 Task 提交 SceneDetect 分析/关键帧操作。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提供项目级 Task/Run 安全投影</name>
  <read_first>
    - `src/app/api/tasks/route.ts`
    - `src/lib/errors/normalize.ts`
    - `src/lib/api-auth.ts`
    - `src/lib/task/service.ts`
  </read_first>
  <files>src/app/api/remake-projects/[projectId]/tasks/route.ts, tests/integration/api/remake-projects-task-projection.test.ts</files>
  <action>实现项目所有权安全的 Task 查询，支持 Project、remake_shot 和 remake_source target；返回 taskId、displayStatus、capability、attempt、时间、runId、workflowVersion、provenance 摘要和脱敏 error。raw payload/result/billingInfo/lease/session/env/命令/log 不可达。查询只投影真实现有 Task/GraphRun，不复制查询状态。</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-task-projection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 未登录、跨用户、错误 target 和越权 project 都无法枚举任务。
    - zero/one/many Task 稳定排序，失败/取消/等待重试语义可理解。
    - Task completed 与人工 approved/rejected 始终是独立字段。
  </acceptance_criteria>
  <done>SceneDetect 任务在 Waoo 工作台中可追踪、可恢复且不泄露执行上下文。</done>
</task>
</tasks>

<verification>
1. `npx vitest run tests/unit/remake-projects/scenedetect-task-contract.test.ts`。
2. `BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-task-projection.test.ts`。
3. `npm run check:test-tasktype-coverage`。
4. ASVS L1：executor allowlist、project ownership、safe DTO 和 late-event monotonicity 证据通过前保持阻塞。
</verification>

<success_criteria>
SceneDetect 已成为 Waoo 统一 Task/GraphRun 中的可替换能力，并为整应用 integration runtime 提供兼容原分析回调的 task port；Phase 6 不需要新建任务系统或重写分析弹窗状态机即可执行真实分析和关键帧提取。
</success_criteria>
