---
phase: 05-remake-project-core-workbench
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/lib/remake-projects/task-contract.ts
  - src/lib/remake-projects/task-projection.ts
  - src/lib/task/types.ts
  - src/lib/task/intent.ts
  - src/app/api/remake-projects/[projectId]/tasks/route.ts
  - tests/unit/remake-projects/task-contract.test.ts
  - tests/integration/api/contract/remake-task-projection.test.ts
autonomous: true
requirements:
  - RMP-04
  - RMP-06
  - TASK-01
  - TASK-02
  - TASK-03
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-07/TASK-01 adjacency+empty+ordering：统一投影明确 queued、processing、completed、failed、canceled、dismissed 与 queued+attempt>0 的 waiting_retry；零 Task 返回空集合，一/多 Task 按 createdAt,id 稳定排序。"
    - "TASK-01 idempotency+concurrency：相同 project/target/capability/inputRevision 生成相同 dedupeKey；并发或重复创建由既有 Task.dedupeKey 唯一约束归并，terminal Task/Run 不会被晚到 queued/processing 事件显示为进行中。"
    - "D-08/TASK-02 empty+encoding：task_type、executor、capability 必须是已注册技术标识；缺失或未知 tuple 拒绝 dispatch 并投影为 unsupported，不把 Unicode 用户文本误当路由标识。"
    - "TASK-02 idempotency+concurrency：重复或并发 dispatch descriptor 产生同一 canonical key；Phase 5 只登记同步 project initialization，不为 SceneDetect、Prompt、图片或视频能力创建可执行 worker 路由。"
    - "D-09/TASK-03 adjacency+empty+ordering：Project、Shot 或 output version 能以 targetType+targetId 查询统一 Task；零/一/多关联项均返回确定顺序及 target link。"
    - "TASK-03 idempotency+concurrency：重复 refetch/event 不复制任务行；Task terminal 状态优先于晚到 active GraphRun 状态，失败、取消和等待重试彼此不混淆。"
    - "D-06/RMP-06：Task DTO 只返回经过验证的 executor/capability、GraphRun workflowVersion、provenance 摘要和 normalizeTaskError；原始 payload/result/billingInfo、命令、Session、env、secret 与 raw log 不可达。"
    - "D-13：Task completed 只表示执行终态，投影不包含也不修改 approved/adopted；execution_failed 与 rejected 使用不同字段和值。"
  artifacts:
    - path: "src/lib/remake-projects/task-contract.ts"
      provides: "统一翻拍 Task descriptor、reserved capability、dedupe 与 target 合同"
      exports: ["REMAKE_TASK_CAPABILITY", "parseRemakeTaskDescriptor", "buildRemakeTaskDedupeKey"]
    - path: "src/lib/remake-projects/task-projection.ts"
      provides: "Task+GraphRun+provenance 的安全工作台 DTO 和单调状态投影"
      exports: ["toRemakeTaskView", "resolveRemakeTaskDisplayStatus"]
    - path: "src/app/api/remake-projects/[projectId]/tasks/route.ts"
      provides: "项目所有权安全的翻拍 Task 查询"
      exports: ["GET"]
    - path: "tests/integration/api/contract/remake-task-projection.test.ts"
      provides: "跨租户、状态、target、排序、脱敏与 late-event 回归"
  key_links:
    - from: "src/app/api/remake-projects/[projectId]/tasks/route.ts"
      to: "src/lib/task/service.ts"
      via: "requireProjectAuthLight 后按 projectId/target 查询现有 Task"
      pattern: "queryTasks"
    - from: "src/lib/remake-projects/task-projection.ts"
      to: "src/lib/errors/normalize.ts"
      via: "errorCode/errorMessage 仅经 normalizeTaskError 形成业务错误"
      pattern: "normalizeTaskError"
    - from: "src/lib/remake-projects/task-projection.ts"
      to: "GraphRun/RemakeProvenanceRecord"
      via: "taskId 连接 workflowVersion 与 provenance 摘要，不暴露 raw input/output"
      pattern: "taskId"
  prohibitions:
    - statement: "MUST NOT 将 Task 成功视为人工批准或采用。"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 暴露 Codex/SceneDetect CLI 命令、Session、环境变量、密钥或原始执行日志到业务 API/UI。"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 为视频翻拍创建第二套 queue、Task 状态表或 Task Center。"
      status: resolved
      verification: judgment
---

<objective>
把视频翻拍能力接入现有 Task + GraphRun 事实来源，定义可替换 executor/capability 合同，并提供不泄密、不回退终态的项目/Shot Task 查询投影。

Purpose: 后续执行能力共享同一 Task 生命周期和定位方式，同时阻止业务页面直接控制 CLI/进程或读取内部执行上下文。
Output: Task descriptor/dedupe 合同、初始化 Task 类型登记、安全 Task API 和任务边界回归。
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-PATTERNS.md
@.planning/phases/05-remake-project-core-workbench/05-01-SUMMARY.md
@src/lib/task/types.ts
@src/lib/task/service.ts
@src/lib/task/submitter.ts
@src/app/api/tasks/route.ts
@prisma/schema.prisma

<interfaces>
TASK_STATUS：queued | processing | completed | failed | canceled | dismissed。
CreateTaskInput：userId、projectId、type、targetType、targetId、payload、dedupeKey 等；Task 已有 `@@index([targetType,targetId])`、`@@index([projectId])` 与唯一 dedupeKey。
submitTask 会为 AI task 创建/复用 GraphRun 并投递既有队列；本计划不为尚未实现的翻拍能力开放 submit route。
GraphRun 可由 taskId 唯一关联，并提供 status、workflowType、workflowVersion；工作台只消费安全投影。
</interfaces>
</context>

## Artifacts this phase produces

| Path | Created/extended symbols |
|---|---|
| `src/lib/remake-projects/task-contract.ts` | `REMAKE_TASK_CAPABILITY`, `REMAKE_EXECUTOR`, `RemakeTaskDescriptor`, `parseRemakeTaskDescriptor`, `buildRemakeTaskDedupeKey` |
| `src/lib/remake-projects/task-projection.ts` | `RemakeTaskView`, `resolveRemakeTaskDisplayStatus`, `toRemakeTaskView` |
| `src/lib/task/types.ts` | `TASK_TYPE.REMAKE_PROJECT_INITIALIZE`（仅同步初始化记录） |
| `src/lib/task/intent.ts` | 初始化 Task 的 `build` intent 映射 |
| `src/app/api/remake-projects/[projectId]/tasks/route.ts` | `GET` safe Task/Run/provenance collection |
| `tests/unit/remake-projects/task-contract.test.ts` | descriptor、dedupe、状态优先级与路由拒绝测试 |
| `tests/integration/api/contract/remake-task-projection.test.ts` | 所有权、target、排序、脱敏、zero-one-many 与 late-event 测试 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一 Task descriptor、dedupe 与单调状态投影</name>
  <reversibility rating="costly">task_type+executor+capability 与 target/dedupe 形状会被后续执行阶段复用；更改需要同步 route、worker 注册和历史任务解释。</reversibility>
  <read_first>
    - `05-CONTEXT.md`：落实 D-07/D-08/D-09/D-13，禁止第二套 lifecycle。
    - `src/lib/task/types.ts`：复用现有 TaskStatus、TaskType、CreateTaskInput。
    - `src/lib/task/intent.ts`：新增 TaskType 必须进入现有 intent 映射。
    - `src/lib/task/service.ts`：复用 dedupeKey、target 与查询语义。
    - `src/lib/task/submitter.ts`：明确何时才允许建立 GraphRun/投递队列。
  </read_first>
  <files>src/lib/remake-projects/task-contract.ts, src/lib/remake-projects/task-projection.ts, src/lib/task/types.ts, src/lib/task/intent.ts, tests/unit/remake-projects/task-contract.test.ts</files>
  <behavior>
    - 初始化 Task descriptor 由 `remake_project_initialize + waoowaoo + remake.project.initialize` 唯一注册并能从 Plan 01 的持久化 payload 解析。
    - SceneDetect、Prompt、图片、视频 capability 作为 reserved 业务枚举可被展示/追溯，但调用 dispatch parser 时因未注册 worker 明确拒绝。
    - dedupeKey 对同一 project/target/capability/inputRevision 确定，对不同 revision 或 target 不同。
    - Task terminal 状态不会被 GraphRun queued/processing 覆盖；queued 且 attempt>0 显示 waiting_retry；progress 无真实证据时为 null。
  </behavior>
  <action>先写失败测试，再创建 task contract 和 projection。仅把 `REMAKE_PROJECT_INITIALIZE` 加入既有 `TASK_TYPE` 并在 `TASK_INTENT_BY_TYPE` 标为 build，使 Plan 01 记录不再依赖裸字符串；为 D-07 列出统一 capability/target/review 关联合同，但把尚未实现的 executor tuple 标为 reserved，dispatch parser 只接受当前有实现的 tuple，避免误投默认 text queue。`buildRemakeTaskDedupeKey` 使用 projectId、targetType、targetId、capability、inputRevision 的规范化值。`resolveRemakeTaskDisplayStatus` 以 Task 记录为权威，在 Task terminal 时忽略晚到 active GraphRun；queued+attempt>0 显示 waiting_retry，只有可验证的 processing progress 才返回百分比。人工审核字段完全不进入该状态函数，满足 D-13。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/task-contract.test.ts &amp;&amp; npm run check:test-tasktype-coverage</automated>
  </verify>
  <acceptance_criteria>
    - TASK-01 的全部显示状态、空状态、排序键和 terminal 优先级有确定测试。
    - TASK-02 的技术 tuple 校验、reserved 拒绝和 dedupeKey 重放/并发等价有确定测试。
    - 新 TaskType 通过仓库 tasktype coverage guard，且没有新增 queue、worker 或 lifecycle 表。
  </acceptance_criteria>
  <done>翻拍 Task 能通过既有类型/intent/target/dedupe 合同被解释，未实现能力无法被业务代码误路由。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提供脱敏且可定位的 Project/Shot Task API</name>
  <read_first>
    - `src/app/api/tasks/route.ts`：复用 queryTasks 与 normalizeTaskError，但不复制其 raw Task 展开。
    - `src/lib/api-auth.ts` 的 `requireProjectAuthLight`：在查询 Task 前确认项目所有权。
    - `src/lib/errors/normalize.ts`：错误只通过 normalizeTaskError 对外。
    - `src/lib/remake-projects/task-projection.ts`：route 必须返回 allowlist DTO。
    - `prisma/schema.prisma` 的 Task/GraphRun/RemakeProvenanceRecord：按 taskId 关联，不扫描其他项目。
  </read_first>
  <files>src/app/api/remake-projects/[projectId]/tasks/route.ts, src/lib/remake-projects/task-projection.ts, tests/integration/api/contract/remake-task-projection.test.ts</files>
  <behavior>
    - 未登录、他人项目或 target 不属于 project 时不返回任何 Task/Run/provenance。
    - Project、remake_shot、remake_output_version target 能返回稳定 link；零/一/多 Task 按 createdAt,id 排序并限制 limit 边界。
    - raw payload/result/billingInfo、errorMessage、leaseOwner、命令、Session、env、secret 与日志键不出现在 JSON。
    - error 只含 normalize 后 code/message/action；GraphRun 只含 runId/status/workflowType/workflowVersion。
  </behavior>
  <action>先写 route 合同测试，再实现 `GET /api/remake-projects/[projectId]/tasks`。handler 首先 `requireProjectAuthLight` 并确认 remake type，解析 targetType/targetId/status/limit 白名单，验证 target 确属该 Project；随后调用现有 `queryTasks`，批量查询同 project 的 GraphRun 与 provenance，交给 `toRemakeTaskView`。DTO 只能返回 Task 身份、type、display status、经证据约束的 progress、attempt、时间、target link、已验证 executor/capability、规范化 error、有限 Run/provenance 摘要；不得对象展开 Task/GraphRun。测试构造含敏感样例的 payload/result/error/log/env/session 字段并断言 JSON allowlist，覆盖 D-06/D-09、跨租户、zero-one-many、late GraphRun 与 terminal Task。</action>
  <verify>
    <automated>BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/contract/remake-task-projection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TASK-03 可从 Project/Shot/output version 定位 Task，且状态、顺序、重试显示与错误语义明确。
    - 响应通过字段 allowlist；敏感 fixture 的每个内部字段均无法从序列化 JSON 到达客户端。
    - Task completed、failed 与人工 approved/rejected 没有任何自动映射。
  </acceptance_criteria>
  <done>工作台具备项目所有权安全、可定位、可恢复且不泄露执行上下文的统一 Task/Run 查询合同。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| 业务 descriptor -> Task submitter/queue | 未注册 task_type/executor/capability 可能被错误路由到真实 worker |
| 浏览器 -> Task query | 不可信 target/limit/status 可能尝试跨项目枚举任务 |
| Task/GraphRun/provenance -> safe DTO | 内部 payload、result、lease、错误与日志可能含敏感执行上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-31 | Spoofing / Elevation | Task query target | high | blocked_until_verified | `requireProjectAuthLight` 后验证 target 属于 project；查询始终带 projectId，跨租户测试覆盖 Project/Shot/output target。 |
| T-05-32 | Tampering / Elevation | executor routing tuple | high | blocked_until_verified | task_type/executor/capability 技术标识 allowlist；未实现 tuple 仅 reserved 且 dispatch parser 拒绝，不落入默认 queue。 |
| T-05-33 | Information Disclosure | Task/Run DTO | high | blocked_until_verified | 显式字段 allowlist、`normalizeTaskError`、受限 Run/provenance 摘要；敏感 fixture 序列化回归阻止 raw payload/result/log/env/session。 |
| T-05-34 | Tampering | late/concurrent events | medium | mitigate | Task terminal 优先级、稳定 dedupeKey 与数据库唯一约束防止显示回退和重复任务身份。 |
| T-05-35 | Repudiation | Task -> target/provenance | medium | mitigate | 每个 DTO 保留 taskId、target、runId、workflowVersion 与 provenance identity，用户错误保持可操作 code。 |

## OWASP ASVS L1 Control-to-Evidence Mapping

| ASVS L1 control area | Threats | Task / control | Named automated evidence |
|---|---|---|---|
| V4 Access Control | T-05-31 | Task 2 project ownership、target 归属与 projectId 约束查询 | `BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/contract/remake-task-projection.test.ts` |
| V5 Validation, Sanitization and Encoding | T-05-32 | Task 1 descriptor/tuple allowlist 与 reserved dispatch 拒绝 | `npx vitest run tests/unit/remake-projects/task-contract.test.ts && npm run check:test-tasktype-coverage` |
| V8 Data Protection | T-05-33 | Task 2 safe DTO allowlist、`normalizeTaskError` 与敏感 fixture | `BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/contract/remake-task-projection.test.ts` |
| V1 Architecture and V7 Error/Logging | T-05-34, T-05-35 | Task 1 terminal 单调状态/dedupe；Task 2 target/run/provenance 可审计标识 | `npx vitest run tests/unit/remake-projects/task-contract.test.ts`；`BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/contract/remake-task-projection.test.ts` |

**ASVS L1 blocking rule:** 本计划在上述每条 named automated evidence 通过前不得完成；所有 high 威胁保持 `blocked_until_verified`，不得仅凭实现存在改记为已缓解。
</threat_model>

<verification>
1. `npx vitest run tests/unit/remake-projects/task-contract.test.ts`。
2. `BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/contract/remake-task-projection.test.ts`。
3. `npm run check:test-tasktype-coverage && npm run typecheck`。
4. ASVS L1 表中的每条 named automated evidence 都是完成门禁；任一失败时对应 high threat 继续阻塞本计划。
</verification>

<success_criteria>
- TASK-01/02/03 的统一状态、descriptor、dedupe、target 与 GraphRun 关联均建立在现有 Task 表和服务上。
- 未实现的 SceneDetect/Prompt/图片/视频 capability 不能被 Phase 5 API 派发，且没有第二套 queue/lifecycle。
- Task API 能恢复并定位真实状态，同时 raw executor/session/env/log 数据不可达。
- 所有 high threat 均映射到所有权校验、tuple allowlist、DTO allowlist 和 named automated evidence，且证据通过前保持阻塞。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/05-remake-project-core-workbench/05-03-SUMMARY.md`。
</output>
