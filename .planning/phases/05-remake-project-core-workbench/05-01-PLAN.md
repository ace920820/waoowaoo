---
phase: 05-remake-project-core-workbench
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/<timestamp>_add_remake_project_core/migration.sql
  - src/lib/remake-projects/service.ts
  - src/app/api/projects/route.ts
  - src/app/api/projects/[projectId]/data/route.ts
  - src/app/[locale]/workspace/page.tsx
  - src/app/[locale]/workspace/[projectId]/page.tsx
  - tests/integration/api/remake-project-core.test.ts
autonomous: true
requirements: [RMP-01, RMP-02, RMP-03, RMP-04, RMP-05, RMP-06]
must_haves:
  truths:
    - "显式 type=remake 才创建翻拍项目；旧剧本/小说推广项目的创建与详情路径不变。"
    - "创建翻拍项目只建立 Project/RemakeProject、接入状态和初始化 Task，不创建没有原视频来源的伪造 Shot。"
    - "RemakeShot、revision、output、provenance 和 invalidation 具备稳定 UUID、显式外键、唯一约束和可恢复的数据库关系，供 SceneDetect adapter 导入真实 Shot。"
    - "项目刷新、重新打开和服务重启后，工作台从真实数据库恢复 project/source/task 摘要；未接入视频显示 not_imported。"
    - "上游 revision 变更保留旧输出和 provenance，并将受影响下游标记为 needs_review；不因 Task 成功自动批准。"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "翻拍项目、稳定 Shot、revision、输出、provenance、失效关系"
    - path: "src/lib/remake-projects/service.ts"
      provides: "创建、读取快照、更新 Shot revision 的领域服务"
    - path: "src/app/api/projects/route.ts"
      provides: "显式 remake 创建分支，保留旧项目创建语义"
    - path: "src/app/api/projects/[projectId]/data/route.ts"
      provides: "按 Project.type 分派的翻拍快照读取"
    - path: "tests/integration/api/remake-project-core.test.ts"
      provides: "真实 DB/API 创建、恢复、权限和失效回归"
  key_links:
    - from: "src/app/api/projects/route.ts"
      to: "src/lib/remake-projects/service.ts"
      via: "显式 type=remake 分支调用事务服务"
    - from: "src/lib/remake-projects/service.ts"
      to: "prisma/schema.prisma"
      via: "Project/RemakeProject/Source/Shot/Revision/Task 关系"
    - from: "src/app/[locale]/workspace/[projectId]/page.tsx"
      to: "src/app/api/projects/[projectId]/data/route.ts"
      via: "Project.type=remake 渲染翻拍壳"
  prohibitions:
    - statement: "不得在 waoowaoo 重写 SceneDetect 镜头检测、时间轴、拆分/合并或关键帧提取逻辑。"
      verification: "tests/integration/api/remake-project-core.test.ts"
    - statement: "不得用 React state、IndexedDB、SceneDetect JSON 或 runtime 文件作为 Waoo 项目事实来源。"
      verification: "tests/integration/api/remake-project-core.test.ts"
    - statement: "不得为翻拍项目创建第二套 queue、Task 状态表或 Task Center。"
      verification: "npm run check:test-tasktype-coverage"
---

<objective>
建立视频翻拍项目的 Waoo 平台框架和持久化基础，不复制 SceneDetect 的分析/编辑能力。

<purpose>后续 adapter 可以把真实 SceneDetect Shot 导入稳定的 Waoo Project/Shot/Asset/Review/Task 状态；项目创建阶段没有原视频时保持真实空状态。</purpose>
<output>翻拍项目类型、稳定领域模型、真实快照 API、工作台 mode 分派和数据库 tracer。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-UI-SPEC.md
</execution_context>

<context>
<interfaces>
Project 既有 userId、type 兼容字段和项目授权；Task/GraphRun 由现有服务维护。RemakeShot 必须使用 Waoo UUID，外部 SceneDetect identity 留在 adapter/provenance 字段，不直接作为主键。
</interfaces>
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: 建立翻拍核心模型与项目创建/恢复事务</name>
  <read_first>
    - `prisma/schema.prisma`
    - `src/app/api/projects/route.ts`
    - `src/app/api/projects/[projectId]/data/route.ts`
    - `src/lib/task/service.ts`
    - `05-CONTEXT.md`
  </read_first>
  <files>prisma/schema.prisma, prisma/migrations/<timestamp>_add_remake_project_core/migration.sql, src/lib/remake-projects/service.ts, src/app/api/projects/route.ts, src/app/api/projects/[projectId]/data/route.ts, tests/integration/api/remake-project-core.test.ts</files>
  <action>先写失败测试，再增加 Project.type=remake 分支和 RemakeProject、RemakeSource、RemakeShot、RemakeShotRevision、RemakeOutputVersion、RemakeProvenanceRecord、RemakeInvalidation 的最小关系。创建事务只写项目、source/import status 和既有初始化 Task；不要创建无来源 Shot。提供 idempotent creationRequestId、project ownership、快照 allowlist 和 revision 失效服务。旧项目无 type 或既有 type 继续原分支。</action>
  <verify>
    <automated>npx prisma validate &amp;&amp; BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-core.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 创建/重放/并发创建只产生一个 remake Project，失败事务不残留半项目。
    - 重启后 GET 快照保留 source status、Task 摘要和空 Shot 集合；不存在 fake Shot/media/progress。
    - revision 更新保留旧记录并返回 needs_review 影响范围。
    - 旧项目创建和读取回归通过。
  </acceptance_criteria>
  <done>Waoo 有可恢复的翻拍项目框架，但未重复实现 SceneDetect 业务。</done>
</task>

<task type="auto">
  <name>Task 2: 接入翻拍 mode 与真实状态摘要</name>
  <read_first>
    - `src/app/[locale]/workspace/page.tsx`
    - `src/app/[locale]/workspace/[projectId]/page.tsx`
    - `05-UI-SPEC.md`
  </read_first>
  <files>src/app/[locale]/workspace/page.tsx, src/app/[locale]/workspace/[projectId]/page.tsx</files>
  <action>增加显式“视频翻拍”创建入口和 mode 分派。工作台只展示 project/source/shot/task/review 摘要与 SceneDetect 接入状态；项目创建后没有外部分析结果时显示未导入空状态，不显示假播放器、假 Shot 或手工新建 Shot 控件。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/integration/api/remake-project-core.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 旧工作区路由和创建行为无回归。
    - URL 使用稳定 projectId/mode，不依赖本地对象恢复项目。
  </acceptance_criteria>
  <done>翻拍项目可从 Waoo 入口创建并恢复真实框架状态。</done>
</task>
</tasks>

<verification>
1. `npx prisma validate`。
2. `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-core.test.ts`。
3. `npm run typecheck`。
4. ASVS L1：项目所有权、输入白名单、快照字段 allowlist 和旧模式兼容测试必须通过；高风险项未通过前保持阻塞。
</verification>

<success_criteria>
Phase 5 形成 Waoo 翻拍工作台壳和稳定领域合同，不创建第二套 SceneDetect 实现，也不制造没有原视频来源的 Shot。
</success_criteria>
