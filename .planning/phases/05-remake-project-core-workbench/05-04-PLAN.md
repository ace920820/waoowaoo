---
phase: 05-remake-project-core-workbench
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - scripts/vendor-scenedetect.mjs
  - src/vendor/scenedetect/VENDOR.json
  - src/vendor/scenedetect/index.ts
  - src/vendor/scenedetect/**
  - src/lib/remake-projects/scenedetect/integration-runtime.ts
  - tests/contracts/remake-scenedetect-native-contract.test.ts
  - tests/guards/remake-scenedetect-vendor-provenance.test.ts
  - tests/guards/remake-scenedetect-no-duplicate-source.test.ts
autonomous: true
requirements: [RMP-03, RMP-04, RMP-06, TASK-02]
must_haves:
  truths:
    - "D-15/D-16：SceneDetect 的复用单元是包含 `App.tsx` 编排与布局的完整应用；Phase 5 建立唯一 canonical source vendor 入口，不拆 Timeline/ShotInspector 等组件重新编排。"
    - "D-17：最小 integration runtime 和 05-02 双向 adapter 直接使用 vendored SceneDetect 原生 `SceneDetectProject`、`Shot`、`VideoMetadata` 类型，不由 Waoo DTO 倒推 UI 合同。"
    - "D-19：vendored source 默认保持原样；embedded root、modal containment、runtime 注入等未来必要 patch 必须在 manifest 中逐项登记，禁止布局或状态机重写。"
    - "D-20：正向 guard 证明 canonical complete-App、manifest/hash 和原生类型兼容；负向 guard 阻止 canonical root 外复制 editor/detector 或 deep-import 子组件重组。"
    - "Phase 5 只建立 compile/type contract，不实现真实 SceneDetect runtime；Phase 6 才替换 persistence/media/analyze/project/export 边界。"
  artifacts:
    - path: "src/vendor/scenedetect/VENDOR.json"
      provides: "SceneDetect 来源、commit/version、文件 hash、同步命令和允许 patch 的可审计清单"
    - path: "scripts/vendor-scenedetect.mjs"
      provides: "可重复同步 SceneDetect App 静态 source closure 并校验 manifest/hash 的工具"
    - path: "src/vendor/scenedetect/index.ts"
      provides: "唯一 canonical `SceneDetectEmbeddedApp` 与原生类型导出入口"
    - path: "src/lib/remake-projects/scenedetect/integration-runtime.ts"
      provides: "由 SceneDetect 原生合同驱动的 persistence/media/analyze/project/export runtime port"
    - path: "tests/guards/remake-scenedetect-vendor-provenance.test.ts"
      provides: "来源/hash/sync closure/allowed patches 正向门禁"
    - path: "tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
      provides: "canonical root 外禁止复制、deep import 和第二套编排的负向门禁"
  key_links:
    - from: "src/vendor/scenedetect/index.ts"
      to: "vendored App.tsx and native types"
      via: "只导出完整 `SceneDetectEmbeddedApp`、`SceneDetectProject`、`Shot`、`VideoMetadata`；不导出子组件编排 API"
    - from: "src/lib/remake-projects/scenedetect/integration-runtime.ts"
      to: "src/lib/remake-projects/scenedetect/{adapter,media,executor}.ts"
      via: "load/save native project、resolve signed media、submit Task/progress callback ports"
    - from: "tests/contracts/remake-scenedetect-native-contract.test.ts"
      to: "canonical native types + 05-02 adapter + integration runtime"
      via: "compile-time assignability 与双向 fixture"
  prohibitions:
    - id: P-01
      statement: "不得在 waoowaoo 重写 SceneDetect 的镜头检测、App.tsx 编排、时间轴、Shot 编辑、关键帧提取或分析弹窗状态机。"
      verification: "tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
    - id: P-02
      statement: "不得把 SceneDetectProject/IndexedDB/runtime/JSON 保存作为 Waoo 持久化事实来源。"
      verification: "tests/contracts/remake-scenedetect-native-contract.test.ts"
    - id: P-03
      statement: "不得绕过 canonical vendor index deep-import Timeline/ShotInspector 等子组件，或为 Waoo 暴露子组件重组 API。"
      verification: "tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
    - id: P-04
      statement: "不得修改未在 VENDOR.json allowedPatches 中登记的 vendored source，或在 source/hash 漂移时静默继续。"
      verification: "tests/guards/remake-scenedetect-vendor-provenance.test.ts"
---

<objective>
把 SceneDetect 完整前端应用纳入唯一、可追溯的 canonical vendor，并建立原生类型驱动的 integration runtime 与正反复用门禁。

<purpose>先锁定 Phase 6 的复用源和边界，确保后续 StageHost 只能挂载完整 `App.tsx`，不能通过拆组件重新实现编辑器。</purpose>
<output>canonical vendored app、VENDOR manifest/hash、native integration runtime contract、双向类型证据和来源/import/no-duplicate guards。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-PATTERNS.md
@.planning/phases/05-remake-project-core-workbench/05-02-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-03-PLAN.md
@/Volumes/KINGSTON/projects/SceneDetect/src/App.tsx
@/Volumes/KINGSTON/projects/SceneDetect/src/types.ts
@/Volumes/KINGSTON/projects/SceneDetect/src/utils/projectStore.ts
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 同步 canonical SceneDetect 完整应用并记录来源</name>
  <read_first>
    - `/Volumes/KINGSTON/projects/SceneDetect/src/App.tsx`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/types.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/utils/projectStore.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/utils/sceneDetector.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/index.css`
    - `05-PATTERNS.md`
  </read_first>
  <files>scripts/vendor-scenedetect.mjs, src/vendor/scenedetect/VENDOR.json, src/vendor/scenedetect/**, src/vendor/scenedetect/index.ts, tests/guards/remake-scenedetect-vendor-provenance.test.ts</files>
  <action>实现可重复 vendor sync：从 SceneDetect `src/App.tsx` 出发复制完整静态 import closure（组件、types、utils、styles 和所需资产），排除 `main.tsx`、构建输出、runtime 媒体、IndexedDB 数据及 macOS `._*` 文件。生成 `VENDOR.json`，记录 source repo/path、source commit 或不可变版本、逐文件/aggregate hash、sync command、依赖对齐和 allowedPatches。canonical `index.ts` 只导出完整 `SceneDetectEmbeddedApp` 与原生 `SceneDetectProject`、`Shot`、`VideoMetadata` 类型，不把 Timeline/ShotInspector 等子组件作为 Waoo API。同步和 `--check` 必须幂等，source/hash drift 或静态 closure 缺失时失败。</action>
  <verify>
    <automated>node scripts/vendor-scenedetect.mjs --check &amp;&amp; npx vitest run tests/guards/remake-scenedetect-vendor-provenance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 连续同步结果一致，manifest 可定位源版本和每个文件 hash；source drift 明确失败。
    - canonical index 只暴露完整 App 与原生类型，不提供子组件重组 API。
    - Phase 5 vendored source 无布局/状态机 patch；未来允许 patch 面在 manifest 中具名。
  </acceptance_criteria>
  <done>SceneDetect 完整 App 有唯一、可审计且可同步的 Waoo 源码入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立原生 runtime 合同和正反复用门禁</name>
  <read_first>
    - `src/vendor/scenedetect/index.ts`
    - `src/lib/remake-projects/scenedetect/adapter.ts`
    - `src/lib/remake-projects/scenedetect/media.ts`
    - `src/lib/remake-projects/scenedetect/executor.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/App.tsx`
  </read_first>
  <files>src/lib/remake-projects/scenedetect/integration-runtime.ts, tests/contracts/remake-scenedetect-native-contract.test.ts, tests/guards/remake-scenedetect-no-duplicate-source.test.ts</files>
  <action>定义最小 `SceneDetectIntegrationRuntime`：load/save native project、resolve Waoo media ref、submit analyze/extract Task 并转换原应用进度/结果/error callback、Waoo project entry policy、export availability policy。所有项目/Shot/metadata 参数直接引用 canonical vendored 原生类型；用 compile fixture 验证 05-02 的 `Waoo snapshot -> SceneDetectProject -> native mutation -> Waoo revision` 往返。建立正向 import/type 断言和负向 AST/import graph/signature scan：canonical root 是唯一合法 editor source，Waoo 其他目录不得复制 detector/editor、deep-import 子组件、新建第二套 `App.tsx` 编排/IndexedDB/queue lifecycle。此任务只定义 port 与 guard，不实现 runtime adapter。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/guards/remake-scenedetect-vendor-provenance.test.ts tests/guards/remake-scenedetect-no-duplicate-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 原生类型不兼容、adapter 往返丢字段、manifest/hash 漂移或未登记 patch 时测试失败。
    - canonical root 外 deep-import、复制 Timeline/ShotInspector/editor signature 或第二套编排时失败；canonical vendor 自身被正确白名单。
    - runtime 只定义 persistence/media/task/project/export 边界，不接管 `App.tsx` editor state。
  </acceptance_criteria>
  <done>完整 App 复用具有可执行的来源、类型、import graph 和防复制门禁。</done>
</task>
</tasks>

<verification>
1. `node scripts/vendor-scenedetect.mjs --check`：manifest、source version、完整静态 closure、hash 和 allowed patches 一致。
2. `npm run typecheck && npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/guards/remake-scenedetect-vendor-provenance.test.ts tests/guards/remake-scenedetect-no-duplicate-source.test.ts`。
3. 检查本计划没有 Waoo UI/React Query/Playwright 职责；这些只在依赖本计划的 05-05 中实施。
</verification>

<success_criteria>
Phase 5 已先锁定来源可审计、原生类型兼容的 SceneDetect 完整 App 与最小 runtime 边界，后续 StageHost 无法合法走向组件拆解重写。
</success_criteria>
