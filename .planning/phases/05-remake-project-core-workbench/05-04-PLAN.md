---
phase: 05-remake-project-core-workbench
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/lib/query/keys.ts
  - src/lib/query/hooks/useRemakeProject.ts
  - src/lib/query/hooks/index.ts
  - src/app/[locale]/workspace/page.tsx
  - src/app/[locale]/workspace/[projectId]/page.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbenchChrome.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeShotWorkspace.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeTaskPanel.tsx
  - src/i18n.ts
  - messages/zh/remake-workbench.json
  - messages/en/remake-workbench.json
  - tests/unit/remake-projects/remake-workbench-url-state.test.ts
  - tests/unit/remake-projects/remake-workbench-ui.test.ts
autonomous: true
requirements:
  - RMP-01
  - RMP-02
  - RMP-03
  - RMP-04
  - RMP-05
  - RMP-06
  - TASK-01
  - TASK-02
  - TASK-03
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "项目创建、Shot 新建/编辑及响应式弹窗在空字段、部分填写、服务端校验失败和正常提交时，分别使用字段级错误、保留用户输入、明确提交状态与已定义的成功跳转；不会以空白弹窗或本地假对象代替真实状态。（empty、error、partial、long-text）"
    - "阶段导航始终显示七个固定阶段；当前项、锁定项、加载/查询失败和移动端长标签均保留文字、图标及可访问状态，不会跳转到空白路由。（error、long-text）"
    - "项目摘要、Shot 列表、Shot 详情、Task 面板和原视频区域在真实数据可用时分别展示六项摘要、稳定 ID 列表、provenance、关联任务及媒体状态；任何缺失值都显示原因而不是伪造零值、播放器、进度或版本。（empty、populated、partial）"
    - "项目查询、Shot 保存和 Task 执行失败时，界面保留仍可用的真实数据并显示已定义的恢复入口或脱敏原因；Task 成功不会推导为人工已批准，Shot 更新也不会清除需要复核状态。（error、partial）"
    - "Shot 与 Task 集合在零项、一项和多项时分别使用空状态、稳定默认选择和稳定 ID 排序；摘要计数始终显示数值，筛选条件始终以文字和图标呈现。（zero-one-many、populated）"
    - "原视频未导入、详情未选中、provenance 不完整和未来阶段未开放时，界面分别显示 `未导入`、选择提示、`未提供` 和锁定状态，且不制造假媒体、假数据或可执行入口。（empty、partial）"
  artifacts:
    - path: "src/lib/query/hooks/useRemakeProject.ts"
      provides: "snapshot、Shot mutation、Task query 与写后失效的 React Query 合同"
      exports: ["useRemakeProject", "useCreateRemakeShot", "useUpdateRemakeShot", "useRemakeTasks"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx"
      provides: "URL 驱动的翻拍工作台控制器、加载/错误/空状态与响应式组合"
      exports: ["default"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeShotWorkspace.tsx"
      provides: "稳定 Shot 列表、详情、编辑、复核与 provenance 视图"
      exports: ["RemakeShotWorkspace"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeTaskPanel.tsx"
      provides: "desktop panel/tablet-right-drawer/mobile-bottom-drawer Task 只读视图"
      exports: ["RemakeTaskPanel"]
    - path: "messages/zh/remake-workbench.json"
      provides: "Phase 5 工作台中文 namespace"
    - path: "messages/en/remake-workbench.json"
      provides: "与中文键完全配对的英文 namespace"
  key_links:
    - from: "src/lib/query/hooks/useRemakeProject.ts"
      to: "src/app/api/remake-projects/[projectId]/route.ts"
      via: "queryKeys.remakeProject.detail(projectId) 获取唯一 snapshot"
      pattern: "remakeProject.detail"
    - from: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx"
      to: "URL search params"
      via: "stage/shot/task 由 router.replace 更新且不建立选择状态副本"
      pattern: "router.replace"
    - from: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeTaskPanel.tsx"
      to: "src/app/api/remake-projects/[projectId]/tasks/route.ts"
      via: "useRemakeTasks 只消费 safe Task view"
      pattern: "useRemakeTasks"
    - from: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeShotWorkspace.tsx"
      to: "src/lib/query/hooks/useRemakeProject.ts"
      via: "create/update mutation 成功后 invalidate snapshot/shot/tasks，而非本地替换数组"
      pattern: "invalidateQueries"
  prohibitions:
    - id: "P-01"
      statement: "MUST NOT 将 Task 成功视为人工批准或采用。"
      status: resolved
      verification: judgment
      source: "05-05-PLAN.md#canonical-phase-5-prohibition-audit"
    - id: "P-02"
      statement: "MUST NOT 暴露 Codex/SceneDetect CLI 命令、Session、环境变量、密钥或原始执行日志到业务 API/UI。"
      status: resolved
      verification: judgment
      source: "05-05-PLAN.md#canonical-phase-5-prohibition-audit"
    - id: "P-03"
      statement: "MUST NOT 覆盖或删除旧输出，也不得在上游 Shot 变化后静默沿用过期下游结果。"
      status: resolved
      verification: judgment
      source: "05-05-PLAN.md#canonical-phase-5-prohibition-audit"
    - id: "P-04"
      statement: "MUST NOT 在增加翻拍模式时改变既有剧本或小说推广项目的行为。"
      status: resolved
      verification: judgment
      source: "05-05-PLAN.md#canonical-phase-5-prohibition-audit"
    - id: "P-05"
      statement: "MUST NOT 为视频翻拍创建第二套 queue、Task 状态表或 Task Center。"
      status: resolved
      verification: judgment
      source: "05-05-PLAN.md#canonical-phase-5-prohibition-audit"
---

<objective>
完成视频翻拍项目的统一操作工作台：创建后进入真实概览，以 URL 恢复 Shot/Task 选择，以 React Query 同步 Project/Shot/Task/provenance，并在桌面、平板和手机保持可扫描、可访问的状态界面。

Purpose: 把底层稳定状态变成用户可持续工作的生产界面，同时锁住后续阶段边界、URL/查询事实来源与六条 UI 状态合同。
Output: React Query hooks、URL 驱动 shell、三/二/一栏响应式组件、独立双语 namespace 与 focused UI 状态测试；40 项 edge、schema/兼容硬化和人工视觉 backstop 由依赖本计划的 Plan 05 完成。
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-UI-SPEC.md
@.planning/phases/05-remake-project-core-workbench/05-PATTERNS.md
@.planning/phases/05-remake-project-core-workbench/05-02-SUMMARY.md
@.planning/phases/05-remake-project-core-workbench/05-03-SUMMARY.md
@src/app/[locale]/workspace/[projectId]/page.tsx
@src/lib/query/keys.ts
@src/lib/query/hooks/useProjectData.ts
@src/lib/query/hooks/useTaskStatus.ts
@src/components/ui/SegmentedControl.tsx
@src/components/task/TaskStatusInline.tsx

<interfaces>
Plan 02 snapshot：project、summary、shots、selected-safe provenance，collections 已稳定排序且 null 显式。
Plan 02 mutations：POST shots 接受 name+clientRequestId；PATCH shot 接受 name/reviewStatus+expectedRevision+requestId 并返回 affectedCount/scopes。
Plan 03 Task view：只含 safe status/progress/target/run/provenance/error，不含 raw payload/result/log。
既有 query 形态：queryKeys 工厂 + useQuery enabled/staleTime；mutation 成功调用 queryClient.invalidateQueries。
既有 URL 形态：new URLSearchParams(searchParams.toString()) 后 router.replace({ pathname, query }, { scroll: false })。
</interfaces>
</context>

## Phase 5 Prohibition Mirror

`05-05-PLAN.md` 的 `## Canonical Phase 5 Prohibition Audit` 是 P-01..P-05 的唯一规范来源。本计划 frontmatter 中的五项仅为执行期镜像，ID、statement、顺序和自动断言名称不得在此独立改写。D-14 的自动最终剪辑、音乐/音频与发布边界不属于这五项，统一作为额外里程碑范围边界 MB-01 处理。

## Artifacts this phase produces

| Path | Created/extended symbols |
|---|---|
| `src/lib/query/keys.ts` | `queryKeys.remakeProject.detail/shots/tasks` |
| `src/lib/query/hooks/useRemakeProject.ts` | `useRemakeProject`, `useCreateRemakeShot`, `useUpdateRemakeShot`, `useRemakeTasks` |
| `src/lib/query/hooks/index.ts` | 翻拍 query/mutation exports |
| `src/app/[locale]/workspace/page.tsx` | hardened remake creation form、重复提交锁与成功直达 |
| `src/app/[locale]/workspace/[projectId]/page.tsx` | remake/novel mode dispatch、模式特定 loading/error shell |
| `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx` | URL state controller、query composition、responsive shell |
| `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbenchChrome.tsx` | 64/56px project bar、44px seven-stage nav、112px summary band |
| `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeShotWorkspace.tsx` | stable Shot list/detail/editor/review/provenance |
| `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeTaskPanel.tsx` | Task panel/right drawer/bottom drawer 与只读详情 |
| `src/i18n.ts` | `remakeWorkbench` namespace loader |
| `messages/{zh,en}/remake-workbench.json` | 成对 UI 文案、状态、错误、空状态与 tooltips |
| `tests/unit/remake-projects/remake-workbench-url-state.test.ts` | stage/shot/task URL 恢复、默认选择与 invalid param 测试 |
| `tests/unit/remake-projects/remake-workbench-ui.test.ts` | UI states、a11y、responsive class contract、i18n parity 测试 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用 React Query 与 URL 恢复创建、Shot 和 Task 选择</name>
  <read_first>
    - `05-UI-SPEC.md` 的“项目创建入口”“工作台壳”“一条真实读写路径”：逐项实现，不加入后续阶段操作。
    - `src/lib/query/keys.ts`：沿用集中 query key 工厂。
    - `src/lib/query/hooks/useProjectData.ts`：沿用 enabled、apiFetch、staleTime、错误提升和 invalidate 形态。
    - `src/lib/query/hooks/useTaskStatus.ts`：对齐 Task query 参数，但只消费 Plan 03 safe route。
    - `src/app/[locale]/workspace/[projectId]/page.tsx`：保留 NovelPromotionWorkspace 的 VALID_STAGES 与 URL 语义。
  </read_first>
  <files>src/lib/query/keys.ts, src/lib/query/hooks/useRemakeProject.ts, src/lib/query/hooks/index.ts, src/app/[locale]/workspace/page.tsx, src/app/[locale]/workspace/[projectId]/page.tsx, tests/unit/remake-projects/remake-workbench-url-state.test.ts</files>
  <behavior>
    - 新建表单空/超长/服务端错误保留输入，提交中禁止重复；成功后直达 `/workspace/{projectId}?stage=overview&shot={stableId}`。
    - stage 仅接受 overview；future stage 保留 UI 位置但不导航；shot/task 参数仅在当前 snapshot 中存在时选中。
    - URL 无 shot 时选服务端排序后的首 Shot 并 replace；零 Shot 保持未选中；无效 shot/task 被清理而不建立本地选择副本。
    - Shot mutation 成功失效 detail/shots/tasks；后台 refetch 保留前次数据，首次无数据才显示结构骨架。
  </behavior>
  <action>先写 URL/selection/query key 失败测试，再新增 `queryKeys.remakeProject` 和 `useRemakeProject/useCreateRemakeShot/useUpdateRemakeShot/useRemakeTasks`。所有 query 带 projectId enabled 条件，调用 Plan 02/03 API；mutation 使用服务端 stable ID/revision，并在成功后 invalidate detail、shots、tasks，不直接 splice/replace cache 数组。强化 D-01 创建表单：类型选择保持既有默认，只有显式视频翻拍提交 `type=remake`+客户端 UUID creationRequestId，提交锁防止双击，成功直接路由。详情 controller 先按持久化 Project.type 分派；remake 使用独立 stage=overview/shot/task URL helper，novel 分支继续使用原 VALID_STAGES、episode 和 NovelPromotionWorkspace。URL helper 处理 zero/one/many、无效参数、refetch 后目标删除和浏览器重新打开，满足 D-02/D-04/D-09。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Project/Shot/Task 选择只由 URL+服务端 snapshot 决定，刷新和重新打开可恢复。
    - 写后读取来自 API/DB，后台刷新不清空成功数据，也不显示伪造本地对象。
    - 视频翻拍与既有项目创建、阶段、episode 参数互不污染。
  </acceptance_criteria>
  <done>用户可以可靠创建翻拍项目、恢复 overview/shot/task URL 状态，并通过 React Query 与真实 API 同步。</done>
</task>

<task type="auto">
  <name>Task 2: 完成可扫描、可访问的三/二/一栏工作台</name>
  <read_first>
    - `05-UI-SPEC.md` 全文：布局、组件、状态、文案、响应式、a11y、颜色和禁止视觉均为合同。
    - `src/components/ui/SegmentedControl.tsx`：复用 controlled API，不改其既有页面表现。
    - `src/components/task/TaskStatusInline.tsx`：复用图标/状态语义，人工 review 保持独立标签。
    - `messages/zh/workspace.json` 与 `messages/en/workspace.json`：复制双语键配对风格到独立 namespace。
    - `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx`：从 tracer shell 扩展，不引入 mock 数据。
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbenchChrome.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeShotWorkspace.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeTaskPanel.tsx, src/i18n.ts, messages/zh/remake-workbench.json, messages/en/remake-workbench.json, tests/unit/remake-projects/remake-workbench-ui.test.ts</files>
  <action>按 D-02/D-11/D-12/D-13/D-14 和 UI-SPEC 实现浅色静态操作界面。Chrome 保留全局 Navbar，项目栏 desktop 64px/mobile 56px，阶段栏 44px 且固定七项，仅 overview 有 `aria-current=page`；其余项使用 AppIcon lock、`aria-disabled` 和 tooltip。摘要带以真实 DTO 显示原视频、Shot 总数、阶段、完成度、失败、待审核，null 显示 `--`+原因。工作区 desktop 使用 288px/minmax(520px,1fr)/320px，tablet 使用 280px/minmax(488px,1fr)+400px 右抽屉，mobile 单栏+底抽屉；三个区域各自滚动。Shot 列表按 stable ID/sequence，支持 `aria-selected`、方向键与 Enter/Space；详情展示 stable ID、独立 review/Task 状态、needs_review 原因/数量、原视频状态与完整 provenance 字段。Task 面板只消费 safe view，点击写 task URL。首次加载分别渲染固定尺寸骨架，refetch 显示小型更新状态；错误、零 Shot、未选择、部分 provenance、失败 Task 与 future stage 均使用指定文案和恢复入口。图标一律 AppIcon，未知图标有 tooltip，点击区至少 44px；不添加时间轴、Prompt、生成、批量、导出、剪辑、音乐或发布控件。创建独立 `remakeWorkbench` zh/en namespace 并在 `src/i18n.ts` 加载，所有键成对。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-workbench-ui.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - UI Considerations 的 6 条 covered truth 均由渲染/状态测试直接覆盖。
    - desktop/tablet/mobile 网格、固定高度、drawer、长文本、焦点、键盘与状态图标/文字合同在组件和测试中存在。
    - 页面不包含假播放器、假 progress、营销 hero、嵌套 section card、渐变装饰或后续阶段可执行入口。
    - MB-01（额外里程碑范围边界）：页面不存在自动最终剪辑、音乐/音频或发布命令；该断言不计入 P-01..P-05。
    - zh/en key 集完全一致，技术值原样显示但标签本地化。
  </acceptance_criteria>
  <done>翻拍工作台达到 UI-SPEC 的信息层级、状态完整性、响应式和可访问性合同，且只展示真实 Phase 5 能力。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/query cache -> workbench | 不可信 stage/shot/task 参数与陈旧缓存可能展示错误 Project/Shot/Task |
| Safe API DTO -> React UI | 长 Unicode 文本、错误和技术标识进入可交互界面 |
| 创建/编辑 UI -> API | 重复点击、过期 revision 和客户端校验绕过进入服务端 |
| Existing workspace -> remake mode dispatch | 新模式分派可能误改既有 novel-promotion 行为 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-41 | Spoofing / Elevation | shot/task URL selection | high | blocked_until_verified | URL ID 仅在已授权 snapshot/safe Task 集合中解析；未知 ID 清理，不以 URL 单独读取跨项目对象。 |
| T-05-42 | Tampering | create/edit mutations | high | blocked_until_verified | 客户端 UUID/request lock 改善 UX，服务端 parser/ownership/expectedRevision 仍是权威；409 保留用户输入并 refetch。 |
| T-05-43 | Information Disclosure | Task/provenance UI | high | blocked_until_verified | 组件只接受 Plan 03 safe types，不渲染 raw payload/result/error/log；超长技术值可复制/滚动但不展开内部对象。 |
| T-05-44 | Tampering / Repudiation | review vs Task UI | medium | mitigate | 人工 review 与 Task 状态分列，needs_review 原因/数量可见；Task completed 不能触发批准或清除复核。 |
| T-05-45 | Denial of Service | refetch/duplicate submit | medium | mitigate | stable query keys、enabled 条件、提交锁、idempotency keys 与写后 invalidation，避免重复 mutation 和刷新风暴。 |
| T-05-46 | Elevation / Regression | legacy mode dispatch | high | blocked_until_verified | 持久化 Project.type 明确分派；URL/query focused tests 锁定 mode-specific stage/selection，Plan 05 再运行专用兼容矩阵。 |

## OWASP ASVS L1 Control-to-Evidence Mapping

| ASVS L1 control area | Threats | Task / control | Named automated evidence |
|---|---|---|---|
| V4 Access Control | T-05-41 | Task 1 只从已授权 snapshot/safe Task 集合恢复 URL 选择 | `npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts` |
| V5 Validation, Sanitization and Encoding | T-05-42 | Task 1 URL 参数、creationRequestId、expectedRevision 与服务端错误处理 | `npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts` |
| V8 Data Protection | T-05-43 | Task 2 safe DTO-only 渲染、长技术值收敛与独立双语标签 | `npx vitest run tests/unit/remake-projects/remake-workbench-ui.test.ts` |
| V1 Architecture and V7 Error/Logging | T-05-44, T-05-45 | Task 1 Query/URL 单一事实来源；Task 2 review/Task 分列与失败态 | `npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts` |
| V4 Access Control / V1 Architecture | T-05-46 | Task 1 按持久化 Project.type 分派并隔离 novel/remake URL 合同 | `npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts` |

**ASVS L1 blocking rule:** 本计划在上述每条 named automated evidence 通过前不得完成；所有 high 威胁保持 `blocked_until_verified`，不得仅凭实现存在改记为已缓解。Plan 05 的兼容 suite 是阶段级追加门禁，不替代本计划的 focused gates。
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | 创建可恢复的视频翻拍项目，并以稳定 Shot/Task 在统一工作台管理生产状态 | 01-05 | COVERED | tracer + domain/API + Task/Run + UI + final regression |
| REQ | RMP-01 | 独立翻拍创建且旧项目行为不变 | 01,04,05 | COVERED | 事务幂等创建、模式隔离与专用兼容回归 |
| REQ | RMP-02 | 原视频/Shot/阶段/完成度/失败/待审核摘要 | 01,02,04,05 | COVERED | 真实 snapshot、六项摘要与 edge suite |
| REQ | RMP-03 | stable Shot 关联全部上下游 | 01,02,04,05 | COVERED | UUID/revision/output/provenance/schema contract |
| REQ | RMP-04 | 刷新、关闭、重启后恢复 | 01,02,03,04,05 | COVERED | DB refetch/restart/final tests |
| REQ | RMP-05 | 上游修改保留历史并 needs_review | 01,02,04,05 | COVERED | sourceShotRevision + invalidation + edge suite |
| REQ | RMP-06 | 输入/参数/executor/model-skill/output 可追溯 | 01,02,03,04,05 | COVERED | provenance + safe projection + final regression |
| REQ | TASK-01 | 所有能力共享统一 Task 状态 | 01,03,04,05 | COVERED | existing Task lifecycle + reserved contract |
| REQ | TASK-02 | task_type+executor+capability 可替换路由 | 01,03,04,05 | COVERED | registered tuple，未实现 dispatch 拒绝 |
| REQ | TASK-03 | 查看并定位 Project/Shot/version Task | 01-05 | COVERED | target DTO + URL task selection + final regression |
| RESEARCH | — | 研究已禁用且无 RESEARCH.md | — | EXCLUDED | 本次运行明确不适用，不是遗漏 |
| CONTEXT | D-01 | 独立翻拍类型与旧模式兼容 | 01,04,05 | COVERED | create branch + dispatch + compatibility suite |
| CONTEXT | D-02 | 统一工作台、摘要与稳定导航 | 01,04 | COVERED | overview shell + UI contract |
| CONTEXT | D-03 | Shot 稳定标识和共同连接点 | 01,02,05 | COVERED | schema/contracts/API/final schema contract |
| CONTEXT | D-04 | DB/对象存储为事实来源 | 01,02,04,05 | COVERED | restart/refetch + React Query + final regression |
| CONTEXT | D-05 | 保留旧结果并传播 needs_review | 01,02,04,05 | COVERED | output version/invalidation/UI/edge suite |
| CONTEXT | D-06 | 通用 provenance | 01-05 | COVERED | persistence + projection + display + final regression |
| CONTEXT | D-07 | 统一 Task lifecycle | 01,03,05 | COVERED | existing Task/GraphRun + tasktype gate |
| CONTEXT | D-08 | 可替换 executor/capability，不直接 CLI | 01,03,05 | COVERED | descriptor allowlist + no dispatch UI + prohibition checks |
| CONTEXT | D-09 | Project/Shot/version Task 定位 | 01,03,04,05 | COVERED | safe route + task URL + edge suite |
| CONTEXT | D-10 | 首个真实纵向 tracer | 01 | COVERED | real DB/API/UI projection |
| CONTEXT | D-11 | 创作工作台，不是营销/聊天/CLI | 04,05 | COVERED | UI components + final prohibitions |
| CONTEXT | D-12 | 仅阶段壳与摘要 | 04,05 | COVERED | future stages locked + absence regression |
| CONTEXT | D-13 | 人工审核显式且独立 | 01-05 | COVERED | review contract + separate UI labels + final regression |
| CONTEXT | D-14 | 不含自动剪辑/音乐/发布 | 04,05 | COVERED | 额外范围边界 MB-01；不计入 canonical 五项 prohibition |
| UI | C-01 | 创建/编辑弹窗 empty/error/partial/long-text | 04 | COVERED | plain must_haves truth 1 |
| UI | C-02 | 七阶段导航 error/long-text | 04 | COVERED | plain must_haves truth 2 |
| UI | C-03 | 五个数据区域 empty/populated/partial | 04 | COVERED | plain must_haves truth 3 |
| UI | C-04 | query/save/task error 与独立审核 | 04 | COVERED | plain must_haves truth 4 |
| UI | C-05 | Shot/Task zero-one-many 与筛选 | 04 | COVERED | plain must_haves truth 5 |
| UI | C-06 | 未导入/未选中/部分/锁定 | 04 | COVERED | plain must_haves truth 6 |
| UI | B-01 | 稳定 loading/refetch 尺寸 | 05 | COVERED | structured flat-scalar backstop truth 1 + blocking human checkpoint |
| UI | B-02 | 三断点长内容与无横向溢出 | 05 | COVERED | structured flat-scalar backstop truth 2 + blocking human checkpoint |
| EDGE | 40 items | 9 requirements 的全部 applicable edge | 05 | COVERED | Plan 05 Task 1 具名真实 DB/API 回归；无 unresolved |
| PROHIBITION | P-01..P-05 | 五条 canonical Phase 5 bespoke must-NOT | 04,05 | COVERED | 唯一来源为 05-05；Plan 04 仅按 ID/source 镜像，其中 P-05 禁止第二套 Task lifecycle |
| SCOPE BOUNDARY | MB-01 | 不引入自动最终剪辑、音乐/音频或发布 | 04,05 | COVERED | D-14 额外里程碑范围边界，不混入 canonical 五项 |

<verification>
1. `npx vitest run tests/unit/remake-projects/remake-workbench-url-state.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts`。
2. `npm run typecheck`，确认 query hooks、mode dispatch、独立 i18n namespace 与组件接口一致。
3. ASVS L1 表中的每条 named automated evidence 都是完成门禁；任一失败时对应 high threat 继续阻塞本计划。
</verification>

<success_criteria>
- 用户创建翻拍项目后直接进入 overview，URL 可恢复 stable Shot/Task，所有内容来自 React Query 的真实 API snapshot。
- 工作台在 desktop/tablet/mobile 呈现完整摘要、Shot 详情、needs_review、provenance 和安全 Task 状态，future stages 仅锁定展示。
- 六条 UI truth 均由 focused tests 覆盖；两条 structured backstop、40/40 edge、schema/兼容硬化、D-01..D-14、canonical P-01..P-05 与额外范围边界 MB-01 明确移交给依赖本计划的 Plan 05，不在本计划中自判通过。
- 所有 high threat 均有 Plan 04 named automated evidence，且证据通过前保持阻塞；阶段级兼容与视觉验收仍由 Plan 05 阻塞。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/05-remake-project-core-workbench/05-04-SUMMARY.md`。
</output>
