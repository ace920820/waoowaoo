---
phase: 05-remake-project-core-workbench
plan: 06
type: execute
wave: 6
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - tests/integration/api/remake-project-phase5-compatibility.test.ts
  - .planning/phases/05-remake-project-core-workbench/05-06-VERIFICATION.md
autonomous: true
requirements: [RMP-01, RMP-02, RMP-03, RMP-04, RMP-05, RMP-06, TASK-01, TASK-02, TASK-03]
must_haves:
  truths:
    - "D-01..D-14：视频翻拍项目、稳定 Shot/revision/provenance、统一 Task/GraphRun、人工审核和人工剪辑边界均有跨层回归证据，旧项目行为保持不变。"
    - "D-15/D-16：最终系统实际加载 05-04 canonical vendored 完整 `SceneDetectEmbeddedApp`，来源/version/hash/sync/allowedPatches 可审计。"
    - "D-17：05-02 adapter、integration runtime 和 05-05 StageHost 使用 SceneDetect 原生 `SceneDetectProject`、`Shot`、`VideoMetadata`，不存在 Waoo DTO 驱动的子组件重组。"
    - "D-18/D-19：多视口证据确认浅色 Waoo 外壳与深色 SceneDetect stage 隔离，只有 manifest 明示的嵌入 patch 面。"
    - "D-20：vendor provenance、native type/import graph 和 no-duplicate guards 在完整 UI 接入后全部通过。"
    - "9/9 Phase 5 requirements 有可执行证据；SHOT-01..08 仍为 Phase 6 Pending，compile-only/test host 不冒充真实上传、分析、编辑或关键帧回写。"
  artifacts:
    - path: "tests/integration/api/remake-project-phase5-compatibility.test.ts"
      provides: "旧模式、双向 adapter、Task/runtime、snapshot 和敏感边界跨层回归"
    - path: ".planning/phases/05-remake-project-core-workbench/05-06-VERIFICATION.md"
      provides: "D-01..D-20、9/9 requirements、正反复用门禁和 Phase 6 handoff 记录"
  key_links:
    - from: "tests/integration/api/remake-project-phase5-compatibility.test.ts"
      to: "05-01..05-05 implementation surfaces"
      via: "真实测试数据库上的 Project -> adapter -> Shot -> Task/GraphRun -> workbench tracer"
    - from: ".planning/phases/05-remake-project-core-workbench/05-06-VERIFICATION.md"
      to: "vendor/type/import/UI/API test evidence"
      via: "逐项映射 D-01..D-20、RMP-01..06、TASK-01..03 与未完成 SHOT-01..08"
  prohibitions:
    - id: P-01
      statement: "不得在 canonical vendor 外重写或重新编排 SceneDetect detector/editor/App.tsx。"
      verification: "tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
    - id: P-02
      statement: "不得绕过 canonical complete-App 入口、原生类型或 manifest/hash/allowedPatches。"
      verification: "tests/contracts/remake-scenedetect-native-contract.test.ts + tests/guards/remake-scenedetect-vendor-provenance.test.ts"
    - id: P-03
      statement: "不得把 IndexedDB/runtime URL/本地 JSON、第二套 queue 或未脱敏执行信息变成 Waoo 事实来源。"
      verification: "tests/integration/api/remake-project-phase5-compatibility.test.ts + npm run check:test-tasktype-coverage"
    - id: P-04
      statement: "不得用 test-only host 关闭 SHOT-01..08 或声称 Phase 6 已完成。"
      verification: ".planning/phases/05-remake-project-core-workbench/05-06-VERIFICATION.md"
---

<objective>
汇总 Phase 5 的旧模式兼容、canonical SceneDetect 整应用复用门禁和目标回归，形成可审计的 Phase 6 handoff。

<purpose>把“平台框架已经准备好”和“真实 SceneDetect 接入尚未交付”清楚分开，防止计划完成状态越界。</purpose>
<output>跨层 compatibility regression 与 Phase 5 goal-backward verification record。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-01-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-02-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-03-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-04-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-05-PLAN.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 完成跨层兼容回归与最终验证记录</name>
  <read_first>
    - `tests/integration/api/remake-project-core.test.ts`
    - `tests/integration/api/remake-projects-scenedetect-import.test.ts`
    - `tests/integration/api/remake-projects-task-projection.test.ts`
    - `tests/guards/remake-scenedetect-vendor-provenance.test.ts`
    - `tests/guards/remake-scenedetect-no-duplicate-source.test.ts`
    - `tests/e2e/remake-workbench-responsive.spec.ts`
  </read_first>
  <files>tests/integration/api/remake-project-phase5-compatibility.test.ts, .planning/phases/05-remake-project-core-workbench/05-06-VERIFICATION.md</files>
  <action>通过真实测试数据库验证旧 project type、remake 创建、adapter fixture 双向转换、snapshot 恢复、Task/runtime port、revision/invalidation、权限和敏感字段 allowlist。重新运行 05-04 vendor/hash/native type/no-duplicate guards和 05-05 UI/E2E 证据。verification record 逐项映射 D-01..D-20 和 9/9 requirements，并明确生产 SceneDetect stage 仍 disabled，Phase 6 才注入真实 Waoo runtime 并交付上传、分析、完整 App 编辑和关键帧回写；compile/test harness 不可关闭 SHOT-01..08。</action>
  <verify>
    <automated>node scripts/vendor-scenedetect.mjs --check &amp;&amp; npm run check:requirements-matrix &amp;&amp; npm run check:test-tasktype-coverage &amp;&amp; npm run typecheck &amp;&amp; npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/guards/remake-scenedetect-vendor-provenance.test.ts tests/guards/remake-scenedetect-no-duplicate-source.test.ts &amp;&amp; BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-phase5-compatibility.test.ts &amp;&amp; npx playwright test tests/e2e/remake-workbench-responsive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 9/9 Phase 5 requirements 与计划任务一一对应；SHOT-01..08 保持 Phase 6 Pending。
    - 旧模式、双向 adapter、canonical host、统一 Task/GraphRun 和 Waoo snapshot 兼容矩阵通过。
    - source/hash/type/import/no-duplicate guards 与完整 UI 接入后仍通过。
    - verification 明确未实现的 Phase 6 行为，不把 vendoring、host 或 fixture 误报为真实编辑闭环。
  </acceptance_criteria>
  <done>Phase 5 有可审计的目标回归、整应用复用证据和明确 Phase 6 handoff。</done>
</task>
</tasks>

<verification>
1. `node scripts/vendor-scenedetect.mjs --check` 与 native type/import/no-duplicate guards 通过。
2. `npm run check:requirements-matrix && npm run check:test-tasktype-coverage` 确认 9/9 Phase 5 requirements，SHOT-01..08 未提前关闭。
3. `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-phase5-compatibility.test.ts` 验证真实数据库 tracer 和旧模式兼容。
4. `npx playwright test tests/e2e/remake-workbench-responsive.spec.ts` 的最终结果写入 verification record。
</verification>

<success_criteria>
Phase 5 的 Waoo 平台框架、canonical SceneDetect 完整 App 宿主和复用门禁均有跨层证据，同时 Phase 6 的真实接入范围保持明确未完成。
</success_criteria>
