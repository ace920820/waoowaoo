---
phase: 05-remake-project-core-workbench
plan: 05
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - package.json
  - package-lock.json
  - src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx
  - src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css
  - src/lib/query/keys.ts
  - src/lib/query/hooks/useRemakeProject.ts
  - messages/zh/remake-workbench.json
  - messages/en/remake-workbench.json
  - tests/unit/remake-projects/remake-workbench-contract.test.tsx
  - tests/integration/api/remake-projects-workbench.test.ts
  - playwright.config.ts
  - tests/e2e/remake-workbench-responsive.spec.ts
autonomous: true
requirements: [RMP-02, RMP-03, RMP-04, RMP-05, TASK-03]
must_haves:
  truths:
    - "D-01/D-02/D-11/D-12：新增视频翻拍项目和创作型 Waoo 工作台壳，旧模式保持不变；Phase 5 提供只读概览和 disabled SceneDetect stage。"
    - "D-03/D-04/D-05/D-06/D-10：概览从真实 Waoo snapshot/Task 读取稳定 Shot/revision/media/provenance，fixture tracer 保留旧结果并传播 needs_review。"
    - "D-07/D-08/D-09：SceneDetect 与后续能力共享既有 Task/GraphRun；业务 UI 不启动 FastAPI/CLI，也不新增 Task Center。"
    - "D-13/D-14：人工审核独立于 Task 状态；自动剪辑、节奏、配乐、音效和发布仍在 v1.1 范围外。"
    - "D-15/D-16/D-17：StageHost 只从 05-04 canonical index 加载完整 `SceneDetectEmbeddedApp`，props 使用原生 initial project + `SceneDetectIntegrationRuntime`，不接 normalized Waoo Shot DTO。"
    - "D-18/D-19：Waoo 外壳保持浅色 glass，SceneDetect stage 保留原深色视觉并隔离；Task drawer overlay，不用概览三栏重排或压缩原编辑器。"
    - "D-20：StageHost 实现后再次运行 05-04 native type/import graph/no-duplicate guards；任何 deep import、复制或子组件重编排都阻断本计划。"
    - "Phase 5 生产 SceneDetect stage 保持 disabled；test-only native fixture 只验证宿主兼容，Phase 6 才注入真实 runtime 并开放上传、分析和编辑回写。"
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx"
      provides: "浅色翻拍外壳、只读概览、阶段导航和 overlay Task 入口"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx"
      provides: "canonical 完整 SceneDetect App 的全阶段挂载面，不含第二套编辑状态机"
    - path: "src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css"
      provides: "SceneDetect 深色 stage 与 Waoo glass 外壳的 scoped visual boundary"
    - path: "src/lib/query/hooks/useRemakeProject.ts"
      provides: "Waoo snapshot/Task 查询与失效刷新"
    - path: "tests/e2e/remake-workbench-responsive.spec.ts"
      provides: "概览与完整 App test harness 的多视口、样式隔离和 containment 证据"
  key_links:
    - from: "SceneDetectStageHost.tsx"
      to: "src/vendor/scenedetect/index.ts"
      via: "只导入 canonical `SceneDetectEmbeddedApp` 与原生类型；禁止 vendor 子组件 deep import"
    - from: "SceneDetectStageHost.tsx"
      to: "src/lib/remake-projects/scenedetect/integration-runtime.ts"
      via: "projectId + SceneDetect-native initial project + minimal runtime；不接 normalized Waoo Shot props"
    - from: "useRemakeProject.ts"
      to: "/api/projects/[projectId]/data"
      via: "复用 05-01 canonical typed snapshot GET 与 React Query key"
    - from: "RemakeWorkbench.tsx"
      to: "SceneDetectStageHost.tsx"
      via: "视频分析/镜头审核的独立全宽 stage；Phase 5 disabled，Phase 6 enable"
  prohibitions:
    - id: P-01
      statement: "不得在 Waoo 重新实现或重新编排 SceneDetect App.tsx、Timeline、ShotInspector、KeyframeSelector、undo/redo、分析弹窗或编辑状态机。"
      verification: "05-04 tests/guards/remake-scenedetect-no-duplicate-source.test.ts"
    - id: P-02
      statement: "不得让 StageHost 接收 normalized Waoo Shot DTO 后自行拼装编辑器；合同必须来自 SceneDetect 原生类型。"
      verification: "05-04 tests/contracts/remake-scenedetect-native-contract.test.ts + remake-workbench-contract.test.tsx"
    - id: P-03
      statement: "不得把 SceneDetect IndexedDB、Blob/runtime URL 或本地项目 JSON 接入 Waoo 持久化事实来源。"
      verification: "tests/integration/api/remake-projects-workbench.test.ts"
    - id: P-04
      statement: "不得用 Waoo 概览三栏包裹 SceneDetect stage，或用 glass 覆盖其深色主题。"
      verification: "tests/e2e/remake-workbench-responsive.spec.ts"
---

<objective>
在 05-04 的 canonical SceneDetect App 和原生 runtime 合同之上，建设可恢复的 Waoo 翻拍工作台与完整 App StageHost，并完成查询与响应式嵌入验证。

<purpose>Phase 5 交付一个不会迫使 Phase 6 重写 SceneDetect 的宿主框架：Waoo 管项目、导航、Task 和持久化，SceneDetect 完整 App 保留内部布局与编辑编排。</purpose>
<output>Waoo 工作台壳、全宽 SceneDetect StageHost、真实 query hooks、样式隔离和多视口验证。</output>
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-UI-SPEC.md
@.planning/phases/05-remake-project-core-workbench/05-PATTERNS.md
@.planning/phases/05-remake-project-core-workbench/05-01-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-02-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-03-PLAN.md
@.planning/phases/05-remake-project-core-workbench/05-04-PLAN.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立 Waoo 外壳与完整 App StageHost</name>
  <read_first>
    - `05-UI-SPEC.md`
    - `src/app/[locale]/workspace/[projectId]/page.tsx`
    - `src/vendor/scenedetect/index.ts`
    - `src/lib/remake-projects/scenedetect/integration-runtime.ts`
    - `/Volumes/KINGSTON/projects/SceneDetect/src/App.tsx`
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/SceneDetectStageHost.tsx, src/app/[locale]/workspace/[projectId]/modes/remake/scenedetect/scenedetect-stage.css, messages/zh/remake-workbench.json, messages/en/remake-workbench.json, tests/unit/remake-projects/remake-workbench-contract.test.tsx</files>
  <action>实现浅色 Waoo 项目栏、阶段导航、项目概览摘要/只读 Shot/Task 状态和 overlay Task drawer。`SceneDetectStageHost` 只从 canonical index 加载完整 `SceneDetectEmbeddedApp`，props 限定为 projectId、SceneDetect-native initial project、`SceneDetectIntegrationRuntime` 和 enabled/availability；不得传 normalized Waoo Shot snapshot、deep-import 或重排 vendor 子组件。Phase 5 生产环境 `enabled=false` 并显示真实接入状态；test-only harness 可挂载只读 native fixture 验证宿主合同，不提交分析或写库。scoped root 保留 SceneDetect 深色视觉并隔离 Waoo glass，Task drawer 使用 overlay；只在 host 处理可用高度、overflow、modal portal/keyboard containment，不改原布局。项目管理由 Waoo 外壳承担，导出保持 Phase 11 未开放策略。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/contracts/remake-scenedetect-native-contract.test.ts tests/unit/remake-projects/remake-workbench-contract.test.tsx tests/guards/remake-scenedetect-no-duplicate-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 概览三栏与 SceneDetect 全宽 stage 是独立布局；Task drawer 不新增常驻列压缩 editor。
    - host import graph 只有 canonical complete-App 入口，不存在 deep import、第二套 editor state 或 Waoo Shot props 编排。
    - SceneDetect scoped root 保留原深色视觉和组件层级，样式不泄漏到 Waoo 外壳。
    - 未导入、查询失败、Task 失败和未开放阶段都有中英可理解状态。
  </acceptance_criteria>
  <done>Waoo 已有可在 Phase 6 原样承载 SceneDetect 完整应用的稳定页面框架。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 连接真实查询并验证响应式嵌入边界</name>
  <read_first>
    - `src/lib/query/keys.ts`
    - `src/lib/query/hooks/useProjectData.ts`
    - `src/lib/query/hooks/useTaskStatus.ts`
    - `src/app/api/projects/[projectId]/data/route.ts`
    - `tests/integration/api/remake-project-core.test.ts`
  </read_first>
  <files>package.json, package-lock.json, src/lib/query/keys.ts, src/lib/query/hooks/useRemakeProject.ts, src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx, tests/integration/api/remake-projects-workbench.test.ts, playwright.config.ts, tests/e2e/remake-workbench-responsive.spec.ts</files>
  <action>增加翻拍 snapshot/Task query key 和 hook，统一读取 05-01 canonical `GET /api/projects/[projectId]/data` typed dispatch，不另造第二套 snapshot/Task route。URL 只保存 projectId/stage/shot/task 视图状态；adapter 导入后精确失效相关 key，不维护客户端 `SceneDetectProject` 持久化镜像。固定 Playwright devDependency/config，用已认证 remake fixture 验证概览在 1440x900、1024x768、390x844 的三/二/一栏；再用 test-only native StageHost harness 验证完整 SceneDetect App 在三档视口保持原布局/深色视觉、无页面级横向溢出、Task drawer overlay、modal/root height containment，且 DOM/import contract 中没有 Waoo 生成的第二套 Timeline/ShotInspector。生产 stage 仍 disabled。</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; BILLING_TEST_BOOTSTRAP=0 npx vitest run tests/integration/api/remake-projects-workbench.test.ts &amp;&amp; npx playwright test tests/e2e/remake-workbench-responsive.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 刷新和重新打开项目可从 API 恢复概览选择与 Task 状态；URL 不含 runtime 路径或敏感字段。
    - 三档视口的外壳和 SceneDetect host 均无页面级横向溢出、文字遮挡或 incoherent overlap。
    - test harness 中原布局/深色视觉存在，glass 不进入 scoped root，Task drawer 和 modal 层级可用。
    - Phase 5 生产页面不开放上传、分析、保存、拆分、合并、删除或关键帧编辑命令。
  </acceptance_criteria>
  <done>工作台连接 Waoo 唯一事实来源，并以可视证据证明完整 App 可安全嵌入。</done>
</task>

</tasks>

<verification>
1. 05-04 `node scripts/vendor-scenedetect.mjs --check`、native type/import/no-duplicate guards 全部通过。
2. `npm run typecheck && npx vitest run tests/unit/remake-projects/remake-workbench-contract.test.tsx tests/integration/api/remake-projects-workbench.test.ts`。
3. `npx playwright test tests/e2e/remake-workbench-responsive.spec.ts`：概览与 test-only SceneDetect host 在三档视口验证原布局、深色隔离、overlay drawer、modal containment、无重复 editor 和无页面横向溢出。
</verification>

<success_criteria>
Waoo 已有可恢复的翻拍外壳，以及只从 canonical source 挂载、原生类型兼容、可在 Phase 6 整体启用的 SceneDetect App host；没有重新实现 SceneDetect 页面或 `App.tsx` 编排。
</success_criteria>
