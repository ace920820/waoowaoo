---
phase: 05-remake-project-core-workbench
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified:
  - tests/unit/prisma/remake-project-core-schema.test.ts
  - tests/integration/api/remake-project-phase5-edges.test.ts
  - tests/integration/api/specific/project-create-remake-compat.test.ts
  - tests/unit/remake-projects/remake-workbench-ui.test.ts
autonomous: false
requirements:
  - RMP-01
  - RMP-02
  - RMP-03
  - RMP-04
  - RMP-05
  - RMP-06
  - TASK-01
  - TASK-02
  - TASK-03
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "RMP-01..06 与 TASK-01..03 的 40/40 applicable edge 均映射到具名真实 DB/API/UI 断言；schema、旧模式兼容、跨租户、恢复、历史保留、Task 单调状态与脱敏全部通过。"
    - "D-01..D-14 均有计划与测试落点；Phase 6-11 的上传/SceneDetect 时间轴、Prompt 编辑/版本比较、图片/视频生成、批量编排和导出均没有 Phase 5 可执行入口；自动最终剪辑、音乐/音频和发布由额外里程碑范围边界 MB-01 单独约束。"
    - "Plan 01 的 `[BLOCKING] npx prisma db push && npx prisma generate` 保持为真实 DB 回归前置门禁；schema contract 验证稳定 Shot、Project 所属、Task target、provenance、失效历史与加法式旧数据兼容。"
    - statement: "加载与后台刷新状态必须保持项目栏、阶段栏、摘要、Shot 列表、Shot 详情和 Task 面板的稳定尺寸；桌面、平板、手机视觉回归均不得出现布局跳变、全屏 spinner 取代已知结构或伪进度。"
      verification: backstop
    - statement: "阶段导航、三栏面板、抽屉、项目/Shot 名称、稳定 ID、模型或 Skill 标识及错误文本在桌面、平板、手机和超长内容样本下必须按合同滚动、换行或省略；不得产生页面级横向滚动、文字遮挡或按钮压缩。"
      verification: backstop
  artifacts:
    - path: "tests/unit/prisma/remake-project-core-schema.test.ts"
      provides: "稳定身份、关系、默认值、唯一约束、索引与向后兼容 schema contract"
    - path: "tests/integration/api/remake-project-phase5-edges.test.ts"
      provides: "9 requirements / 40 edges 的跨计划真实 DB/API 回归"
    - path: "tests/integration/api/specific/project-create-remake-compat.test.ts"
      provides: "既有项目创建、data route、workspace stage/episode 与默认值兼容回归"
    - path: "tests/unit/remake-projects/remake-workbench-ui.test.ts"
      provides: "六条 UI truth、i18n parity、a11y/responsive 结构与视觉 checkpoint 前置证据"
  key_links:
    - from: "tests/integration/api/remake-project-phase5-edges.test.ts"
      to: "Plan 01-04 的真实 routes/services/query/UI contracts"
      via: "逐 requirement/category 组合创建、读取、更新、Task 投影和重启式 refetch"
      pattern: "RMP-(01|02|03|04|05|06)|TASK-(01|02|03)"
    - from: "tests/integration/api/specific/project-create-remake-compat.test.ts"
      to: "src/app/api/projects/route.ts 与 workspace mode dispatch"
      via: "无 type/旧 type 与 remake type 的并列回归"
      pattern: "NovelPromotionProject|RemakeProject"
    - from: "tests/unit/prisma/remake-project-core-schema.test.ts"
      to: "prisma/schema.prisma 与 add_remake_project_core migration"
      via: "模型、FK、默认值、唯一约束、索引和旧数据默认类型断言"
      pattern: "model RemakeProject|model RemakeShot"
    - from: "tests/unit/remake-projects/remake-workbench-ui.test.ts"
      to: "05-UI-SPEC.md 的六条 covered truth 与两条 visual backstop"
      via: "自动结构/a11y/i18n 证据先通过，再进入真实浏览器人工 checkpoint"
      pattern: "remakeWorkbench"
  prohibitions:
    - id: "P-01"
      statement: "MUST NOT 将 Task 成功视为人工批准或采用。"
      status: resolved
      verification: judgment
      source: "canonical-phase-5-prohibition-audit"
    - id: "P-02"
      statement: "MUST NOT 暴露 Codex/SceneDetect CLI 命令、Session、环境变量、密钥或原始执行日志到业务 API/UI。"
      status: resolved
      verification: judgment
      source: "canonical-phase-5-prohibition-audit"
    - id: "P-03"
      statement: "MUST NOT 覆盖或删除旧输出，也不得在上游 Shot 变化后静默沿用过期下游结果。"
      status: resolved
      verification: judgment
      source: "canonical-phase-5-prohibition-audit"
    - id: "P-04"
      statement: "MUST NOT 在增加翻拍模式时改变既有剧本或小说推广项目的行为。"
      status: resolved
      verification: judgment
      source: "canonical-phase-5-prohibition-audit"
    - id: "P-05"
      statement: "MUST NOT 为视频翻拍创建第二套 queue、Task 状态表或 Task Center。"
      status: resolved
      verification: judgment
      source: "canonical-phase-5-prohibition-audit"
---

<objective>
完成 Phase 5 的阶段级回归与诚实验收：锁定 schema/迁移合同、旧模式兼容、9/9 requirements 的 40/40 edge、D-01..D-14、canonical P-01..P-05、额外里程碑范围边界 MB-01，并让两条 UI visual backstop 经过真实桌面/平板/手机人工检查。

Purpose: 在 query/UI 实现完成后，以独立 Wave 4 验证计划阻止跨层回归和视觉自判，保证 Phase 5 可以安全成为 Phase 6-11 的共同底座。
Output: schema contract、40-edge 真实 DB suite、旧项目兼容 suite、UI/i18n focused 回归和一个 blocking human visual checkpoint。
</objective>

<execution_context>
@/Users/jamiezhao/.codex/gsd-core/workflows/execute-plan.md
@/Users/jamiezhao/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-remake-project-core-workbench/05-CONTEXT.md
@.planning/phases/05-remake-project-core-workbench/05-UI-SPEC.md
@.planning/phases/05-remake-project-core-workbench/05-PATTERNS.md
@.planning/phases/05-remake-project-core-workbench/05-01-SUMMARY.md
@.planning/phases/05-remake-project-core-workbench/05-02-SUMMARY.md
@.planning/phases/05-remake-project-core-workbench/05-03-SUMMARY.md
@.planning/phases/05-remake-project-core-workbench/05-04-SUMMARY.md
@prisma/schema.prisma
@prisma/migrations/20260807150000_add_remake_project_core/migration.sql
@tests/integration/api/remake-project-tracer-db.test.ts
@tests/integration/api/remake-project-shots.test.ts
@tests/integration/api/contract/remake-task-projection.test.ts

<interfaces>
Plan 01 tracer：真实 Project -> stable Shot -> Task/GraphRun -> DB snapshot/UI projection -> Shot revision/needs_review，且 schema push/generate 是 DB 测试前置门禁。
Plan 02 domain/API：stable Shot、revision、output/provenance、invalidation、summary、幂等/并发与所有权合同。
Plan 03 Task：既有 Task/GraphRun 事实来源、task_type+executor+capability、safe DTO、target link 与 terminal 单调状态。
Plan 04 UI：React Query + URL 单一事实来源、独立 remakeWorkbench i18n namespace、六条 UI truth 和三/二/一栏工作台实现。
</interfaces>
</context>

## Canonical Phase 5 Prohibition Audit

本表是 Phase 5 bespoke prohibition 的唯一规范来源。canonical 集合固定为五项 P-01..P-05；`05-04` 与本计划 frontmatter 中的 `must_haves.prohibitions` 只是逐字投影，必须保持相同 ID、statement、顺序和 descriptor-less `resolved/judgment` 形状。任何增删或改写先修改本表，再同步投影，不得从 task prose 反推集合。

| ID | Canonical statement | Named automated assertion | Evidence file |
|---|---|---|---|
| P-01 | MUST NOT 将 Task 成功视为人工批准或采用。 | `[P-01] task completion never mutates human review or adoption` | `tests/integration/api/remake-project-phase5-edges.test.ts` |
| P-02 | MUST NOT 暴露 Codex/SceneDetect CLI 命令、Session、环境变量、密钥或原始执行日志到业务 API/UI。 | `[P-02] business projections redact executor internals` | `tests/integration/api/remake-project-phase5-edges.test.ts` |
| P-03 | MUST NOT 覆盖或删除旧输出，也不得在上游 Shot 变化后静默沿用过期下游结果。 | `[P-03] upstream Shot changes preserve outputs and force needs_review` | `tests/integration/api/remake-project-phase5-edges.test.ts` |
| P-04 | MUST NOT 在增加翻拍模式时改变既有剧本或小说推广项目的行为。 | `[P-04] remake mode preserves legacy project and workspace behavior` | `tests/integration/api/specific/project-create-remake-compat.test.ts` |
| P-05 | MUST NOT 为视频翻拍创建第二套 queue、Task 状态表或 Task Center。 | `[P-05] remake reuses Task and GraphRun without a parallel lifecycle` | `tests/integration/api/remake-project-phase5-edges.test.ts` |

## Additional Milestone Scope Boundary

`MB-01`（D-14）是 canonical 五项之外的额外里程碑范围边界：Phase 5 MUST NOT 提供自动最终剪辑、音乐/音频或发布能力。它由具名断言 `[MB-01] Phase 5 exposes no automatic final editing music or publishing action` 在 `tests/unit/remake-projects/remake-workbench-ui.test.ts` 验证；MB-01 不改变 P-01..P-05 的数量或口径。

## Artifacts this phase produces

| Path | Created/extended symbols |
|---|---|
| `tests/unit/prisma/remake-project-core-schema.test.ts` | `Project.type/creationRequestId`、Remake 核心模型、FK、唯一约束、索引、默认值与 migration parity 断言 |
| `tests/integration/api/remake-project-phase5-edges.test.ts` | RMP-01..06、TASK-01..03 的 40 个具名 edge cases，以及 P-01/P-02/P-03/P-05 断言 |
| `tests/integration/api/specific/project-create-remake-compat.test.ts` | 旧 request/body、NovelPromotionProject、project data route、workspace stage/episode/default 与 P-04 兼容断言 |
| `tests/unit/remake-projects/remake-workbench-ui.test.ts` | 六条 UI truth、zh/en key parity、a11y/responsive/long-content structure 与 MB-01 断言 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [BLOCKING] 锁定 schema、40 项 edge、旧模式兼容与 Phase 6-11 排除边界</name>
  <read_first>
    - `.planning/REQUIREMENTS.md`：RMP-01..06 与 TASK-01..03 每个 requirement/category 必须有具名断言。
    - `05-CONTEXT.md`：D-01..D-14 全覆盖，Deferred Ideas 和 Phase 6-11 边界不得进入实现。
    - `05-UI-SPEC.md`：六条 covered truth 与两条 backstop 的原始合同。
    - `prisma/schema.prisma` 与 `prisma/migrations/20260807150000_add_remake_project_core/migration.sql`：验证 Plan 01 的稳定身份、关系、默认值、FK、唯一约束与索引。
    - `tests/unit/prisma/remake-project-core-schema.test.ts`：新增/完善 schema 与 migration parity 断言。
    - `tests/integration/api/remake-project-phase5-edges.test.ts`：新增按 requirement/category 命名的 40-edge suite。
    - `tests/integration/api/specific/project-create-remake-compat.test.ts`：集中旧项目创建、读取和 workspace 兼容回归。
    - `tests/unit/remake-projects/remake-workbench-ui.test.ts`：保留 Plan 04 六条 UI truth 并补 i18n parity/排除边界断言。
    - 本计划 `## Canonical Phase 5 Prohibition Audit`：P-01..P-05 的唯一 statement、顺序、具名断言和 evidence file 来源。
  </read_first>
  <files>tests/unit/prisma/remake-project-core-schema.test.ts, tests/integration/api/remake-project-phase5-edges.test.ts, tests/integration/api/specific/project-create-remake-compat.test.ts, tests/unit/remake-projects/remake-workbench-ui.test.ts</files>
  <behavior>
    - schema contract 覆盖 stable UUID Shot、Project 所属、sequence/clientRequestId/revision/output operationKey 唯一性、Task target 索引、provenance/invalidation 关系、旧 Project 默认类型及 migration parity。
    - RMP-01 覆盖 idempotency/concurrency；RMP-02 覆盖 boundary/adjacency/empty/ordering/precision。
    - RMP-03 覆盖 adjacency/empty/encoding/ordering/idempotency/concurrency；RMP-04 覆盖 idempotency/concurrency。
    - RMP-05 覆盖 adjacency/empty/ordering/idempotency/concurrency；RMP-06 覆盖 adjacency/empty/encoding/ordering/idempotency/concurrency。
    - TASK-01 覆盖 adjacency/empty/ordering/idempotency/concurrency；TASK-02 覆盖 empty/encoding/idempotency/concurrency；TASK-03 覆盖 adjacency/empty/ordering/idempotency/concurrency。
    - 无 type 与既有 type 仍创建原 NovelPromotionProject 并保留 workspace stage/episode/default；显式 remake 才进入新分支。
    - `[P-01] task completion never mutates human review or adoption` 证明 Task terminal success 不写 approved/adopted。
    - `[P-02] business projections redact executor internals` 证明业务 DTO/UI 不可达 CLI/session/env/secret/raw log。
    - `[P-03] upstream Shot changes preserve outputs and force needs_review` 证明旧输出留存且 stale 下游显式复核。
    - `[P-04] remake mode preserves legacy project and workspace behavior` 证明旧项目创建、读取、stage/episode/default 不变。
    - `[P-05] remake reuses Task and GraphRun without a parallel lifecycle` 证明 schema/route/service 复用现有 Task/GraphRun，且不存在 RemakeTask/RemakeQueue/第二 Task Center。
    - `[MB-01] Phase 5 exposes no automatic final editing music or publishing action` 单独证明 D-14 范围边界，不计入 canonical 五项。
  </behavior>
  <action>先建立失败断言，再组合 Plan 01-04 的真实 DB/API/UI 合同验证全部 40 项 edge。schema test 逐模型检查 UUID、FK/删除策略、唯一约束、索引、旧数据默认 type 和 migration SQL parity。项目创建断言重复/并发 dedupe 与事务回滚；summary/Shot/provenance/Task 断言 zero-one-many、稳定排序、null、Unicode、0..100 或 null progress；Shot 更新断言 revision conflict、重复失效、旧版本保留和晚到输出不能回退 needs_review；Task 断言 canonical dedupe、技术 tuple 校验、terminal 优先和 safe DTO；重建查询客户端后再次读取验证 restart/refetch。严格按 canonical audit 的标题在对应文件新增五个 `it`/`test` 断言：P-01/P-02/P-03/P-05 写入 phase5 edge suite，P-04 写入 legacy compat suite；P-05 同时检查真实初始化/查询只使用现有 Task+GraphRun、schema 没有平行 Task 状态模型、工作台 Task 面板消费现有 safe Task route 而非第二 Task Center。将 D-14 作为独立 MB-01 写入 UI suite，验证没有自动最终剪辑、音乐/音频或发布命令；不得用 MB-01 替换 P-05，也不得把 MB-01 计入 canonical 五项。对 D-01 至 D-14 建立可追踪断言并保持 Phase 6-11 控件缺席；修复仅限 Phase 5 相关实现，不改既有产品语义。</action>
  <verify>
    <automated>npx prisma validate &amp;&amp; BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/unit/prisma/remake-project-core-schema.test.ts tests/integration/api/remake-project-phase5-edges.test.ts tests/integration/api/specific/project-create-remake-compat.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts &amp;&amp; npm run check:test-tasktype-coverage &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 9/9 requirements 与 40/40 applicable edge 均映射到具名自动化断言，无 unresolved 行。
    - D-01..D-14 每项至少有一个 plan/task/test 引用，且 Phase 6-11 exclusions 没有可执行路由或控件。
    - schema/migration、旧模式兼容、跨租户授权、敏感数据脱敏、历史不可覆盖、terminal/review 不回退和六条 UI truth 全部通过。
    - canonical P-01..P-05 各自存在与审计表标题逐字一致的具名自动化断言；任一缺失或失败都阻塞 Task 2 和本计划完成。
    - MB-01 以独立具名断言覆盖自动最终剪辑、音乐/音频和发布边界，且测试/报告不得把它计为第六条 prohibition 或替代 P-05。
  </acceptance_criteria>
  <done>schema、40/40 edge、D-01..D-14、9/9 requirements、六条 UI truth、旧模式兼容、canonical P-01..P-05 与额外 MB-01 的具名自动门禁全部通过，方可进入人工 visual backstop。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] 在真实工作台验收两条 UI visual backstop</name>
  <read_first>
    - `05-UI-SPEC.md` 的布局与响应式合同、UI Considerations 两条 Verification backstops：以原始视觉合同作为唯一判定标准。
    - `tests/unit/remake-projects/remake-workbench-ui.test.ts`：确认 Task 1 的结构、a11y、i18n 与 responsive 前置证据已通过。
    - `src/app/[locale]/workspace/[projectId]/modes/remake/RemakeWorkbench.tsx`、`RemakeWorkbenchChrome.tsx`、`RemakeShotWorkspace.tsx`、`RemakeTaskPanel.tsx`：定位真实页面与抽屉状态，不使用静态替代页。
  </read_first>
  <action>仅在 Task 1 的完整自动命令退出码为 0 后启动本地 Next.js 应用，并打开由真实 API/数据库创建的视频翻拍项目。准备包含长中英文项目名、长 Shot 名、长稳定 ID/模型或 Skill 标识、长脱敏错误以及后台 refetch 的真实状态；将视口依次设为 1280px、768px 和 390px。此任务只收集人工可见证据并等待用户结论，不以组件类名、单元测试或执行器自评代替视觉验收。</action>
  <verify>
    <automated>npx vitest run tests/unit/remake-projects/remake-workbench-ui.test.ts</automated>
    <human-check>按 how-to-verify 在 1280px、768px、390px 的真实工作台逐项检查两条 structured backstop；未获得用户明确 approved 前保持阻塞。</human-check>
  </verify>
  <what-built>真实 DB/API 驱动的翻拍 overview，包括项目栏、七阶段栏、六项摘要、Shot 列表/详情、provenance、Task panel/right drawer/bottom drawer、加载/refetch/错误/长内容状态。</what-built>
  <how-to-verify>
    1. 在 1280px 检查 288px / minmax(520px,1fr) / 320px 三栏、64px 项目栏、44px 阶段栏和 112px 摘要单元；触发首次加载与后台 refetch，确认已知结构不被全屏 spinner 或伪进度替代，且尺寸不跳变。
    2. 在 768px 检查 280px + minmax(488px,1fr) 双栏和 400px 右侧 Task 抽屉；打开/关闭抽屉并切换 Shot/Task，确认焦点、滚动与 URL 选择稳定。
    3. 在 390px 检查 56px 项目栏、横向阶段栏、两列摘要、单栏 Shot/详情与底部 Task 抽屉；返回、任务、关闭等命令保持至少 44px 点击区。
    4. 使用长中英文名称、stable ID、模型/Skill 和脱敏错误文本，确认合同要求的省略、两行、换行或局部横向滚动生效；页面级无横向滚动、文字遮挡或按钮压缩。
    5. 确认 future stages 仅锁定展示，页面没有假播放器、假数据、时间轴、Prompt/版本、生成参数、批量或导出入口；另按 MB-01 确认没有自动最终剪辑、音乐/音频或发布入口。
  </how-to-verify>
  <acceptance_criteria>
    - 两条 flat `verification: backstop` truth 均有 1280px、768px、390px 的真实可见证据。
    - 用户明确输入 `approved`；任何布局跳变、页面级横向滚动、遮挡、压缩或错误 drawer/focus 行为都必须返回 Plan 04 相关实现修复并重跑 Task 1。
    - checkpoint 不接受自动批准、截图文件存在、测试通过或执行器陈述作为用户视觉结论的替代品。
  </acceptance_criteria>
  <done>用户已明确批准两条 visual backstop，或已描述问题并使本计划保持阻塞等待修复。</done>
  <resume-signal>Type "approved" or describe the visual/layout issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Cross-plan implementation -> final regression suites | schema、API、Task、query 和 UI 的组合行为可能在单计划测试均通过时仍发生跨层回归 |
| Test identity/fixtures -> real test database | 测试身份、projectId/shotId 与清理逻辑必须避免跨租户误读或污染固定数据 |
| Internal Task/Run/provenance fixtures -> serialized assertions/UI | 敏感执行上下文可能通过回归 fixture 或 UI 详情重新暴露 |
| Existing workspace -> remake compatibility suite | 新类型、i18n 和 mode dispatch 可能改变无 type 的旧创建与 stage/episode 行为 |
| Automated UI assertions -> human visual checkpoint | 结构测试无法证明三断点布局、滚动、遮挡与真实加载状态的视觉正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-51 | Spoofing / Elevation | final cross-tenant API suite | high | blocked_until_verified | 40-edge suite 通过 route handler、真实 DB 和独立 UUID fixture 验证未登录/他人 project/不匹配 target，查询始终受 project ownership 限制。 |
| T-05-52 | Tampering / Repudiation | schema/revision/output history | high | blocked_until_verified | schema contract 与 40-edge suite 共同验证 FK/唯一约束、expectedRevision、追加历史、operationKey、invalidation 和晚到输出单调性。 |
| T-05-53 | Information Disclosure | Task/provenance/error projection | high | blocked_until_verified | 40-edge/UI suites 对 raw payload/result/log/env/session/secret 使用不可达断言，只接受 safe DTO 与规范化错误。 |
| T-05-54 | Elevation / Regression | legacy project/workspace behavior | high | blocked_until_verified | 专用 compat suite 锁定无 type/旧 type、NovelPromotionProject、data route、stage/episode/default 与项目列表。 |
| T-05-55 | Spoofing / Denial of Service | responsive/loading visual state | medium | mitigate | focused UI test 先验证结构，随后 blocking human checkpoint 在 1280/768/390px 检查真实 loading/refetch、滚动、drawer 和长内容。 |

## OWASP ASVS L1 Control-to-Evidence Mapping

| ASVS L1 control area | Threats | Task / control | Named automated evidence |
|---|---|---|---|
| V4 Access Control | T-05-51 | Task 1 跨租户/target/project ownership 真实 DB 回归 | `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-phase5-edges.test.ts` |
| V1 Architecture and V5 Validation | T-05-52 | Task 1 schema/migration parity、revision/idempotency/concurrency edge | `npx prisma validate`；`BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/unit/prisma/remake-project-core-schema.test.ts tests/integration/api/remake-project-phase5-edges.test.ts` |
| V7 Error/Logging and V8 Data Protection | T-05-53 | Task 1 safe DTO、规范化错误与敏感 fixture 不可达 | `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/remake-project-phase5-edges.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts` |
| V1 Architecture and V4 Access Control | T-05-54 | Task 1 持久化 Project.type 分派与旧 workspace 合同隔离 | `BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/integration/api/specific/project-create-remake-compat.test.ts` |
| V1 Architecture / client integrity | T-05-55 | Task 1 UI structure evidence；Task 2 真实三断点人工 backstop | `npx vitest run tests/unit/remake-projects/remake-workbench-ui.test.ts` |

**ASVS L1 blocking rule:** 本计划在上述每条 named automated evidence 通过前不得完成；所有 high 威胁保持 `blocked_until_verified`，不得仅凭实现存在改记为已缓解。自动门禁全绿后仍必须完成 Task 2 的 blocking human checkpoint，人工 backstop 不替代任何 high-threat 自动证据。
</threat_model>

<verification>
1. Plan 01 的 `[BLOCKING] npx prisma db push && npx prisma generate` 已成功完成；否则本计划不得运行真实 DB suites。
2. `npx prisma validate && BILLING_TEST_BOOTSTRAP=1 npx vitest run tests/unit/prisma/remake-project-core-schema.test.ts tests/integration/api/remake-project-phase5-edges.test.ts tests/integration/api/specific/project-create-remake-compat.test.ts tests/unit/remake-projects/remake-workbench-ui.test.ts && npm run check:test-tasktype-coverage && npm run typecheck` 必须通过。
3. ASVS L1 表中的每条 named automated evidence 都是完成门禁；任一失败时对应 high threat 继续阻塞本计划。
4. <human-check>Task 2 在 1280px、768px、390px 的真实工作台验证两条 structured visual backstop；用户未明确 `approved` 前不得完成本计划。</human-check>
</verification>

<success_criteria>
- 9/9 Phase 5 requirements、D-01..D-14 和 40/40 applicable edge 均有具名通过的自动化证据。
- Plan 01 的 schema push gate 保持阻塞顺序，final schema contract 证明稳定 Shot/Task/provenance/失效关系与旧 Project 数据兼容。
- Plan 04 的六条 UI truth 自动回归通过；两条 flat structured backstop 获得用户在 1280px、768px、390px 的明确视觉批准。
- canonical P-01..P-05 以唯一审计表、相同 frontmatter 投影和五个逐字具名自动断言保持覆盖；P-05 明确阻止第二套 queue、Task 状态表或 Task Center。
- MB-01 作为 canonical 五项之外的额外里程碑范围边界，证明 Phase 5 没有自动最终剪辑、音乐/音频或发布入口。
- 所有 high threat 均映射到 named automated evidence 并在证据通过前保持阻塞；旧剧本/小说推广创建、路由、stage、episode 和运行语义保持不变。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/05-remake-project-core-workbench/05-05-SUMMARY.md`。
</output>
