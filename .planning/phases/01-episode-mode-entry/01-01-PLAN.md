---
phase: 01-episode-mode-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260419120000_add_episode_production_mode/migration.sql
  - src/app/api/novel-promotion/[projectId]/episodes/route.ts
  - src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts
  - src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts
  - src/lib/query/hooks/useProjectData.ts
  - src/types/project.ts
  - src/app/[locale]/workspace/[projectId]/page.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/types.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts
  - tests/integration/api/contract/novel-promotion-episode-production-mode.test.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-02
must_haves:
  truths:
    - "Each episode carries its own persisted production mode instead of relying on project-global or component-local state."
    - "Newly created episodes resolve to multi-shot mode without requiring manual initialization."
    - "Legacy episodes with persisted traditional storyboard/video traces are backfilled to traditional mode; other legacy episodes are backfilled to multi-shot mode."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Episode-level production mode field with a multi-shot default"
      contains: "episodeProductionMode"
    - path: "prisma/migrations/20260419120000_add_episode_production_mode/migration.sql"
      provides: "Schema + legacy backfill for production mode"
      contains: "ALTER TABLE `novel_promotion_episodes`"
    - path: "src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts"
      provides: "PATCH/GET support for episodeProductionMode"
      exports:
        - "GET"
        - "PATCH"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts"
      provides: "Script-stage episode payload including episodeProductionMode"
      contains: "episodeProductionMode"
  key_links:
    - from: "src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts"
      to: "prisma.novelPromotionEpisode.update"
      via: "PATCH body validation + persistence"
      pattern: "episodeProductionMode"
    - from: "src/lib/query/hooks/useProjectData.ts"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts"
      via: "episode query payload"
      pattern: "episodeProductionMode"
    - from: "prisma/migrations/20260419120000_add_episode_production_mode/migration.sql"
      to: "existing novel_promotion_storyboards / novel_promotion_panels rows"
      via: "legacy backfill CASE expression"
      pattern: "traditional"
---

<objective>
Persist episode-level production mode and expose it through the existing episode data contracts so the script page can read one server-backed source of truth.

Purpose: Phase 1 depends on the episode row, not local UI state, for MODE-01 and MODE-02.
Output: Schema/migration coverage, API contract support, and typed workspace episode data carrying `episodeProductionMode`.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-episode-mode-entry/01-CONTEXT.md
@.planning/phases/01-episode-mode-entry/01-RESEARCH.md
@.planning/phases/01-episode-mode-entry/01-UI-SPEC.md
@prisma/schema.prisma
@src/app/api/novel-promotion/[projectId]/episodes/route.ts
@src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts
@src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts
@src/lib/query/hooks/useProjectData.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts

<interfaces>
From `src/lib/query/hooks/useProjectData.ts`:

```ts
export interface Episode {
  id: string
  episodeNumber: number
  name: string
  description?: string | null
  novelText?: string | null
  storyboardDefaultMoodPresetId?: string | null
  audioUrl?: string | null
  srtContent?: string | null
  createdAt: string
}
```

From `src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts`:

```ts
export const GET = apiHandler(async (...) => { ... })
export const PATCH = apiHandler(async (...) => { ... })
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts`:

```ts
interface EpisodeStagePayload {
  name?: string
  novelText?: string | null
  storyboardDefaultMoodPresetId?: string | null
  clips?: NovelPromotionClip[]
  storyboards?: NovelPromotionStoryboard[]
  shotGroups?: NovelPromotionShotGroup[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the persisted episode production-mode contract and legacy backfill</name>
  <files>prisma/schema.prisma, prisma/migrations/20260419120000_add_episode_production_mode/migration.sql, src/app/api/novel-promotion/[projectId]/episodes/route.ts, src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts, src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts</files>
  <read_first>prisma/schema.prisma, prisma/migrations/20260418143000_add_shot_group_video_advanced_settings/migration.sql, src/app/api/novel-promotion/[projectId]/episodes/route.ts, src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts, src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts, .planning/phases/01-episode-mode-entry/01-CONTEXT.md, .planning/phases/01-episode-mode-entry/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: creating a new episode persists `episodeProductionMode: 'multi_shot'`.
    - Test 2: batch-created episodes persist `episodeProductionMode: 'multi_shot'`.
    - Test 3: patching an episode with `episodeProductionMode: 'traditional'` stores the new value and returns it in the API payload.
  </behavior>
  <action>Update `NovelPromotionEpisode` with a string field named `episodeProductionMode` and DB default `multi_shot` per D-07. Add a Prisma SQL migration that backfills legacy rows per D-08: mark an episode `traditional` only when persisted traditional traces exist (`novel_promotion_storyboards` with panels or panel `videoUrl` data), and set every other existing row to `multi_shot`. Extend the single-create, batch-create, and single-episode PATCH route handlers so `episodeProductionMode` is explicitly accepted, validated to `multi_shot|traditional`, and persisted without introducing project-level fallback state or silent alternative values.</action>
  <acceptance_criteria>
    - `rg -n "episodeProductionMode.*@default\\(\"multi_shot\"\\)" prisma/schema.prisma` finds the new schema field and default.
    - `rg -n "traditional|multi_shot" prisma/migrations/20260419120000_add_episode_production_mode/migration.sql` shows both backfill outcomes in the migration.
    - `rg -n "episodeProductionMode" 'src/app/api/novel-promotion/[projectId]/episodes/route.ts' 'src/app/api/novel-promotion/[projectId]/episodes/batch/route.ts' 'src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts'` shows create, batch-create, and PATCH coverage.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate && npx vitest run tests/integration/api/contract/novel-promotion-episode-production-mode.test.ts</automated>
  </verify>
  <done>The database, creation routes, and PATCH route all understand `episodeProductionMode`, and legacy rows receive deterministic backfilled values that satisfy D-07 and D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread episodeProductionMode through typed query and workspace contracts</name>
  <files>src/lib/query/hooks/useProjectData.ts, src/types/project.ts, src/app/[locale]/workspace/[projectId]/page.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/types.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts, tests/integration/api/contract/novel-promotion-episode-production-mode.test.ts</files>
  <read_first>src/lib/query/hooks/useProjectData.ts, src/types/project.ts, src/app/[locale]/workspace/[projectId]/page.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/types.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts, .planning/phases/01-episode-mode-entry/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: episode query types expose `episodeProductionMode` on both the episode detail payload and the project episode list payload.
    - Test 2: `useWorkspaceEpisodeStageData()` returns the selected episode’s `episodeProductionMode` so the script stage can render per-episode state.
  </behavior>
  <action>Add `episodeProductionMode?: 'multi_shot' | 'traditional' | null` to the shared project/episode TypeScript contracts, page-local episode summaries, and novel-promotion workspace episode props so MODE-01 stays episode-scoped across project data and episode data. Extend `useWorkspaceEpisodeStageData()` to expose `episodeProductionMode` directly from the selected episode payload, and keep this field server-backed per the repo’s no-multiple-sources-of-truth guardrail rather than mirroring it into component-local caches.</action>
  <acceptance_criteria>
    - `rg -n "episodeProductionMode" src/lib/query/hooks/useProjectData.ts src/types/project.ts 'src/app/[locale]/workspace/[projectId]/page.tsx' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/types.ts' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts'` returns matches in all listed contracts.
    - `rg -n "episodeProductionMode:" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceEpisodeStageData.ts'` shows the stage hook emitting the field.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/api/contract/novel-promotion-episode-production-mode.test.ts</automated>
  </verify>
  <done>The workspace can read `episodeProductionMode` from the same episode-backed query data that already drives script-stage rendering, with no project-global or local-only duplicate state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> episode create/PATCH routes | User-controlled payloads can attempt invalid mode values or bypass the episode-level source of truth |
| migration SQL -> legacy episode rows | Backfill logic can misclassify historical episodes if it uses weak traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | T | `src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/route.ts` | mitigate | Accept only `multi_shot` or `traditional` and reject any other PATCH payload instead of persisting arbitrary strings. |
| T-01-01-02 | T | `prisma/migrations/20260419120000_add_episode_production_mode/migration.sql` | mitigate | Backfill `traditional` only from persisted storyboard/panel/video traces per D-08; do not infer mode from prose fields, clips, or project-level heuristics. |
| T-01-01-03 | D | query/type layer | mitigate | Add the field to every episode contract consumed by the workspace so refreshes, route transitions, and optimistic patches do not lose mode state. |
</threat_model>

<verification>
Run `npx prisma validate` and the focused API contract test after the plan lands.
</verification>

<success_criteria>
`episodeProductionMode` exists as an episode-level persisted field with a multi-shot default, legacy episodes are backfilled conservatively, and the selected episode query payload exposes the field everywhere the script stage needs it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-episode-mode-entry/01-01-SUMMARY.md`
</output>
