---
phase: 01-episode-mode-entry
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
  - src/lib/query/mutations/useProjectConfigMutations.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceConfigActions.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
  - messages/zh/scriptView.json
  - messages/en/scriptView.json
  - messages/zh/novel-promotion.json
  - messages/en/novel-promotion.json
  - tests/unit/workspace/rebuild-confirm.test.ts
  - tests/unit/novel-promotion/script-view-mode-entry.test.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-04
  - UI-01
  - UI-03
must_haves:
  truths:
    - "The script page shows the active production mode for the selected episode in the right-side action stack immediately above `确认并开始绘制`."
    - "The active path is clearly visible before launch, with multi-shot and traditional mode feedback mapped to the selected episode."
    - "Users can switch modes freely before downstream artifacts exist."
    - "Switching a started episode requires an explicit confirmation before the persisted mode changes."
    - "The draw CTA still keeps the base label `确认并开始绘制`, but launches using the current episode-backed mode instead of an ambiguous implicit default."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx"
      provides: "Mode selector card, active-path helper text, and CTA-area feedback"
      contains: "episodeProductionMode"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts"
      provides: "Mode-switch confirmation support for started episodes"
      contains: "switchEpisodeProductionMode"
    - path: "messages/zh/scriptView.json"
      provides: "Localized copy for mode selector card and CTA helper text"
      contains: "productionMode"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx"
      via: "episodeProductionMode prop + update callback"
      pattern: "episodeProductionMode"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts"
      via: "onEpisodeProductionModeChange"
      pattern: "onEpisodeProductionModeChange"
    - from: "src/lib/query/mutations/useProjectConfigMutations.ts"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts"
      via: "downstream artifact stats"
      pattern: "shotGroupCount"
---

<objective>
Add the script-page mode selector, mode-specific CTA feedback, and started-episode switch confirmation without breaking the existing workspace shell.

Purpose: Phase 1’s visible product change is the final pre-launch episode mode decision point required by MODE-04, UI-01, and UI-03.
Output: A right-rail selector card with localized copy, runtime wiring for mode changes, and confirmation gating for episodes that already have downstream artifacts.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-episode-mode-entry/01-CONTEXT.md
@.planning/phases/01-episode-mode-entry/01-RESEARCH.md
@.planning/phases/01-episode-mode-entry/01-UI-SPEC.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/NovelPromotionWorkspace.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts
@src/lib/query/mutations/useProjectConfigMutations.ts
@messages/zh/scriptView.json
@messages/en/scriptView.json
@messages/zh/novel-promotion.json
@messages/en/novel-promotion.json

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx`:

```ts
export interface WorkspaceStageRuntimeValue {
  onRunScriptToStoryboard: () => Promise<void>
  onOpenAssetLibrary: () => void
  onClipUpdate: (clipId: string, data: unknown) => Promise<void>
}
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts`:

```ts
type RebuildActionType = 'storyToScript' | 'scriptToStoryboard'

export function useRebuildConfirm(...) {
  return {
    showRebuildConfirm,
    rebuildConfirmTitle,
    rebuildConfirmMessage,
    pendingActionType,
    runWithRebuildConfirm,
  }
}
```

From `messages/zh/scriptView.json`:

```json
"generate": {
  "generating": "正在生成画面...",
  "startGenerate": "确认并开始绘制 →"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the workspace runtime and confirmation path for mode switching</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts, src/lib/query/mutations/useProjectConfigMutations.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceConfigActions.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts, tests/unit/workspace/rebuild-confirm.test.ts</files>
  <read_first>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts, src/lib/query/mutations/useProjectConfigMutations.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceConfigActions.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts, tests/unit/workspace/rebuild-confirm.test.ts, .planning/phases/01-episode-mode-entry/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: switching a clean episode to another mode updates the episode immediately without opening the confirmation dialog.
    - Test 2: switching a started episode opens the existing workspace confirmation flow before mutation runs.
    - Test 3: downstream checks treat storyboards, shot groups, and persisted video artifacts as confirmation-worthy, not only panel counts.
  </behavior>
  <action>Extend the stage runtime with `onEpisodeProductionModeChange(nextMode)` so the script page can update the selected episode per D-03 and D-04 without inventing a second modal system. Reuse `useRebuildConfirm` by adding a `switchEpisodeProductionMode` action type and broaden its downstream stats source to include storyboard counts, panel counts, shot-group counts, and persisted video-bearing artifacts before a mode flip is allowed silently. Keep the workspace-level `ConfirmDialog` in `NovelPromotionWorkspace.tsx` as the single confirmation surface, and keep the persisted episode field as the only source of truth for the selected mode.</action>
  <acceptance_criteria>
    - `rg -n "onEpisodeProductionModeChange" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts'` shows the runtime contract and controller wiring.
    - `rg -n "switchEpisodeProductionMode|shotGroupCount|videoArtifactCount" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useRebuildConfirm.ts' 'src/lib/query/mutations/useProjectConfigMutations.ts'` shows broader confirmation coverage for D-04.
    - `rg -n "switchEpisodeProductionMode" tests/unit/workspace/rebuild-confirm.test.ts` shows regression coverage for the new action path.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/workspace/rebuild-confirm.test.ts</automated>
  </verify>
  <done>Mode switching is mediated by the existing workspace confirmation flow, immediate for clean episodes, and blocked behind explicit confirmation for episodes with downstream artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the script-page mode selector card and CTA path feedback</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx, messages/zh/scriptView.json, messages/en/scriptView.json, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, tests/unit/novel-promotion/script-view-mode-entry.test.ts</files>
  <read_first>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx, messages/zh/scriptView.json, messages/en/scriptView.json, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, .planning/phases/01-episode-mode-entry/01-CONTEXT.md, .planning/phases/01-episode-mode-entry/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the right rail renders a compact selector card between `剧中资产` and the main CTA with `多镜头片段模式` and `传统模式` as mutually exclusive options.
    - Test 2: the base CTA label remains `确认并开始绘制`, while helper text/badges reflect whether the episode will enter the multi-shot or traditional path.
    - Test 3: changing the selected option calls the runtime mode-change callback and immediately updates the visible active-path copy when no confirm gate is required.
  </behavior>
  <action>Render the mode selector in `ScriptViewAssetsPanel` exactly where D-01 and D-02 require: below the `剧中资产` card and above the primary CTA. Preserve the CTA base label per D-05, but add unmistakable mode labeling and supporting copy per D-06 and UI-03 so the user can see whether the next step is the multi-shot fast path or the traditional path. Source all selector state from the selected episode’s `episodeProductionMode`, not a local mirror, and keep copy in the locale JSON files rather than hardcoding strings inside the component.</action>
  <acceptance_criteria>
    - `rg -n "多镜头片段模式|传统模式|确认并开始绘制" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx' messages/zh/scriptView.json` shows selector labels and unchanged CTA base copy.
    - `rg -n "productionMode" messages/zh/scriptView.json messages/en/scriptView.json` shows the new localized selector/helper-copy namespace.
    - `rg -n "episodeProductionMode|onEpisodeProductionModeChange" 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/ScriptStage.tsx' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewRuntime.tsx' 'src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/script-view/ScriptViewAssetsPanel.tsx'` shows prop threading from stage data to the right-rail UI.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/script-view-mode-entry.test.ts tests/unit/workspace/rebuild-confirm.test.ts</automated>
  </verify>
  <done>The script page has a visible, localized, per-episode mode selector and CTA helper state in the final pre-launch position, with the CTA label unchanged and the active path unmistakable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected episode query -> script-page selector UI | The visible mode could drift from persisted state if the UI creates a local mirror |
| selector interaction -> workspace confirmation path | A mode switch could bypass the required confirmation for started episodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `ScriptViewAssetsPanel.tsx` | mitigate | Render selector state directly from `episodeProductionMode` and update through the runtime callback; do not add standalone `useState` as a second source of truth. |
| T-01-02-02 | E | `useRebuildConfirm.ts` | mitigate | Route mode switches for started episodes through the existing workspace confirm flow before mutating the persisted mode. |
| T-01-02-03 | I | locale copy + CTA helper text | mitigate | Keep the CTA base label intact but add explicit path copy so users do not accidentally launch the wrong production path due to ambiguous UI. |
</threat_model>

<verification>
Run the focused unit tests for rebuild confirmation and the new script-view selector behavior.
</verification>

<success_criteria>
The script page shows a per-episode production-mode selector in the locked right-rail location, visibly labels the active downstream path next to the unchanged draw CTA, and requires confirmation before switching a started episode.
</success_criteria>

<output>
After completion, create `.planning/phases/01-episode-mode-entry/01-02-SUMMARY.md`
</output>
