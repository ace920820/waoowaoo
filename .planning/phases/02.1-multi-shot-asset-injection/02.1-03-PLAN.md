---
phase: 02.1-multi-shot-asset-injection
plan: 03
type: execute
wave: 3
depends_on:
  - 02.1-02
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts
  - src/lib/shot-group/prompt.ts
  - src/lib/workers/handlers/shot-group-image-task-handler.ts
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  - tests/unit/worker/shot-group-image-task-handler.test.ts
  - tests/integration/api/specific/novel-promotion-generate-shot-group-image-route.test.ts
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Generating a multi-shot reference board uses the saved asset bindings and mood controls from the card."
    - "Hovering an existing generated preview exposes view, regenerate, replace upload, edit asset refs, and remove-current-reference actions on the same card."
    - "Removing or replacing a reference stays card-local and does not force users into the traditional storyboard flow."
  artifacts:
    - path: "src/lib/shot-group/prompt.ts"
      provides: "Prompt builder that includes saved asset and mood constraints"
    - path: "src/lib/workers/handlers/shot-group-image-task-handler.ts"
      provides: "Reference-board task wiring that forwards asset-injection context"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      provides: "Preview hover management tray and card-local reference actions"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      to: "src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts"
      via: "card actions call existing regenerate/generate route"
      pattern: "useGenerateProjectShotGroupImage"
    - from: "src/lib/workers/handlers/shot-group-image-task-handler.ts"
      to: "src/lib/shot-group/prompt.ts"
      via: "loaded shot-group metadata becomes generation prompt context"
      pattern: "buildShotGroupCompositePrompt"
---

<objective>
Finish Phase 02.1 by making generated preview management card-local and ensuring the actual reference-board generation path consumes the saved asset and mood constraints.

Purpose: deliver D-10 through D-12 and close the loop from saved card metadata to real reference-board generation, without entering deferred omnireference or downstream video-editing scope.
Output: hover management tray on generated previews plus worker/prompt wiring that injects saved asset and mood context into shot-group image generation.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-03-SUMMARY.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-02-PLAN.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts
@src/lib/shot-group/prompt.ts
@src/lib/workers/handlers/shot-group-image-task-handler.ts
@src/components/ui/ImagePreviewModal.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ShotGroupSection.tsx

<interfaces>
From `src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts`:

```ts
const billingPayload = {
  shotGroupId,
  templateKey: shotGroup.templateKey,
  groupPrompt: shotGroup.groupPrompt,
  referenceImageUrl: shotGroup.referenceImageUrl,
  imageModel: projectModelConfig.storyboardModel,
}
```

From `src/lib/workers/handlers/shot-group-image-task-handler.ts`:

```ts
const prompt = buildShotGroupCompositePrompt({
  group: {
    ...shotGroup,
    dialogueLanguage: normalizeShotGroupDialogueLanguage(shotGroup.dialogueLanguage),
    items: shotGroup.items,
  },
  template,
  artStyle,
  locale: job.data.locale,
  canvasAspectRatio: SHOT_GROUP_COMPOSITE_ASPECT_RATIO,
})
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ShotGroupSection.tsx`:

```tsx
<button type="button" onClick={onPreview} className="absolute inset-0 z-10 cursor-zoom-in" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject saved asset and mood constraints into shot-group reference-board generation</name>
  <files>src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts, src/lib/shot-group/prompt.ts, src/lib/workers/handlers/shot-group-image-task-handler.ts, tests/unit/worker/shot-group-image-task-handler.test.ts, tests/integration/api/specific/novel-promotion-generate-shot-group-image-route.test.ts</files>
  <behavior>
    - Test 1: the generate route and worker carry saved character/prop/location/mood metadata into the task payload or prompt context.
    - Test 2: missing asset warnings do not prevent task submission, but the prompt builder omits absent categories cleanly per D-05.
    - Test 3: generated composite prompts include explicit asset and mood guidance without enabling deferred omnireference inputs.
  </behavior>
  <action>Keep the existing `generate-shot-group-image` endpoint and worker shape, but extend the selected shot-group read path so the composite prompt incorporates persisted asset bindings and mood instructions from Phase 02.1 metadata. Reuse the current reference-image channel; do not add `@` syntax, multi-video inputs, audio reference inputs, or any other deferred advanced reference mode capability.</action>
  <verify>
    <automated>npx vitest run tests/unit/worker/shot-group-image-task-handler.test.ts tests/integration/api/specific/novel-promotion-generate-shot-group-image-route.test.ts</automated>
  </verify>
  <done>Reference-board generation consumes the saved segment asset and mood state, while still allowing generation to proceed when some categories are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add generated-preview hover management actions to the multi-shot confirmation cards</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</files>
  <behavior>
    - Test 1: when a card has a generated preview, hover exposes `查看大图 / 重新生成 / 替换上传 / 编辑资产引用 / 移除当前引用` per D-10.
    - Test 2: `编辑资产引用` reopens the existing card-local asset controls instead of navigating away per D-11 and D-12.
    - Test 3: `移除当前引用` clears the current preview/reference through existing mutation paths and leaves the card in a recoverable warning-ready state.
  </behavior>
  <action>Mirror the hover interaction language from the traditional storyboard shot-group preview surface, adapted to the multi-shot review cards. Use `ImagePreviewModal` for large-preview viewing, reuse existing generate/upload/update mutations for regenerate/replace/remove flows, and keep all actions card-local. Do not introduce global batch actions or leave the confirmation stage for common corrections.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</automated>
  </verify>
  <done>Generated preview cards expose the full hover management tray locally, and users can preview, regenerate, replace, edit asset references, or remove the current reference without leaving the confirmation stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted shot-group metadata→image task prompt | Saved JSON affects model instructions sent to the storyboard image generator |
| hover UI→mutation endpoints | Destructive or replacement actions change server-side reference state from the card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | T | `src/lib/shot-group/prompt.ts` | mitigate | Render asset and mood constraints from normalized metadata only, escaping or trimming freeform mood text before inserting it into the prompt context. |
| T-02.1-08 | R | `ShotGroupVideoSection.tsx` | mitigate | Reuse existing generate/upload/update mutations so hover actions remain auditably mapped to server-side shot-group updates instead of hidden client-only state changes. |
| T-02.1-09 | D | `generate-shot-group-image/route.ts` | mitigate | Keep missing assets as warnings, not blockers, so users are not denied reference generation when only some categories are unavailable. |
</threat_model>

<verification>
Run the worker test, the new generate-route integration test, and the review-stage UI test. Confirm the generated prompt path contains asset/mood context and that hover actions remain inside the confirmation-stage review boundary.
</verification>

<success_criteria>
The multi-shot confirmation cards can manage generated previews locally, and the underlying reference-board generation path actually uses the saved asset and mood inputs that the cards expose.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-multi-shot-asset-injection/02.1-03-SUMMARY.md`
</output>
