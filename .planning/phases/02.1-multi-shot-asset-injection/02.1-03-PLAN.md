---
phase: 02.1-multi-shot-asset-injection
plan: 03
type: execute
wave: 3
depends_on:
  - 02.1-01
  - 02.1-02
files_modified:
  - src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts
  - src/lib/shot-group/prompt.ts
  - src/lib/shot-group/template-registry.ts
  - tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Shot-group image generation consumes the same effective asset and mood bindings that were saved from the multi-shot review cards."
    - "Manual card edits override auto-preselected assets when the generation route builds the reference-board request."
    - "Incomplete asset coverage still allows generation, but the route and tests preserve the weaker-constraint warning model."
  artifacts:
    - path: "src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts"
      provides: "Server-side consumption of persisted asset and mood bindings for reference-board generation"
      exports: ["POST"]
    - path: "src/lib/shot-group/prompt.ts"
      provides: "Prompt/context builder including effective asset and mood constraints"
      contains: "character|location|prop|mood"
    - path: "tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts"
      provides: "Integration verification that saved card state influences generation"
      contains: "manual selection overrides auto-preselection"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      to: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      via: "save-before-generate payload for asset/mood fields"
      pattern: "buildShotGroupPayload"
    - from: "src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts"
      to: "src/lib/shot-group/draft-metadata.ts"
      via: "read persisted effective asset bindings from draft metadata"
      pattern: "draftMetadata"
    - from: "src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts"
      to: "src/lib/shot-group/prompt.ts"
      via: "asset- and mood-aware prompt construction"
      pattern: "prompt"
---

<objective>
Wire the selected/preselected assets and mood inputs into multi-shot reference-board generation, then verify the end-to-end asset-constrained flow.

Purpose: ensure the new UI controls actually influence the generated reference-board request rather than staying decorative state.
Output: generation-route consumption of asset bindings, prompt wiring, and integration tests proving the asset constraints reach the reference-generation pipeline.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts
@src/lib/shot-group/prompt.ts
@src/lib/shot-group/template-registry.ts
@src/lib/shot-group/draft-metadata.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject effective asset and mood bindings into shot-group image generation</name>
  <files>src/app/api/novel-promotion/[projectId]/generate-shot-group-image/route.ts, src/lib/shot-group/prompt.ts, src/lib/shot-group/template-registry.ts, tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts</files>
  <behavior>
    - Test 1: generation uses effective selected location / character / prop assets when present.
    - Test 2: user manual selections override auto-preselected bindings in the generation payload.
    - Test 3: missing assets do not block generation, but warning/weak-constraint metadata is preserved.
    - Test 4: mood preset and custom mood inputs are included in the generation context for the reference board.
  </behavior>
  <action>Update the shot-group image generation route and prompt-building path so the effective asset bindings saved on the multi-shot card are translated into the generation request for the reference board. Use the resolved priority order from plan `02.1-01`. Ensure location, character appearance, and prop references can participate together, and include mood preset/custom mood context in the prompt payload. Do not expand scope into advanced omnireference media or audio/video references.</action>
  <verify>
    <automated>npx vitest run tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "manual|preselected|effective|storyboardMoodPresetId|customMood" src/app/api/novel-promotion/\[projectId\]/generate-shot-group-image/route.ts src/lib/shot-group/prompt.ts` returns matches.
    - `rg -n "character|location|prop" tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts` returns assertions proving all three asset families reach generation.
    - `npx vitest run tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts` exits 0.
  </acceptance_criteria>
  <done>The reference-board generation route now reads the persisted effective asset/mood bindings, respects manual-over-auto priority, and includes those constraints in the generated request context.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify end-to-end multi-shot asset injection behavior</name>
  <files>tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx, tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts</files>
  <behavior>
    - Verify the card state, persisted metadata, and generation route all line up.
    - Verify users can continue generating a reference board even when asset coverage is incomplete.
  </behavior>
  <action>Close the phase with focused verification coverage across UI and API layers so execution can later prove: card controls save, saved bindings influence generation, and missing assets degrade gracefully instead of hard-blocking the flow.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx tests/integration/api/specific/novel-promotion-generate-shot-group-image-assets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The combined verification command exits 0.
    - Test names and assertions clearly cover persistence, priority resolution, warning-but-continue behavior, and route consumption of selected assets.
  </acceptance_criteria>
  <done>The phase has focused UI and API verification proving that card edits persist, persisted bindings reach the generation path, and missing assets degrade gracefully instead of blocking the user.</done>
</task>

</tasks>

<verification>
Manually verify one real multi-shot episode: confirm the card auto-preselects assets, manually override at least one binding, generate or regenerate the reference board, and confirm the output request honors the override rather than the original auto-preselection.
</verification>

<success_criteria>
1. Multi-shot reference-board generation consumes the same effective asset/mood state shown on the card.
2. Manual selections override auto-preselected assets in generation.
3. Missing assets degrade to warning mode rather than blocking the user.
4. The phase has UI and API verification proving the asset-injection flow is real end-to-end.
</success_criteria>
