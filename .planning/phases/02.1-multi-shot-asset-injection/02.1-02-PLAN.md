---
phase: 02.1-multi-shot-asset-injection
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/MultiShotSegmentAssetControls.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - src/lib/query/mutations/storyboard-panel-mutations.ts
  - messages/zh/novel-promotion.json
  - messages/en/novel-promotion.json
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-segment-asset-controls.test.ts
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Each multi-shot confirmation card shows its current character, prop, and location selections directly on the card."
    - "Users can change or clear per-card asset and mood inputs without leaving the confirmation stage."
    - "Warning badges and helper copy stay attached to the affected card instead of blocking the whole stage."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/MultiShotSegmentAssetControls.tsx"
      provides: "Reusable card-local asset and mood editor"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      provides: "Review-card rendering and save wiring for asset injection"
    - path: "src/lib/query/mutations/storyboard-panel-mutations.ts"
      provides: "Client mutation support for persisting per-card asset and mood changes"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/MultiShotSegmentAssetControls.tsx"
      to: "src/lib/query/mutations/storyboard-panel-mutations.ts"
      via: "useUpdateProjectShotGroup"
      pattern: "draftMetadata|assetBindings|mood"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      to: "src/lib/shot-group/draft-metadata.ts"
      via: "card render state derived from persisted metadata"
      pattern: "parseShotGroupDraftMetadata"
---

<objective>
Turn the existing multi-shot confirmation cards into editable asset-injection cards that mirror the traditional storyboard control family without adding a new page or flow.

Purpose: deliver D-02, D-03, D-07, D-08, and D-09 directly on the Phase 2 confirmation cards so users can correct each segment before generating reference boards.
Output: card-local asset selectors, mood preset/custom mood controls, warning badges, and save/update flows on the existing review cards.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-03-SUMMARY.md
@.planning/phases/02-multi-shot-fast-path/02-05-SUMMARY.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-01-PLAN.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/StoryboardGroup.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ImageEditModalAssetPicker.tsx
@src/lib/query/mutations/storyboard-panel-mutations.ts

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx`:

```tsx
<ShotGroupVideoSection
  projectId={projectId}
  episodeId={episodeId}
  shotGroups={shotGroups}
  defaultVideoModel={runtime.videoModel || ''}
  videoModelOptions={runtime.userVideoModels}
  capabilityOverrides={runtime.capabilityOverrides}
  mode="review"
/>
```

From `src/lib/query/mutations/storyboard-panel-mutations.ts`:

```ts
export function useUpdateProjectShotGroup(projectId: string, episodeId: string) {
  return useMutation({
    mutationFn: async (payload: {
      shotGroupId: string
      title?: string
      templateKey?: 'grid-4' | 'grid-6' | 'grid-9'
      groupPrompt?: string | null
      videoPrompt?: string | null
      referenceImageUrl?: string | null
      compositeImageUrl?: string | null
      generationOptions?: Record<string, string | number | boolean>
    }) => ...
  })
}
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/StoryboardGroup.tsx`:

```ts
const [clipMoodPresetId, setClipMoodPresetId] = useState<string>(clip?.storyboardMoodPresetId || '')
const [clipCustomMood, setClipCustomMood] = useState<string>(clip?.customMood || '')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build reusable per-card asset and mood controls in the storyboard interaction family</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/MultiShotSegmentAssetControls.tsx, tests/unit/novel-promotion/workspace/multi-shot-segment-asset-controls.test.ts</files>
  <behavior>
    - Test 1: the control block renders scene, character, and prop selections plus mood preset and custom mood fields per D-07.
    - Test 2: manual edits can replace auto-preselected values and visibly mark manual ownership per D-02 and D-04.
    - Test 3: warning badges/helper copy remain card-local when a key asset category is missing per D-05 and D-06.
  </behavior>
  <action>Create a reusable review-card controls component that consumes the Phase 02.1 metadata contract and presents the same control family users already know from storyboard image generation. Reuse existing asset-picker and mood-preset patterns where practical; do not introduce a new setup screen, a project-global editor, or omnireference controls. Keep the control surface fast enough for many cards in sequence per D-12.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-segment-asset-controls.test.ts</automated>
  </verify>
  <done>The multi-shot card control block exposes editable scene/character/prop selections and mood inputs in a storyboard-familiar layout, with card-level warning presentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the controls into the confirmation cards and persist card-local edits</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, src/lib/query/mutations/storyboard-panel-mutations.ts, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</files>
  <behavior>
    - Test 1: review cards render the saved asset and mood state from shot-group metadata instead of placeholders only.
    - Test 2: editing and saving a card sends the metadata back through `useUpdateProjectShotGroup` without disturbing the review-stage boundary.
    - Test 3: missing-asset warnings remain visible on the affected card after save/re-render and do not block the stage continue CTA globally.
  </behavior>
  <action>Embed the new controls in the existing `ShotGroupVideoSection` review branch, using the shot-group PATCH route as the only persistence path. Keep the confirmation stage scoped to review work only: asset/mood edits, warnings, prompt visibility, and reference prep. Do not add video-generation actions, dialogue editing, or manual single-shot supplement controls in this phase.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-segment-asset-controls.test.ts</automated>
  </verify>
  <done>Users can inspect, edit, and save per-card asset/mood choices directly on the multi-shot confirmation cards, and the UI keeps warning state attached to each specific segment card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted metadata→review UI | Saved JSON controls what the confirmation cards render |
| review card→PATCH mutation | User-entered ids and custom mood text are sent back to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | T | `MultiShotSegmentAssetControls.tsx` | mitigate | Restrict selectable asset kinds to `character`, `prop`, and `location`, and avoid trusting arbitrary client-crafted metadata fields in the UI state. |
| T-02.1-05 | I | `ShotGroupVideoSection.tsx` | mitigate | Render warning/helper copy from normalized metadata fields only; do not expose raw JSON blobs or internal parse errors in the card surface. |
| T-02.1-06 | D | `storyboard-panel-mutations.ts` | mitigate | Reuse the existing shot-group invalidation path so card saves refresh episode data instead of creating stale local mirrors that can drift from server state. |
</threat_model>

<verification>
Run the new card-control unit test and the existing multi-shot confirmation stage test together. Confirm the review stage still renders as `mode="review"` and that card saves invalidate the episode query rather than mutating an unsourced local copy only.
</verification>

<success_criteria>
The multi-shot confirmation stage exposes card-local asset and mood controls in the established storyboard style, persists manual changes via the existing shot-group mutation path, and keeps missing-asset warnings visible on the affected cards without leaving the fast path.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-multi-shot-asset-injection/02.1-02-SUMMARY.md`
</output>
