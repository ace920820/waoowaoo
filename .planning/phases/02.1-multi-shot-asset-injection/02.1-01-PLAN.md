---
phase: 02.1-multi-shot-asset-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shot-group/draft-metadata.ts
  - src/lib/shot-group/multi-shot-asset-bindings.ts
  - src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
  - src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
  - src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
  - src/types/project.ts
  - tests/unit/novel-promotion/multi-shot/segment-asset-preselection.test.ts
  - tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
  - tests/integration/api/specific/novel-promotion-shot-groups-route.test.ts
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Each derived 15-second multi-shot segment starts with persisted character, prop, and location reference candidates."
    - "Manual asset choices override system preselection on subsequent saves and draft reuse."
    - "Missing asset constraints show a persisted warning state without blocking draft creation or later reference generation."
  artifacts:
    - path: "src/lib/shot-group/multi-shot-asset-bindings.ts"
      provides: "Segment asset-binding and warning contract"
    - path: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      provides: "Auto-preselection persistence during draft creation/reuse"
    - path: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      provides: "Manual override persistence for per-card asset and mood metadata"
  key_links:
    - from: "src/lib/novel-promotion/multi-shot/episode-draft-builder.ts"
      to: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      via: "draft payload includes preselected asset and mood metadata"
      pattern: "draftMetadata|assetBindings|mood"
    - from: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      to: "src/lib/shot-group/draft-metadata.ts"
      via: "PATCH merge preserves manual overrides and warnings"
      pattern: "draftMetadata"
---

<objective>
Persist multi-shot asset-injection state on the existing shot-group draft contract so Phase 02.1 can extend the confirmation cards without introducing a second source of truth.

Purpose: deliver D-01 through D-06 and D-09 on the already-shipped shot-group draft axis, while keeping Phase 2's draft reuse behavior intact.
Output: auto-preselected per-segment asset/mood metadata, manual-override persistence, and non-blocking warning state stored inside shot-group draft metadata.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-01-SUMMARY.md
@.planning/phases/02-multi-shot-fast-path/02-04-SUMMARY.md
@src/lib/shot-group/draft-metadata.ts
@src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
@src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
@src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
@src/types/project.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/hooks/storyboard-panel-asset-utils.ts

<interfaces>
From `src/lib/shot-group/draft-metadata.ts`:

```ts
export interface ShotGroupDraftMetadata {
  segmentOrder: number
  clipId: string
  segmentKey: string
  sourceClipId: string
  segmentIndexWithinClip: number
  segmentStartSeconds: number
  segmentEndSeconds: number
  sceneLabel: string
  narrativePrompt: string | null
  embeddedDialogue: string | null
  shotRhythmGuidance: string | null
  expectedShotCount: number
  sourceStatus: 'ready' | 'placeholder'
  placeholderReason: 'missing_clip_content' | 'generation_failed' | null
}
```

From `src/app/api/novel-promotion/[projectId]/shot-groups/route.ts`:

```ts
const draftMetadata = body.draftMetadata && typeof body.draftMetadata === 'object'
  ? body.draftMetadata as ShotGroupDraftMetadata
  : null
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/hooks/storyboard-panel-asset-utils.ts`:

```ts
export function buildDefaultAssetsForClip({
  clipId,
  clips,
  characters,
  locations,
}: BuildDefaultAssetsForClipParams): SelectedAsset[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the draft contract for segment asset bindings, mood, and warning state</name>
  <files>src/lib/shot-group/draft-metadata.ts, src/lib/shot-group/multi-shot-asset-bindings.ts, src/types/project.ts, tests/unit/novel-promotion/multi-shot/segment-asset-preselection.test.ts</files>
  <behavior>
    - Test 1: draft metadata parsing preserves new asset-binding, warning, and mood fields while keeping legacy Phase 2 rows readable.
    - Test 2: manual selections sort ahead of auto-preselected/raw-script candidates per D-04.
    - Test 3: missing key asset categories emit card-level warning descriptors without marking the segment unusable per D-05 and D-06.
  </behavior>
  <action>Create a reusable metadata contract for per-segment `scene/character/prop` selections, selection source priority, warning payloads, mood preset id, and custom mood text. Keep the data nested under the existing `draftMetadata` snapshot or a sibling config block inside `videoReferencesJson`; do not introduce a new table or a second persistence location. Preserve backwards compatibility by making Phase 2 rows parse successfully when the new fields are absent.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/segment-asset-preselection.test.ts</automated>
  </verify>
  <done>Shot-group draft metadata can represent auto/system/manual asset picks plus mood/warning state, and legacy draft rows still parse without migration failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-preselect and persist segment asset state during draft creation and manual save flows</name>
  <files>src/lib/novel-promotion/multi-shot/episode-draft-builder.ts, src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts, src/app/api/novel-promotion/[projectId]/shot-groups/route.ts, tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts, tests/integration/api/specific/novel-promotion-shot-groups-route.test.ts</files>
  <behavior>
    - Test 1: `POST /multi-shot-drafts` stores auto-preselected character, prop, and location bindings for each derived segment per D-01.
    - Test 2: `PATCH /shot-groups` accepts manual asset/mood overrides and preserves them across later updates per D-02 and D-04.
    - Test 3: missing assets leave the segment draft creatable, but the persisted warning state identifies the missing card-level constraint per D-05 and D-06.
  </behavior>
  <action>Reuse the Phase 2 segment derivation flow to compute default selections from clip `characters/location/props`, screenplay text, and current segment metadata. Persist those selections when the batch multi-shot draft route creates or refreshes shot groups, and allow the existing shot-group PATCH route to replace them with manual selections plus per-card mood values. Respect D-04 by storing both the effective selection set and enough source metadata to distinguish manual overrides from system guesses.</action>
  <verify>
    <automated>npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts tests/integration/api/specific/novel-promotion-shot-groups-route.test.ts</automated>
  </verify>
  <done>Every derived shot-group row carries persisted asset/mood metadata on creation, manual overrides can replace auto picks without being overwritten on future saves, and missing-asset warnings remain soft-blocking only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→shot-group routes | Untrusted asset ids, mood text, and warning-related metadata arrive from the browser |
| draft builder→persisted metadata | Auto-preselection logic converts script text into stored generation constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | T | `src/app/api/novel-promotion/[projectId]/shot-groups/route.ts` | mitigate | Validate asset-binding payload shape and accepted kinds at the route boundary before persisting into `videoReferencesJson`. |
| T-02.1-02 | I | `src/lib/novel-promotion/multi-shot/episode-draft-builder.ts` | mitigate | Limit persisted auto-preselection fields to asset ids, labels, and selected image refs instead of dumping full asset-library records or raw screenplay blobs. |
| T-02.1-03 | D | `src/lib/shot-group/draft-metadata.ts` | mitigate | Keep legacy parsing tolerant so old Phase 2 rows do not break card rendering or draft reuse when new metadata is absent. |
</threat_model>

<verification>
Run the targeted unit and API suites from this plan. Confirm that draft creation, reuse, and manual PATCH flows all persist the same asset-binding contract and that warning-only segments still return `200`.
</verification>

<success_criteria>
Auto-created shot-group drafts contain segment-level character/prop/location selections, manual overrides win over system guesses, and missing-asset warning data is persisted without breaking Phase 2 draft creation or reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-multi-shot-asset-injection/02.1-01-SUMMARY.md`
</output>
