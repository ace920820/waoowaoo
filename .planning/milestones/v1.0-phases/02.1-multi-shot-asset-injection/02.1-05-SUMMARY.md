---
phase: 02.1-multi-shot-asset-injection
plan: 05
status: completed
executed_at: 2026-04-19T20:04:30+08:00
source_plan: 02.1-05-PLAN.md
---

## Outcome

Closed the remaining mood-inheritance gap for Phase 02.1.

- project-level `项目默认分镜氛围` now reaches the real multi-shot confirmation path instead of being dropped during review-stage composition
- storyboard and multi-shot card surfaces visibly inherit the effective mood preset when no local override exists
- local card or panel selections still act as true overrides on top of the inherited default
- save / generate payloads now persist the inherited preset when a card is following default mood, so downstream reference generation matches what the user sees in the UI
- project-config mutation refresh now actively refetches project and episode queries so the settings modal and downstream card UI stay aligned after edits

## Verification

Passed:

- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts tests/unit/novel-promotion/storyboard-mood-presets.test.ts`

## Files Touched

- `src/lib/query/mutations/useProjectConfigMutations.ts`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/StoryboardGroup.tsx`
- `src/components/ui/patterns/PanelEditFormV2.tsx`
- `tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts`

## Notes

- The key root cause was not only cache refresh; the review-mode `ShotGroupVideoSection` branch was also dropping `projectDefaultMoodPresetId` / `episodeDefaultMoodPresetId`, so multi-shot cards could never display or persist the inherited value.
- This gap closure stays within Phase 02.1 scope: it hardens the existing asset-aware mood inheritance model rather than introducing new mood-authoring features.
