---
phase: 02.1-multi-shot-asset-injection
plan: 04
type: execute
wave: 4
gap_closure: true
depends_on:
  - 02.1-01
  - 02.1-02
  - 02.1-03
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "The real `multi-shot-storyboard` route must render the actual multi-shot confirmation surface instead of falling back to the legacy storyboard shell."
    - "Users on the real multi-shot confirmation path can see card-level asset controls for `场景 / 角色 / 物品 / 分镜氛围预设 / 自定义氛围`, along with binding-summary states such as `手动覆盖 / 系统预选 / 剧本回退`."
    - "When a card already has a reference image, the real hover management tray exposes `查看大图 / 重新生成 / 替换上传 / 编辑资产引用 / 移除当前引用` rather than the old preview-only action set."
    - "Missing-asset warning behavior and manual override behavior become testable on the real route because the correct stage shell is now reachable."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx"
      provides: "Correct stage-to-component routing so `multi-shot-storyboard` mounts the multi-shot confirmation stage"
      exports: ["default"]
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      provides: "Actual multi-shot confirmation shell used by the real user path"
      exports: ["default"]
    - path: "tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts"
      provides: "Regression coverage proving the stage router reaches the multi-shot confirmation shell instead of `StoryboardStage`"
      contains: "multi-shot-storyboard"
    - path: "tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts"
      provides: "Real-path UI assertions for asset controls, summary chips, and hover management actions"
      contains: "编辑资产引用"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      via: "explicit `multi-shot-storyboard` branch"
      pattern: "multi-shot-storyboard"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      via: "real confirmation-stage composition for per-segment cards"
      pattern: "ShotGroupVideoSection"
    - from: "tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx"
      via: "render assertions for stage-to-component mapping"
      pattern: "WorkspaceStageContent"
---

<objective>
Close the Phase 02.1 UAT gap where the real multi-shot confirmation path still shows the old preparation surface instead of the implemented asset-aware card UI.

Purpose: make the already-built multi-shot asset-injection work reachable on the real user path.
Output: corrected stage routing, regression coverage on the real route, and testable visibility for the missing asset controls and hover-management actions reported in UAT.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-UAT.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-01-PLAN.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-02-PLAN.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-03-PLAN.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/StoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
@tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the real stage router so `multi-shot-storyboard` reaches the multi-shot confirmation shell</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/WorkspaceStageContent.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts</files>
  <behavior>
    - Test 1: rendering `WorkspaceStageContent` with `currentStage='multi-shot-storyboard'` mounts `MultiShotStoryboardStage`, not `StoryboardStage`.
    - Test 2: rendering `WorkspaceStageContent` with `currentStage='storyboard'` still mounts the traditional `StoryboardStage`.
    - Test 3: the multi-shot routing regression proves the real path no longer bounces users back into the old storyboard shell.
  </behavior>
  <action>Update the real stage-selection branch in `WorkspaceStageContent.tsx` so `multi-shot-storyboard` explicitly renders `MultiShotStoryboardStage`, while `storyboard` continues to render `StoryboardStage`. Keep the traditional route untouched. If the multi-shot stage shell is missing any required composition glue for the real path, repair only that glue and do not redesign the page. Extend the routing regression test so it fails whenever the multi-shot route is accidentally mapped back to the legacy storyboard shell again.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "currentStage === 'multi-shot-storyboard'|case 'multi-shot-storyboard'" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/WorkspaceStageContent.tsx` returns a match.
    - `rg -n "MultiShotStoryboardStage" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/WorkspaceStageContent.tsx tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` returns matches in both files.
    - `rg -n "StoryboardStage" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/WorkspaceStageContent.tsx tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` still returns matches proving the traditional branch remains.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` exits 0.
  </acceptance_criteria>
  <done>The real workspace stage map reaches the multi-shot confirmation shell, so users entering `multi-shot-storyboard` are no longer trapped in the old preparation view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add real-path UI coverage for asset controls, binding summaries, and hover actions</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts, tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts</files>
  <behavior>
    - Test 1: the reachable multi-shot confirmation stage renders card-level controls for `场景 / 角色 / 物品 / 分镜氛围预设 / 自定义氛围`.
    - Test 2: the stage exposes binding-summary labels such as `手动覆盖 / 系统预选 / 剧本回退` on the real card surface.
    - Test 3: when a card has a reference image, the hover or management action set includes `查看大图 / 重新生成 / 替换上传 / 编辑资产引用 / 移除当前引用`.
    - Test 4: the tests exercise the real reachable stage path instead of isolated helper markup that users never actually see.
  </behavior>
  <action>Extend the stage-focused tests around `MultiShotStoryboardStage` and the asset-injection card surface so they assert what the user reported missing in UAT: visible asset controls, visible binding summaries, and the full hover-management tray. Prefer reusing the real composition path into `ShotGroupVideoSection` rather than duplicating a synthetic shell. Where current tests only cover internal component fragments, tighten them so they prove the reachable stage shell exposes the required controls and action labels end-to-end within the stage render tree.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "场景|角色|物品|分镜氛围预设|自定义氛围" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts` returns matches.
    - `rg -n "手动覆盖|系统预选|剧本回退" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts` returns matches.
    - `rg -n "查看大图|重新生成|替换上传|编辑资产引用|移除当前引用" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts` returns matches.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.ts` exits 0.
  </acceptance_criteria>
  <done>The real multi-shot confirmation stage now has regression coverage for the exact controls and hover actions the user expects to use.</done>
</task>

</tasks>

<verification>
Run the routing and stage tests for this gap plan, then do one manual workspace pass:
1. Enter an episode in `multi_shot` mode and open `多镜头确认`.
2. Confirm the page no longer falls back to the story or old storyboard preparation surface.
3. Confirm at least one segment card shows `场景 / 角色 / 物品 / 分镜氛围预设 / 自定义氛围`.
4. Confirm the card shows binding-summary state like `手动覆盖 / 系统预选 / 剧本回退`.
5. Hover an existing reference preview and confirm `查看大图 / 重新生成 / 替换上传 / 编辑资产引用 / 移除当前引用` are available.
</verification>

<success_criteria>
1. The real `multi-shot-storyboard` route renders `MultiShotStoryboardStage`, not `StoryboardStage`.
2. The reachable multi-shot confirmation page exposes the asset-aware segment cards already implemented in Phase 02.1.
3. The hover-management tray on the real path matches the expected multi-action behavior instead of the old preview-only options.
4. UAT for missing-asset warnings and manual override flow is unblocked because the correct card UI is now visible.
</success_criteria>
