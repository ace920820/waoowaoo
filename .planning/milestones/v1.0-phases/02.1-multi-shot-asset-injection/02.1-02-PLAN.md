---
phase: 02.1-multi-shot-asset-injection
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ImageEditModalAssetPicker.tsx
  - messages/zh/novel-promotion.json
  - messages/en/novel-promotion.json
  - tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Every multi-shot review card exposes card-local controls for location, character, prop, mood preset, and custom mood."
    - "Card edits flow into the review-card draft state and save path instead of staying presentation-only state."
    - "Hover management on generated previews covers both image actions and asset-correction entry points."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      provides: "Review-card UI, draft state, payload builder, and hover management for multi-shot asset injection"
      contains: "buildShotGroupPayload"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      provides: "Stage shell embedding the enhanced review cards"
      exports: ["default"]
    - path: "messages/zh/novel-promotion.json"
      provides: "Localized labels and warnings for multi-shot asset controls"
      contains: "asset"
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      to: "src/lib/query/mutations/storyboard-panel-mutations.ts"
      via: "useUpdateProjectShotGroup(...) save path for card edits"
      pattern: "useUpdateProjectShotGroup"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      to: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      via: "buildShotGroupPayload(...) including asset and mood fields"
      pattern: "buildShotGroupPayload"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      via: "review-stage embedding"
      pattern: "ShotGroupVideoSection"
---

<objective>
Add the multi-shot card controls and hover-management UI that let users inspect and correct per-segment asset constraints before generating or replacing the reference board.

Purpose: make the new asset-binding model actually usable from the existing multi-shot confirmation cards.
Output: card-level asset/mood controls, warning states, and hover actions that match the traditional storyboard interaction family.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/StoryboardGroup.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ShotGroupSection.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/ImageEditModalAssetPicker.tsx
@messages/zh/novel-promotion.json
@messages/en/novel-promotion.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-card asset and mood controls to the multi-shot confirmation surface</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, messages/zh/novel-promotion.json, messages/en/novel-promotion.json, tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx</files>
  <behavior>
    - Test 1: each multi-shot card renders control areas for `场景 / 角色 / 物品 / 分镜氛围预设 / 自定义氛围`.
    - Test 2: cards with missing effective assets show a visible warning state but keep the reference-generation actions enabled.
    - Test 3: auto-preselected assets are visually distinct from manual overrides.
    - Test 4: changing `场景 / 角色 / 物品 / 氛围` updates review-card draft state and is included in the save payload used by the shot-group update path.
  </behavior>
  <action>Extend the multi-shot review card in `ShotGroupVideoSection.tsx` so each segment exposes the same control family the user referenced from traditional storyboard cards: location, character, prop, mood preset, and custom mood. Reuse the existing visual language from `StoryboardGroup.tsx` and related storyboard asset pickers instead of inventing a brand-new UI. Warning badges must be card-local and must not block the upload/generate reference controls. This task must also update the review-card draft state and `buildShotGroupPayload(...)`/save-before-generate path so edits to asset and mood fields are persisted through `useUpdateProjectShotGroup(...)` before later generation reads them.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "场景|角色|物品|分镜氛围预设|自定义氛围" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns matches.
    - `rg -n "buildShotGroupPayload|useUpdateProjectShotGroup|storyboardMoodPresetId|customMood" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns matches proving card edits are wired into persistence.
    - `rg -n "warning|缺少|未绑定|可继续生成" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx messages/zh/novel-promotion.json` returns matches.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The confirmation cards expose the full asset/mood control family, distinguish auto-preselected from manual state, and persist those edits through the existing shot-group save path instead of keeping them only in local UI state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hover-based image/reference management actions</name>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx</files>
  <behavior>
    - Test 1: hovering a generated preview reveals `查看大图 / 重新生成 / 替换上传 / 编辑资产引用 / 移除当前引用`.
    - Test 2: the hover tray covers both image actions and asset-binding correction entry points.
    - Test 3: cards without a generated preview still expose a clear non-hover fallback path for first-time reference generation.
  </behavior>
  <action>Introduce hover management on the generated image/reference preview region for each multi-shot card. The hover tray must include the exact management family locked in context: view large image, regenerate, replace upload, edit asset references, and remove current reference. Keep detailed editing lightweight and card-local, using inline/popover/modal mechanics only as needed.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "查看大图|重新生成|替换上传|编辑资产引用|移除当前引用" src/app/\[locale\]/workspace/\[projectId\]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns matches.
    - `rg -n "hover|mouse|preview" tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx` returns assertions covering hover behavior.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-asset-injection-stage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Generated previews on multi-shot cards support hover-based management for both image operations and asset-correction entry points, while first-time generation remains accessible on cards without existing previews.</done>
</task>

</tasks>

<verification>
Open one multi-shot confirmation page locally and confirm a card can be read and corrected end-to-end without leaving the page: inspect preselected assets, override one of them, see warning state if an asset is missing, then manage the preview from hover actions.
</verification>

<success_criteria>
1. Each multi-shot segment card exposes card-local asset and mood controls.
2. Missing assets are obvious on-card warnings but do not block progress.
3. Generated previews have hover-based management for both image and asset-correction actions.
</success_criteria>
