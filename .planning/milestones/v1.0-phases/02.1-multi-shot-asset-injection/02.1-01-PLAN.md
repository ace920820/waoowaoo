---
phase: 02.1-multi-shot-asset-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shot-group/draft-metadata.ts
  - src/lib/shot-group/video-config-snapshot.ts
  - src/types/project.ts
  - src/lib/novel-promotion/multi-shot/persist-drafts.ts
  - src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
  - src/lib/query/mutations/storyboard-panel-mutations.ts
  - tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts
autonomous: true
requirements:
  - AST-01
  - AST-02
  - AST-03
  - AST-04
must_haves:
  truths:
    - "Each multi-shot segment can persist card-level location, character, prop, and mood binding state alongside the existing segment draft metadata."
    - "Manual asset choices remain distinguishable from auto-preselected bindings so later generation can respect `manual > preselected > script-derived` priority."
    - "Missing assets degrade into visible warning-capable state instead of blocking the multi-shot workflow."
  artifacts:
    - path: "src/lib/shot-group/draft-metadata.ts"
      provides: "Merged multi-shot draft metadata contract for segment data plus asset/mood binding state"
      exports: ["parseShotGroupDraftMetadata", "mergeShotGroupDraftMetadata"]
    - path: "src/lib/shot-group/video-config-snapshot.ts"
      provides: "Central snapshot merge path that preserves asset-binding metadata through POST/PATCH"
      exports: ["buildShotGroupVideoConfigSnapshot"]
    - path: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      provides: "Shot-group PATCH contract for saving manual asset and mood changes"
      exports: ["PATCH"]
  key_links:
    - from: "src/lib/novel-promotion/multi-shot/persist-drafts.ts"
      to: "src/lib/shot-group/video-config-snapshot.ts"
      via: "buildShotGroupVideoConfigSnapshot(...)"
      pattern: "buildShotGroupVideoConfigSnapshot"
    - from: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      to: "src/lib/shot-group/draft-metadata.ts"
      via: "draftMetadata merge and preservation on PATCH"
      pattern: "draftMetadata"
    - from: "src/lib/query/mutations/storyboard-panel-mutations.ts"
      to: "src/app/api/novel-promotion/[projectId]/shot-groups/route.ts"
      via: "shot-group update payload for asset/mood persistence"
      pattern: "useUpdateProjectShotGroup"
---

<objective>
Define the persistent multi-shot asset-binding contract so each shot-group card can carry auto-preselected and user-adjusted `场景 / 角色 / 物品 / 氛围` inputs without inventing a second source of truth.

Purpose: establish the metadata and PATCH/update surfaces before any UI or image-generation wiring depends on them.
Output: stored shot-group asset-binding metadata, typed client/server payloads, and tests for priority / missing-asset behavior.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-multi-shot-asset-injection/02.1-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
@src/lib/shot-group/draft-metadata.ts
@src/lib/shot-group/video-config-snapshot.ts
@src/lib/novel-promotion/multi-shot/persist-drafts.ts
@src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
@src/lib/query/mutations/storyboard-panel-mutations.ts
@src/types/project.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shot-group metadata with asset and mood binding state</name>
  <files>src/lib/shot-group/draft-metadata.ts, src/lib/shot-group/video-config-snapshot.ts, src/types/project.ts, tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts</files>
  <behavior>
    - Test 1: shot-group draft metadata can persist and parse selected/preselected location, character, and prop references.
    - Test 2: the metadata shape can persist per-card `storyboardMoodPresetId` and `customMood`.
    - Test 3: missing assets are represented as warning-capable state, not hard failure state.
  </behavior>
  <action>Extend the multi-shot shot-group metadata contract so `videoReferencesJson` can persist card-level asset bindings and mood overrides for multi-shot segments. Keep the existing segment-draft fields intact, then add fields for: auto-preselected location/character/prop references, user-selected location/character/prop references, effective resolved selections, optional warning/missing state, `storyboardMoodPresetId`, and `customMood`. Update `src/types/project.ts` with typed shapes used by the client. Update `buildShotGroupVideoConfigSnapshot(...)` and parse/merge helpers so these new fields survive both POST and PATCH without clobbering older draft metadata.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "storyboardMoodPresetId|customMood|effective.*Asset|selected.*Asset" src/lib/shot-group/draft-metadata.ts src/types/project.ts` returns matches.
    - `rg -n "draftMetadata" src/lib/shot-group/video-config-snapshot.ts src/lib/shot-group/draft-metadata.ts` returns matches proving merge/persist paths are still centralized.
    - `npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts` exits 0.
  </acceptance_criteria>
  <done>The repo has one persistence contract for segment draft metadata plus asset/mood bindings, and that contract can represent manual selections, auto-preselections, and warning-capable missing state without adding a new table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add update payload support for asset-binding priority and warning state</name>
  <files>src/app/api/novel-promotion/[projectId]/shot-groups/route.ts, src/lib/query/mutations/storyboard-panel-mutations.ts, src/lib/novel-promotion/multi-shot/persist-drafts.ts, tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts</files>
  <behavior>
    - Test 1: PATCH can persist card-level manual asset selections and mood overrides.
    - Test 2: persisted draft creation can seed auto-preselected asset slots without fabricating user selections.
    - Test 3: the resolved priority order is `manual > auto-preselected > script-derived`, and missing assets remain non-blocking but flagged.
  </behavior>
  <action>Update the shot-group PATCH route and matching client mutation types so multi-shot cards can save card-level asset-binding selections and mood overrides. Seed empty or auto-preselected asset-binding metadata during draft persistence in `persist-drafts.ts`, but do not fake manual selections. Encode the explicit priority order from context: user manual selection wins, then auto-preselected assets, then raw script-derived fallback. Preserve non-blocking warning state when assets are absent.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "manual|preselected|scriptDerived|warning" src/app/api/novel-promotion/\[projectId\]/shot-groups/route.ts src/lib/novel-promotion/multi-shot/persist-drafts.ts` returns matches.
    - `rg -n "storyboardMoodPresetId|customMood" src/lib/query/mutations/storyboard-panel-mutations.ts` returns matches in the shot-group update payload types.
    - `npx vitest run tests/unit/novel-promotion/multi-shot/shot-group-asset-bindings.test.ts` exits 0.
  </acceptance_criteria>
  <done>Multi-shot shot-group draft records can be updated with manual asset and mood edits, while preserving the explicit priority chain and non-blocking warning behavior expected by downstream generation.</done>
</task>

</tasks>

<verification>
Confirm one persisted multi-shot shot group carries both the original segment draft metadata and the new asset-binding metadata in the same `videoReferencesJson` payload, without creating any extra persistence table.
</verification>

<success_criteria>
1. Multi-shot shot groups can store location / character / prop / mood binding state per segment card.
2. Manual and auto-preselected asset selections are distinguishable in persisted metadata.
3. Missing assets remain a visible warning path, not a generation blocker.
</success_criteria>
