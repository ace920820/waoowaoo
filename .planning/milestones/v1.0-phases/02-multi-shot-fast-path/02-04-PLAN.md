---
phase: 02-multi-shot-fast-path
plan: 04
type: execute
wave: 1
depends_on:
  - 02-03
files_modified:
  - src/lib/shot-group/draft-metadata.ts
  - src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
  - src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
  - tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts
  - tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
autonomous: true
gap_closure: true
requirements:
  - MSHT-02
  - MSHT-03
  - MSHT-04
  - MSHT-05
  - MSHT-06
  - SHOT-01
must_haves:
  truths:
    - "A 2-minute multi-shot episode produces a usable 15-second segment set even when the script page only has a few coarse clips."
    - "Two coarse 60-second clips yield eight ordered multi-shot drafts, four derived segments per source clip, instead of only two shot-group drafts."
    - "Each derived segment remains one video-generation unit with model-ready prompt text, embedded dialogue, compact rhythm guidance, and explicit placeholder handling."
    - "The batch draft route persists and reuses segment drafts by segment identity rather than collapsing everything to one row per source clip."
  artifacts:
    - path: "src/lib/novel-promotion/multi-shot/episode-draft-builder.ts"
      provides: "Clip-derived subsegment builder for ordered 15-second multi-shot drafts"
      contains: "buildEpisodeMultiShotDrafts"
    - path: "src/lib/shot-group/draft-metadata.ts"
      provides: "Segment-level metadata contract with stable segment identity and source-clip ancestry"
      contains: "segmentKey"
    - path: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      provides: "Episode draft creation and reuse keyed by derived segment identity"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      to: "src/lib/novel-promotion/multi-shot/episode-draft-builder.ts"
      via: "buildEpisodeMultiShotDrafts(...)"
      pattern: "buildEpisodeMultiShotDrafts"
    - from: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      to: "src/lib/shot-group/draft-metadata.ts"
      via: "draftMetadata.segmentKey"
      pattern: "segmentKey"
    - from: "tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts"
      to: "src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts"
      via: "2 coarse clips -> 8 ordered segment drafts regression"
      pattern: "totalSegments|segmentOrder"
---

<objective>
Close the Phase 2 data-contract gap that currently collapses one coarse script clip into one shot-group draft.

Purpose: make the fast path generate the fine-grained 15-second multi-shot segment set the user actually needs before any confirmation-stage UI work runs on top of it.
Output: a segment-derived draft builder, segment-level persisted metadata, and route coverage proving `2 coarse clips -> 8 ordered multi-shot drafts`.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-01-SUMMARY.md
@.planning/phases/02-multi-shot-fast-path/02-UAT.md
@.planning/phases/02-multi-shot-fast-path/02-VERIFICATION.md
@src/types/project.ts
@src/lib/shot-group/draft-metadata.ts
@src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
@src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
@tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts
@tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts

<interfaces>
From `src/types/project.ts`:
```ts
export interface NovelPromotionClip {
  id: string
  start?: number
  end?: number
  duration?: number
  shotCount?: number
  summary: string
  location: string | null
  characters: string | null
  props: string | null
  content: string
  screenplay?: string | null
}
```

From `src/lib/shot-group/draft-metadata.ts`:
```ts
export interface ShotGroupDraftMetadata {
  segmentOrder: number
  clipId: string
  sceneLabel: string
  narrativePrompt: string | null
  embeddedDialogue: string | null
  shotRhythmGuidance: string | null
  expectedShotCount: number
  sourceStatus: 'ready' | 'placeholder'
  placeholderReason: 'missing_clip_content' | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redefine the draft builder from clip-owned drafts to clip-derived 15-second segments</name>
  <read_first>
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    .planning/phases/02-multi-shot-fast-path/02-UAT.md
    src/types/project.ts
    src/lib/shot-group/draft-metadata.ts
    src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
    tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts
  </read_first>
  <files>src/lib/shot-group/draft-metadata.ts, src/lib/novel-promotion/multi-shot/episode-draft-builder.ts, tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts</files>
  <behavior>
    - Test 1: two clips covering `0-60` and `60-120` seconds produce eight drafts ordered `1..8`, with `4` derived segments per source clip.
    - Test 2: each derived segment carries a stable `segmentKey`, `sourceClipId`, `segmentIndexWithinClip`, and 15-second window metadata so later route reuse does not collapse all subsegments back to one row.
    - Test 3: ready segments still build model-ready `narrativePrompt`, embedded dialogue, and compact shot-rhythm guidance from the source clip content.
    - Test 4: if a coarse clip is missing content but spans 60 seconds, the builder still reserves four placeholder segments for that clip instead of dropping them.
  </behavior>
  <action>Update `buildEpisodeMultiShotDrafts` in `src/lib/novel-promotion/multi-shot/episode-draft-builder.ts` so it implements D-02, D-03, D-05, D-06, D-07, D-08, D-13, and D-14 against the actual user-reported target behavior. The builder must no longer map `params.clips` one-to-one to output drafts. Instead, derive 15-second multi-shot segments from each ordered clip by duration. Use clip timing first: `segmentCountForClip = max(1, ceil(resolvedClipDurationSeconds / 15))`, where duration is resolved from `clip.duration`, then `clip.end - clip.start`, then the episode-level fallback described below. For the concrete user case, two coarse 60-second clips must yield `4 + 4 = 8` derived segments. If timing is missing for all clips but the episode still needs a fast-path draft set, fall back to the locked D-02 assumption of a roughly 2-minute episode by distributing `8` total target segments proportionally across the ordered clips; when there are exactly two coarse clips with no timing, allocate `4` segments to each so the real user flow still lands on eight reviewable segments instead of two. Add stable segment identity to the metadata contract in `src/lib/shot-group/draft-metadata.ts`: `segmentKey`, `sourceClipId`, `segmentIndexWithinClip`, `segmentStartSeconds`, and `segmentEndSeconds`. Preserve existing fields such as `segmentOrder`, `sceneLabel`, `narrativePrompt`, `embeddedDialogue`, `shotRhythmGuidance`, `expectedShotCount`, `sourceStatus`, and `placeholderReason`. For ready segments, continue using the existing prompt-building style and keep each derived draft one video-generation unit. For placeholder clips, create one placeholder draft per derived segment slot, not one placeholder per source clip. Do not add Prisma columns, do not create storyboards or panels, and do not reintroduce any hardcoded one-draft-per-clip assumption.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "segmentKey|sourceClipId|segmentIndexWithinClip|segmentStartSeconds|segmentEndSeconds" src/lib/shot-group/draft-metadata.ts src/lib/novel-promotion/multi-shot/episode-draft-builder.ts` returns matches in both files.
    - `rg -n "ceil\\(.*/ 15\\)|/ 15\\)" src/lib/novel-promotion/multi-shot/episode-draft-builder.ts` returns a segment-window calculation.
    - `rg -n "2 coarse clips|eight drafts|segmentOrder" tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts` returns the new regression case.
    - `npx vitest run tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts` exits 0.
  </acceptance_criteria>
  <done>The builder emits a true segment-level draft set, including the concrete `2 coarse clips -> 8 ordered 15-second segments` case required by the UAT gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and reuse multi-shot drafts by derived segment identity</name>
  <read_first>
    .planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
    .planning/phases/02-multi-shot-fast-path/02-UAT.md
    src/lib/shot-group/draft-metadata.ts
    src/lib/novel-promotion/multi-shot/episode-draft-builder.ts
    src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts
    tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts
  </read_first>
  <files>src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts, tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts</files>
  <behavior>
    - Test 1: `POST /api/novel-promotion/[projectId]/multi-shot-drafts` returns `summary.totalSegments = 8` and eight shot groups for two coarse 60-second clips.
    - Test 2: the persisted rows reuse existing output-bearing shot groups only when `draftMetadata.segmentKey` matches, so four derived segments from the same source clip remain distinct rows.
    - Test 3: placeholder segments are persisted per reserved segment slot and counted in `summary.placeholderCount`.
    - Test 4: the route still creates no `NovelPromotionStoryboard` rows or panel artifacts.
  </behavior>
  <action>Update `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts` so route persistence follows the new segment-level contract from Task 1. Replace the current `existingByClipId` matching logic with `existingBySegmentKey`, using `draftMetadata.segmentKey` as the first reuse key and `sourceClipId + segmentIndexWithinClip` only as a backwards-compatible fallback when old rows lack the new key. `summary.totalSegments` must now reflect the full derived segment count, not the source clip count. When the builder returns eight derived drafts from two coarse clips, the route must create or reuse eight shot groups in stable order, with `segmentOrder` `1..8` preserved in the stored metadata and response payload. Keep reusing rows that already have `compositeImageUrl` or `videoUrl`, but only for the exact matching derived segment. Outputless matches must be refreshed to the latest draft payload and slot layout. Placeholder derived segments must persist as separate rows so the confirmation step can show repair targets for every missing 15-second slot. Preserve the current auth boundary and the no-storyboard/no-panel invariant from SHOT-01. Extend `tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` with a regression case that seeds two coarse clips, asserts the eight-row response, and verifies that no result collapses back to a one-row-per-clip shape.</action>
  <verify>
    <automated>npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "existingBySegmentKey|segmentKey" src/app/api/novel-promotion/\\[projectId\\]/multi-shot-drafts/route.ts` returns matches.
    - `rg -n "totalSegments" tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` returns assertions for the eight-segment regression.
    - `rg -n "clip-1|clip-2|segmentOrder" tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` returns the coarse-clip derivation case.
    - `npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts` exits 0.
  </acceptance_criteria>
  <done>The batch route now persists and reuses the real derived segment set the confirmation stage needs, instead of collapsing each source clip to a single draft row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace client -> multi-shot drafts route | Untrusted episode IDs and repeated clicks trigger persisted draft creation |
| builder -> stored draft metadata | Segment identity controls reuse and must not collapse multiple derived rows into one |
| route -> shot-group persistence | Incorrect reuse keys can silently overwrite the wrong segment draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | T | `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts` | mitigate | Keep `requireProjectAuthLight` and `buildEpisodeInProjectWhere`, then validate and scope draft creation by authenticated project/episode ownership. |
| T-02-15 | T | `src/lib/shot-group/draft-metadata.ts` | mitigate | Introduce stable `segmentKey` plus explicit segment window fields so reuse is deterministic and cannot merge multiple derived segments from one source clip. |
| T-02-16 | R | `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts` | mitigate | Preserve `segmentOrder`, `sourceClipId`, and `segmentIndexWithinClip` in stored metadata so route behavior is auditable in tests and later UI stages. |
| T-02-17 | D | `src/lib/novel-promotion/multi-shot/episode-draft-builder.ts` | accept | A long episode can legitimately yield more than eight segments; this is product-correct and bounded by clip duration rather than untrusted arbitrary expansion. |
</threat_model>

<verification>
1. Run `npx vitest run tests/unit/novel-promotion/multi-shot/episode-draft-builder.test.ts`.
2. Run `npx vitest run tests/integration/api/specific/novel-promotion-multi-shot-drafts-route.test.ts`.
3. Confirm the new regression proves `2 coarse 60-second clips -> 8 ordered segment drafts`.
</verification>

<success_criteria>
1. Phase 2 no longer hard-binds one coarse script clip to one multi-shot draft row.
2. A 2-minute episode can materialize roughly eight 15-second derived segments even when the script page clip structure is coarse.
3. Each derived segment persists and reuses independently by stable segment identity.
4. Placeholder slots remain visible per missing 15-second segment instead of disappearing with the coarse clip.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-shot-fast-path/02-04-SUMMARY.md`
</output>
