---
phase: 02-multi-shot-fast-path
plan: 05
type: execute
wave: 2
depends_on:
  - 02-04
files_modified:
  - src/lib/novel-promotion/stage-readiness.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
autonomous: true
gap_closure: true
requirements:
  - MODE-03
  - TRAD-01
  - TRAD-02
  - UI-02
must_haves:
  truths:
    - "The workspace capsule nav shows the dedicated `multi-shot-storyboard` step with the `multiShotStoryboard` label instead of generic storyboard wording."
    - "After clicking `确认并开始绘制` in `multi_shot`, the workspace stays on the confirmation stage and treats draft shot groups as the active review step even before any reference board exists."
    - "The confirmation stage renders the derived segment set from Plan 04 and exposes usable upload or generate or replace reference affordances for each segment before `videos`."
    - "Traditional episodes still keep the classic storyboard label and route behavior."
  artifacts:
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts"
      provides: "Dedicated multi-shot confirmation nav label and status handling"
      contains: "stages.multiShotStoryboard"
    - path: "src/lib/novel-promotion/stage-readiness.ts"
      provides: "Readiness logic that recognizes draft shot-groups as confirmation-stage artifacts in multi-shot mode"
      contains: "draftMetadata"
    - path: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      provides: "Usable confirmation-stage shell rendering per-segment reference controls"
      exports: ["default"]
  key_links:
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts"
      to: "src/lib/novel-promotion/stage-readiness.ts"
      via: "stageArtifacts derived from live episode shot-groups"
      pattern: "resolveEpisodeStageArtifacts"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts"
      to: "messages/en/stages.json"
      via: "stages.multiShotStoryboard label"
      pattern: "multiShotStoryboard"
    - from: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx"
      to: "src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx"
      via: "review-mode segment confirmation surface"
      pattern: "mode=\"review\"|mode={'review'}"
---

<objective>
Close the remaining user-facing Phase 2 gaps in navigation clarity and confirmation-stage usability.

Purpose: ensure the workspace shell actually behaves like the dedicated multi-shot confirmation step the earlier plans introduced, and make the pre-video reference workflow usable on the live derived segment set.
Output: dedicated nav labeling, confirmation-stage readiness based on live draft data, and regression coverage for rendered labels plus reference affordances.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-multi-shot-fast-path/02-CONTEXT.md
@.planning/phases/02-multi-shot-fast-path/02-03-SUMMARY.md
@.planning/phases/02-multi-shot-fast-path/02-04-PLAN.md
@.planning/phases/02-multi-shot-fast-path/02-UAT.md
@.planning/phases/02-multi-shot-fast-path/02-VERIFICATION.md
@src/lib/novel-promotion/stage-readiness.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
@tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts`:
```ts
{
  id: episodeProductionMode === 'multi_shot' ? 'multi-shot-storyboard' : 'storyboard',
  label: t('stages.storyboard'),
  status: getStageStatus('storyboard'),
}
```

From `src/lib/novel-promotion/stage-readiness.ts`:
```ts
export function hasStoryboardArtifacts(
  storyboards: unknown[] | null | undefined,
  shotGroups?: unknown[] | null | undefined,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the shell recognize and label the dedicated multi-shot confirmation stage</name>
  <read_first>
    .planning/phases/02-multi-shot-fast-path/02-VERIFICATION.md
    .planning/phases/02-multi-shot-fast-path/02-UAT.md
    src/lib/novel-promotion/stage-readiness.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts
    tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
  </read_first>
  <files>src/lib/novel-promotion/stage-readiness.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts, tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts</files>
  <behavior>
    - Test 1: the multi-shot capsule item renders `t('stages.multiShotStoryboard')` while traditional still renders `t('stages.storyboard')`.
    - Test 2: draft shot-groups with `draftMetadata.segmentKey` count as confirmation-stage readiness for `multi_shot` even when `compositeImageUrl` is still empty.
    - Test 3: after the draft mutation invalidates and refetches episode data, the shell keeps the user on `multi-shot-storyboard` instead of treating the episode as if it were still on the story/script side.
  </behavior>
  <action>Update `useWorkspaceStageNavigation.ts` to fix the verified label regression directly: when `episodeProductionMode === 'multi_shot'`, the `multi-shot-storyboard` nav item must use `t('stages.multiShotStoryboard')`; the traditional branch must continue using `t('stages.storyboard')`. Then close the shell-state gap that blocks real confirmation-stage entry. In `src/lib/novel-promotion/stage-readiness.ts`, add explicit multi-shot awareness so a shot-group containing draft metadata from Plan 04 counts as confirmation-stage readiness even before a composite image or final reference board exists. The multi-shot confirmation stage is a review step, not a post-reference-complete step. In `useNovelPromotionWorkspaceController.ts`, derive `stageArtifacts` from the freshest episode data path available to the shell after the draft mutation invalidates the episode query; do not rely solely on the stale server `episode` prop when React Query already has newer `shotGroups`. Keep the traditional branch unchanged. Extend `tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` so it asserts both the rendered label and the readiness behavior: the multi-shot nav array includes `label: 'stages.multiShotStoryboard'` when `t` echoes keys, and draft-only shot-groups still make the confirmation step reachable/active in the shell. Do not rename the stage id, do not remove the traditional storyboard item, and do not gate the confirmation stage on existing composite images.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "stages.multiShotStoryboard" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useWorkspaceStageNavigation.ts tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` returns matches.
    - `rg -n "draftMetadata|segmentKey" src/lib/novel-promotion/stage-readiness.ts` returns a draft-readiness check.
    - `rg -n "resolveEpisodeStageArtifacts" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts` returns the updated live-data usage.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` exits 0.
  </acceptance_criteria>
  <done>The shell now labels and treats `multi-shot-storyboard` as a real confirmation step instead of generic storyboard or an incomplete empty stage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the confirmation stage usable for reference upload and board generation before videos</name>
  <read_first>
    .planning/phases/02-multi-shot-fast-path/02-UAT.md
    .planning/phases/02-multi-shot-fast-path/02-03-SUMMARY.md
    .planning/phases/02-multi-shot-fast-path/02-04-PLAN.md
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  </read_first>
  <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx, tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</files>
  <behavior>
    - Test 1: the confirmation stage renders all derived segments from Plan 04 and explains that this page is the storyboard/reference confirmation step before videos.
    - Test 2: each segment renders actionable reference controls in review mode: upload when empty, generate board when empty, and replace/regenerate when a reference already exists.
    - Test 3: the review surface still omits video-generation CTAs and keeps `onContinueToVideos` as the only forward path.
    - Test 4: placeholder segments remain visible repair targets rather than disappearing from the confirmation page.
  </behavior>
  <action>Harden `MultiShotStoryboardStage.tsx` and the review branch inside `ShotGroupVideoSection.tsx` around the live gap report. The stage must behave like the usable storyboard confirmation page the user described: after `确认并开始绘制`, it shows the derived multi-shot segment set from Plan 04, lets the user stay on this page, and exposes pre-video reference work for every segment. Keep the existing review-only rule from Plan 03: no direct video submission on this page. Ensure the stage body explicitly frames the page as `script -> multi-shot storyboard/reference confirmation -> videos` per D-09, D-10, and D-12. In review mode, every segment card must surface the actionable affordances the user called out: upload reference image, generate reference board, and replace/regenerate when already present. Preserve placeholder guidance for incomplete segments so missing slots can be repaired before the user continues. Extend `tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` to assert that multiple derived segments render, that reference actions are present for empty and prefilled states, and that generation CTAs remain absent. Do not add asset-library injection, omni-reference expansion, or automatic video generation; this plan is only about making the existing confirmation step usable and reachable on the real segment set.</action>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "上传参考图|生成参考板|替换参考图|替换参考板" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` returns assertions for all required reference affordances.
    - `rg -n "视频生成尚未开始|Continue to videos|进入 videos 前" src/app/\\[locale\\]/workspace/\\[projectId\\]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` returns matches.
    - `rg -n "创建并开始生成|生成视频|重新生成视频" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` returns negative-boundary assertions proving those CTAs stay out of review mode.
    - `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` exits 0.
  </acceptance_criteria>
  <done>The confirmation stage is now a usable pre-video reference workflow on the real derived segment set, with actionable upload/generate/replace controls and no accidental jump into video generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Query episode data -> workspace shell | Stale server props can mislabel or hide the confirmation step after draft creation |
| shell nav labels -> user navigation intent | Wrong labels can route users mentally and visually back into the traditional storyboard path |
| confirmation review UI -> reference actions | Missing or hidden controls prevent the user from preparing required storyboard/reference inputs before videos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | S | `useWorkspaceStageNavigation.ts` | mitigate | Use the dedicated `stages.multiShotStoryboard` label for `multi-shot-storyboard` while preserving the traditional `stages.storyboard` branch. |
| T-02-19 | T | `stage-readiness.ts` and controller shell | mitigate | Treat draft shot-groups as confirmation-stage artifacts for multi-shot mode and feed the shell from fresh episode query data after invalidation. |
| T-02-20 | D | `MultiShotStoryboardStage.tsx` | mitigate | Keep the review branch visible and actionable before references are complete instead of hiding the stage behind composite-image readiness. |
| T-02-21 | E | `ShotGroupVideoSection.tsx` review mode | mitigate | Preserve the no-video-generation boundary in review mode so the confirmation step cannot escalate directly into submission. |
</threat_model>

<verification>
1. Run `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts`.
2. Run `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts`.
3. Manual smoke check: in a `multi_shot` episode, click `确认并开始绘制`, confirm the capsule label shows the dedicated multi-shot confirmation step, confirm the page stays on confirmation with multiple derived segments, and confirm each segment exposes upload/generate/replace reference actions before continuing to `videos`.
</verification>

<success_criteria>
1. The dedicated `multi-shot-storyboard` label appears in workspace navigation.
2. Draft shot-groups make the confirmation stage reachable before any composite reference exists.
3. The live confirmation page renders the derived segment set rather than bouncing the user back to the earlier tabs.
4. Each segment exposes usable pre-video reference actions, while review mode still blocks direct video submission.
5. Traditional storyboard labeling and behavior remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-shot-fast-path/02-05-SUMMARY.md`
</output>
