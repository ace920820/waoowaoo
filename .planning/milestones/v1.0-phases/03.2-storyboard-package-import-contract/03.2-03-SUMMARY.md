# Plan 03.2-03 Summary — Import Mapper

## Status

Completed on 2026-04-26.

## Delivered

- Added `src/lib/novel-promotion/storyboard-package/mapper.ts`.
- Maps validated director packages into deterministic, side-effect-free internal import plans for Phase 03.3.
- Preserves review configuration:
  - `referencePromptText`
  - `compositePromptText`
  - storyboard mode id / label / prompt text
  - mood metadata
- Preserves video configuration:
  - `videoPrompt`
  - dialogue override text
  - dialogue language
  - include-dialogue and audio flags
  - reference mode
  - video model and sanitized generation options
- Converts declared asset refs into match requests and script-derived asset metadata placeholders.
- Maps cinematic shots into item prompts with title, prompt, duration, shot size, angle, movement, composition, lighting, blocking, and emotional beat.
- Added mapper tests in `tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts`.

## Verification

- `npx vitest run tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts`
- Full Phase 03.2 suite: `npx vitest run tests/unit/novel-promotion/storyboard-package/storyboard-package-parser.test.ts tests/unit/novel-promotion/storyboard-package/storyboard-package-validator.test.ts tests/unit/novel-promotion/storyboard-package/storyboard-package-mapper.test.ts`
- Result: 3 files / 19 tests passed.
- `npm run typecheck` passed.

## Notes

- Mapper output includes stable package / scene / segment identity for later overwrite detection.
- Missing declared assets produce warnings / unresolved match requests instead of throwing after schema validation succeeds.
- No API route, Prisma persistence, or UI logic was added in Phase 03.2; those remain Phase 03.3 and 03.4 scope.
