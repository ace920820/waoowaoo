---
phase: 03.4-script-page-upload-ui
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/lib/query/mutations/storyboard-package-import-mutations.ts
  - src/lib/query/hooks/index.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts
autonomous: true
requirements:
  - SBPI-23
  - SBPI-27
  - SBPI-28
---

<objective>
Add the frontend import API client contract and workspace runtime wiring needed by the script page upload UI. This plan should expose preview/commit mutations and a runtime callback that can commit an imported storyboard package, refresh episode data, and route to multi-shot confirmation without implementing the visual preview yet.
</objective>

<context>
Read first:
- `.planning/phases/03.4-script-page-upload-ui/03.4-CONTEXT.md`
- `.planning/phases/03.4-script-page-upload-ui/03.4-RESEARCH.md`
- `.planning/phases/03.3-import-api-and-persistence/03.3-VERIFICATION.md`
- `src/lib/query/mutations/multi-shot-draft-mutations.ts`
- `src/lib/query/hooks/index.ts`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts`
- `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts`

Use progressive code disclosure. Do not read package parser/backend service unless response typing is unclear from Phase 03.3 verification or tests.
</context>

<tasks>
  <task id="03.4-01-T1" type="client-mutation">
    <files>src/lib/query/mutations/storyboard-package-import-mutations.ts, src/lib/query/hooks/index.ts</files>
    <action>
      Add React Query mutations for storyboard package import preview and commit. The mutation should call `/api/novel-promotion/${projectId}/episodes/${episodeId}/storyboard-package-import` with `mode`, `content`, `filename`, `contentType`, `overwriteStrategy`, and `preserveGeneratedMedia`. Preview should not invalidate data. Commit should invalidate `queryKeys.episodeData(projectId, episodeId)` and any existing project/episode data query keys needed by the workspace after successful import.
    </action>
    <verify>
      Add or extend focused mutation tests to assert request path/body for preview and commit, and invalidation on commit only.
    </verify>
    <acceptance_criteria>
      - Hook returns typed preview and commit mutation objects or two focused hooks.
      - Preview and commit use the Phase 03.3 route path exactly.
      - Commit invalidates episode data so imported shot groups appear in multi-shot confirmation/video stages.
      - No image/video generation request is triggered by these mutations.
    </acceptance_criteria>
  </task>

  <task id="03.4-01-T2" type="runtime-wiring">
    <files>src/app/[locale]/workspace/[projectId]/modes/novel-promotion/WorkspaceStageRuntimeContext.tsx, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useWorkspaceStageRuntime.ts, src/app/[locale]/workspace/[projectId]/modes/novel-promotion/hooks/useNovelPromotionWorkspaceController.ts</files>
    <action>
      Extend workspace runtime types and controller wiring with storyboard package import callbacks/state. Provide preview mutation state and a commit callback that awaits commit, refreshes workspace/episode data, and navigates to `multi-shot-storyboard` on success. Keep this separate from `onRunScriptToStoryboard` because upload import bypasses auto-generated drafts.
    </action>
    <verify>
      Extend multi-shot stage routing/runtime tests to assert successful import commit navigates to `multi-shot-storyboard` and does not call the script-to-storyboard generation flow.
    </verify>
    <acceptance_criteria>
      - Runtime exposes enough state for upload UI: preview pending, commit pending, last error, and callbacks.
      - Successful commit calls refresh/invalidation before navigation when practical.
      - Traditional script-to-storyboard flow is unchanged.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts tests/unit/novel-promotion/multi-shot-draft-mutations.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Frontend has a reusable import preview/commit client contract.
- Workspace runtime can commit an import and route to multi-shot confirmation.
- Existing generated-draft workflow remains unchanged.
</success_criteria>
