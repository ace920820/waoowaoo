---
phase: 03.3-import-api-and-persistence
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - src/lib/novel-promotion/storyboard-package/import-service.ts
  - src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import/route.ts
  - tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts
autonomous: true
requirements:
  - SBPI-13
  - SBPI-14
  - SBPI-18
  - SBPI-21
---

<objective>
Add the secured storyboard package import API contract and non-mutating preview path. Preview must parse/validate director package text, map it through the Phase 03.2 contract, match package assets to existing project assets, and return a create/update plan without mutating shot groups.
</objective>

<context>
Read first:
- `.planning/phases/03.3-import-api-and-persistence/03.3-CONTEXT.md`
- `.planning/phases/03.3-import-api-and-persistence/03.3-RESEARCH.md`
- `.planning/phases/03.2-storyboard-package-import-contract/03.2-VERIFICATION.md`
- `src/lib/novel-promotion/storyboard-package/index.ts`
- `src/app/api/novel-promotion/[projectId]/multi-shot-drafts/route.ts`
- `src/app/api/novel-promotion/[projectId]/assets/route.ts`
- `src/lib/novel-promotion/ownership.ts`

Use progressive code disclosure. Do not read UI files. Expand only if route imports, type errors, or failing tests require it.
</context>

<tasks>
  <task id="03.3-01-T1" type="route-contract">
    <files>src/app/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import/route.ts</files>
    <action>
      Add a `POST` route under the episode API surface. Use `apiHandler`, `requireProjectAuthLight`, `isErrorResponse`, and episode ownership checks consistent with existing novel-promotion routes. Accept body fields `mode`, `content`, optional `filename`, optional `contentType`, optional `overwriteStrategy`, and optional `preserveGeneratedMedia`. For this plan, support `mode: "preview"`; return a structured error for unsupported modes until plan 02 adds commit.
    </action>
    <verify>
      Add an integration test asserting unauthorized behavior can still be mocked through existing auth helpers and that missing/invalid `episodeId` or content returns an API error.
    </verify>
    <acceptance_criteria>
      - Route is scoped to `/api/novel-promotion/[projectId]/episodes/[episodeId]/storyboard-package-import`.
      - Route verifies project auth and episode ownership before returning preview data.
      - Route does not call Prisma write methods in preview mode.
      - Route delegates parsing and preview construction to a library service instead of embedding business logic inline.
    </acceptance_criteria>
  </task>

  <task id="03.3-01-T2" type="preview-service">
    <files>src/lib/novel-promotion/storyboard-package/import-service.ts</files>
    <action>
      Create a pure-ish import service that receives package text and project/episode context. Use `parseStoryboardPackageText` and `mapStoryboardPackageToImportPlan` from Phase 03.2. Build a preview response containing package summary, segment rows, shot counts, create/update classification, overwrite strategy, asset match results, warnings, and validation issues. Existing imported shot groups must be detected by parsing draft metadata identity from existing episode shot groups.
    </action>
    <verify>
      Add preview integration tests using mocked Prisma episode/project data and a package fixture from `tests/unit/novel-promotion/storyboard-package/storyboard-package-fixtures.ts` or a local integration fixture.
    </verify>
    <acceptance_criteria>
      - Raw JSON and Markdown fenced package text both work through the route because parsing is delegated to Phase 03.2.
      - Invalid package content returns validation errors and does not mutate data.
      - Preview classifies each imported segment as `create` or `update` using package/segment identity.
      - Preview returns shot counts, template keys, titles, warnings, and asset match results for locations, characters, and props.
    </acceptance_criteria>
  </task>

  <task id="03.3-01-T3" type="asset-matching">
    <files>src/lib/novel-promotion/storyboard-package/import-service.ts</files>
    <action>
      Implement deterministic asset matching for preview. Match package asset declarations and segment refs to existing project characters, locations, and props. Locations are `NovelPromotionLocation` rows where `assetKind !== "prop"`; props are `NovelPromotionLocation` rows where `assetKind === "prop"`. Prefer exact id/externalId matches, then normalized `matchName`, then normalized package asset `name`. Missing assets must produce warnings and retain script-derived fallback metadata; never create assets silently.
    </action>
    <verify>
      Add integration assertions for matched character/location/prop assets and missing-asset warnings.
    </verify>
    <acceptance_criteria>
      - Matching is stable and case/trim insensitive for names.
      - Matched assets include type, asset id, label/name, source status, and enough data for Phase 03.4 preview display.
      - Missing matches do not block preview.
      - No asset create/update Prisma calls are made.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run tests/integration/api/specific/novel-promotion-storyboard-package-import-route.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- A secured import route exists and supports preview mode.
- Preview parses, validates, maps, asset-matches, and reports create/update plans without mutation.
- Preview contract is ready for Phase 03.4 upload UI consumption.
</success_criteria>
