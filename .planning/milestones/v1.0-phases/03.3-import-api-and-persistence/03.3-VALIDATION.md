# Phase 03.3 Plan Validation

## Verdict

PASS — planned as three executable backend-focused plans with clear dependencies and verification gates.

## Checks

- Scope isolation: PASS. Plans avoid Phase 03.4 upload UI and generation workers.
- Dependency correctness: PASS. Plans depend on Phase 03.2 parser/validator/mapper exports.
- Persistence coverage: PASS. Plans cover shot group fields, item prompts, video config snapshot, draft metadata, asset bindings, and repeated import behavior.
- Preview/commit split: PASS. Preview is non-mutating; commit reruns validation and writes transactionally.
- Asset handling: PASS. Plans require matching existing project assets and script-derived fallback for misses; no silent asset creation.
- Test coverage: PASS. Plans include integration coverage for preview, validation failure, commit, repeated import, missing assets, metadata, and item prompts.

## Plan Set

- `03.3-01-PLAN.md` — route contract, preview service, asset matching.
- `03.3-02-PLAN.md` — commit persistence, transaction, overwrite behavior.
- `03.3-03-PLAN.md` — integration hardening and regression coverage.

## Risks To Watch During Execution

- Existing `videoReferencesJson` may include user edits; commit must preserve generated media by default and only overwrite imported metadata fields intentionally.
- Props are project locations with `assetKind=prop`, so asset matching must not assume a separate prop table.
- Preview and commit must use the same planner logic to avoid UI preview saying one thing and commit doing another.
