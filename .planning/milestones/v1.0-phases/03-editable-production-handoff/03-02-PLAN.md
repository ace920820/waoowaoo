---
phase: 03-editable-production-handoff
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
  - tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
autonomous: true
requirements:
  - MSHT-07
  - MSHT-08
  - UI-04
must_haves:
  truths:
    - 用户进入视频阶段后，可以直接编辑每个多镜头片段的“视频提示词”和“台词 / 说话内容”
    - 用户在多镜头确认页不会看到台词编辑器，台词编辑只出现在视频阶段
    - 多镜头模式从确认进入视频阶段后，所有多镜头生产单元默认就是可见且可编辑的
  artifacts:
    - path: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
      provides: 多镜头视频阶段双编辑器 UI 与保存逻辑
    - path: tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
      provides: 多镜头视频阶段文案、字段与保存行为回归测试
    - path: tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
      provides: 模式一致的 handoff/routing 回归测试
  key_links:
    - from: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
      to: src/app/api/novel-promotion/[projectId]/shot-groups/route.ts
      via: useUpdateProjectShotGroup mutation payload
      pattern: dialogueOverrideText
    - from: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
      to: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
      via: multi-shot videos mode handoff
      pattern: mode
---

<objective>
Turn the multi-shot video stage into the real editable production handoff by exposing separate prompt and dialogue editors on every production unit without moving confirmation-stage responsibilities downstream.

Purpose: This delivers D-03, D-04, D-05, D-09, and D-10 while preserving the fast path required by MSHT-08 and UI-04.
Output: Video-stage multi-shot cards with editable `视频提示词` and `台词 / 说话内容` inputs, correct helper copy, and routing tests that prove the current episode mode still drives the handoff.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-editable-production-handoff/03-CONTEXT.md
@.planning/phases/03-editable-production-handoff/03-UI-SPEC.md
@.planning/phases/02-multi-shot-fast-path/02-05-SUMMARY.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoPanelCardBody.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/runtime/videoPanelRuntimeCore.tsx

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx`:
```ts
interface VideoDraftState {
  groupPrompt: string
  videoPrompt: string
  includeDialogue: boolean
  dialogueLanguage: NovelPromotionDialogueLanguage
}
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/runtime/videoPanelRuntimeCore.tsx`:
```ts
const promptEditor = usePanelPromptEditor(...)
const dialogueEditor = usePanelPromptEditor(...)
```

From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx`:
```tsx
<VideoStage
  shotGroups={shotGroups}
  onUpdateVideoPrompt={runtime.onUpdateVideoPrompt}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace multi-shot video-stage dialogue toggles with split prompt/dialogue editors</name>
  <read_first>
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/VideoPanelCardBody.tsx
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video/panel-card/runtime/videoPanelRuntimeCore.tsx
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
    - .planning/phases/03-editable-production-handoff/03-UI-SPEC.md
  </read_first>
  <files>
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
  </files>
  <action>In the `mode="video"` branch only, replace the current `包含台词` checkbox plus `台词语言` selector block with two explicit text editors per D-03 and D-04: label the first `视频提示词`, label the second `台词 / 说话内容`, and keep them as separate draft fields. Seed the dialogue editor from `draftMetadata.dialogueOverrideText ?? draftMetadata.embeddedDialogue ?? ''`, show helper copy `已从剧本草稿带入，可在生成前直接改写或清空。` when `embeddedDialogue` exists, otherwise show `可选。留空时按当前生产单元的默认语音内容处理。`, and save `dialogueOverrideText` as `null` when the edited text is blank or matches the original embedded dialogue exactly so clearing the field reverts to default behavior instead of persisting an empty override.</action>
  <acceptance_criteria>
    - `rg -n "视频提示词|台词 / 说话内容" src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns the two field labels.
    - `rg -n "已从剧本草稿带入，可在生成前直接改写或清空。|可选。留空时按当前生产单元的默认语音内容处理。" src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` returns the exact helper copy from the UI contract.
    - `rg -n "does not render dialogue editors in review mode|saves dialogueOverrideText as null when cleared" tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts` returns both regression cases.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts</automated>
  </verify>
  <done>Every multi-shot production unit in video mode exposes separate prompt and dialogue editors, review mode remains free of dialogue editing, and save behavior preserves the override-over-default contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the multi-shot handoff routing and visible-on-entry behavior</name>
  <read_first>
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    - tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
  </read_first>
  <files>
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx
    tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts
    tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts
  </files>
  <action>Preserve UI-04 and MSHT-08 by keeping the multi-shot `multi-shot-storyboard -> videos` handoff mode-aware while ensuring the first render of `mode="video"` exposes all multi-shot production cards without extra expand/reveal gates per D-09 and D-10. Do not introduce accordions, modal-only editing, or any confirmation-page dialogue editor; the video stage must remain the first place where the dual-input production contract appears.</action>
  <acceptance_criteria>
    - `rg -n "mode: 'video'|data-shot-group-mode" tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` returns assertions for the video-stage branch.
    - `rg -n "does not render dialogue editors in review mode|renders editable shot-group units on entry" tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts` returns the handoff visibility regression coverage.
    - The automated verify command passes with both routing and video-stage tests in one run.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts tests/unit/novel-promotion/workspace/multi-shot-video-stage.test.ts</automated>
  </verify>
  <done>The multi-shot handoff still follows the selected episode mode, and users land on immediately editable multi-shot production units without extra reveal steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser editor state→shot-group mutation | user-edited prompt/dialogue text becomes persisted production configuration |
| stage routing→video-stage rendering | incorrect mode branching could send users to the wrong production surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/ShotGroupVideoSection.tsx` | mitigate | Persist only the dedicated `dialogueOverrideText` field through the existing shot-group mutation instead of inventing a parallel client-only store. |
| T-03-05 | E | `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/VideoStageRoute.tsx` | mitigate | Keep mode-based handoff logic unchanged except for explicit tests that assert multi-shot continues to `videos` and back to `multi-shot-storyboard`. |
| T-03-06 | R | `tests/unit/novel-promotion/workspace/multi-shot-stage-routing.test.ts` | mitigate | Add route and render assertions for `mode="video"` so future routing refactors cannot silently collapse the editable handoff. |
</threat_model>

<verification>
Run the targeted multi-shot routing and video-stage vitest suites, then inspect the rendered markup assertions for the exact field labels and helper strings from the UI contract.
</verification>

<success_criteria>
- Multi-shot video units expose the dual-input editing model in the video stage only.
- Clearing an override reverts to the default dialogue source instead of persisting blank dialogue.
- Multi-shot confirmation still hands off into the correct video-stage surface for the active episode mode.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editable-production-handoff/03-02-SUMMARY.md`
</output>
