---
phase: 03-editable-production-handoff
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
  - src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
  - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
  - tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  - tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts
autonomous: true
requirements:
  - SHOT-02
  - MSHT-08
  - UI-04
must_haves:
  truths:
    - 用户可以在多镜头确认页直接走“手动补充单镜头”入口，而不需要返回传统分镜页
    - 视频阶段始终先展示多镜头片段，再展示手动补充的单镜头区块
    - 手动补充单镜头被呈现为多镜头流程的补充生产单元，而不是切回传统整集链路
  artifacts:
    - path: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
      provides: 多镜头确认页的手动单镜头补充入口
    - path: src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
      provides: 视频阶段中多镜头区块在前、单镜头补充区块在后的布局
    - path: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
      provides: 单镜头补充区块标题与说明文案
    - path: tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
      provides: 确认页补充入口文案与 CTA 回归测试
  key_links:
    - from: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
      to: src/lib/query/mutations/storyboard-panel-mutations.ts
      via: useCreateProjectStoryboardGroup mutation
      pattern: insertIndex
    - from: src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
      to: src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
      via: supplement section wrapper rendered after ShotGroupVideoSection
      pattern: 手动补充单镜头
---

<objective>
Add the exception-path single-shot supplement flow on top of the multi-shot fast path without dragging users back into the traditional storyboard-first workflow.

Purpose: This delivers D-01, D-02, D-06, D-07, and D-08 while satisfying SHOT-02 and keeping the default multi-shot production path visually primary.
Output: A manual supplement CTA on the multi-shot confirmation page and a clearly secondary single-shot section below multi-shot production units in the video stage.
</objective>

<execution_context>
@/Users/jamiezhao/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jamiezhao/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-editable-production-handoff/03-CONTEXT.md
@.planning/phases/03-editable-production-handoff/03-UI-SPEC.md
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
@src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
@src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/hooks/useStoryboardGroupActions.ts
@src/app/api/novel-promotion/[projectId]/storyboard-group/route.ts

<interfaces>
From `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx`:
```tsx
const onContinueToVideos = () => {
  runtime.onStageChange('videos')
}
```

From `src/lib/query/mutations/storyboard-panel-mutations.ts`:
```ts
export function useCreateProjectStoryboardGroup(projectId: string) {
  mutationFn: async (payload: { episodeId: string; insertIndex: number }) => { ... }
}
```

From `src/lib/novel-promotion/stages/video-stage-runtime-core.tsx`:
```tsx
<ShotGroupVideoSection ... />
<VideoRenderPanel ... />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the manual single-shot supplement entry to multi-shot confirmation</name>
  <read_first>
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/storyboard/hooks/useStoryboardGroupActions.ts
    - src/app/api/novel-promotion/[projectId]/storyboard-group/route.ts
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
    - .planning/phases/03-editable-production-handoff/03-UI-SPEC.md
  </read_first>
  <files>
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx
    tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts
  </files>
  <action>Per D-01 and D-02, add a secondary CTA on the multi-shot confirmation page labeled `手动补充单镜头` with helper copy `只在需要额外补镜头时使用，不会切回传统整集分镜流程。`. Wire it to `useCreateProjectStoryboardGroup(projectId)` using `{ episodeId, insertIndex: storyboards.length }` so the created single-shot unit lands at the end of the episode sequence, then transition to `videos` after success so users continue the same production flow instead of returning to storyboard mode. Do not use copy such as `返回分镜` or `进入传统模式`.</action>
  <acceptance_criteria>
    - `rg -n "手动补充单镜头|只在需要额外补镜头时使用，不会切回传统整集分镜流程。" src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` returns the exact trigger and helper copy.
    - `rg -n "useCreateProjectStoryboardGroup|insertIndex" src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` returns the creation path.
    - `rg -n "手动补充单镜头|不会切回传统整集分镜流程" tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` returns the confirmation-stage regression assertions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</automated>
  </verify>
  <done>The multi-shot confirmation page exposes a clear supplement-only single-shot entry and keeps the user inside the multi-shot production handoff instead of sending them back to the traditional storyboard workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Present manual single-shot supplements as the secondary video-stage section</name>
  <read_first>
    - src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
    - src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
    - tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts
    - .planning/phases/03-editable-production-handoff/03-CONTEXT.md
    - .planning/phases/03-editable-production-handoff/03-UI-SPEC.md
  </read_first>
  <files>
    src/lib/novel-promotion/stages/video-stage-runtime-core.tsx
    src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx
    tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts
  </files>
  <action>Per D-06, D-07, and D-08, keep `ShotGroupVideoSection` as the first production section and wrap `VideoRenderPanel` in a second section titled `手动补充单镜头` with body copy `这些镜头作为多镜头片段之外的补充单元独立生成。`. Render the supplement section only after the full multi-shot list so the fast path remains visually primary, and keep the existing single-shot rendering behavior inside that section instead of nesting supplements under any shot-group card.</action>
  <acceptance_criteria>
    - `rg -n "手动补充单镜头|这些镜头作为多镜头片段之外的补充单元独立生成。" src/lib/novel-promotion/stages/video-stage-runtime-core.tsx src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/video-stage/VideoRenderPanel.tsx` returns the exact section copy.
    - `rg -n "ShotGroupVideoSection|VideoRenderPanel" src/lib/novel-promotion/stages/video-stage-runtime-core.tsx` still shows multi-shot section before the single-shot section wrapper.
    - `rg -n "renders the single-shot supplement section after shot groups" tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts` returns the ordering regression.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/novel-promotion/video-stage-runtime-regressions.test.ts tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts</automated>
  </verify>
  <done>The video stage keeps multi-shot production units first, shows manual single-shot supplements as a clearly secondary section beneath them, and preserves the fast path as the main production spine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| confirmation CTA→storyboard-group creation route | a user action creates new storyboard/panel records in the active episode |
| video-stage layout→production-unit interpretation | section ordering communicates workflow meaning and can mislead users if wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | `src/app/[locale]/workspace/[projectId]/modes/novel-promotion/components/MultiShotStoryboardStage.tsx` | mitigate | Reuse `useCreateProjectStoryboardGroup` and the existing authenticated route instead of inventing an ad hoc create path from the confirmation stage. |
| T-03-08 | S | `src/lib/novel-promotion/stages/video-stage-runtime-core.tsx` | mitigate | Keep section ordering explicit in JSX and cover it with regression tests so supplements cannot accidentally appear before multi-shot units. |
| T-03-09 | R | `tests/unit/novel-promotion/workspace/multi-shot-storyboard-stage.test.ts` | mitigate | Assert exact CTA/helper copy that distinguishes supplements from traditional storyboard mode. |
</threat_model>

<verification>
Run the confirmation-stage and video-stage regression tests together, then inspect the rendered strings to confirm the supplement path uses the exact UI-contract copy and appears after the multi-shot section.
</verification>

<success_criteria>
- Users can create a manual single-shot supplement from the multi-shot confirmation page.
- The video stage displays supplements as a secondary section below the full multi-shot list.
- The copy and layout make it clear that supplements are exceptions within the multi-shot flow, not a mode switch back to traditional storyboard work.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editable-production-handoff/03-03-SUMMARY.md`
</output>
